schema_version: 2
taxonomy_version: market-discovery-v1
provenance:
  authored_session: S1413
  contract_head: bbba79e1fde831f3d3d154a2af9c27388d3c16c5
  independent_review_status: pending
  independent_review_evidence: null
  split: implementation_fixture
  evaluation_method: >-
    Deterministic global top-three retrieval against the complete immutable
    corpus. This fixture is implementation evidence, never authority or a
    substitute for the separately sealed held-out evaluation.

cases:
  - id: baseline-backend-schema-migration
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [backend, database-schema, deploy-migrate, grandfathered/ai-market-backend.md]
    market_area: backend
    query: deploy backend and run a database schema migration
    required_paths: [ai-market-backend.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: baseline-aim-data-seller-publish
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [aim-data, seller-dataset, publish, grandfathered/aim-data-seller-publish-journey.md]
    market_area: aim-data
    query: publish a seller dataset through AIM Data
    required_paths: [aim-data-seller-publish-journey.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: baseline-qdrant-restore
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [vector-search, qdrant-data, restore-loss, grandfathered/qdrant.md]
    market_area: vector-search
    query: restore Qdrant after data loss
    required_paths: [qdrant.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: baseline-signup-two-factor
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [account-security, signup-two-factor, debug, grandfathered/auth]
    market_area: account-security
    query: debug signup and two-factor authentication
    required_paths: [auth-signup-flow.md, two-factor-auth.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-capability-scoped-403
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [account-security, seller-capability, diagnose-403, grandfathered/account-capability-onboarding.md]
    market_area: account-security
    query: diagnose a capability scoped 403 while onboarding a seller account
    required_paths: [account-capability-onboarding.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-kaggle-http-200-error
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [dataset-publishing, kaggle-card, publish-http-error, grandfathered/dataset-card-publishing.md]
    market_area: dataset-publishing
    query: publish a dataset card when Kaggle returns HTTP 200 with Error in the body
    required_paths: [dataset-card-publishing.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-data-request-422-white-screen
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [data-requests, request-validation, diagnose-422-ui, grandfathered/data-requests.md]
    market_area: data-requests
    query: diagnose a 422 urgency or format validation failure and white screen in a data request
    required_paths: [data-requests.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-seo-redis-ttl-apscheduler
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [seller-seo, readiness-cache, verify-ttl-scheduler, grandfathered/seo-seller-validation.md]
    market_area: seller-seo
    query: verify SEO seller readiness with Redis TTL and APScheduler cache behavior
    required_paths: [seo-seller-validation.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-crm-briefing
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [crm, seller-pipeline-briefing, generate-current, grandfathered/crm]
    market_area: crm
    query: generate the current CRM morning briefing from the seller pipeline
    required_any_paths: [morning-briefing.md, crm-pipeline.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: [archive/evidence/briefing-verification-2026-04-25.md]
    historical_path_cannot_satisfy: archive/evidence/briefing-verification-2026-04-25.md
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-support-t-ref-quarantine
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [support, ticket-t-ref, quarantine-investigate, grandfathered/support-ticket-system.md]
    market_area: support
    query: quarantine and investigate a support ticket with a T-ref identifier
    required_paths: [support-ticket-system.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-qdrant-killed-worker-outbox
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [vector-search, qdrant-outbox, recover-killed-worker, grandfathered/qdrant-sync-outbox.md]
    market_area: vector-search
    query: recover Qdrant synchronization after a killed worker leaves an outbox row
    required_paths: [qdrant-sync-outbox.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}

  - id: market-account-teardown-fk-pii
    taxonomy_version: market-discovery-v1
    taxonomy_tuple: [account-security, account-teardown, delete-fk-pii, grandfathered/account-teardown.md]
    market_area: account-security
    query: tear down an account while preserving FK weak-link and PII deletion rules
    required_paths: [account-teardown.md]
    expected_policy_class: grandfathered_discovery_lead
    historical_exclusions: []
    split: implementation_fixture
    provenance: {author: Mars, session: S1413}
