id: I-05
runbook: agent-dispatch.md
type: isolate
refs:
- F-02
- G-02
- council:I-05
scenario: 'id: F-02. trigger: A live-state-authorized non-gate AG advisory dispatch returns no result because MAX_TURNS is exhausted. pre_conditions: infra:council-comms explicitly permits the advisory dispatch, and the transcript, max-turn marker, diff size, and prompt are available. tool_or_endpoint: council_request task transcript. argument_sourcing: max-turn evidence from transcript; changed files from git diff; role expectation from infra:council-comms. idempotency: READ_ONLY_DIAGNOSTIC. expected_success: classify as AG advisory budget exhaustion. expected_failures: accepting a partial result, widening timeout blindly, confusing it with outage, or counting AG toward a gate. next_step_success: retry narrowly only while live-state eligibility remains explicit. next_step_failure: preserve AG non-response and fail closed for gate purposes; obtain CC/Kimi/GLM votes without substitution.

  '
expected_answers:
- kind: human_action
  verb: classify
  object: AG MAX_TURNS=25 review-mode exhaustion
  target: BQ-COUNCIL-AG-MAX-TURNS-REVIEW-MODE then G-02
weight: 0.08333333333333333
