id: I-02
runbook: council-gate-process.md
type: operate
refs:
- E-02
- F-02
- G-02
scenario: 'id: E-02. trigger: Gate 1 has passed and the BQ needs a bounded Gate 2 implementation spec before chunk build dispatch. pre_conditions: gate1.status is APPROVED or mandate-resolution evidence exists, spec path is selected, files touched and test plan are known, the live CC/Kimi/GLM panel is available, and compliance gate state is readable. tool_or_endpoint: specs/BQ-*-GATE2.md plus bq_update(entity=build:bq-*, gate=gate2, status=<status>). argument_sourcing: spec_path from BQ slug; files_touched from repository survey and approved design; status from the complete valid CC/Kimi/GLM implementation-spec review; MP authors but does not vote. idempotency: IDEMPOTENT_WITH_KEY on entity + spec_path + spec_commit. expected_success: panel-reviewed Gate 2 spec names chunk ACs, file scope, risks, and tests, and BQ gate2 state matches the spec commit. expected_failures: stale Gate 1 mandates, invalid active voter, omitted scope, or unreviewed spec. next_step_success: dispatch the approved chunk through the MP builder path.
  next_step_failure: apply G-02 or revise the Gate 2 spec; never substitute MP, AG, or DeepSeek for a voter.

  '
expected_answers:
- kind: tool_call
  tool: bq_update
  argument_keys:
  - entity
  - gate
  - status
  argument_values:
    gate: gate2
weight: 0.08333333333333333
