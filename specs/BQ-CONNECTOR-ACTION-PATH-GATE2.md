# BQ-CONNECTOR-ACTION-PATH — Gate 2 detailed build

Status: proposed build specification. Gate 1 passed 3/3 and merged in runbooks `46e6591d61190683d1ae603f4dd3ccc0e00c7064`. Authority: `BQ-CONNECTOR-ACTION-PATH-GATE1.md`, `BQ-CUSTOMER-MCP-CONNECTOR-DESIGN.md`, and core Gate 1 §9. This document specifies code and proof; it does not claim a live release. Backend citations are to `ai-market-backend origin/main 445f43af87d2db734c064b492ed0a9d69edb2b86`; frontend citations are to `ai-market-frontend origin/main 062503bf0bf5b22d31975ce59ff6be0c91e0b562`. Paths described as **new** are build targets, not baseline code. Runtime configuration, current production data, and vendor flows are **UNVERIFIED**.

## 1. Delivery contract and build order

Implement the Gate 1 action path without exposing P1 buyer or seller tools during the core P0 `get_my_account` release. Every connector write enters a registered action through `ActionExecutorService.execute_connector`; a code-defined floor runs before the DB policy engine and can only be tightened. A REVIEW creates one pending request whose exact validated arguments are executed once only after first-party browser confirmation. Checkout handoff makes a URL and no purchase. A single browser checkout service owns the human purchase path. OAuth and core own identity, transport, grants, scope filtering, switches, and the single connector audit table.

Required sequence: core SDK Chunk 0 → core inert foundation revision `s_connector_foundation_001` → OAuth revision `s_connector_oauth_001` → action-path migration and flag-off code may deploy → OAuth AS off → resource off → test-host joint proof → core/OAuth joint enable restricted to `CONNECTOR_EARLY_ACCESS_USER_IDS` containing test users and Max → action-path writes enabled only after joint enable. Opening beyond that list waits for core Gate 1 §9's P1 buyer-tool stage. At the pinned backend `445f43af`, `alembic/versions/20260925_001_s1738_text_custom_license.py:8-9` defines `s1738_text_custom_license` after `s1749_gateway_buyer`. The merged core Gate 2 at runbooks `dc851ec` defines `s_connector_foundation_001` with down-revision `s1738_text_custom_license`; the merged OAuth Gate 2 defines `s_connector_oauth_001` after the foundation. Set `s_connector_action_path_001` down-revision to **`s_connector_oauth_001`**, so the three connector revisions form one chain and Alembic has one head. Backend main may advance before the build; in particular, `s1739_peer_pickup_roster` from backend PR #483 may merge first. At build time inspect the actual single backend Alembic head, chain the first new connector migration to that head, keep foundation → OAuth → action-path in order, and assert exactly one head in CI before and after the upgrades. Do not build a second foundation or an action-path audit table.

## 2. Verified seams and limits

`ActionDefinition` and `ActionRegistry` are at `app/actions/registry.py:25-64`; `ActionExecutorService.execute` and dispatch are at `app/services/action_executor_service.py:71,469`, and `_log_action` at `:737`. `POST /actions/execute` is at `app/api/v1/endpoints/actions.py:88`. `PolicyEngineService` and its DB priority evaluation are at `app/services/policy_engine_service.py:182-215`; existing policy evaluation may be overridden by higher-priority DB rules, so the floor below is a separate pre-check. The canonical browser checkout starts at `app/api/v1/endpoints/checkout.py:139-154`, uses `TransactionService.initiate` at `app/services/transaction_service.py:290` and `create_checkout` at `:1417`. `ENABLE_CANONICAL_TX` defaults true and `LISTING_LICENSES_ENABLED` defaults false at `app/core/config.py:186,191`. The S1735 constructor remains `OrderService.create_order` at `app/services/order_service.py:662`; its licence comparison is at `:827-1027`, with `LicenseAcceptanceInput.authority_confirmed` at `app/services/license_acceptance_service.py:94-113`. `canonical_json` is at `app/services/license_hashing.py:63`. The other order creators are the legacy browser branch `app/api/v1/endpoints/checkout.py:253-268`, MCP tool `app/services/mcp_marketplace_tools.py:862-880`, MCP REST `app/api/v1/endpoints/mcp_marketplace.py:725-740`, agent checkout `app/services/transaction_service.py:1105` (order creation inside `_prepare_agent_checkout` at `:1021-1030`), and wallet/agent flow `app/services/agent_service.py:589-622`. Gate 1 §2.4 counts five creators (canonical web, MCP tool, MCP REST, agent-key and wallet); the flag matrix tests six executable paths because the web creator has canonical and legacy branches. Their existing guards and results must remain under flags off. `OrderService.confirm_order` can reach `dispatch_order_payout` at `app/services/order_service.py:1843,1929-1930`; that payout entry is `app/services/order_money_service.py:1150`.

The current frontend has `api/checkout.ts:4-15`, login redirect handling `app/login/LoginForm.tsx:51-52,93-94`, and redirect validation `lib/redirect.ts:3,13-21`; there is no `app/confirm/[id]` or `app/checkout/h/[token]` at this pinned tree (new routes). The precise existing listing Buy/licence component for reuse is **UNVERIFIED**; before implementing Chunk 4, identify it by code inspection and pin its path in that chunk's PR without creating a second acceptance form. `package.json:8-12` defines build, lint, typecheck and Vitest. Existing backend file:line citations in this spec were checked against `445f43af87d2db734c064b492ed0a9d69edb2b86`; build targets, runtime behavior, and explicitly **UNVERIFIED** seams require build or Gate 4 proof.

## 3. Cross-spec contract

`app/mcp/connector_shared/` is key-free and imported by the resource, AS, and main backend. It owns `contracts.py` with the exact Gate 1 §5.3 interfaces below, `grants.py` with **both** `revoke_grant` and `revoke_all_for_user`, `profiles.py`/`profiles.yaml`, `scopes.py`, `audit.py`, `oauth_models.py`, and `oauth_store.py` with create/replace grant and single-use consent-ticket functions. `oauth_store.py` must not define either revocation hook. `app.mcp.connector` and `app.main`/`app.api`/`app.services`/`app.actions` may import shared but never `app.mcp.connector_auth`; AS may import shared but never resource; shared imports neither. Static AST and fresh-process import tests enforce this. Only `ai-market-connector-auth` has `CONNECTOR_OAUTH_SIGNING_KEYS`; `ai-market-connector` and backend refuse or fail the pre-enable variable-name check if it is present.

```python
@dataclass(frozen=True)
class VerifiedToken:
    user_id: UUID; client_id: str; grant_id: UUID; token_jti: str
    scopes: frozenset[str]; issuer: str; audience: str; expires_at: datetime

class TokenVerifier(Protocol):
    async def verify(self, token: str, *, resource: str) -> VerifiedToken | None: ...

@dataclass(frozen=True)
class Grant:
    grant_id: UUID; user_id: UUID; org_id: UUID | None; client_id: str
    scopes: frozenset[str]; profile: Profile; revoked_at: datetime | None

class GrantStore(Protocol):
    async def load_active_grant(self, grant_id: UUID) -> Grant | None: ...
```

Construct `VerifiedToken` with keyword arguments; `token_jti` is the JWT `jti` and fourth declared field. The verifier returns `None` for invalid ES256/issuer/audience/expiry/jti; the resource then checks token/grant user, client, scopes and live org membership on every request (personal grant means no active membership). No grant cache in P0. Fakes implement both protocols before the AS implementation lands; the same contract suite runs with fakes and with OAuth's real verifier/store. Canonical issuer `https://auth.ai.market`, audience/resource `https://connect.ai.market/mcp`, public JWKS `https://auth.ai.market/.well-known/jwks.json`; no `/mcp/openai` alias. Scope filtering uses all required scopes. A DCR client is always `default`; elevated Claude/OpenAI profiles require validated allowlisted CIMD identity, pinned document hash and matching redirect, with downgrade of live grants on drift.

Foundation means Chunk 0 deployed, then one shared additive revision `s_connector_foundation_001` creating `connector_switches`, `connector_audit_events` and `connector_review_queue`, shared profile/scope catalogue and interfaces/fakes/contract tests, both Railway JSON files, A23 isolation check, and the health-only `app/mcp/connector_auth/asgi.py` stub. Backend web owns migration execution; neither new service migrates on startup. The auth service is configured with both flags off only after this inert foundation. OAuth revision `s_connector_oauth_001` depends on the foundation revision, is additive, and its downgrade retains all five OAuth tables.

`connector_audit_events` is the sole connector audit table, append-only and retained pending legal sign-off. The fixed `event_type` CHECK catalogue is `tool.call`, `http.auth_failure`, `oauth.grant_created`, `oauth.grant_superseded`, `oauth.grant_revoked`, `oauth.grant_downgraded`, `oauth.refresh_reuse_detected`, `oauth.registration_created`, `action.confirm`, `action.decline`. Tool-call fields are nullable for OAuth/HTTP rows; `tool.call` requires tool/user/grant/client/args HMAC and is unique by request id. Writers use `AuditSink`; `record_required` joins a write effect's transaction. `tests/connector/test_audit_contract.py` inserts one valid row for every event type and rejects a duplicate tool call. Action-path uses this table after foundation and does not extend `agent_audit_log`.

Rollout is Chunk 0 → inert foundation → both Railway services configured with flags off and capacity recorded → OAuth AS off → resource server off → test-host proof → joint enable for `CONNECTOR_EARLY_ACCESS_USER_IDS` containing test users and Max. Emptying the list waits for the P1 buyer-tool stage of core Gate 1 §9. AS-off keeps public JWKS served; the resource retains last successfully fetched public keys on failed refresh and alerts, but a cold process without keys fails closed. On a compromised key, rotate while JWKS is reachable and require a successful verifier refresh; if it fails, use the resource kill switch because stale keys may still verify tokens.

## 4. Build chunks

Each chunk is a reviewable backend/frontend change with the named tests. All new files below are build targets. One migration is shared by Chunks 1–4; later chunks do not add schema. Action-path flags are `CONNECTOR_ACTION_PATH_ENABLED=false` and `CHECKOUT_DOMAIN_SERVICE_ENABLED=false` in `app/core/config.py` and deploy environments. A false connector flag returns the core hidden-tool response and makes pending web APIs unavailable; a false checkout flag preserves the pinned browser route. The global core/OAuth switches are additional floors.

| Chunk | Files and deliverable | Gate 1 acceptance |
|---|---|---|
| 1 — schema and request engine | `alembic/versions/20260926_001_connector_action_path.py` (`s_connector_action_path_001`), **new** `app/models/connector_action.py`, `app/services/connector_action_store.py`, `app/actions/connector_definitions.py`, `app/actions/registry.py`, `app/services/action_executor_service.py`, `app/services/connector_policy_floor.py`, `app/services/policy_engine_service.py` adapter only. Register all 29 rows; reserve idempotency and evaluate floor first; no handlers exposed. | AP1, AP2, AP4–AP7, AP14–AP16 |
| 2 — pending web decision | **new** `app/api/v1/endpoints/pending_actions.py`, `app/services/pending_action_service.py`, `app/schemas/pending_action.py`; wire router in `app/api/v1/api.py` (exact router inclusion path **UNVERIFIED**, inspect at build). **New frontend** `app/confirm/[id]/page.tsx`, `api/pending-actions.ts`, continuation rule in `lib/redirect.ts`. Same-session Confirm/Decline, reauth and exact terms. | AP3–AP8, AP16 |
| 3 — checkout domain and handoff | **new** `app/services/checkout_domain_service.py`, `app/models/checkout_handoff.py`, `app/api/v1/endpoints/checkout_handoffs.py`; adapt `app/api/v1/endpoints/checkout.py`, `app/schemas/order.py`, and `app/services/transaction_service.py` without changing `OrderService.create_order`. **New frontend** `app/checkout/h/[token]/page.tsx`, adapt `api/checkout.ts` and the existing listing Buy component identified in review. | AP9–AP13, AP15 |
| 4 — generated map, integration and release | **New** Pydantic models in `app/mcp/connector/schemas/`, `scripts/gen_connector_schemas.py`, checked-in `app/mcp/connector/schemas/generated/<tool>.json`, and `tests/connector_action/` fixtures and generated negative matrix; `app/actions/connector_definitions.py` owns registration and references those models, with no separate `app/actions/connector_schemas.py` schema copy. Core tool adapter imports the action registry and generated schemas and calls `execute_connector`; final Gate 3/Gate 4 integration. No new MCP route. | AP1–AP16, core A15/A17, joint E2E |

### 4.1 Chunk 1: data, idempotency, floor and dispatch

The migration creates four additive tables plus the nullable transaction idempotency claim. `connector_action_requests`: UUID `id` PK; `user_id` FK, nullable `org_id`, `oauth_client_id`, `grant_id`, `token_jti`, unique `trace_request_id` matching the core audit request id, `action_name`, `action_version`, `idempotency_key VARCHAR(255)`, `args_hash CHAR(64)`, `args_json JSONB`, `status` CHECK `executing|succeeded|failed|pending_review|confirmed|denied|expired`, `policy_decision`, `policy_source`, nullable `policy_rule_id`, redacted `result_json`, nullable `error_code`, `created_at`, `finished_at`; unique `(user_id,oauth_client_id,action_name,idempotency_key)`, indexes `(org_id,created_at)` and `(user_id,created_at)`. `pending_actions`: UUID PK, unique request FK, `user_id`, nullable `org_id`, `confirm_token_hash CHAR(64)`, `summary_json`, `summary_hash CHAR(64)`, `facts_hash CHAR(64)`, `status` CHECK `pending_review|confirmed|denied|expired|failed`, `expires_at`, `decided_at`, `decided_by_user_id`, `decision_session_hash`, `decision_ip`, `decision_user_agent`, `decision_auth_time`, `consumed_at`; CHECK terminal decision time except `expired`. `checkout_handoffs`: UUID PK, unique `token_hash`, `buyer_user_id`, nullable `buyer_org_id`, `listing_id`, `listing_version_id`, nullable `quote_id` UUID with **no FK until offers owns its table**, `price_cents_snapshot`, `currency`, licence/covenant/rider SHA-256 snapshots, `oauth_client_id`, request FK, `status` CHECK `open|consumed|expired|superseded`, nullable `consumed_order_id`, `expires_at`, `created_at`. The one-open index is `UNIQUE (buyer_user_id, listing_id, COALESCE(quote_id, '00000000-0000-0000-0000-000000000000'::uuid)) WHERE status='open'`. Quote input is excluded until P3. Supersede the existing open row and insert in one transaction; on unique conflict, reread and retry once under lock. Add one durable `checkout_provider_attempts` table: UUID primary key; unique `checkout_request_id` (the handoff uses a stable server-derived request ID); unique nullable `handoff_id` FK; `buyer_id`, unique `transaction_id` FK, unique `order_id` FK; provider idempotency key, immutable non-secret provider identity (effective `live` or `test` mode, Stripe account ID, key version and keyed fingerprint of the exact selected secret key), and exact provider request JSON/hash, immutable for paid attempts and NULL for no-provider paths; nullable provider session ID and URL; state CHECK `reserved|provider_unknown|finalised|failed|payment_conflict|refunded`; nullable conflict webhook event/session/payment-intent IDs, refund ID and conflict/refund timestamps; timestamps and last error code. Resolve ordinary checkout's effective key from `STRIPE_TEST_MODE` (`app/core/config.py:413-424,723-730`) and approved real synthetic checkout's forced test key (`app/core/stripe_async.py:34-47`) before reservation; record the actual selected account/mode/key identity, not the actor label. Never persist the secret key. Retain versioned key material only in the approved secret store; if the original identity/material is unavailable or differs, do not issue another create and leave the attempt `provider_unknown` for operator reconciliation. The attempt is the durable handoff-to-transaction association and owns one stable order/provider request across retries. A downgrade refuses to drop the three action tables or the attempt table when any has rows, and refuses to drop `transactions.checkout_request_id` or its partial unique index while any non-null claim exists. Operational rollback never downgrades.

Define the 29 Gate 1 §4.7 `ActionDefinition`s with `effect`, `open_world`, `scopes_required`, Pydantic input/output models, `idempotent_write`, `connector_exposed`, `binding`, and `review_expiry`, using defaults that leave legacy action definitions unchanged. Import-time lint fails on duplicate action names, missing write schema, non-idempotent exposed write, and a tool without exactly one action. `ConnectorPrincipal` is formed only from the verified token and live grant: user, nullable org/role, client, grant, stored profile, all scopes and JWT `jti`; never accept identity or profile in args. Validate `extra=forbid`, bounds and canonical shape before hashing `b'aim.connector.args.v1\x00' + canonical_json(args)` via `app/services/license_hashing.py:63`. Scope must satisfy all required scopes. Resolve every target by owner and org before policy; foreign ids return `NOT_FOUND`.

For writes, `INSERT ... ON CONFLICT DO NOTHING RETURNING id`, then lock the existing row `FOR UPDATE` if it lost. Scope is `(user_id,oauth_client_id,action_name,idempotency_key)`; same hash returns saved pending or terminal state, differing hash returns 409. Reservation failure fails closed. A result with `status=executing` is never committed; it exists only inside the transaction. Every retry is a new MCP call and gets its own `tool.call` audit row under that call's fresh request id, even though it reuses the saved action outcome and creates no second effect. Derive price, threshold, dispute, listing flags and ownership from locked DB rows, never caller fields. Apply the hard-coded floor **first**, then call DB policy; combine decisions by severity `DENY > REVIEW > APPROVE`, regardless of DB priority or scope. DB APPROVE cannot lift floor REVIEW/DENY. User preferences may tighten. Floor DENY for any connector-originated transaction or order creation, licence-acceptance recording, Stripe object creation/charge/transfer/payout, wallet mutation, payout or x402 offer/verification/settlement/journal/access-token effect; REVIEW for a handoff strictly above the buyer's configured limit, or $500 (50,000 cents) while unset; flagged publish, disputed delivery and outside-limit offer acceptance; `confirm_receipt` REVIEW always, and DENY when `transaction_id` is NULL. The binding set is Gate 1 §4.8 rows 11, 14, 21, 22, 25, 26 and 27: `create_checkout_handoff`, `confirm_receipt`, `publish_listing`, `unpublish_listing`, `mark_delivered`, `make_offer` and `respond_to_offer`. A binding action cannot be auto-authorized by `pre_authorized`; `execute_connector` has no such parameter. The current first-party `/actions/execute` bypass is separately tracked as T-2026-000852 and is not altered here.

APPROVE calls a handler using the caller transaction; no handler commits. Save effect, request outcome and `AuditSink.record_required(tool.call, ...)` in that transaction, then commit once. REVIEW commits request, pending row and required audit together, returning a URL. DENY commits request and required audit with actionable reason. On handler exception rollback all effects, then record a terminal failed request and required audit in a short transaction; same-key retry returns that failure. `_log_action` in `app/services/action_executor_service.py:737` receives `commit=False` for connector use only, preserving legacy behavior. External notifications use a transactional outbox keyed by request id; the precise existing outbox implementation at this SHA is **UNVERIFIED** and must be identified before enabling any handler that sends mail. The static call graph starts at every registered connector handler and follows imports/calls to forbidden sinks, including direct SQL/ORM inserts into `transactions`, `orders`, `license_acceptances` and payment/journal tables; dynamic calls and unknown money-service targets fail closed. The context-var runtime guard enforces the same boundary at service entry and write/DB adapters. Its named sinks are `TransactionService.initiate` and `initiate_agent` (`app/services/transaction_service.py:290,365`), `OrderService.create_order` (`app/services/order_service.py:662`), `record_license_acceptance` (`app/services/license_acceptance_service.py:187-239`), `TransactionService.create_checkout` and `settle` (`app/services/transaction_service.py:1417,1980`), all Stripe checkout/PaymentIntent/charge/transfer/payout/refund mutators, including calls outside `run_stripe` (`app/core/stripe_async.py:50-66`; checkout create at `app/services/transaction_service.py:1783-1802`; transfer at `app/services/order_money_service.py:1197`), all `WalletService` mutators (including `get_or_create_wallet`, `app/services/wallet_service.py:44`), and `dispatch_order_payout` (`app/services/order_money_service.py:1150`). It also forbids the entire `app.services.x402_service` money module, explicitly `X402Service.create_payment_offer`, `verify_payment`, `_generate_access_token`, and `_create_journal_entry` (`app/services/x402_service.py:96,223,712,720`); x402 on-chain verification, seller credit and journal writes cannot escape through a helper. These deny rules apply even when x402 or listing licences are configuration-off. Checkout-domain browser work enters only after connector context ends; a handoff handler cannot invoke that browser service.

For paid `payment_conflict`, extend `checkout_provider_attempts` with nullable immutable `refund_intent_id` UUID, `refund_idempotency_key`, `refund_request_json`/hash, original charge ID, and refund-intent creation time; make the refund key unique where non-null. Persist these together under the attempt lock before `Refund.create`, deriving the stable key from the attempt/transaction and binding it to exactly one full captured amount, currency, PaymentIntent and charge. Retain the intent, provider refund ID and event IDs through rollback; no second refund intent or key may be minted for a retry. The existing `Payment`, `Refund`, order-money state and finance journals remain the money authority rather than an attempt-only ledger (`app/services/refund_processing_service.py:512-525,528-582`; `app/services/order_money_service.py:847-909`).

### 4.2 Chunk 2: pending action and human decision

Default pending expiry is 24 hours, handoff REVIEW expiry one hour. Add `confirm_nonce BYTEA(32)` to `pending_actions` in `s_connector_action_path_001`; fill it with 256 random bits. Derive the 256-bit URL token as HMAC-SHA-256 of `pending_action_id || confirm_nonce` under the selected version of the dedicated backend-only `CONNECTOR_CONFIRM_URL_KEYS` ring; store its SHA-256 in `confirm_token_hash`, never the token. Persist `confirm_key_version` on each pending row and keep a backend-only versioned `CONNECTOR_CONFIRM_URL_KEYS` key ring. New rows use the active version; validation and same-key retry select the stored version and reconstruct the **identical** URL. Do not retire a version while any unexpired pending row names it; after expiry or terminal consumption, remove it only after the retention check. Missing stored key fails closed and reports the link unavailable without issuing a different token. URL is `https://ai.market/confirm/<id>?t=<token>`, fixed host. Store the exact validated args only in `connector_action_requests.args_json` while pending; show a server-rendered `summary_json` including actor/client, target and all binding terms, and hash it. Clear args and nonce on terminal transition; retain request identity/outcome at least 90 days. Expiry is enforced lazily on read and by a scheduled sweeper; the existing scheduler hook is **UNVERIFIED** and must be located before Chunk 2 is enabled. The single-use token is consumed only by a successful or terminal confirm/decline transition; after terminal it may return the saved status read-only, never re-execute. No GET changes state except a compare-and-set expiry update.

Web routes: `GET /api/v1/pending-actions/{id}?t=...`, `POST .../{id}/confirm` with `{token,csrf,summary_hash}`, `POST .../{id}/decline`, and owner-filtered `GET /api/v1/pending-actions?status=pending_review`. They accept only an authenticated first-party web session with CSRF and exact frontend Origin; reject MCP-audience OAuth bearer, API key and internal key even when the user id matches. Wrong user/token/org gets uniform 404. Confirm requires login within 15 minutes and 2FA re-verification for binding actions. Lock pending and request rows; `UPDATE ... WHERE status='pending_review'` is the only decision transition. Recheck grant, membership, role, target ownership, current facts and `facts_hash`; changed terms expire with `CHANGED`, while a newly DENY policy produces `failed`; both need a new connector request. Reevaluate floor and DB policy with `actor_kind=human_web`; a floor REVIEW is satisfied only by this verified human route and must never turn into connector-side APPROVE. Execute once in the locked transaction, set `confirmed`, write `action.confirm` via `AuditSink.record_required` in the effect transaction, and commit. Add `auto_commit: bool=True` to `OrderService.confirm_order` at `app/services/order_service.py:1843` and call it with false from this route; its existing `self.db.commit()` sites at `:1899` and synthetic early-return `:1926` must both be suppressed by `auto_commit=False` (`app/services/order_service.py:1899,1918-1927`), including any nested commit reached from this method; the caller alone commits the decision/audit unit. Transaction-less orders are DENIED before this method can reach `dispatch_order_payout` at `:1929-1930`. The `transaction_id` pre-check and runtime payout guard remain mandatory. Decline writes `denied` and `action.decline`. Both events go to `connector_audit_events`, whose `event_type` catalogue already contains them. Confirmation creates no new tool-call row: the connector attempt already has exactly one. Record session hash, IP, user agent and auth time only in pending evidence, with `binding_terms` carrying actor kind and approval evidence references, not raw secrets or free text.

The page says “Your assistant <verified client name> asks to…”, shows exact amount/licence/effect, expiry and “Only confirm if you asked for this”, with explicit Confirm and Decline controls and no auto-submit. Set `Cache-Control: no-store`, `Referrer-Policy: no-referrer`, and `frame-ancestors 'none'`. Login continuation must validate the exact internal `/confirm/<uuid>?t=<base64url>` form in `lib/redirect.ts:3,13-21`; reject external URL and extra parameters. The existing login resume at `app/login/LoginForm.tsx:51-52,93-94` is the first integration seam; verify other sign-in/2FA flows before release. `get_activity` reads the caller's owner-filtered `connector_action_requests`, `pending_actions`, and order/inquiry events, exactly as Gate 1 §4.7 row 2 requires; it returns terminal status without exposing the token. Test the full four-source read set and cross-org exclusion.

### 4.3 Chunk 3: one browser checkout and handoff

`CheckoutDomainService.preflight` is read-only and never commits. It checks published/listed version, not-own-listing, synthetic-purchase refusal, seller payout readiness, active-order guard, price/currency, licence block plus current seller acceptance, and delivery readiness. Workspace readiness uses `SellerWorkspaceDeliveryService.prepare_purchase` at `app/services/seller_workspace_delivery.py:156-179`; the existing `create_order` workspace helper checks workspace at `app/services/order_service.py:289-298`. For reference delivery, add a read-only validation against the listing's `source_delivery` and referenced object metadata; for Trust-Channel/vectorAIz, the liveness seam is **UNVERIFIED**, so return `blocked(DELIVERY_READINESS_UNKNOWN)` until a concrete read-only check is reviewed. No warning-based proceed. Human-readable block reason includes a web path. Handoff snapshots use `listing_license_block` at `app/services/license_acceptance_service.py:42-76`; snapshots are display/staleness evidence, never buyer acceptance.

`create_handoff` accepts listing id only in P1, reruns preflight, stores the open handoff and returns `https://ai.market/checkout/h/<token>` plus expiry (24 hours). It creates no transaction, order, licence acceptance, Stripe object, payment intent, wallet mutation, upload/download token or money movement. Supersede/consume by token hash under row lock. Above buyer limit is REVIEW; the unset limit uses Max's $500 platform default. The buyer can set their own limit on the website; exact existing buyer-limit field/UI is **UNVERIFIED**. If none exists at build time, implement that setting in a separately reviewed buyer-tool chunk before enabling configurable limits; the fixed $500 behavior is complete without it.

`start_checkout` is called **only** by the flag-on first-party browser POST. At `app/api/v1/endpoints/checkout.py:138-154` the baseline `get_current_user_flexible` accepts API credentials (`app/api/v1/endpoints/listings.py:85-203`); replace that dependency for the `CHECKOUT_DOMAIN_SERVICE_ENABLED=true` adapter with a dedicated web-session-only dependency before service invocation. It authenticates an interactive first-party session, requires exact configured frontend Origin and CSRF on the POST, and rejects user API keys, internal keys and MCP-audience OAuth bearer tokens even if they identify the buyer. With a handoff, require `web_user.id == checkout_handoffs.buyer_user_id`; wrong owner is a uniform 404. The flag-off branch retains its current dependency and behavior. The adapter supplies web-derived user/session/IP/UA and never trusts a submitted `buyer_id`, `authority_confirmed` without verified session, or connector attribution. The connector never calls `start_checkout`.

For a new request, the flag-on service re-runs read-only preflight and snapshot comparison before reserving; a same-request retry first resolves the durable attempt and its frozen accepted terms, without rerunning mutable listing preflight. Direct web requires a browser-generated UUID `checkout_request_id`; handoff checkout derives one stable server-side from its handoff ID, so a retry never changes it. Persist `transactions.checkout_request_id UUID` with unique `(buyer_id,checkout_request_id) WHERE checkout_request_id IS NOT NULL` in `s_connector_action_path_001`, add the nullable ORM field to `app/models/transaction.py:133-188` and request field to `app/schemas/order.py:104`. Existing clients without the field stay on the flag-off adapter until updated. The durable `checkout_provider_attempts` row from §4.1 is uniquely claimed by that request ID and, when present, the handoff ID; it also records free/non-provider checkout as a directly finalised attempt so retries and handoff association remain durable. Under a short DB transaction, lock the open handoff on first use (or the consumed handoff and attempt on replay); create or reuse exactly one transaction via `TransactionService.initiate(auto_commit=False)` (`app/services/transaction_service.py:290-363`), exactly one order via `OrderService.create_order(auto_commit=False)` as reached at `app/services/transaction_service.py:1536-1558`, and the web licence acceptance under S1735. Freeze the complete Stripe request (amount, currency, line items, success/cancel URLs and order/transaction metadata), the §4.1 non-secret identity of the actual selected Stripe account/effective mode/key version/fingerprint, and `checkout:<transaction_id>` idempotency key in the attempt. In that same reservation transaction, compare-and-set any `open` handoff to `consumed` and set `consumed_order_id` to the constructed order; commit the transaction/order link, acceptance, consumed handoff and attempt together **before** provider I/O. This directly implements Gate 1 §4.6 `specs/BQ-CONNECTOR-ACTION-PATH-GATE1.md:151` and its binding §9 constructor-transaction handoff-consumption invariant at `:249`. A consumed handoff is read-only while its provider attempt is processing or reconciling; it cannot expire or be superseded. Same-key retry locks and reuses this durable transaction, order and exact provider request; changed submitted acceptance or request parameters fail rather than creating a second attempt, while later listing changes cannot invalidate the already accepted reservation. This extraction of the local prepare phase from baseline `create_checkout` is required because its Stripe call at `app/services/transaction_service.py:1783-1805` currently precedes its `:1813` commit and rolls local writes back on provider error.

Release the row lock before a finite-timeout Stripe call. Call `stripe.checkout.Session.create` with the persisted parameters and the pinned key passed explicitly as `api_key`; `run_stripe` forwards kwargs (`app/core/stripe_async.py:50-66`). On success, reacquire the attempt lock, verify the same provider session/order/transaction and frozen accepted terms, store the session/result, mark `finalised` and commit one finalization unit; the handoff was already consumed in the constructor transaction. On timeout, connection loss or process death after possible provider acceptance, persist or recover `provider_unknown`; never roll back the committed reservation, reopen the consumed handoff, or create new identifiers. A retry or reconciler verifies the persisted provider account, effective mode, key version and fingerprint against available original key material before any Stripe call; it uses that exact key, the same idempotency key and exact parameters, retrieves the known provider session if one was saved, and finalises only when provider outcome is known. If current configuration differs, recover only through the persisted versioned key whose fingerprint, account and mode match; if that key is unavailable or mismatched, remain `provider_unknown` and alert an operator. Never select the currently configured replacement key for another create. After the provider's idempotency retention window, never issue another create unless provider reconciliation proves no session exists; otherwise remain `provider_unknown` for operator resolution. Only a synchronous, definitive provider rejection before session creation that guarantees no session exists may mark `failed`; a timeout, lookup miss, eventual-consistency observation or elapsed idempotency window is not such proof. In one finalization transaction, mark the attempt `failed`, cancel the `checkout_pending` transaction and its created order, and retain the acceptance, consumed handoff, request claim, attempt and error as history. The consumed link renders a terminal checkout-failed status; it is never reopened or reused. Same-request retries return that failure; a subsequent checkout needs a new request ID and, for connector origin, a new handoff. The cancelled order is excluded by the baseline active-order guard (`app/api/v1/endpoints/checkout.py:99-113`); `checkout_pending → cancelled` is permitted at `app/models/transaction.py:87-90`. Never commit `failed` without those local transitions, and keep any uncertain outcome `provider_unknown` for reconciliation or operator resolution. Normal paid transitions are `reserved → provider_unknown → finalised/failed` and direct `reserved → finalised/failed`. A validly signed late `checkout.session.completed` for an attempt already `failed` with a cancelled order contradicts no-session proof. Before the baseline order-status refusal or ordinary duplicate return, join the event by frozen order/transaction metadata and session to the attempt, lock it and atomically CAS `failed → payment_conflict`, persist the signed event/session/PaymentIntent IDs and an append-only transaction event, and alert an operator. Keep the order and transaction cancelled, block fulfillment and new money effects, and expose a distinct conflict response rather than `already_processed`. After validating amount, currency, account/mode, payment binding and captured funds, commit the §4.1 refund intent before provider I/O; `Refund.create` uses only its frozen original account/effective mode/key identity, exact retained key material, request and idempotency key. Persist the returned refund ID, but keep `payment_conflict` until signed refund confirmation and canonical finance reconciliation commit. If verification or refund fails, leave `payment_conflict` with an operator-owned alert and retry/reconcile the same refund identity; do not create a second order or reinstate/fulfill the cancelled one. Redelivery of the same signed event is idempotent and reports conflict/refunded, not an ordinary duplicate. Preserve the baseline `orders.status != "created"` refusal for completed sessions at `app/api/v1/endpoints/webhooks.py:1572,1634-1643`, the `status = 'created'` PaymentIntent guard at `:1697`, and `_repair_canonical_payment` paid/pending-only guard at `:1509`; these prevent payment binding or fulfillment on a cancelled order but the dispatcher at `:780-794` must route a paid conflict to this explicit reconciliation before returning duplicate success. Do not hold a database row lock across provider I/O. Web and handoff paths use the same state machine and the same stable transaction/provider attempt.

Extend the canonical order-money/refund protocol with one conflict-aware owner for this attempt, keyed by its frozen order/transaction/PaymentIntent/charge and refund intent. At `app/api/v1/endpoints/webhooks.py:956-970`, after signature and durable event admission but **before** `admit_order_refund_sync`/`process_order_refund`, dispatch signed `charge.refunded` for a conflict-owned PaymentIntent (including redeliveries) to this owner; never let it fall through to the baseline processor. This is necessary because baseline PI resolution at `app/services/order_money_service.py:732-749` can reject an unbound cancelled order, while baseline refund effects at `app/services/refund_processing_service.py:437-450,512-525,582-603` can rewrite its status. The owner verifies the signed event, frozen provider identity and binding, and authoritative PI/charge/refund facts using only the retained original key. Missing or rotated account/mode/key material, uncertain provider facts or mismatched refund ID leave `payment_conflict` operator-owned; a replacement context is never used. Extend `ensure_order_capture` and `apply_order_refund_effects` narrowly for this locked conflict case: in one database transaction, bind the original PI/charge to the existing order, create or reconcile the canonical `Payment` capture, `Refund` and capture/refund finance journals exactly once, mark the signed event applied, store the refund ID and CAS `payment_conflict → refunded` only after a confirmed full refund. Preserve `orders.status='cancelled'` and `transactions.status='cancelled'`, including on redelivery; do not run the normal refund status rewrite or paid fulfillment transition. Keep the existing order-money locks, refund-ID uniqueness, applied-event markers and journal idempotency (`app/services/refund_processing_service.py:437-450,512-525,528-603`). Explicitly refuse fulfillment, `dispatch_order_payout`/`reserve_order_payout` and any Stripe transfer for this conflict throughout refund and retry; enforce the refusal in the payout eligibility/dispatch path (`app/services/order_money_service.py:847-909,1007-1045,1150-1190`) and settlement admission (`app/services/settlement_service.py:57-65`), not only by incidental cancelled statuses. A signed refund event arriving before the API response is retained and reconciled against the same durable intent; provider return and event redelivery converge on the same refund and finance rows. No database lock spans provider I/O.

Refactor `TransactionService.create_checkout` (`app/services/transaction_service.py:1417`) into reusable local prepare, provider call and local finalize stages; keep its public wrapper and default behavior for legacy callers. Add `auto_commit: bool=True` to the wrapper and its local stages; the domain service calls only the stages with `auto_commit=False`, never the whole wrapper across the provider network call. Gate **every** commit: workspace-free `:1654`, reference-free `:1690`, other-free `:1693`, synthetic paid/fulfilment `:1758`, and Stripe `:1813`. Pass `auto_commit=False` to nested `OrderService.deliver_reference_order` at `:1660-1664` (baseline passes true); workspace `fulfill_workspace_purchase` already receives false at `:1653` and `:1751`. Audit any further nested commit from fulfilment, event or order helpers before enabling; none may commit in the domain-controlled path. For free, workspace, reference and mock-synthetic paths, the domain service owns exactly one local final commit, including the handoff transition and directly finalised attempt row; no provider call is sent. Preserve their baseline effects and statuses. Approved real synthetic paid checkout follows the paid provider saga with immutable actual test account/mode/key identity; mock synthetic checkout alone has no provider call (`app/services/transaction_service.py:1487-1496,1707-1758,1772-1802`; `app/core/stripe_async.py:34-47,50-66`). Retry must preserve that identity and must never switch Stripe account, mode or key. `ENABLE_CANONICAL_TX=false` remains on the exact legacy browser branch.

The browser handoff page reuses the existing licence + Buy screen and submits the same `LicenseAcceptanceInput` fields, including licence/covenant/rider hashes and `authority_confirmed=True`, typed name, web channel, IP and user agent. Preserve S1735's refusal matrix in `app/services/order_service.py:827-1027` and its acceptance recording at `app/services/license_acceptance_service.py:187-242`; no acceptance is inferred from a handoff or LLM response. Preserve canonical browser, legacy browser, MCP tool, MCP REST and agent/wallet order creator guarantees under flag-off. The latter legacy MCP purchase paths are left for T-2026-000853 / surface retirement; no connector action calls them. For a new checkout, web and connector-originated handoff converge on the same transaction/order shape, with only attribution (`oauth_client_id`, `handoff_id`) differing. Free listing, workspace, reference delivery and existing paid Stripe behavior get parity fixtures.

## 5. Tool → action → service → role → licence → effect map

This is the complete Gate 1 §4.7 catalogue. Definitions and schemas land here; P1–P3 handlers become callable only in their owner BQs and after the matching profile/scope gate. `A` means floor APPROVE, `R` REVIEW, `D` DENY. Every write requires an idempotency key. Every row also requires all-of scope visibility at list, call and executor. `member` means active membership in the grant org, or a personal grant whose user has no active org membership. Existing seams cited in §2 and below are pinned to backend `445f43af`; **new** service names are build targets.

| Tool → action | Service or owner | Role rule | Licence rule | Floor and side effect |
|---|---|---|---|---|
| `get_my_account` → `aim.account.get` | core account / `app/services/action_executor_service.py:569-576` | member | none | A; read only |
| `get_activity` → `aim.activity.list` | new owner-filtered read over `connector_action_requests`, `pending_actions`, order/inquiry events (Gate 1 §4.7 row 2) | own user/org | none | A; read only |
| `search_listings` → `aim.listing.search` | listing search (`app/api/v1/endpoints/listings.py:250`) | authenticated | show code/badge | A; read only |
| `get_listing` → `aim.listing.get` | detail (`app/api/v1/endpoints/listings.py:410`) | authenticated | full public block/hashes; no acceptance | A; read only |
| `ask_allai` → `aim.allai.query` | marketplace query (`app/api/v1/endpoints/mcp_marketplace.py:386-405`) | authenticated | none | A; LLM cost only |
| `ask_seller` → `aim.inquiry.create` | inquiry create (`app/api/v1/endpoints/inquiries.py:143-144`) | buyer member | none | A, D on rate cap; inquiry and outbox email |
| `list_inquiries` → `aim.inquiry.list` | inquiry owner list (`app/api/v1/endpoints/inquiries.py:423-424`) | buyer/seller party | none | A; read only |
| `reply_to_inquiry` → `aim.inquiry.respond` | inquiry respond (`app/api/v1/endpoints/inquiries.py:506-507`) | inquiry party | none | A; message/outbox email |
| `post_data_request` → `aim.request.create` | request create (`app/api/v1/endpoints/requests.py:109-110`) | buyer member | none | A; public request |
| `list_data_requests` → `aim.request.list` | request list (`app/api/v1/endpoints/requests.py:125-126`) | own or public matching | none | A; read only |
| `create_checkout_handoff` → `aim.checkout.handoff.create` | new `CheckoutDomainService.create_handoff` | buyer, not seller | current seller acceptance; buyer accepts only on web | R above limit; D blocked; handoff row only |
| `list_orders` → `aim.order.list` | order mine (`app/api/v1/endpoints/orders.py:149`, **UNVERIFIED** current line) | owner-filtered; seller anonymised | licence record link | A; read only |
| `get_delivery_handoff` → `aim.order.delivery_handoff` | new URL-only adapter | buyer of order | web issuance retains `require_active_license_acceptance` (`app/services/license_access_service.py:90`) | A; web URL only |
| `confirm_receipt` → `aim.order.confirm` | `OrderService.confirm_order` (`app/services/order_service.py:1843`) only from confirmed web decision | buyer of order | active acceptance | R always; D no transaction id; order completion, scheduled settlement only |
| `rate_order` → `aim.order.rate` | rating create (`app/api/v1/endpoints/ratings.py:47-48`) | buyer of completed order | none | A; public rating |
| `get_seller_readiness` → `aim.seller.readiness` | read-only `get_seller_payout_readiness` (`app/services/seller_setup_service.py:91-120`); the assert variant at `:123-129` belongs to checkout | seller member | covenant/terms shown | A; read and web link |
| `create_listing_draft` → `aim.listing.create` | listing create (`app/api/v1/endpoints/listings.py:677-678`) | seller member | licence choice, no covenant acceptance | A; private draft |
| `update_listing_draft` → `aim.listing.update` | listing update (`app/api/v1/endpoints/listings.py:707-708`) | draft owner | draft licence may change | A; private edit |
| `get_sample_upload_url` → `aim.listing.sample_upload_url` | sample service **UNVERIFIED** | draft owner | licence chosen | A; scoped upload URL |
| `enrich_listing` → `aim.listing.enrich` | enrichment service **UNVERIFIED** | draft owner | none | A; draft fields |
| `publish_listing` → `aim.listing.publish` | publish (`app/api/v1/endpoints/listings.py:639,655-667`) | active seller and owner | covenant accepted on web; otherwise web link | R if flagged; public listing |
| `unpublish_listing` → `aim.listing.unpublish` | unpublish (`app/api/v1/endpoints/listings.py:624-625`) | owner | none | A; hide and supersede handoffs |
| `list_my_listings` → `aim.listing.list` | mine (`app/api/v1/endpoints/listings.py:348-349`) | owner-filtered | none | A; read only |
| `respond_to_request` → `aim.request.respond` | response create (`app/api/v1/endpoints/requests.py:413-414`) | seller member | none | A; proposal |
| `mark_delivered` → `aim.order.mark_delivered` | `OrderService.initiate_delivery` (`app/services/order_service.py:1326`) | seller of order | active acceptance | R if disputed; D when file path required; notification/window |
| `make_offer` → `aim.offer.make` | `bq-negotiation-offers` **not built** | party | pin variant | A in limits, else R; offer term |
| `respond_to_offer` → `aim.offer.respond` | `bq-negotiation-offers` **not built** | party | pin variant | A in limits, else R; quote only |
| `withdraw_offer` → `aim.offer.withdraw` | `bq-negotiation-offers` **not built** | author | none | A; withdraw term |
| `list_offers` → `aim.offer.list` | `bq-negotiation-offers` **not built** | owner-filtered | none | A; read only |

`get_delivery_handoff` is URL-only and has **no P0 dependency** on `bq-buyer-download-unification-s1711` (Gate 0, not built); no download token, signed URL, raw bytes, cross-door state merge or issuance occurs in this BQ. A P1 buyer end-to-end claim that spans the unified download doors is blocked on S1711; until it passes, keep that claim and any unified-download handler out of P0 and behind the P1 gate. Offers rows 26–29 similarly have schemas/definitions only until `bq-negotiation-offers` lands; `quote_id` remains nullable with no FK or accepted input until then.

## 6. Schema generation, negative proof and acceptance IDs

Place each action's Pydantic input/output models in `app/mcp/connector/schemas/`. `app/actions/connector_definitions.py` imports those models into the registry; `scripts/gen_connector_schemas.py` generates and checks in `app/mcp/connector/schemas/generated/<tool>.json` for every tool, and core `tools/list` serves those exact files. Generate JSON Schema 2020-12 with `additionalProperties:false`, field `maxLength`/bounds and enums. Every write requires `idempotency_key` matching `^[A-Za-z0-9_-]{16,128}$`; validate it before the reservation. There is no hand-maintained schema copy in `app/actions`. CI fails on wrong output path, wrong schema version, missing `additionalProperties:false`, drift, unknown scope, missing effect/annotation, missing write idempotency, duplicate name or a profile listing a disabled tool. Generate negative cases for each applicable row: missing one of multiple required scopes, wrong org or owner, mismatched role, stale/missing licence or covenant, different-args same idempotency key, missing/short/overlong/invalid-character idempotency keys for every write, arbitrary extra input fields, and attempts to route to every money sink in §4.1 (including transaction creation, acceptance recording and x402). Core `tools/list` and `tools/call` use the same action catalogue and `scopes_required ⊆ principal.scopes`. Negative tests assert `NOT_FOUND` for foreign IDs, `insufficient_scope` for visible under-scoped tools, hidden-tool response for disabled ones, and no side effects. The first six tools are not automatically exposed by this BQ; exposure follows core/buyer owner gates.

| ID | Gate 1 acceptance and exact test target |
|---|---|
| AP1 | 29 unique registered tool/action rows, checked-in JSON Schema 2020-12 at `app/mcp/connector/schemas/generated/<tool>.json`, `additionalProperties:false`, bounds and required write idempotency pattern `^[A-Za-z0-9_-]{16,128}$`; fail missing, short, overlong and invalid keys: `tests/connector_action/test_registry.py::test_29_tools_mapped_and_writes_idempotent`, `tests/connector_action/test_schemas_generated.py::test_generated_schemas_match_registry`. |
| AP2 | Code floor evaluated before DB policies; DB APPROVE at every scope/priority cannot lift REVIEW/DENY, caller price/flags ignored: `tests/connector_action/test_policy_floor.py::test_db_approve_cannot_lift_floor`. |
| AP3 | REVIEW rejects OAuth bearer, API key and internal key on all web routes; same-user web session, CSRF, Origin, recent auth and binding 2FA required: `tests/connector_action/test_pending_auth.py::test_review_rejects_non_session_auth` and `::test_confirm_requires_fresh_session_and_binding_2fa`. |
| AP4 | 20 concurrent connector retries/confirm posts run the handler once on real Postgres; same-key final response and changed-args 409: `tests/connector_action/test_idempotency_pg.py::test_concurrent_retries_and_changed_args`, `tests/connector_action/test_pending_pg.py::test_concurrent_confirm_executes_once`. |
| AP5 | Handler exception rolls back effect and required audit, then terminal failure recorded; audit insert failure prevents write commit: `tests/connector_action/test_atomicity_pg.py::test_forced_failure_rolls_back_effect_and_records_failed` and `::test_required_audit_failure_rolls_back_write`. |
| AP6 | Confirm/decline/expiry/stale facts and revoked grant use CAS, exact stored args, URL one use; key rotation with a pending row and same idempotency key reconstructs the identical URL from its stored key version; no GET execution: `tests/connector_action/test_pending_pg.py::test_decision_cas_and_stale_rechecks`, `::test_get_never_executes`, `::test_key_rotation_replays_same_url`. |
| AP7 | Static call graph from each handler and runtime connector context reject direct transaction/order/licence-acceptance inserts, `TransactionService.initiate`/`initiate_agent`/`create_checkout`/`settle`, `OrderService.create_order`, `record_license_acceptance`, all Stripe and wallet mutators, `dispatch_order_payout`, and x402 `create_payment_offer`/`verify_payment`/`_generate_access_token`/`_create_journal_entry`; parameterize each sink and assert the runtime call raises with no row, provider object, credit or journal committed; transaction-less receipt DENY: `tests/connector_action/test_no_money.py::test_static_and_runtime_money_guards`, `::test_transactionless_receipt_denied`. |
| AP8 | `confirm_receipt` always returns REVIEW in connector call at any DB priority; human confirm only for transaction-linked order: `tests/connector_action/test_receipt_review.py::test_receipt_always_needs_human`. |
| AP9 | Handoff is URL only with no tx/order/acceptance/Stripe/wallet; unready workspace/reference/unknown source blocked: `tests/connector_action/test_handoff.py::test_handoff_url_only_and_source_preflight`. |
| AP10 | Two simultaneous quote-less handoffs leave one open, supersession and consume single-use on real Postgres: `tests/connector_action/test_handoff_pg.py::test_concurrent_quoteless_handoffs_one_open`, `::test_superseded_and_consumed_once`. |
| AP11 | Web and connector-handoff checkout have equal transaction/order/licence shapes except attribution, using channel web and human acceptance hashes/authority; free, workspace, reference and mock-synthetic fixtures assert no nested commit and exactly one domain commit; paid fixtures, including approved real synthetic paid with its actual test account/mode/key identity, assert only the planned durable reservation commit and finalization commit, with no nested commit: `tests/connector_action/test_checkout_parity.py::test_web_and_handoff_produce_identical_orders`. |
| AP12 | All six executable paths (canonical web, legacy web, MCP tool, MCP REST, agent-key and wallet; Gate 1 §2.4's five creator families) preserve prior guards under both flags off, including S1735 refusal matrix and free/workspace/reference paths: `tests/connector_action/test_flag_matrix.py::test_six_order_paths_and_delivery_sources`. |
| AP13 | On real Postgres, fault injection after reservation commit proves order construction/linking, acceptance, `checkout_handoffs.status='consumed'` with `consumed_order_id`, and the provider attempt committed atomically. Provider acceptance then connection loss and process restart retains that consumed handoff and stable `reserved/provider_unknown` attempt; retry/reconciler preserves transaction ID, order ID, exact provider parameters, idempotency key and actual account/effective mode/key version/fingerprint, yielding exactly one session/order under unchanged identity. Rotate the configured key/account or flip `STRIPE_TEST_MODE` after a fault-injected timeout: assert no second `Session.create` or provider session, with recovery only using the exact original key identity or an alerted operator-owned `provider_unknown` when it is unavailable. Concurrent expiry or supersession cannot detach the claimed order. A synchronous definitive pre-creation rejection atomically fails the attempt, cancels transaction and order, retains acceptance and consumed handoff history, excludes the cancelled order from the active-order guard, returns failure on same-key retry, and permits a new request/handoff. On real Postgres, deliver signed fake/provider events for the full `checkout.session.completed → payment_conflict → Refund.create → charge.refunded → refunded` sequence: assert atomic conflict/event/alert admission, committed refund intent and one stable idempotency key before provider I/O, exactly one provider refund and durable refund ID, and exactly one canonical `Payment` capture, `Refund`, capture/refund journal reconciliation and applied signed event. Deliver `charge.refunded` again and assert idempotent event/finance state; repeat the paid completed event and `payment_intent.succeeded` against the baseline `status='created'` guards. Throughout, assert unchanged cancelled order/transaction, no second order or fulfillment, and explicit refusal by settlement, `dispatch_order_payout` and Stripe transfer. Rotate the configured account/key or flip effective mode before refund creation and before signed refund-event reconciliation: only the retained original identity/material may be used; if unavailable or mismatched, assert no replacement-key provider call and an alerted operator-owned `payment_conflict` with its original intent/key and cancelled states intact. Fault inject provider return/event ordering, process restart and event redelivery to prove one refund and one ledger application. Include approved real synthetic paid/test-key retry and mock-synthetic/no-provider fixtures; stale terms before reservation fail without consumption: `tests/connector_action/test_checkout_provider_retry.py::test_timeout_retry_reuses_provider_attempt`, `::test_key_rotation_or_mode_change_fails_closed`, `::test_definitive_rejection_cancels_local_claims`, `::test_late_completed_session_refund_conflict`, `::test_stale_terms_preserve_handoff`. |
| AP14 | One `tool.call` per connector attempt; `action.confirm`/`action.decline` rows, required audit in write transaction, no `agent_audit_log` extension/free text: `tests/connector_action/test_audit_contract.py::test_tool_and_web_decision_rows`. |
| AP15 | Flags-off/on rollback retains all three action tables, provider-attempt records, non-null direct-web `transactions.checkout_request_id` claims and ≥90-day outcomes; downgrade refuses when any of these contains data, including a transaction-claim-only database: `tests/connector_action/test_migration_pg.py::test_nonempty_downgrade_refused`, `tests/connector_action/test_rollback_pg.py::test_rollback_retains_history_and_replays_old_key`. |
| AP16 | Generated per-tool foreign-org, scope, role, licence and every §4.1 forbidden money effect (including a hypothetical x402 handler that floor-DENYs and fails static analysis) matrix; browser no-store/no-referrer/no-frame, safe continuation and flag-on checkout auth rejects API key, internal key, MCP bearer, wrong Origin, missing CSRF and wrong handoff owner with no transaction, order, acceptance or Stripe object: `tests/connector_action/test_negative_generated.py::test_each_tool_negative_matrix`, `tests/connector_action/test_pending_auth.py::test_browser_response_headers_and_continuation`, `tests/connector_action/test_checkout_auth.py::test_flag_on_rejects_non_web_and_wrong_owner`. |

## 7. CI, deploy, rollback and Gate 4

Each backend chunk runs its named `python -m pytest tests/connector_action/...` targets against a real PostgreSQL database for row locks, unique indexes and migration checks, plus `python -m pytest tests/connector` for the shared audit/contract suite. Run `alembic heads` and upgrade/downgrade on an empty scratch database in Chunk 1; test refusal on a populated scratch database. Run the repository backend CI suite after Chunk 4; the exact CI command/runner at `445f43af` is **UNVERIFIED** and the chunk PR must record it. Frontend Chunks 2–3 run `npm test -- pending-actions`, `npm test -- checkout-handoff` (test filenames chosen in their PRs), then `npm run lint`, `npm run typecheck`, `npm run build` (`package.json:8-12`). Run the existing S1735 buyer checkout/licence tests and legacy MCP/agent checkout tests in the flag matrix. Do not treat a synthetic database as proof of provider idempotency.

Deploy the additive migration from backend web after core foundation; leave both action flags false. Deploy checkout service code with `CHECKOUT_DOMAIN_SERVICE_ENABLED=false`, then stage its browser path and record parity and checkout success versus the preceding seven days before production web enable. Deploy pending/action code off; test-host enable after joint core/OAuth enable. Only then enable `CONNECTOR_ACTION_PATH_ENABLED=true` for allowlisted test users; core's tool profile still controls visibility, so P0 remains account-only until the buyer-tool BQ. Before each enable, verify the deploy SHA, migration head, flags, audit grants, connected app and pool capacity. Rollback flips action-path and checkout flags false; retain provider attempts and direct-web checkout claims, expire open pending actions with `ROLLBACK`, show an expired/restart message for open handoffs and a read-only processing or terminal status for consumed handoffs, keep request/pending/handoff history and connector audit rows. An old key after re-enable must return its recorded result. Never run the downgrade in operational rollback.

Gate 4 receipt must include exact backend/frontend/deploy SHAs, foundation/action revision IDs, real-Postgres concurrency and rollback outputs, all AP1–AP16 test results, core/OAuth joint identity and key-isolation receipt, flag states and early-access list membership (identifiers redacted), generated schema digest, static/runtime money guard outputs, audit `request_id` joins with one tool row and web decision rows, S1735 acceptance hashes and `authority_confirmed`, provider idempotency trace, baseline/after checkout success, and a browser trace: Claude/ChatGPT test buyer requests a handoff over threshold → receives REVIEW link → same user signs in and confirms → browser accepts licence and pays → one order/transaction → same-key retry returns the final status. Record an under-threshold URL-only handoff, declined/expired/stale confirmation, transaction-less receipt DENY, wrong-org 404 and flag-off behavior. Live vendor and payment proof remains **UNVERIFIED** until Gate 4 executes; do not mark it passed from this document.

## 8. R1–R3 review disposition and open decisions

R1 findings from GLM, DeepSeek and CC are incorporated in the §4/§6 design and AP1–AP16 targets, including the money guard, web-only checkout, durable provider attempt, nested-commit control, downgrade retention, schema generation, receipt and activity rules. R2 GLM Finding 1 is addressed by constructor-transaction handoff consumption (§4.3; AP13), Finding 2 by atomic definitive-failure cancellation and retained history (§4.3; AP13), and Finding 3 by separating mock-synthetic from approved real synthetic paid checkout and freezing its Stripe mode (§4.1/§4.3; AP11/AP13). R2 DeepSeek's three citation nits are folded in §2/§4.1/§4.3; R2 CC's stale-provenance nit is closed by this section. R3 GLM MEDIUM 1 is addressed by persisted actual key/account/mode identity and rotation fail-closed AP13 proof; MEDIUM 2 by a synchronous-only no-session proof, explicit `failed → payment_conflict → refunded` reconciliation and signed late-webhook AP13 proof. R3 DeepSeek NIT 1 is addressed by naming the pinned `webhooks.py:1572,1697` order-status guards while retaining explicit paid-conflict handling; NIT 2 by the Gate 1 §4.6 `:151` citation. These are candidate dispositions, not a new Council verdict. R2 reviews: `/Users/max/council/glm/response-20260926-133849-580942.md`, `/Users/max/council/deepseek/response-20260926-133851-006331.md`, `/Users/max/council/cc/response-20260926-133852-422776.md`. R3 reviews: `/Users/max/council/glm/response-20260926-141244-541082.md`, `/Users/max/council/deepseek/response-20260926-141247-579974.md`.

Open for Max: none on the fixed $500 default, always-REVIEW receipt, or P0/P1 boundary; these are adopted decisions. Build-time evidence still needed: identify the current listing Buy component, existing outbox/sweeper and provider-attempt seams, buyer-limit persistence/UI, reference/Trust-Channel readiness checks, and the actual test/production deployment state. If any seam cannot be verified, keep its handler disabled and mark the corresponding Gate 4 row **UNVERIFIED** rather than guessing.
