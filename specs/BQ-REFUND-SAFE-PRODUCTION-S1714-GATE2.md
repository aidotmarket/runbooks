# Gate2 — refund-safe production release, S1714

Owner Mars. BQ-REFUND-SAFE-PRODUCTION-S1714. Gate2 unanimously accepted on ee94bb25aa630d5e45c9dd0865e8636052787bb7; section13 records final implementation clarifications and proof obligations. This is build authority only, not Gate3 or deployment acceptance. Gate1 unanimously APPROVED_WITH_MANDATES on a016107478d5b4fe48b6d30d92e8360ef3ab5c53; decision/mandates recorded at1d20011fe56ed34c4478e3524a018b1ad11e21de in sibling Gate1 section9. Product builder MP only. Environment implementation may be Mars-authored; both exact candidates require unanimous Gate3.

## 1. Outcome, authority, boundaries

A refunded or revoked order cannot acquire new seller-payout ownership. Refund admission competes with payout through the same order lock. One atomic effects transaction makes finance, human/agent transaction, order/access, per-refund spend and event completion converge. Exactly one succeeded-refund aggregate drives full/partial state. A late external refund after payout ownership is a recorded reconciliation exception, not a claim that payout was prevented. No automatic reversal.

Max's supplied S1713 plan section4 requires settlement to refuse refunded/revoked orders (#4067). Max directed Mars to follow that plan and subsequently authorized all necessary production dependencies in Event99af61fd-db46-42fd-a18d-1d0698e457d1 (bus4096). This is the policy-owner acceptance for strict all-revoked exclusion requested by CC RM3; no bare-revoked-pays exception. Existing completed Transfers are preserved. Refunds involving partial amounts retain access unless independently revoked; automatic payout is refused for any positive succeeded refund total, because a new prorated fee/payout policy is outside this change.

Unrelated credit-grant logic, metering payouts, general dispute UX, fresh real-money first sale and buyer-download redesign remain excluded. Narrow guards preventing an old payment/dispute writer from erasing refunded state are included. Run25's source, runtime, identity and receipt remain protected until its approved existing lifecycle completes.

## 2. Exact baseline and chunk manifest

Product baseline aidotmarket/ai-market-backend@d766b8e4e66d7803ebe48f8a3132521abbebe957. Environment baseline aidotmarket/money-path-test-environment@c6b8770e69514035b2c78f0433f86209b4cefe0f. Run25 uses03d3f30acf854537edd76fdb19e1cf521b577c9a/bothpins519b1d5893dc410392cc66051dad0902572dcef9, never these evolving worktrees. Product build branch build/bq-refund-safe-production-s1714; isolated checkout required.

Chunk A, MP, product:
- NEW app/models/order_money_state.py — order-bound payout authority.
- EDIT app/models/finance.py — refund admission fields on existing StripeEvent and applied-effect fields on Refund.
- NEW app/services/order_money_service.py — shared locks, eligibility, reservation, dispatch and reconciliation.
- NEW app/services/refund_processing_service.py — admission, provider validation, atomic refund effects and protocol readiness.
- NEW app/services/s1681_refund_test_admission.py — exact persisted N authority and TEST predicate; no general synthetic bypass.
- EDIT app/api/v1/endpoints/webhooks.py — refund-only admission/dispatch/error path, existing suppression seam, dispute writers.
- EDIT app/services/billing_service.py — ordinary canonical order capture branch, refund route before generic finance processing, and non-regressing payment replay.
- EDIT app/services/finance/engine.py — exact order capture/refund/Transfer journal methods and deterministic downstream account locks; unrelated template behavior unchanged.
- EDIT app/services/order_service.py — confirm/legacy payout, revoke and dispute writers route through authority.
- EDIT app/services/transaction_service.py — transactional transition helper and refund/confirm/settle guards.
- EDIT app/services/settlement_service.py and app/tasks/settlement_scheduler.py — common authority, refusal/reconciliation results and rollback before retry bookkeeping.
- EDIT app/services/finance/payment_service.py — dormant refund branch refuses direct use and names accepted processor; no app caller exists at baseline.
- EDIT app/models/transaction.py and app/models/order.py — explicit supported refund transitions/order partial-refund enumeration; no new public transaction status.
- EDIT app/core/config.py — ORDER_PAYOUT_DISPATCH_ENABLED, default false.
- NEW alembic/versions/20260912_001_order_money_protocol.py — additive revision s1714_order_money_v1, parent f0e47ebd81ee; preserve other graph branches, no unrelated merge.
- NEW scripts/order_money_protocol.py — read-only adoption/report, guarded reconciliation, and private TEST-only register-test-negative/register-test-failed-refund authority commands; no Transfer.create or refund-create command.
- NEW tests/test_order_money_protocol.py, tests/test_refund_processing_protocol.py, tests/test_refund_webhook_protocol.py — PostgreSQL service/race/fault tests.
- EDIT tests/test_finance_phase2.py — dormant direct-refund call now refuses, existing unrelated finance behavior unchanged.

No additional product files without a reviewed Gate2 manifest amendment. A need for an import-registry file must be demonstrated before adding it; prefer imports from the two new services so new ORM metadata is registered explicitly.

Chunk B, environment:
- EDIT browser/s1681-delivery-leg.ts, bin/check-settlement, bin/verify, compose.yaml, tests/compose-contract.sh, tests/test-delivery-contract.py, tests/test-journey-contract.py, tests/test-nightly-contract.py. compose.yaml passes ORDER_PAYOUT_DISPATCH_ENABLED=true only in the reviewed isolated TEST backend; no protected runtime edit. Static compose contract asserts it alongside existing TEST-mode guards.
- NEW tests/test-refund-proof-contract.py.
- Source/pin changes and the three relevant runbook specs/head record are separate reviewed recording commits after accepted code. No snapshot cache; Docker direct operations remain; no Compose polling.
- Mars owns N TEST admission; Vulcan owns W afterward at the same seam, bilaterally selected bus4118/4119. These are separate persisted authorities.

## 3. Schema and compatibility

New table order_money_states:
order_id UUID primary key references orders(id); transaction_id UUID nullable unique references transactions(id); protocol_version integer NOT NULL=1; revision bigint NOT NULL=0; payout_state varchar32 NOT NULL=idle; request_json JSONB nullable; request_sha256 char64 nullable; idempotency_key varchar255 nullable unique; transfer_group varchar255 nullable; dispatch_token UUID nullable; reserved_at/dispatch_started_at/completed_at/updated_at timestamptz; stripe_transfer_id varchar255 nullable unique; reconciliation_reason text nullable; independent_revocation boolean NOT NULL=false; refund_applied_cents bigint NOT NULL=0; billing_entity_id UUID nullable FK billing_entities, immutable once capture/payout/refund is bound; capture_origin varchar32 nullable CHECK in payment_event/refund_reconciliation; capture_payment_id UUID nullable FK payments; capture_journal_entry_id/payout_journal_entry_id UUID nullable UNIQUE FKs journal_entries; seller_refunded_cents/seller_payout_posted_cents bigint NOT NULL=0, CHECK both>=0.
CHECK payout_state in idle,reserved,dispatching,unknown,succeeded,rejected,reconciliation_required; refund_applied_cents>=0; dispatching/unknown/succeeded require immutable request/key/group/token; succeeded requires transfer ID. The service validates request shape and binding before every use. No cascade deletion of money authority/history.

Second new table s1681_refund_test_authorities (model in app/models/order_money_state.py): order_id UUID primary key/FK, positive_order_id UUID nullable FK with CHECK order_id<>positive_order_id when nonnull; purpose varchar20 NOT NULL CHECK negative/failed_run; role char1 NOT NULL CHECK P/N; CHECK negative requires roleN and nonnull positive_order_id; failure_receipt_sha256 char64 nullable, required for failed_run; transaction_id UUID NOT NULL UNIQUE FK; run_id text NOT NULL; phase text CHECK online/offline; buyer_id/seller_id UUID NOT NULL FK; payment_intent_id/charge_id text NOT NULL UNIQUE; amount_cents integer CHECK=2500; currency text CHECK=usd; environment_sha/spec_sha char40 NOT NULL with lowercase40hex check; pair_receipt_sha256 char64 nullable with lowercase64hex check, required for negative; failed_run requires failure_receipt_sha256 instead and may precede creation of a full pair; protocol_version integer CHECK=1; admitted_at timestamptz NOT NULL. UNIQUE(run_id,phase,purpose,role). Immutable binding: exact re-registration returns existing match, any different field refuses. No public mutation endpoint and no cleanup deletion of this audit authority.

Extend stripe_events (same physical table used by raw webhook SQL and finance ORM): refund_phase varchar20 nullable CHECK NULL/admitted/applied/reconciliation; refund_order_id UUID nullable FK; refund_payment_intent_id varchar255 nullable; refund_charge_id varchar255 nullable; refund_binding_sha256 char64 nullable; refund_admitted_at/refund_applied_at timestamptz nullable. Partial index on payment_intent ID where refund_phase=admitted. Event's existing payload_json/signature/raw digest retain signed provenance. A null phase means legacy processing, never proof of safe completion.

Extend refunds: effects_applied_at timestamptz nullable; order_id UUID nullable FK; transaction_id UUID nullable FK; agent_spend_delta_cents bigint NOT NULL DEFAULT0 CHECK>=0; marketplace_fee_refund_cents/marketplace_seller_refund_cents bigint nullable CHECK>=0 with their sum equal amount_cents for an applied order-linked refund. Existing unique real stripe_refund_id and unique JournalEntry.source_ref are backstops. A new succeeded applied refund must bind a journal and immutable payment/entity/currency/amount; agent delta is recorded in this same row. Existing rows with no applied marker are not silently stamped or reapplied.

Additive tables/columns use repository idempotent existence/definition checks; this revision also replaces the existing order-status sync function/trigger only for the refund semantics specified in section5. The migration must preserve non-refund mappings and refuse an unrecognized prior trigger definition rather than overwriting unrelated changes. Verify a clean installation and upgrade from exact baseline, repeat upgrade, and all constraints. Current production alembic_version=f0e47ebd81ee observed01:52Z; test actual ScriptDirectory heads on the pinned checkout, not a regex-derived sole-head claim. Downgrade with any protocol state/refund markers must refuse. Production rollback is forward repair or payout disabled while retaining schema/data; never restore the old unsafe payout implementation.

Chart/entity contract (R3, supersedes R2 account2100 mapping): resolve exactly one active merchant_of_record=true US/USD BillingEntity with code AIM_WY_LLC, matching configured BILLING_ENTITY_CODE; no first-active-row or legacy null-code fallback. A configuration choosing another entity refuses this protocol until its mapping is separately reviewed. Preserve all existing entity legal/tax/address fields and IDs; normal services cannot create entities or accounts. Bind the resolved entity_id on the authority and require the same entity on Payment, Refund, journals and all account rows. Existing order/transaction rows have no entity column; the configured merchant resolver plus immutable authority is the actual binding, never buyer/provider metadata selecting a finance entity.

The new f0-parent migration must validate this existing seeded entity and deterministically establish EXACTLY these rows for that entity:1000 'Cash - Stripe Clearing'/asset/debit;2110 'Seller Payouts Payable'/liability/credit;4000 'Marketplace Commission Revenue'/revenue/credit;5100 'Unrecovered Seller Payouts'/expense/debit. All four require is_active=true, system_managed=true and allow_manual_posting=false. Insert missing account rows idempotently under unique(entity_id,code); existing exact rows are reused; any conflicting name/type/normal-balance/flags or ambiguous entity refuses without rewriting, moving journals or blessing a zero balance. No change to2100, the null-code legacy entity, pricing, legal identity or unrelated templates. Existing1000/4000 already match in production;2110/5100 are absent for AIM_WY_LLC. This is a reviewed bounded account seed, not runtime repair. On a clean full migration upgrade the existing ancestor entity seed must produce the required entity before this revision; if it does not, fail the migration and its acceptance test rather than fabricate legal details or skip the prerequisite. Repeat upgrade changes no IDs/rows. Downgrade cannot remove any referenced account or protocol data.

Source correction: GLM R2 reported the two finance seed revisions absent from f0 ancestry; an AST walk covering both Assign and AnnAssign plus tuple parents at exactd766 verifies BOTH ancestors (320 total nodes, no missing graph node). Paths through merge_all_heads are recorded in s1656/s1714-finance-chart-lineage.json. Do not add a finance branch merge. Its separate semantic finding is real: the actual selected AIM_WY_LLC entity has2100 Customer Credit Liability and no5100, while the legacy null-code entity has2100 seller liability with wrong debit normal balance. Read-only production audit s1656/s1714-finance-chart-audit.json at2026-09-12T02:38:59.040646Z records exact entity/account IDs, names/types/flags and zero journal-line usage for shown existing accounts. These are observed facts, not migration success.

Both readiness helpers and the private report must validate/report exact entity code/id, merchant/country/currency, four account identities/name/type/normal-balance/flags, missing/conflicting/duplicate counts, and preserved2100 identity; never log tax IDs, addresses or credentials. Before deployment, report distinguishes expected missing2110/5100 (planned bounded seed) from contradictions; after migration and BEFORE enable, missing/conflicting count must be zero. Gate3 tests upgrade an empty disposable PostgreSQL database through the actual full ancestry to new head, assert one selected entity and exactly one of each required account WHERE entity_id equals that resolved merchant entity, repeat upgrade unchanged, exercise existing valid/missing/conflicting accounts and legacy entity non-mutation, then execute capture/partial/full/Transfer tests without ad hoc finance fixtures. Raw graph traversal is only source evidence; actual migration execution is mandatory proof.

Readiness interface: app.services.refund_processing_service.ORDER_MONEY_PROTOCOL_VERSION = 1; assert_order_money_protocol_ready(db, *, required_version=1) async; assert_order_money_protocol_ready_sync(db, *, required_version=1) sync. Both verify required migration ancestry, actual required columns/constraints and protocol version; missing/incompatible state raises OrderMoneyProtocolUnavailable. A generic config flag alone cannot satisfy readiness. No cached success across a deployment. Vulcan imports this accepted processor interface and calls readiness before admitting its TEST refund.

## 4. Lock order and acknowledgment protocol

Choose an explicit event mutex followed by the common order sequence. Every refund phase first locks its own stripe_events row, then unique order (if present), linked transaction, order_money_states, Payment, then Refund rows sorted by stripe_refund_id ascending, then the linked agent_api_keys row, followed by the accounting-period/GL-account/journal sequence proven by the full graph. Calls that do not touch a node skip it but never reverse an edge. Payout/revoke/confirm paths start at order and never acquire an existing stripe_events row after it. Dispute webhook already owns its own event mutex, then follows order/transaction/authority. No path locks another event's row while owning an order. Refund dispatch to BillingService occurs before BillingService's generic event acquisition so it cannot silently reverse the sequence. For admitted order-linked refunds the new processor is the sole effects writer: skip the old BillingService charge.refunded mutation branch entirely, including on applied replay; generic non-order behavior remains separately routed.

This is a refinement of Gate1's common order-first money lock: the event mutex is an outer same-event serialization boundary, not a money-operation lock acquired in reverse. A COMPLETE lock graph and deterministic tests must prove no reverse edge across the whole chain: stripe_events, orders, transactions, order_money_states, Payment, Refund, agent_api_keys, accounting periods, GL accounts, journals and applicable DB triggers. Enumerate every explicit FOR UPDATE/implicit UPDATE/FK-trigger edge in participating services and existing finance callers. Baseline app grep found no explicit AgentApiKey FOR UPDATE; its implicit UPDATE still locks and must be included. Apply the same deterministic ORDER BY (entity_id,code,id) in the EXISTING FinanceEngine._build_entry_from_template shared lock seam as in the new methods; this ordering change preserves unrelated template business behavior. Lock GL accounts by (entity_id,code,id) and journal identities deterministically after period resolution. Every adjacent pair requires a two-connection barrier case; a committed lock-order assertion fails on a reversed edge. No event/order-only proof is sufficient. Resolve order IDs with nonlocking lookups first, then validate all bindings again under locks. Conflicting/multiple/missing reverse links fail closed; never select the first matching order.

The signed refund reaches the existing suppression decision. Only an admitted normal event (or separately approved exact TEST predicate) proceeds. Existing raw dedup's uncommitted processing row is not a durable admission. Under the above locks, validate event identity/binding, record refund_phase=admitted, status=failed/error_message=refund_pending, and commit. All sync locks are released. A pending admission without an order remains bound by PI and blocks a later order with that PI; provider reads and delayed payment records may complete it later.

Read provider objects outside database locks, then run the atomic async effects transaction in the same HTTP request. HTTP2xx occurs ONLY after effects and event completion commit. There is no fire-and-forget task or new refund worker. Duplicate completed protocol events validate their applied binding and return2xx without effects. Failed/pending events remain retriable; unavailable async session, provider inconsistency and finance failures return500. Signature/scope validation retains existing400/suppression policy. A legacy completed refund event with no protocol marker is classified for reconciliation, not treated as a newly safe applied refund.

Atomic effects acquire locks in the same order, revalidate provider/payment/order bindings and pending phase, apply new succeeded deltas, update order/transaction/access/Payment, mark event refund_phase=applied and raw status=completed/processed_status=processed, then commit. Remove old _handle_refund effect writes; its replacement only admits. Existing unconditional outer error upsert must not overwrite a committed applied/completed refund: conditional update only where refund_phase is not applied and status is not completed. A crash after effects commit/before HTTP acknowledgment converges on replay.

## 5. Ordinary purchase finance, provider refunds and atomic effects

Verified source gap: canonical checkout puts only order_id/transaction_id in PI metadata (transaction_service.py782-784); BillingService582-599 requires customer_id/user_id and marks that event ignored. Read-only run25 inspection found its real completed order/confirmed transaction has ZERO matching Payment rows. Therefore waiting for a Payment row is not a valid recovery strategy. No fixture/harness-created Payment is permitted.

Add a normal order-bound capture branch in BillingService.handle_stripe_event BEFORE the customer_id/user_id branch. For signed payment_intent.succeeded with canonical order/transaction metadata, resolve the unique order using trusted DB reverse links and provider PI/charge, derive buyer from that order rather than caller-supplied customer metadata; resolve the merchant billing entity through the configured BillingService entity code and the strict section3 chart contract, then bind it immutably to order_money_states (Order itself has no billing_entity_id), and record the actual Payment with gross/currency/charge/PI/customer binding and payment_type=commission. Legacy order PI lookup is supported when its binding is unique. Require authoritative succeeded/captured positive provider facts. The original charge capture is an immutable fact even when refunds already exist; late payment replay must not reset refund state or re-grant credits/access.

The shared helper ensure_order_capture(db, *, order, transaction, provider_payment, provider_charge, origin) lives in refund_processing_service.py and owns NO commit. It locks under section4, validates an existing Payment or creates the missing original Payment from these real captured facts. It writes one capture journal with source_ref marketplace_payment:<PI> and records capture_origin=payment_event or refund_reconciliation on order_money_states and in the journal description (JournalEntry has no metadata column). Bind JournalEntry.source_id to Payment ID for capture, Refund ID for refund and Order ID for Transfer; capture/Transfer use source_type=stripe_payment and the order refund uses source_type=refund. A refund arriving before the payment event calls the SAME helper inside its effects transaction; later payment replay validates/reuses it. A missing Payment with authoritative captured order facts and no conflicting historical posting is recoverable; contradictory/multiple Payment or prior unbound/conflicting journal facts remain reconciliation-required. Do not relabel legacy commission:<order> or refund:<ID> entries or invent a capture when provider validation fails. Record when historical capture facts are reconstructed; use the current open accounting period, preserving provider received_at, never silently write into closed books.

Bounded order-ledger mapping is necessary: existing record_commission records ONLY the fee (DR1000/CR4000) but generic record_refund reverses the FULL passed gross on those accounts. Do not recognize gross as commission or reverse gross against only a fee. Add three explicit FinanceEngine methods for order-linked flows; existing generic credit/invoice/subscription templates remain unchanged:
- record_marketplace_capture: source marketplace_payment:<PI>, DR1000 CashStripe gross; CR2110 SellerPayoutsPayable seller portion; CR4000 CommissionRevenue fee.
- record_marketplace_refund: source refund:<real re_ID>, CR1000 gross refund; DR4000 fee-refund delta; DR2110 seller-refund portion up to this order's remaining booked seller payable; DR5100 Unrecovered Seller Payouts for any remainder already paid. This is recognition of an unrecovered seller-payment loss, NOT a receivable, automatic recovery or Transfer reversal.
- record_marketplace_transfer: source marketplace_transfer:<real tr_ID>, CR1000 Transfer amount; DR2110 up to remaining booked seller payable; DR5100 for a portion already extinguished by refund. This handles provider success learned after a refund without a negative payable.
Use validated active chart accounts1000/2110/4000/5100 for the bound merchant entity under section3; 2110 is the dedicated seller payable created only by the reviewed migration, and existing2100 Customer Credit Liability is never reinterpreted. Absent/wrong accounts fail at runtime; services never create ad hoc accounts. New methods flush, never commit, with balanced exact lines. Existing unrelated templates and their business behavior are unchanged.

Freeze integer allocation: gross=G, original fee=F, seller=S, require G=F+S and G>0. For applied succeeded total R, cumulative fee refunded=floor(R*F/G); per-refund fee delta is the new cumulative fee less previous cumulative fee. Seller-refund delta=refund amount minus fee delta. At full R=G, exactly F and S are reversed. Remaining order seller payable=max(S - seller_refunded_cents - seller_payout_posted_cents,0). Update counters and journal bindings atomically with each effect. An already successful provider Transfer does not get posted twice; source and authority references uniquely identify its journal. For G2500/F125/S2375, full refund before payout leaves cash/payable/revenue zero; payout then full refund leaves cash-2375, payable0, revenue0 and unrecovered payout expense2375. Refund while Transfer unknown followed by recovery yields the SAME final lines/totals regardless of observation order. No fee-rate or provider-price change.

Use Charge.retrieve, PaymentIntent.retrieve and Refund.list(charge=...,limit=100) with complete starting_after pagination outside locks. Primary references: https://docs.stripe.com/api/refunds/list, https://docs.stripe.com/api/transfers/list, https://docs.stripe.com/api/idempotent_requests. Require actual re_ identity, exact charge/PI/entity/account/livemode, positive integer amount/currency; provider original capture gross must match Payment/order/transaction. No arbitrary embedded first refund, fallback ID or permissive amount OR. Finance-only non-order refunds retain the existing generic finance entry path and acquire no order authority; no credit-grant changes are included.

Succeeded-set sum must agree with authoritative provider refunded total, never exceed gross and never decrease. Provider changes across pagination or contradicted successful identities retry. Pending/failed/canceled apply no financial/spend delta; unknown statuses fail retryably. Lock sorted Refund rows and apply newly succeeded identities once. Validate existing journal's exact source/entity/payment/refund/amount/line allocation before reusing; unmatched historic rows reconcile, not duplicate. Refund.effective fee/seller fields and effects marker persist with the journal.

For agents preserve zero floor once per NEW refund: actual spend decrement=min(refund amount,current nonnegative spend_used), record actual delta on Refund. Smaller current spend alone does not block a valid refund; wrong key/owner or corrupt negative counter does. Never repeatedly subtract the cumulative provider refund total.

One applied succeeded sum drives Payment and order refund total/status. Partial retains access and transaction status. Full revokes order and brings both human and agent transaction to refunded, preserving settled_at and all prior successful Transfer/settlement facts if already paid; payout authority becomes reconciliation_required in that case. Full refund before payment webhook must still converge from captured checkout_pending/agent_payment_pending, using provider-validated capture facts. Generic user transition requests cannot manufacture refunded state.

The REAL sync_order_status_to_transaction trigger currently maps partially_refunded to refunded and inserts transaction_events itself; tests cannot omit it. Amend the existing function/trigger in the new migration, not old migration history:
- partially_refunded order updates do NOT change transaction status or insert a full-refund transition;
- widen the existing outer OLD.status IS DISTINCT FROM NEW.status guard so refunded handling runs even when OLD.status was already refunded by a local unresolved dispute, but only if protocol authority.refund_applied_cents equals positive order gross and the referenced real Refund has an applied marker bound to that order;
- under the already held transaction lock, change a non-refunded linked transaction once and insert exactly one event with ACTUAL prior transaction status, to_status=refunded, order ID and completing provider refund ID. If already refunded, insert no second event. Preserve earlier settled timestamps/events/Transfer;
- other status mappings retain existing behavior.
The processor sets applied Refund markers and authority aggregate, and assigns orders.stripe_refund_id to the deterministic newly applied refund that completes the full sum, before updating order refund fields, all within the same transaction, so the trigger sees validated state. The trigger is the SOLE emitter of the full-refund transaction status/event; the processor does not then call transition(refunded) again. The caller-owned transition helper remains required for confirmation/settlement atomicity. Extend/verify the model's allowed refund transitions for validated captured paid states, explicitly including missing DELIVERED/CONFIRMED/SETTLED to REFUNDED edges, without adding a new public status.

Payment replay never sets an already refunded Payment to succeeded; confirmation never overwrites full refund/independent revocation. Dispute-won clears only its own restriction with no independent revoke, pending/positive refund or reconciliation. Existing revoked state adopts conservatively unless proven exclusively current dispute. A local dispute status/refund amount without actual provider refund identity never fabricates finance or a protocol-validated full-refund event.

## 6. Payout authority and recovery

Common eligibility: exact consistent order/transaction/owner/listing/gross/fee/currency binding, paid charge, correct Connect destination; transaction confirmed plus genuine48h since the latest persisted transaction_events.created_at with to_status='confirmed' for this exact transaction; compare against PostgreSQL clock_timestamp()/UTC at dispatch and bind that exact event ID/created_at in the receipt, never updated_at or process elapsed time; legacy order completed and existing legacy timing policy; order not revoked, refunded/partially_refunded/disputed, no positive refund total, no unresolved dispute, no pending refund admission by order OR PI, no reconciliation and no prior Transfer. Return structured refusal reasons listing every failed money guard before reporting hold timing. Record one audit per (order,authority revision,reason set) under the order lock; repeated unchanged scheduler scans do not flood history.

Preserve provider identities: transaction key settle_<tx_number>, group tx_number; legacy key transfer_<order UUID>, group order_number. Freeze exact amount/currency/destination/metadata in request_json and SHA. A reserved row is cancellable. Fetch final provider refund facts outside locks, then re-lock order/transaction/authority and recheck current admission/revision/eligibility. Commit dispatching plus unique dispatch token. THIS dispatching commit is the linearization point: refund/revoke admitted before it prevents Transfer; an arrival afterward records a post-ownership reconciliation exception. Do not use the loose phrase 'before network dispatch' as an atomic guarantee.

Only the request owning that committed token may issue the initial Transfer.create outside locks. On success re-lock and record Transfer, its exact marketplace Transfer journal and settlement atomically; if refund/revoke won later, preserve successful Transfer and reconciliation-required rather than overwriting refunded state. On any uncertain response or crash leave unknown/dispatching; never use lease expiry or a fresh key to re-dispatch.

Recovery: retrieve known Transfer ID or completely paginate Transfer.list(transfer_group=...,destination=...). Exactly one match requires same amount,currency,destination,group,order/transaction metadata and nonreversed state; finalize and post only that identity through record_marketplace_transfer, using current remaining payable/refund state. Zero matches after possible dispatch is still uncertain; multiple/contradictory matches require reconciliation. Recovery command never creates a Transfer or reversal. Explicit provider rejection also refuses automatic new-identity creation; operator must diagnose before a separately reviewed retry. Existing scheduler retry metadata cannot override this state.

All order payout writers route through this service: settlement_service.py:89 and order_service.py:1544. Auto-confirm reaches the latter. aim_payout_service.py:53 uses SellerPayoutEntry/SettlementRecord metering, no Order linkage in models/aim.py, and is excluded. accounting_service.py:180 only retrieves a Transfer, unchanged. Scheduler rolls back failures before bookkeeping and counts blocked/uncertain separately; one bad order does not poison subsequent eligible orders. Do not claim this module is scheduled merely because its wrapper says Celery-compatible: registration or external invocation must be demonstrated in runtime evidence.

## 7. Required tests and acceptance

PostgreSQL tests must invoke real service paths with deterministic barriers and separate connections. Mock only Stripe network and external logging boundaries. SQLite/mocks-only cannot certify row locks/atomicity. Run the three new suites plus existing finance/order/transaction/settlement tests impacted by this manifest, using the repository venv interpreter. Minimum commands: python -m pytest -q tests/test_order_money_protocol.py tests/test_refund_processing_protocol.py tests/test_refund_webhook_protocol.py tests/test_finance_phase2.py; enumerate and run the existing tests that call changed order/transaction/settlement methods. Tests require a dedicated disposable PostgreSQL database, never the protected TEST or production DB. Environment checks: ./tests/compose-contract.sh --static-only; python3 tests/test-seed-contract.py; python3 tests/test-delivery-contract.py; python3 tests/test-journey-contract.py; python3 tests/test-nightly-contract.py; python3 tests/test-refund-proof-contract.py. MP must report exact commands, actual counts and any failed/skipped coverage; no fabricated success from test collection.

Required matrix:
- Positive transaction and legacy payout once; full refund human and agent; finance-only non-order refund.
- Same event replay, different IDs/same refund, concurrent partials and partial-to-full; exact Payment/order total/status, journals, Refund markers, agent deltas, events and access. Use real canonical checkout metadata, never inject customer_id to hide the baseline gap; full refund before payment event reconstructs the same actual capture and late payment replay adds no second Payment/journal.
- Install the real migrated order sync trigger; partial keeps actual transaction status and no refunded transition, full produces exactly one correctly sourced transition even after legacy local-refunded order status. Verify literal pre/post account balances and split lines for capture, partial/full refund, payout then refund, and unknown payout recovered after refund; balanced debits alone are insufficient.
- Credit PI containing customer_id but no canonical order binding still reaches the unchanged credit branch and creates no marketplace capture; signed admitted order refund executes only one Refund/journal/Payment-status writer. Cross-order new marketplace posting vs legacy credit/subscription posting exercises shared GL lock ordering.
- Missing Payment, duplicate PI/order link, entity/owner/currency/amount mismatch, fake refund ID, truncated pagination, changed provider snapshot and unexpected statuses.
- Fault after admission commit, midway effects, effects commit before ack, missing async session; no success-before-effects and no lost retry.
- Concurrent admission/refund/revoke/dispute/confirm vs both payout paths, at reserved and dispatching barriers; lock timeout/deadlock checks.
- Provider success then local crash; known/unknown/zero/multiple/reversed Transfer recovery; key-pruning-age unknown never creates again.
- External full refund after dispatching/unknown: refund effects persist, reconciliation_required, no second Transfer/no automatic reversal, audit, nondecreasing totals.
- Late payment/dispute-won/confirmation replay preserves refund and independent revocation; distinct unrelated orders still progress.
- Migration baseline/repeat upgrade/constraint rejection/readiness unavailable; no historical row silently blessed; dispatch-disabled refuses every order Transfer caller.

Connected phase roles are exactly P and N, two fresh TEST2500-cent USD purchases per online/offline phase, fee125/seller2375. Four purchases total across the corrected two-phase proof; TEST provider balances, not live-mode spending. Recheck Vulcan's cumulative real-provider cost budget before execution. Per-phase proof is retained because both online and offline delivery must independently preserve exact order→device delivery→download→confirmation→payout correlation; no cross-phase reused positive order.

P performs the current positive delivery/download/restart/refresh/token isolation and confirmation proof and stays unrevoked. N receives separate actual delivery, is confirmed BEFORE revocation, then proves revoked access denial. Both wait their own genuine hold. Once BOTH deadlines elapsed, P settles exactly once; N direct and scheduled paths refuse with revoked reason, then N receives actual full provider refund and proves refunded status/refunded_at/gross refund amount, Payment/Refund/journal/human transaction/access consistency and refund-specific refusal. Assert structured refund reasons, not only zero Transfer. Never clear N's revoked flag or alter clocks to expose a test disjunct.

Receipt schema v2 binds exact role→order/tx/PI/charge/confirmation IDs and source hashes, per-role provider and DB evidence, deadline and observed UTC timestamps. Reject missing/extra roles or any unexpected order. Existing immutable v1 run25 receipts remain v1; no in-place rewrite. Failed paid corrected runs release each exact untransferred paid role through reviewed refund/release logic before any new purchase; never use the v1 purchasing-abandon path on a confirmed pending order; v2 terminal FAILED role release follows the separately specified section8 cancellation/refund authority.

## 8. TEST admission and Workspace seam — selected ownership

Mars owns the exact N predicate; Vulcan owns the separate W predicate applied serially after the accepted Mars candidate (bus4118/4119). N is seller01/buyer01 S1681; W is seller02/buyer01 Workspace. They use separate exact persisted authorities, never actor-only permission. Neither bypasses payout guards.

Export app.services.s1681_refund_test_admission.admits_s1681_negative_refund(event, db, *, normalized_stripe_account) -> bool. Its caller is the existing suppression decision in webhooks.py, after signature verification and generic event dedup but before admission: add a distinct s1681_test_refund alternative alongside existing test payin/purchase alternatives. Vulcan later adds its W alternative at this same decision, not inside the refund processor. Predicate success permits normal durable processing only; it never marks refund complete.

For purpose=negative registration, the harness commits an immutable v2 pair receipt naming P and N, both bound source pins and real order/tx/PI/charge IDs. Under its existing owner lock it passes that exact receipt on stdin to the private product CLI scripts/order_money_protocol.py register-test-negative. No secret, authority document or JSON appears in argv. This CLI is a trusted operator/deployment surface, not an HTTP route; arbitrary provider metadata or customer input cannot invoke it. It refuses unless the configured environment satisfies the existing S1656 TEST shape (ENVIRONMENT=test, STRIPE_TEST_MODE=true, no live-mode provider key), accepted schema/readiness is present, and the packaged account-pool loader resolves exactly seller01/buyer01 with DB UUID/email/is_test agreement. It verifies P/N differ, exact order/transaction reverse links, same owned fixture listing, amount2500/usd, both real captured payment/charge bindings and receipt hash/pins; it stores only N with purpose=negative and the positive counterpart ID for explicit exclusion. Provider validation occurs outside locks, then bindings revalidate under order locks sorted by UUID before the immutable authority insert. Wrong role/phase/pins/party/source/amount or an existing conflicting authority refuses. Registration itself neither refunds nor settles nor changes access.

Cross-repository digest contract: pair receipt and frozen request objects contain only ASCII keys/string values, finite integers/booleans/null, arrays and objects. Reject unknown fields, non-ASCII strings and floating-point money values. Recursively sort object keys, emit compact JSON with no whitespace or trailing newline, then SHA256 those exact ASCII bytes (Python json.dumps(sort_keys=True,separators=(",",":"),ensure_ascii=True,allow_nan=False); JS recursively sorted JSON.stringify under the same ASCII/type constraints). The pair receipt digest excludes the enclosing digest field; stdin envelope is {receipt:<canonical object>,sha256:<digest>}. The immutable request includes amount,currency,destination,transfer_group,metadata AND idempotency_key; hash all of them. Test both implementations against the same golden preimage including empty objects/arrays and nested arrays, plus every-field mutation. Git source identities are lowercase40hex; SHA256 digests lowercase64hex, never interchangeable.

The webhook predicate requires existing _stripe_event_has_s1656_test_shape, authentic charge.refunded, no connected-account context, exact N authority, packaged seller01/buyer01 identities and current order/tx/PI/charge equality. The signed Charge must show paid=true, full positive captured gross2500, amount_refunded=amount=2500 and currencyusd; missing/ambiguous values refuse. The normal processor still performs full independent provider refund pagination/binding. P, W and unrelated synthetic purchases are denied even if actors match. A claimed exact N event with missing/incompatible readiness returns retryable failure, never an acknowledged suppressed completion. Production/non-TEST shape retains existing suppression, with no registration path.

Failed-run cleanup is a separate purpose, not blanket P admission. Extend documented bin/check-settlement --abandon-refunded for v2 FAILED checkpoints only; v1 and successful/pending checkpoints retain existing refusal rules (run25 cannot enter this path). Before any provider refund, under the existing owner lock, build a canonical failed-role receipt with run/phase/source/role/order/tx/PI/charge IDs, paid proof and immutable failure-evidence SHA. The private register-test-failed-refund command applies the same TEST/packaged-identity/provider/source checks and stores purpose=failed_run for that exact paid role. It requires an owner-authored failed checkpoint, a captured order bound to the active v2 run and no completed/dispatching/unknown/reconciliation payout; any uncertain Transfer prevents release. If merely reserved, cancel it under shared authority before admitting refund. In the same committed registration transaction, set independent_revocation=true and revoke access through the normal product service so no new payout reservation can race the subsequent provider refund; the failed-run authority is a durable exclusion, not merely a momentary absence of Transfer. If N already has an exact negative authority, validate and retain it unchanged, bind the failed receipt in the immutable operator release record, and use its existing negative-refund predicate rather than inserting a conflicting second authority; any mismatch refuses. A confirmed role may be released only via this explicitly failed v2 path after cancellation/exclusion, never by pretending it is still purchasing. Both source hashes and exact failed receipt must match; unrelated/older/pending/successful runs refuse. The failed authority records its failure receipt digest and never changes a negative authority's meaning.

Add admits_s1681_failed_run_refund as a separate alternative at the same suppression seam, filtering purpose=failed_run; all full-refund/provider/readiness checks still apply and it delegates to the normal processor. Normal P/N negative tests prove P is denied without this separate explicit failed-run authority. W authority can satisfy neither S1681 purpose. Already settled successful roles retain their Transfer evidence; do not reverse them to clean a failed sibling. After provider refund and normal atomic product effects are verified, write the immutable per-role release receipt and close only the exact failed checkpoint. No rerun while any unsettled failed paid role lacks release proof. Tests cover failure after first P payment before N exists, after both payments/confirmation, refusal of pending run25/v1, wrong failure hash/role/source, uncertain Transfer, and replay producing one refund/journal.

Tests must drive a signed endpoint event, not only a predicate mock. Cover N admitted and processed; P, seller02 W, wrong buyer/seller, altered stored bindings, partial/zero/negative amounts, livemode, connected account, missing schema, fake role metadata and duplicate conflicting registration denied. W tests later prove its authority cannot admit N and N authority cannot admit W. Refund journal/status/refusal assertions remain mandatory, so TEST admission cannot hide a normal processor defect.

## 9. Adoption, rollout and completion

Evidence: s1714-refund-adoption-counts.json at01:22Z confirmedtx0, duplicate order links0. Expanded read-only production snapshot s1714-refund-adoption-expanded.json at2026-09-12T01:52:11.509635Z: confirmedtx0; bare-revoked otherwiseeligible0; legacycompleted/noTransfer0; bare-revoked legacy0; duplicate order/payment PI0; refunds0; malformed provider refund identities0; succeeded-withoutjournal0; refund/payment mismatch0; schemaf0e47ebd81ee. First expanded query failed on nonexistent dispute_opened_at, rolled back; corrected actual column disputed_at. No production writes. These are time-bounded counts, not blanket finance certification.

scripts/order_money_protocol.py report must reproduce these buckets plus missing/multiple/reverse-link, owner/amount/currency, pending admissions, existing Transfer and unknown state; report no credentials. Re-run immediately before enabling. Nonzero contradictions stop enablement and identify exact private operator records; no repair-to-green. Reconcile only supported provider identities and record evidence. Bare revocation is not automatically cleared or paid; operator uses existing authorized dispute/refund processes with the accepted guards.

ORDER_PAYOUT_DISPATCH_ENABLED defaults false for new code; it is not a replacement for schema readiness. All new order Transfer callers consult it under authority. Before merging any product candidate to main, freeze the exact production GitHub deployment triggers and other operator/variable-sync redeploy sources; all four current triggers have checkSuites=false, so merge itself can deploy. Save exact trigger definitions, use only verified scoped deploymentTriggerDelete/deploymentTriggerCreate controls (no invented pause flag), verify absence and later exact restoration in release receipts. Before production replacement, quiesce the old order-confirm/payout writers and account for in-flight requests/workers; old binaries do not recognize this new flag. Exact platform quiescence/restore commands and expected old/new service inventory must be added to the release record before deploy, verified with Vulcan (bus4120). Local Railway scale help currently panics on removed Region.railwayMetal; do not use that old CLI command or infer scaling support. Read-only variables/database access remains healthy. A rolling mixed-version deployment is not proof of safe cutover. If no safe quiescence mechanism is available, release remains disabled and the missing operational mechanism is escalated, not simulated.

After unanimous Gate3 and supported protected-run completion, deploy corrected proof to an isolated reviewed environment at new exact pins. Complete online/offline real holds, summary, AIMData1.23.3 stable, required launchd plists and actual scheduled proof in original order. Open Workspace W window explicitly on bus after prerequisites; existing seller01 delivered-order/device/trust/serial remain untouchable. W cleanup must not change P/N receipts or purchase rights.

Production: exact candidate/schema across backend, ordinary worker, Beat and profile worker as applicable; verify old writers gone before enable flag; repeat adoption checks; enable only after accepted guard/readiness. Outside customer-path verification must prove purchased access/bytes/hash and owner denials on authorized existing production records, plus no regressions in Workspace. No new live first sale is inferred. Record release/rollback identities, health, event/finance outcomes and cleanup. Gate4 cross-review and both required BQ completions need actual production evidence; TEST proof alone never closes this goal.

## 10. Review questions

1. Does the event mutex plus common order lock sequence have any actual reverse edge, including generic dedup, BillingService and dispute replay?
2. Do schema/admission/atomic effects survive every listed crash without lost refunds, double journals/spend or false2xx?
3. Does the dispatching linearization and provider recovery prevent all locally pre-admitted payouts and classify post-ownership external refunds honestly?
4. Do manifest, partial/full state, legacy writers and TEST P/N/W boundaries preserve the requested production outcome without unrelated fixes?
5. Are the real-clock proof, adoption buckets, rollout quiescence and forward-only rollback sufficient? Name any exact missing implementable requirement before MP.


## 11. Gate2 Round1 synthesis and required Round2 fold

R1 on34dad4f3: CC response20260912-040205-201258 APPROVE_WITH_NITS; GLM040206-001276 APPROVE_WITH_NITS; DeepSeek040205-657003 APPROVE_WITH_MANDATES (full-chain lock proof MEDIUM). No MP build is authorized until the corrected complete candidate is unanimously accepted. CC/GLM both accept staged cutover evidence as a deploy blocker rather than a build blocker; retain that explicit Gate3/release checklist. CC digest and BillingService-entry nits are resolved by section8 canonical bytes and section5 named capture/refund/Transfer engine methods. DS hold-source and transition-wording nits are folded. DS's claim that baseline DELIVERED/SETTLED already permit REFUNDED was not relied on: direct source atd766 shows they do not; extend only the validated paths described here.

Operator source verification independently found two missing prerequisites after R1 dispatch: real run25 Paymentcount0 with canonical metadata ignored by finance, and the live order-sync trigger converting partial to full refund with its own event insert. Evidence /Users/max/koskadeux-state/s1656/s1714-real-purchase-finance-gap.md, read-only; no runtime/receipt changes. Sections3/5 and the added engine manifest resolve these on the normal product path. The R2 bounded cash/payable/fee mapping used existing chart accounts; R3 corrects its seller-payable identity under section3 and otherwise changes no price, fee rate, credit grant or unrelated invoice/subscription posting. Round2 must explicitly assess this required ordinary-order integration and both event-order-independent ledger examples; do not merely repeat the prior verdict.

Cutover evidence now available from Vulcan: /Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/SELLER-PRODUCTION-QUIESCENCE-RUNBOOK-S1712.md and its exact inventory/control receipts; published runbooks PR168 merge7d0c35aa2f6b0e726b041a720790f621193747d4 (candidatebc74846e8d31e5618f12c73eb4ca98be4058f9b1), canonical runbooks/seller-production-payment-cutover.md and four nonsecret receipts. Projecte81dd66f-808c-412e-b32c-f6d910f0ac5d, production23e322c3-b195-45d8-9151-c4c27a998c33. Existing ambient account/workspace API credential, same identity as working CLI, now proves direct activeDeployments/deploymentStopped reads; no credential change. Mutation signatures deploymentStop(id), deploymentCancel(id), serviceInstanceDeployV2(serviceId,environmentId,commitSha) are read-verified, never executed in preflight. All four roles observed on d766; null drain/overlap settings do not prove warm drain. Freeze old deployment triggers, stop exact old API/Beat/participating workers, require terminal stopped plus no old active/pending processes/DB transactions, reconcile interrupted provider outcomes, then migrate/deploy exact candidate dispatch-disabled. Preserve queues and data stores. Actual stop permission/termination/recovery receipts remain mandatory at execution; no schema-introspection-as-success claim, no old unsafe rollback.

Trigger addendum, bus4134: Vulcan read-only inventory02:18Z found exactly one main trigger per API/ordinary worker/Beat/profile worker with checkSuites=false; outputs/SELLER-RAILWAY-TRIGGER-INVENTORY-S1712.json binds full IDs. Freeze MUST precede product merge, not just manual deploy/enable. Actual trigger deletion/restore receipts remain execution requirements; no production mutation was performed during review.

## 12. Gate2 Round2 disposition and final Round3 contract

R2 at6b78d050: CC042517-494540 and DeepSeek042518-191497 APPROVE_WITH_NITS; GLM042517-890760 REVISE one blocking MEDIUM (entity/chart prerequisite and2100 semantic collision). All R1 mandates closed. Source and read-only production verification above distinguish GLM's incorrect lineage claim from the confirmed account conflict. Selected correction is a dedicated2110 seller liability and bounded migration account seed/strict readiness, preserving2100 customer credit and all legacy entity/history. No unrelated branch merge or runtime account fabrication. CC trigger-guard/shared-lock/credit-route nits and DS sole-writer/shared-lock/digest nits are folded; transition wording already names the disputed DELIVERED/SETTLED cases and remains source-verified. Final full panel must assess the selected chart contract and explicit source rebuttal before MP; no acceptance is inferred from two votes.

## 13. Final unanimous Gate2 decision and implementation clarifications

Accepted candidate ee94bb25aa630d5e45c9dd0865e8636052787bb7. Final receipts: CC /Users/max/council/cc/response-20260912-044353-135269.md; GLM /Users/max/council/glm/response-20260912-044241-942974.md; DeepSeek /Users/max/council/deepseek/response-20260912-044242-336883.md. All APPROVE_WITH_NITS; no remaining blocking MEDIUM/HIGH, no design split. All R1/R2 blockers closed. These final precision/test clarifications implement the accepted contract; no extra account, policy or mechanism beyond enforcing its already-required immutable binding:

- Entity/account reports and test counts key by (resolved billing_entity_id,code), quote the exact section3 account names, and never confuse legacy-entity accounts with the selected merchant chart. Section5 now uses the exact5100 name.
- Enforce the existing immutable billing_entity_id requirement explicitly: the new table CHECK requires a nonnull entity when capture_payment_id/capture_journal_entry_id/payout_journal_entry_id/request_json/stripe_transfer_id is present or refund_applied_cents>0. A BEFORE UPDATE guard rejects changing any existing nonnull billing_entity_id (including clearing it); initial NULL to the validated merchant ID is allowed once. This guard lives in the already-listed migration, with service validation before each effect. Both readiness helpers/report detect any bound authority with null or different merchant entity and refuse money work; tests cover rejected reassignment/clearing, nonnull binding, chart mismatch, initial binding and replay unchanged.
- Model transition coverage explicitly includes DELIVERED/CONFIRMED/SETTLED to REFUNDED under validated provider proof; trigger remains the sole actual full-refund emitter. No generic public transition bypass.
- GLM's production-audit nit remains a required execution proof, not another speculative blocker: the runnable read-only audit script is /Users/max/koskadeux-state/s1656/s1714-finance-chart-audit.py, with no embedded credentials. MP must execute the dedicated disposable PostgreSQL full-ancestry/repeat/missing/conflict/nonmutation tests, and operators must obtain fresh exact pre/post production reports before enablement. Existing input snapshots never substitute for those results.

MP may now build Chunk A on exact backendd766 and the named build branch. Mars may implement the reviewed environment Chunk B separately. Both complete implementations require unanimous exact Gate3; production trigger freeze before any product merge, genuine hold/proof sequence, quiescence, and actual production verification remain mandatory. This decision does not complete any production BQ or permit editing protectedrun25.

## 14. Bounded fixture amendment F1 — unanimously accepted

This section is accepted implementation authority. It supersedes ONLY the P/N buyer mapping and additional environment files; the complete money protocol accepted in section13 remains unchanged.

Ground truth at backendd766: checkout.py97-106/204-210 refuses a second purchase for the same buyer/listing when the existing order is completed, delivered, or pending_delivery. Thus R3's same buyer01/listing P/N cannot execute sequentially. Do not exploit two unpaid checkout tabs, manufacture order rows, clear access, refund early, change ordinary checkout policy or reuse a positive order. Mars and Vulcan independently verified this (bus4145/4146). The packaged e2e/account_pool.json already contains buyer02 UUID4b10c456-5936-49d7-be3f-d9c68c9a263e, rolebuyer, secret references E2E_SYNTHETIC_BUYER_02_EMAIL/PASSWORD.

Selected fixture: P remains buyer01 purchasing seller01's existing single S3 fixture listing; N is buyer02 purchasing that SAME listing through normal sequential canonical checkout after P's purchase. N still downloads, confirms BEFORE revocation, waits its own genuine48h, proves bare-revoked refusal then actual full refund and refund-specific refusal. P stays unrevoked and proves its own real payout. Same four purchases total across online/offline; no second dataset/listing/S3object/device/Connectaccount, no larger paid-run budget. W remains seller02/buyer01 in its separate later window. Exact per-role UUID/email/is_test and provider/DB ownership are mandatory; neither buyer's authority permits the other role. No actor-only bypass.

Product amendment is limited to already-listed s1681_refund_test_admission.py, scripts/order_money_protocol.py and their listed tests: negative authority requires N buyer02 and counterpart P buyer01, same exact seller01/listing, distinct order/tx/PI/charge and buyer IDs. Store actual buyer_id already in schema; no new schema column is needed. Failed-run authority resolves buyer01 for roleP or buyer02 for roleN; wrong role/buyer/current binding refuses. All other invoice/customer/marketplace code and public checkout remain untouched. Test signed N admission, swapped P/N buyer rejection, N authority cannot admit P/W, wrong seller/listing/pins/provider identity, and exact failed-role release. Main protocol MP build can continue; its test-identity amendment must be implemented by MP after this amendment's acceptance and included in the exact Gate3 candidate. No mutation of the running dispatch prompt or unreviewed product edit.

Additional environment manifest beyond section2: EDIT seed/seed.py, bin/seed, browser/s1590-money-path.spec.ts and README.md; tests/test-seed-contract.py is now an implementation-edit target (already a required test). Existing section2 targets compose.yaml, bin/verify, bin/check-settlement, browser/s1681-delivery-leg.ts, tests/compose-contract.sh, tests/test-delivery-contract.py, tests/test-journey-contract.py, tests/test-nightly-contract.py and tests/test-refund-proof-contract.py carry the matching updates. seed/host-seed.py needs no code change: it executes seed.py in the backend whose explicit Compose env transports the two new buyer02 values. No other new file is authorized by F1.

Seed exactly FOUR packaged fixtures seller01/buyer01/buyer02/seller02 plus the existing unique non-test admin system user. buyer02 is local TEST buyer only, no Connect/seller setup/TOTP/device identity. Validate the complete four-ID set, UUID/email/role/is_test, both buyers without seller or payout state, and no unknown users; refuse partial/incompatible existing sets. New source still rejects every pending/purchasing protected checkpoint before lifecycle commands, so never add buyer02 to run25. Only the already-authorized next clean lifecycle creates the fourth local row after protectedrun25 completes. Update both fresh and existing seed checks, receipt fixture alias list and docs; original seller02 ineligible tests/order remain unchanged, N login occurs only in the delivery leg. Browser aliases add buyer02; ordinary login function remains credential-by-alias and must receive only the allowed explicit value.

Credential custody: read-only exact name checks on2026-09-12 show buyer02 email/password absent (404) in backend Infisical test-env and prod. No secret values were emitted or changed. After F1 acceptance, provision ONLY the two absent test-env keys under projectbd272d48-c5a1-4b52-9d24-12066ae4403c: email buyer-02@e2e-test.ai.market, password generated by cryptographic RNG with >=32 random bytes. No prod write, reuse/rotation of existing credential, new pool UUID or global synthetic account. Use the established authenticated Infisical API and seed User-Agent; values stay in process memory, not argv/log/evidence. Refuse concurrent existing conflicting values rather than overwrite; rerun validates/reuses the exact existing reserved email and strong password. Verify presence/shape only. Explicitly pass these two names to TEST backend/browser in Compose and bin/verify's injected credential requirements. New values are inert for protected already-running containers; do not recreate or repin them. Seeding/account login executes only after supported lifecycle at new exact pins.

Required executable tests: exact fourth packaged fixture/unknown-user and partial-set refusal; both buyers' role/Connect-state assertions; missing/wrong email or short password before transport; no secrets in argv/artifacts; P and N normal sequential checkout with distinct buyers; per-role snapshot/order selectors reject missing/extra/swapped orders; actor/role-specific negative/failed authority and W exclusions. Run all previous seed/delivery/journey/nightly/compose tests plus v2 refund-proof suite. The same schema2 role/phase/source/confirmation/realhold/refundreceipt contract applies. No passing oldv1 summary counts as corrected P/N proof.

F1 also clarifies the existing canonical hash requirement: use safe cross-language integers (absolute<=9007199254740991), recursively sorted ASCII object-key emission. JavaScript must emit sorted key/value pairs directly rather than rely on JSON.stringify of a reconstructed object, whose integer-like keys reorder numerically. The committed environment work-in-progress db95e8d48a6d4b405bd30a9217954ad050fe70e5 executes the actual JS and Python serializers against the same numeric-key/empty/nested golden bytes; 6 tests plus108 existing delivery contracts pass. Helpers are not yet wired to v2 lifecycle; this is evidence of byte-contract implementation only. MP's matching provider request/receipt hashes must use these exact bytes and golden fixtures.

Unchanged: product MP builder, exact unanimous Gate3 for complete product/environment, all production accounting/refund/payout guards, genuine48h online/offline phases, failed paid refund release before rerun, no Compose polling/cache, run25 preservation and pre-MERGE production trigger freeze/quiescence/production verification. No additional Max decision is needed for this required TEST fixture correction within authorized production scope.


### F1 unanimous decision and precision folds

Accepted candidate 9ab2e7cfef6272b6c708093d28c135d61f7b93c9, base baac7653f93df83fd4ddf9c56e3fc2b68ac2351a. Full receipts: /Users/max/council/cc/response-20260912-050540-035992.md; /Users/max/council/glm/response-20260912-050540-379593.md; /Users/max/council/deepseek/response-20260912-050540-731458.md. All APPROVE_WITH_NITS; no HIGH or MEDIUM findings. The following folds resolve their nonblocking precision requests without expanding scope:

- Every section8 N buyer01 literal (candidate lines151,155,159, including negative and failed-role authority) is superseded by N buyer02. P remains buyer01; W remains seller02/buyer01. Existing role/source/owner/provider binding checks remain mandatory.
- Online/offline reuse follows the already accepted guarded clean-seed phase rule in specs/BQ-MONEY-PATH-DELIVERY-LEG-S1681-GATE1.md:159; no active purchase is bypassed. Protected run25 completes before that lifecycle.
- Immediately before creating credentials, repeat authenticated exact-project/test-env/name reads. Abort on a conflicting existing value; never overwrite. Historical presence receipts alone do not authorize assuming absence.
- Exact host test evidence at environment db95e8d48a6d4b405bd30a9217954ad050fe70e5: `rtk proxy python3 tests/test-refund-proof-contract.py` exited0, ran6 tests, OK, no failures or skips; `rtk proxy python3 tests/test-delivery-contract.py` exited0, ran108 tests, OK, no failures or skips. Full output and SHA256 are bound in /Users/max/koskadeux-state/s1656/s1714-receipt-tests-db95e8d.json and its two named logs. GLM independently executed6 byte-contract tests. DeepSeek's temporary-directory restriction was a review-environment limitation, not a product test result. These results prove only the stated helper/existing contracts, not v2 integration.

Mars may implement the bounded environment fixture amendment now. MP must implement its listed product admission/CLI/test identity changes in a subsequent dispatch after the current core build terminates; no running prompt mutation. Complete product and environment candidates still require exact unanimous Gate3 before release.


## 15. Bounded posting-trigger amendment F2 — unanimously accepted with fulfilled evidence mandate

F2 is accepted implementation authority after the unanimous panel and fulfilled evidence mandate recorded below. Exact MP partial candidate88d6cdc7019cf2d7c99345fc7e7f7ef58bcdeb6a contains strict refresh/readback of actual journal posting and exposes an existing prerequisite defect. Source at backend d766b8e4e66d7803ebe48f8a3132521abbebe957, alembic/versions/20260318_001_bq_financial_system_gate2_phase1.py:577-595, and the full-ancestry disposable database installs finance_prevent_posted_entry_mutation as BEFORE UPDATE OR DELETE on journal_entries, rejects OLD.status='posted' OR OLD.posted_at IS NOT NULL, then unconditionally RETURN OLD. Thus an allowed draft UPDATE is silently replaced with its old row, including the normal draft-to-posted transition. Do not call balanced draft lines posted accounting evidence. The existing engine now refuses that stored outcome, correctly exposing eleven posting-dependent tests.

Evidence: /var/tmp/s1714-continuation-63aef854.json; exact command `rtk proxy python3 /var/tmp/s1714-run-63aef854.py -m pytest -q tests/test_order_money_protocol.py tests/test_refund_processing_protocol.py tests/test_finance_phase2.py --tb=line`, full log /var/tmp/s1714-service-tests-63aef854.log,63passed/15failed/0skipped. Eleven failures are real journal posting failures; four are pre-existing missing subscription_service imports. Broader finance counts without a retained standalone log are not normative evidence. Six inherited failures were independently reproduced on unchanged partial base 0d3c25f98ff7a222590b0dc5c203d93636b894b9 in /var/tmp/s1714-baseline-failures-63aef854.log. Exact baseline command: `rtk proxy env S1714_TEST_LOG=/var/tmp/s1714-baseline-failures-63aef854.log python3 /var/tmp/s1714-run-63aef854.py -m pytest -q tests/test_finance_phase1.py::TestMigrationStructure::test_migration_downgrade_drops_tables tests/test_finance_gate2_phase1.py::TestGate2Models::test_billing_entity_has_gate2_columns tests/test_finance_phase2.py::TestSubscriptionService --tb=line`, cwd /var/tmp/koskadeux/minimal-bridge-worktrees/7bbcbe97e027-d031f2; invocation retained in MP235 builder-output log. Earlier14pass balanced-line results are expressly superseded and do not prove posted journals. All evidence is disposable PostgreSQL, no production mutation.

Selected minimal correction in the already-listed new migration alembic/versions/20260912_001_order_money_protocol.py: recognize the exact old function body and already-corrected body, refusing absent/unknown function or incompatible trigger binding. Preserve the existing rejection for any OLD posted status or nonnull posted_at. For an allowed DELETE return OLD; for an allowed UPDATE return NEW. Keep the same function name/signature/language, trigger timing/events/row scope, all posted-entry/posted-line protections and all posting-balance/period/entity guards. Do not disable triggers, write around posting, silently bless a draft, alter historical journal data, introduce a second posting path, or edit historical migrations. The proposed MP-authored patch /var/tmp/s1714-journal-trigger-amendment-63aef854.patch is evidence, not yet applied; MP must complete binding verification and tests as specified here, not blindly apply a partial patch.

No extra production file is added to the manifest. Existing order-money/refund/finance test targets must execute the actual migrated trigger: (1) normal balanced draft-to-posted UPDATE persists status, posted_at, totals and exact account balances; (2) any UPDATE or DELETE of OLD posted status OR nonnull posted_at still fails and leaves history unchanged; (3) an ordinary unposted draft edit persists and permitted draft delete succeeds, while existing line/period/balance/foreign-key guards remain effective; (4) missing, altered or unexpectedly attached function/trigger refuses migration/readiness; (5) upgrade on full ancestry and a repeat upgrade converge without data mutation; (6) all eleven posting-dependent positive paths pass and real capture/refund/Transfer allocations are asserted, including late-refund and recovered-Transfer order independence; (7) shared old finance posting still works without changing credit grant or invoice/subscription semantics. Keep unrelated baseline failures separately evidenced rather than expanding scope to hide them.

Both readiness helpers and the private production report must verify the installed corrected function and its exact expected journal_entries trigger binding before money work. Pre/post deployment read-only reports must include that schema check. Production quiescence, pre-MERGE trigger freeze, no mixed old writers, default-disabled deployment, fresh adoption checks and no automatic historical repair remain mandatory. Any unexpected historical draft or inconsistent journal is reported and blocks affected adoption; this fix does not automatically post old entries. MP alone implements accepted F2; complete product still requires unanimous Gate3. F1 and all other accepted contract sections remain unchanged.


### F2 Round1 disposition and exact Round2 binding

R1 at47697895: CC response-20260912-053228-757615.md and GLM response-20260912-053229-492952.md APPROVE_WITH_NITS; DeepSeek response-20260912-053229-123947.md APPROVE_WITH_MANDATES, one MEDIUM requiring an enumerated binding. Full reviews read. The following complete precision fold closes that gap and their nonblocking evidence/regression requests. F2 still requires the corrected full panel before MP implementation.

- Exact supported function: public.finance_prevent_posted_entry_mutation(), zero arguments, return type pg_catalog.trigger, LANGUAGE plpgsql, SECURITY INVOKER (prosecdef=false), normalized body equal to the recognized old or corrected body. Verify catalog function identity, not search_path-dependent name alone. Prove both historical whitespace/indentation shapes (000_initial and 20260318_001) normalize to the same accepted old body; do not widen acceptance to arbitrary body changes.
- Exact effective attachment: the sole attachment of that function is the noninternal trigger trg_immutable_posted_entries on public.journal_entries, enabled for origin (tgenabled='O'), BEFORE DELETE OR UPDATE, FOR EACH ROW, no WHEN, no UPDATE OF column restriction, no trigger arguments, not a constraint/deferred trigger, bound to the exact public function OID above. Assert the full tuple BEFORE replacement and read it back AFTER replacement; readiness/report require the corrected body and same tuple. Refuse any different function (including prevent_posted_entry_mutation), WHEN-guarded s100 variant, statement trigger, different events/timing/table, disabled/replica-only binding, extra attachment or restriction. Do not silently replace a variant trigger to make it pass.
- Preserve and verify the existing separate enabled attachments before/after: public.journal_entries trg_closed_period_guard (BEFORE INSERT OR UPDATE, ROW, finance_guard_closed_period), trg_balanced_on_post (BEFORE INSERT OR UPDATE, ROW, finance_validate_journal_posting); public.journal_lines trg_immutable_posted_lines (BEFORE DELETE OR UPDATE, ROW, finance_prevent_posted_line_mutation), trg_line_entity_consistency (BEFORE INSERT OR UPDATE, ROW, finance_guard_line_account_entity). Names resolve to public functions; no policy/body rewrite is authorized for these guards. Tests explicitly UPDATE and DELETE posted journal_lines after a real persisted post and require unchanged rows on refusal.
- Existing shared callers are FinanceEngine.record_commission, record_credit_purchase, record_subscription_payment, record_stripe_fee, record_refund and record_api_cost. The correction makes their intended posted state actually persist; do not claim their old silent-discard behavior already worked. Exercise each through the migrated trigger, refresh/re-select status and posted_at, assert exact journal lines/balances and subsequent posted mutation refusal. The manual approve_and_post_entry path is a separate implementation against the same journal_entries trigger: it sets posted_at without calling _post_entry or setting status. Its regression test must assert actual posted_at persistence and subsequent mutation refusal, and report any residual status inconsistency through the normal product process; do not invent a passing status transition. A newly exposed post-then-mutate defect is reported and fixed through the normal accepted product process, never by weakening immutability or widening invoice/subscription features.
- The proposed patch must NOT be applied verbatim. It checks the normalized body but lacks return-type/language/security metadata, trigger-binding, other-guard-preservation checks and tests. MP implements the completed contract in the already-listed migration/readiness/report and test files only. No second migration, historical rewrite, automatic data repair, or unsafe old-body rollback is authorized. Keep release disabled if the corrected candidate fails; preserve all other existing forward-only cutover requirements.
- Fresh independent production read at2026-09-12T03:32:01.195181Z verified the exact old tuple and separate enabled guards; /Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/SELLER-JOURNAL-TRIGGER-LIVE-READ-S1712.json includes the full read-only SQL and nonsecret catalog result, old raw-body SHA2561f2b86b5ac66d98691c8c4d8b1c6b98d48d16fb551aed59de795475e8260e724. SELLER-JOURNAL-TRIGGER-TEMP-PROBE-S1712.json and assessment in the same directory bind an independently executed session-temporary table/function probe on verified disposable PostgreSQL17, rolled back: OLD discards draft posting; NEW persists it; posted status or nonnull posted_at still refuses UPDATE/DELETE; draft DELETE works. This proves only return/immutability semantics, never full migrated accounting or production readiness.


### F2 unanimous decision and fulfilled evidence mandate

The full Round 2 panel on candidate 632f03fc8b7cd5bacabea19e168e44981dcbb912 is complete and read: CC /Users/max/council/cc/response-20260912-054331-014728.md APPROVE_WITH_NITS; GLM /Users/max/council/glm/response-20260912-054331-410402.md APPROVE_WITH_NITS; DeepSeek /Users/max/council/deepseek/response-20260912-054331-718976.md APPROVE_WITH_MANDATES. All accept the design and confirm the Round 1 binding mandate closed. DeepSeek's sole remaining MEDIUM is evidence provenance and an explicit operational statement, not a design disagreement. It is fulfilled by the following fresh independently obtained evidence and statement, without altering the reviewed correction:

Vulcan S1712 executed a new read-only production observation at 2026-09-12T03:50:26.015103Z, delivered on persisted peer bus message 4170 and read/acknowledged by Mars S1714. New immutable receipt: /Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/SELLER-JOURNAL-TRIGGER-PRODUCTION-PROVENANCE-S1712.json; reusable invocation helper ../work/journal-production-provenance-s1712.py. The receipt binds Railway project e81dd66f-808c-412e-b32c-f6d910f0ac5d (ai-market), environment 23e322c3-b195-45d8-9151-c4c27a998c33 (production), Postgres service b86ef2a3-97e1-42fe-922a-73fe18b1d518, instance a2d74c5e-7bf6-4a5d-9d8b-7bee8e20d2df, actual database railway, target assertion success, exact credential-resolution argv and full BEGIN READ ONLY/ROLLBACK SQL/result. Connection credentials remained in memory; none appear in the receipt. The original receipt remains unchanged.

**The defective function is confirmed installed in production at that observation.** Its raw body SHA256 is 1f2b86b5ac66d98691c8c4d8b1c6b98d48d16fb551aed59de795475e8260e724, matching the original and source; the actual enabled BEFORE UPDATE/DELETE trigger uses it. Allowed draft updates can therefore silently discard status/posted_at persistence in the six shared callers. This catalog proof does not establish which historical rows were affected or any incorrect historical balance. Fresh adoption/report checks must establish those facts; no automatic historical posting or repair is authorized.

Final nonblocking precision folds: require tgenabled='O' for the target AND all four preserved guards; reject D, R and A variants. Count all noninternal attachments by the exact public function OID (`pg_trigger WHERE tgfoid = <resolved public OID> AND NOT tgisinternal`), require exactly one, then assert its full tuple. The earlier table-filtered receipt does not by itself prove the sole-attachment count; migration/readiness/report must execute that query. Tests use explicit database refresh or an independent committed read, never the caller identity map alone. The separate manual posting path is accurately distinguished above. The normalized corrected function body is exactly the following (only existing accepted whitespace normalization is permitted):

```sql
BEGIN
    IF OLD.status = 'posted' OR OLD.posted_at IS NOT NULL THEN
        RAISE EXCEPTION 'posted journal entries are immutable';
    END IF;
    IF TG_OP = 'DELETE' THEN RETURN OLD; END IF;
    RETURN NEW;
END;
```

MP may now implement this bounded F2 in its next dispatch after the active build terminates. No active prompt mutation. The full design panel is unanimous with its evidence mandate fulfilled; no new product surface or release exemption was introduced by this decision. Corrected migration/finance tests and all complete product/environment candidates still require actual passing evidence and unanimous Gate3 before any release. Production freeze, quiescence, real hold sequence and forward-only release rules remain unchanged; the Council's advisory suggestion to restore the old buggy body is not authorized.


## 16. Bounded adjacent-test manifest amendment F3 — unanimously accepted with mandates

This amendment changes only the executable test-edit manifest needed to verify the already accepted core/F1/F2 product contract. It authorizes no new production behavior, runtime mutation, paid proof, migration, unrelated finance feature or weakening of an accepted guard. Product/test author remains MP, and the complete exact product/environment candidate still needs unanimous Gate3.

Evidence: terminal MP236 candidate88f8231edec5bc25c6450f1d1e5d7b657ab6f72c, base88d6cdc7019cf2d7c99345fc7e7f7ef58bcdeb6a; /var/tmp/s1714-continuation-6391682b.json contains exact source hashes, commands and classifications. Retained adjacent log /var/tmp/s1714-exact-committed-adjacent-6391682b.log SHA2563a881eb562940cdff7a833f40da746140f436cdf75678bfcfa2db712f78ee309 reports250passed/28failed/6skipped. Two failures were independently reproduced before this increment (historical downgrade source assertion and legacy doppler_key_prefix model expectation); the other26 are not classified as baseline. Core log /var/tmp/s1714-exact-committed-core-6391682b.log SHA25638e8a5b5b85c20da648d6794c5fb3f70dc998a1b41a3eacbcf3efb157bad5cbd reports112passed/23failed/0skipped, including19 posting-dependent failures and4 previously verified missing subscription imports. F2 implementation is now independently running in MP237; this proposal does not mutate that dispatch or authorize its early use.

Add these EDIT targets, limited to their changed-contract cases and necessary fixtures:

- tests/test_settlement.py: exercise the shared order-money reservation/dispatch authority, actual linked order, PG latest confirmation clock, frozen settle_<tx_number> request and idempotent persisted Transfer. Preserve successful payout, missing/nonconfirmed/unlinked/seller-ineligible refusals and retry-tier assertions. Replace fragile sequential db.execute-count assumptions with assertions of actual business outcomes. A unit test may mock the shared service to prove delegation; such a test is not the real lock, hold or provider-once acceptance proof.
- tests/test_transaction_state_machine.py: reflect the explicitly accepted SETTLED-to-REFUNDED edge; settled is no longer unconditionally terminal. Assert precisely that permitted edge and every forbidden settled target; REFUNDED/CANCELLED/EXPIRED remain terminal. Test the actual restriction on order-linked transitions separately with real authority. Do not infer validity merely by iterating the implementation transition map.
- tests/test_order_escrow.py: update confirm/legacy payout tests to the accepted common authority and default-disabled dispatch. Confirming an order does not entitle an immediate Transfer; both legacy and transaction-managed routes respect the real hold, revoke/refund/dispute guards and TEST suppression. Preserve escrow and ordinary owner checks, do not simulate authority using a permissive product fallback.
- tests/test_gold_path_transaction.py: correct mock fixtures for caller-owned transition/read-lock semantics and confirm/settle delegation where they actually changed. Reproduce repeated-state errors on real database before attributing them to stale mock side effects; if real, fix the production defect inside the existing manifest. Preserve all existing quote/accept/pay/deliver/dispute outcomes and exact event counts; no new public status or silently accepted duplicate transition.
- tests/test_finance_gate2_phase1.py: update only affected BillingService capture/refund tests and fixtures. Canonical order PI with order/transaction metadata but no customer_id must reconstruct a trusted Payment; unrelated payment behavior remains unchanged. Mock provider reads at the actual boundary, with complete realistic PI/charge/refund identities and pagination, never make network calls in unit tests. Refund-before-capture now reconstructs valid canonical history through the accepted normal processor rather than requiring a preexisting Payment; malformed identities still refuse. Preserve credit-topup behavior and at-most-once effects. The unrelated doppler_key_prefix baseline assertion is not authorized for deletion or unrelated model changes by this amendment.
- tests/test_transaction_trigger.py: replace unconditional skipped pass placeholders with executable real PostgreSQL tests against the full migrated ancestry. Preserve paid/completed synchronization and event provenance, verify that a partial refund leaves the accepted transaction/access state unchanged, and full refund after partial produces exactly one correct transition/event. Include no-transaction legacy behavior and preservation of already-settled Transfer evidence. If no disposable PostgreSQL is available, report that prerequisite and stop those acceptance claims; a conditional unit-only skip does not satisfy Gate3. Existing new protocol test files may supply shared fixture code without a new unreviewed conftest/helper file.

Classification is a mandatory deliverable: for each of the26 failures and6 skips, record exact test/node ID, old assertion or fixture, accepted specification rule, whether a real product regression or test-contract mismatch, the resolution and the exact replacement evidence. The count alone cannot justify updating expectations. Existing baseline failures remain explicitly reported, without claiming all green or broadening into missing subscription implementation. No blanket xfail/skip, deletion of coverage, broad exception acceptance or assertions copied from observed output. Any existing assertions unrelated to the changed accepted contract remain intact.

Use deterministic provider doubles only for network boundaries; core protocol tests retain real PostgreSQL locks, trigger/FK behavior, committed readback, fault injection and full endpoint signatures. Require exact success and refusal reasons at the public/service boundary prescribed by the accepted contract, not Python mock artifacts. A changed status code requires a documented existing contract basis; otherwise preserve the public behavior or report a product defect. Test updates may not retrofit runtime to test mocks or conceal a defect in environment code. Run the complete listed adjacent suite, core/F1/F2 suite, full-ancestry migration proof and accepted race/fault matrix on the final committed candidate; retain nontruncated logs with command, exit code, counts, candidate/source hashes and all remaining baseline failures/skips. No passing placeholder or old balanced-draft journal evidence counts as completion.

After unanimous acceptance, MP may implement this bounded amendment in a subsequent dispatch; no running prompt mutation. Mars and Vulcan may inspect/classify in parallel but may not author product tests. This amendment itself neither approves a code candidate nor changes production cutover, protected run25 or ordered settlement windows.


### F3 unanimous decision and required precision folds

Accepted candidate5f8eac19aba6ede9c053980c35b9aff16a4a771c, base8b4a315ddfc5325959190ca6ae1b5e07fc915b35. Full responses read: /Users/max/council/cc/response-20260912-061112-124186.md (APPROVE_WITH_NITS), /Users/max/council/glm/response-20260912-061111-790164.md (APPROVE_WITH_NITS), /Users/max/council/deepseek/response-20260912-061112-501808.md (APPROVE_WITH_MANDATES). No HIGH finding. The following folds are mandatory implementation requirements; they fulfill DeepSeek's two MEDIUM requests and the panel's inexpensive precision requests without adding product behavior or write targets.

1. Resolve classification before changing each affected test. Any genuine product regression whose correct fix lies outside the accepted manifest must not be resolved by editing an adjacent test: stop that affected path, report its exact defect and minimum additional file/behavior requirement to Mars, and obtain a further bounded panel decision. Continue independent authorized work; do not ask Max for routine permission. The classification includes an explicit disposition (fix in accepted product file, test-contract correction with evidence, or blocked/reported outside manifest), not merely a regression/mismatch label. A trace naming an outside-manifest file is not by itself proof that the defect lies there; reproduce the real behavior first.
2. Enforce test isolation in each edited adjacent test module using an autouse provider-transport/network guard that fails any unmocked Stripe/provider HTTP request; do not depend on an absent API key. Install deterministic doubles at the actual boundary, with a guard self-test demonstrating fail-fast rejection without network transport. Retain evidence that actual test cases made zero unmocked provider/HTTP attempts. Constructing inert SDK data objects is not a provider call; a real client request cannot escape the guard. Database connections are allowed only to an explicitly verified disposable PostgreSQL17 host/port/database/container, using the established disposable_url checks. F3 execution uses a separately owned disposable database with exact candidate migrations; never the active MP237 database or any production/protected acceptance database. No credentials or receipt documents in argv/logs. Existing module-local fixtures or the already accepted protocol test module may supply guards; no conftest edit is authorized.
3. The closed write set is the complete accepted product/test manifest in section2, as amended by F1/F2, plus the six named F3 test files. The15-file MP236 incremental diff is not the complete accepted manifest and must not be mistaken for it. No other new files, edits to existing conftest files, or unlisted helper modules are authorized. Existing environment ownership remains Mars; MP does not edit it. tests/test_finance_phase1.py (only the known baseline failure) and tests/test_finance_phase3.py (clean in retained adjacent run) are intentionally read/run-only, not added edit targets.
4. Explicitly replace the sixth skipped case tests/test_order_escrow.py::TestOrderEscrow::test_confirm_fails_no_stripe_account with an executable assertion of the accepted missing-destination outcome: no Transfer request, exact refusal/audit and preserved authority/order state. Do not restore an obsolete unconditional exception solely to satisfy the old fixture. The other five skipped nodes are test_order_paid_syncs_transaction, test_order_completed_syncs_to_confirmed, test_order_partially_refunded_syncs_to_refunded, test_trigger_creates_event and test_order_without_transaction_is_noop in tests/test_transaction_trigger.py. Replace their pass bodies with real full-ancestry PG assertions; the old partial-to-refunded name/expectation must be corrected to the accepted partial-state preservation requirement. Record old-to-new node mapping explicitly.
5. The classification artifact names every26 nonbaseline failure and6 skipped node exactly once, alongside both adjacent baseline nodes and the4 core subscription-import baseline nodes. Each baseline assertion requires its retained identical base-run proof or a precise per-node unchanged-source/manifest-exclusion proof; if later edits affect its dependencies, repeat the baseline check rather than retain an unexamined label. Reconcile every original node against the final committed candidate's actual outcome and identify every newly failing/skipped node. New failures/skips cannot be omitted because they were outside the MP236 snapshot count. A machine-readable JSON ledger in private retained evidence is useful, not a new production repository file or extra permission gate.
6. Independent read-only classification was completed by Vulcan and read by Mars: /Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/SELLER-MP236-ADJACENT-CLASSIFICATION-S1712.md. It binds exact88f8231e and the retained adjacent log SHA above, maps all26 failures/6skips, and confirms the same six test files. Its static/AST findings are not substitutes for final real-PG proof. Preserve two actual product defects in that classification: (a) TransactionService.transition at88f8231e refuses generic refunded only when order_id is present, so an orderless buyer/system caller can request an unproved refund; (b) create_quote UPDATEs transactions before transition acquires the parent order lock, reversing the required order-before-transaction order for linked orders. Both fixes lie in the already accepted transaction_service.py; neither may be hidden by changing mocks.
7. The section16 model-edge bullet means provider-owned full refunds may transition a settled transaction to refunded; it does not authorize the generic transition API to manufacture refunds. Assert linked and orderless generic buyer/system refund attempts produce no state write, event or commit, while the normal signed/provider-validated refund processor succeeds exactly once. Do not merely remove SETTLED from a terminal-state list. For quote, acquire the accepted parent/transaction locks before the first transaction write on a valid supported origin, or prove an already-unsupported linked-order origin is refused before any write. Never broaden quote behavior to fit an unrealistic fixture. Run a deterministic two-connection test for the valid path and preserve ordinary quote/accept/payment state/event behavior.

These folds accept F3 for MP implementation after MP237 terminates, in the next dispatch. No running prompt is mutated. Full final code, migration, race/fault, provider isolation, adjacent/core regression and harness proof remain Gate3 requirements; F3 acceptance is not a release approval.

## 17. Bounded partial-refund access amendment F4 — consolidated Round3 pending unanimous review

The accepted policy retains previously delivered partial-refund access unless independently revoked or otherwise closed, and forbids full-refund access or financial-state regression. Round1 3c0eab01 and Round2 154cf40c are NOT accepted: each full CC/GLM/DeepSeek panel was read, with DeepSeek REVISE despite CC/GLM APPROVE_WITH_NITS. Round2 responses are cc/response-20260912-070602-415174.md, glm/response-20260912-070602-796604.md and deepseek/response-20260912-070603-141035.md. The actual WebSocket callback is a fourth gate; request dispatch alone cannot prove successful refresh. This single finite amendment replaces both proposals and includes the complete related access surfaces and listener safety disposition, without successive hidden implementation scope.

Ground truth: terminal MP238 7890b0c0bc6a5fed4d50b796e11fda9e49102c51, clean preserved checkout /var/tmp/koskadeux/minimal-bridge-worktrees/398e25943ee2-f6ab09 and independently matching canonical remote build/bq-refund-safe-production-s1714. Manifest /var/tmp/s1714-continuation-398e2594.json SHA256 6c8ebe3488cc852cf60ee8fb71be69d2ae6280698c93aa2f0e09dbeb7be8a793 binds 28 product files, 321 migration files, 3 protected files and 62 artifact hashes, independently checked by Mars and Vulcan. Core 266 pass/5 fail and adjacent 284 pass/2 fail, zero skips: six separately reproduced unrelated baselines plus the unchanged partial-refresh route failure. Access files traced at c9d8840b72c0fa208e9cf2eb5506767845dee9ac are unchanged in this terminal candidate. Vulcan's complete source map and checklist are retained at /Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/SELLER-F4-END-TO-END-SOURCE-MAP-S1712.md and SELLER-F4-IMPLEMENTATION-CHECKLIST-S1712.md. Mars independently read the actual route/service, callback, broker, agent, MCP and listener gates. Source traces and the retained nine-trigger catalog are not executed race or service reproductions; MP must retain real before/after proof.

### 17.1 Complete finite manifest addition

Add exactly these eight EDIT targets to the complete accepted section2/F1/F2/F3 manifest. OrderService and the three protocol test modules are already authorized. No new test module, conftest, authorization subsystem, endpoint, database migration, provider permission, token protocol, fixture identity, public registration, subscription/credit redesign or Workspace implementation is authorized by F4.

| EDIT target | Bounded required correction |
|---|---|
| app/api/v1/endpoints/orders.py | GET access is_delivered/reference and existing usable-URL eligibility plus POST refresh admission use actual persisted prior-delivery evidence and independent current-access exclusions. Select existing transaction_id, delivered_at, completed_at, confirmed_at as needed; compute async eligibility once. Authorize before current_valid disclosure. Correct the existing non-device S3 GET builder argument mismatch (currently order/listing UUIDs passed to a builder requiring actual order/version objects) within its actual branch, retaining source/version binding and informational semantics. |
| app/services/fulfillment_service.py | Correct real request_fulfillment(is_refresh=True) admission only for legitimate delivered partials, before device/provider work. Preserve initial fulfillment admission, Workspace routing, pending dedup and frozen request/device/generation bindings. Reuse current guarded S3 callback completion, which updates credentials only and preserves financial/order state. Do not add partial to the raw first-delivery UPDATE that writes delivered/timestamps. |
| app/api/v1/endpoints/trust_websocket.py | Correct actual vai.fulfillment.response admission only for the owned seller/listing/order and exact matching pending refresh request/device/generation with current access. Preserve authenticated device context, immutable S3 identity/raw URL bytes and idempotency. No unconditional partial status allowlist, stale callback write, fabricated tuple or fallback to first delivery. |
| app/services/raw_download_service.py | Real generate_download_token admits a previously delivered partial only with current guarded access and actual buyer/listing/file/version/source authority before signature, nonce or success audit. Preserve existing nonpartial paid/in_escrow/pending_delivery admission and expiry/single-use behavior. No undelivered partial grant. |
| app/services/fulfillment_broker.py | request_access and get_artifact_url recognize legitimate delivered partials before the existing guarded OrderService token path. Apply authorization before already_available or usable credentials. Preserve pending preparation and existing refusal types, with no duplicate purchase. |
| app/services/agent_service.py | Duplicate purchase returns the existing order with data_url only when partial access is actually available using persisted evidence. Preserve existing idempotency and charging behavior; no second order, wallet deduction or provider charge. |
| app/services/mcp_marketplace_tools.py | check_access discovers an owned legitimate delivered partial without false ready claims for closed access. Preserve current nonpartial paid/pending informational semantics and initiate_purchase's existing partial duplicate exclusion. No purchase-finance policy change. |
| app/services/fulfillment_listener_service.py | Narrow safety correction: before filesystem finalization, new token or delivered rewrite, refuse inappropriate or closed late completion of a staged transfer. Never regress partial/full refund order or linked transaction, reset delivery history or create a new partial first delivery. Check current owned order/transaction access before staged-token count consumption/file disclosure, including completed-transfer replay. Preserve ordinary initial streaming and legitimate already-delivered partial token consumption. |

Shared helper refinement belongs in already-authorized app/services/order_service.py. _has_delivered_access proves prior delivery only: its timestamp branch returns before transaction checks. Independently apply revoke and actual ACCESS_CLOSED_TRANSACTION_STATUSES (refunded/disputed/cancelled/expired), with actual reverse binding when transaction evidence is used. Missing/foreign links cannot manufacture delivery evidence. Credential issuance/refresh preserves buyer ownership, access window, purchased-version availability/identity, download/reservation limits and existing rate limits. can_download's advisory result is not full authorization. GET access remains informational, consumes no download and adds no unrelated expiry/version/limit error codes; newly exposed usable URLs still require the independent partial closure checks. Partial status never clears a restriction.

Listener locking must preserve the accepted order-first money protocol. Its current complete locks transfer then order and token consumption updates token without checking order; introduce a consistent order-before-transfer/token access serialization, revalidate the stored ownership tuple after locking, and audit interacting metadata/chunk/error/complete paths in this same file for inversions. Do not acquire an order lock beneath a transfer/token lock on an opposing path. Retain actual migrated trigger semantics: the sync trigger does not prevent a later delivered write from regressing a refunded transaction. Do not rely on an invented database guard or solve closure only by token revocation on one refund path. A transfer begun before refund but never delivered must not acquire first delivery after partial/full refund. Already delivered partial may consume an existing otherwise-valid token, with no financial state rewrite; complete replay must not leak an unauthorized token.

### 17.2 Required executable acceptance, through actual boundaries

MP alone authors product/tests, in the existing authorized protocol modules, using a real disposable fully migrated PostgreSQL database, normal refund processor and actual routes/services/handlers. Deterministic doubles are allowed only at actual external transport/provider/filesystem-isolation boundaries; retain the accepted fail-fast unmocked-provider guard and zero-attempt proof. Do not stub authorization, request_fulfillment, the callback completion or the money processor. Record exact base failing evidence for newly traced defects, then exact candidate passing evidence, not inspection-only claims.

1. **Complete device S3 refresh chain.** Ordinary prior delivery with actual purchased immutable object and delivery_config containing s3_presigned_url; apply normal partial refund, expire credentials while purchased access remains open, actual HTTP refresh -> real FulfillmentService -> one actual outbound vai.fulfillment.deliver -> actual flat/nested vai.fulfillment.response via trust_websocket._handle_decrypted_payload -> real persisted completion -> actual token issue and redeem. Cover online/offline queue paths. Assert exact frozen request/device/generation, generation increment once, pending tuple cleared, pending fulfillment completed, exact raw refreshed URL bytes and immutable object/version preserved. Order stays partially_refunded; transaction, ledger, delivery/confirmation history and counts remain unchanged except the explicitly authorized credential/token operations. Repeated callback cannot finalize twice; prior-generation token refuses and fresh token redeems. HTTP200, success:false, current_valid, pending or dispatch alone is not successful completion proof. Raw file_download is not this success fixture.
2. **Negative barriers at request and callback.** Exercise wrong buyer/seller/listing/request/device/generation, no prior delivery, missing/foreign reverse evidence, independent revoke, full refund and each existing closed linked transaction state. Also apply normal partial/full refund or revoke between request and callback, elapsed access, exhausted applicable downloads, unavailable/quarantined/mismatched purchased version and changed immutable object identity. Assert actual refusing boundary and unchanged config/pending state/counters/financial history as appropriate; no unauthorized URL, token, signature, nonce or success audit. Preserve normal delivered/completed behavior and existing refusal shapes.
3. **Every access surface.** Real GET reference, non-device S3 builder and device-S3 informational branches; real POST refresh and both raw routes with actual signature/nonce/audit and single/multi-file/source rules. Cover both timestamp prior delivery and null timestamps with correctly reverse-bound delivered/confirmed/settled transaction; malformed/absent evidence refuses. Preserve nonpartial raw semantics. Real broker request_access/artifact via agent routes, direct agent data regression, duplicate agent purchase metadata, MCP check_access and duplicate rejection must retain the same purchase without a second charge. Closed partial receives no credentials or false ready claim. Existing Workspace issue/file -> _context -> shared OrderService guard regression proves legitimate prior partial access and undelivered/closed refusal without adding a W build target.
4. **Staged listener safety.** Real ordinary metadata/transfer before normal partial/full refund or revoke/closure, then actual complete: no financial/order/timestamp regression, new unauthorized token or file disclosure. Actual staged token issued before full refund must refuse through the real consumption endpoint before count/file access. Verify complete replay closure, normal initial delivery and legitimate retained partial consumption. Use two real database connections with explicit barriers for refund versus complete and refund versus token consumption in both orderings; bounded completion/no deadlock, serial outcomes and exact effects. Include interacting transfer/token lock paths in the accepted lock inventory. A catalog alone is not race proof. No metadata allowlist widening into partial initial delivery.
5. **Whole candidate evidence.** Preserve all 19 former posting positives, zero skips, accepted full core/adjacent, migration ancestry, lock/FK/trigger, provider fault and concurrency requirements. Emit one complete closed manifest (all accepted files plus eight additions), requirements-to-executable-node matrix, all failure/skip classifications and retained exact commands/full logs/source and migration hashes. State finite lock coverage honestly; do not label all edges proven because a catalog enumerates them. Six unrelated baseline failures remain separately reproduced and source-bound. Bind accepted specification hash to git show of the exact accepted commit, not a moving spec checkout.

### 17.3 Explicit transport limits and release sequence

VC's alternate ActionExecutor fulfillment.response omits the frozen request/device tuple and cannot perform the existing delivered/completed correlated S3 refresh either. This is a named pre-existing alternate-transport limitation, not permission to bypass correlation or build a new VC protocol. The required S1681 chain uses the ordinary decrypted payload path. Raw HMAC consumption has no internal app caller/current-order read at this source; backend token issuance proof does not establish external VZ revocation of already-issued tokens. Retain these explicit limits for release assessment and prove the actual production delivery transport used. If production acceptance depends on either uncovered transport property, it remains an unresolved release requirement requiring exact bounded design/implementation and normal gates; do not silently certify it or use a harness workaround.

All three Round2 responses have been read and MP238 is terminal. Only complete unanimous CC/GLM/DeepSeek acceptance of this exact corrected candidate authorizes the next MP dispatch from 7890b0c0bc6a5fed4d50b796e11fda9e49102c51. Full product/environment exact Gate3, final v2 main/resume/summary/terminal-failure integration, production trigger freeze and old-writer quiescence, protected run25, genuine holds, forward-only release and externally verified production completion remain mandatory. This amendment is not implementation, Gate3 or production approval.
