# Automatic payment recovery: current candidate

Read-only assessment of evolving MP250 source. REVIEW-MANIFEST.json binds the full source copies and actual app/scripts caller search. This is not deployed proof. The independently read infra:constitution version28/CORE9.16 section3 requires operation during human absence; Max requires minimum support and human input.

| Situation | Actual mechanism | Classification |
| --- | --- | --- |
| Original agent create returns | Original checkout caller validates and persists response | Internal path implemented |
| Original response lost, matching signed success arrives | Webhook invokes BillingService, locates original attempt, reads PI/Charge and completes under event then Order/Transaction locks | Automatic on event arrival; deployed delivery proof remains required |
| Duplicate or late signed event | Existing event/payment identity validation; owned unresolved failures return500 with retained provenance | Automatic on delivery; signed-r2 nine-case diagnostic passes |
| Pending attempt, known PI | create_agent_checkout delegates to reconcile_agent_checkout; original PI read and validated local observation | Internal callable; exposed agent or scheduled caller not established |
| Prepared attempt, unknown PI after timeout | Reconciliation immediately returns provider_outcome_unknown | Product gap without a qualifying signed event; repetition alone cannot recover |
| Cancellation response lost | Known-PI reconciliation can confirm terminal cancellation and release once | Internal mechanism, same caller gap |
| Temporary database/provider failure |503 or owned webhook500 preserves retryability | Retryability is not proof of an application recovery scheduler |
| Identity conflict or insufficient spend | Financial guards refuse mutation | Agent-owned investigation; not automatically a Max decision |

## Source findings

mcp_tools.py checkout checks purchase scope and ownership before calling create_agent_checkout. It is a service function. The app/scripts search finds no production caller/import exposing checkout, initiate_purchase or check_order_status. Public mcp_sse.py imports only five discovery/read helpers and has no checkout manifest entry. The separate mcp_server manifest names buy_instant, but that does not establish a call into this function. Tests importing a function do not prove a usable transport. External packages were not audited; any external caller claim needs its actual source and end-to-end proof.

All reconcile_agent_checkout invocations found are within create_agent_checkout: existing attempt, original cancellation, or originally canceled response. No scheduled/background caller was found. Known-PI polling is not demonstrated. Reconciliation authenticates first and returns existing terminal state; absent observed PI refuses before provider lookup. Known PI gets a10second read. Original create uses a20second asyncio wait. run_stripe uses run_in_executor around synchronous Stripe calls; cancellation of the await does not establish network cancellation. No explicit max_network_retries or StripeClient configuration was found in app/scripts.

Signed agent events reuse billing/webhooks and fresh PI/Charge reads bounded10seconds each/20seconds total, with existing transaction bounds. The signed500 issue is corrected in diagnostic-agent-signed-r2 by webhooks.py alone, tests unchanged. This does not prove an absent event will arrive or a never-dispatched prepared attempt will recover automatically.

## Minimum correction to assess

Reuse existing authenticated transaction services and agent transport. Name and prove the real caller for recovery of the original purchase. Evaluate standard Stripe SDK bounded same-key/same-parameter retries within an explicit retained idempotency window for original response loss. Handle an underlying network call still running and commit uncertainty without another purchase, changed request or indefinite replay after retention. This is a proposal for MP and GLM/DeepSeek review; do not silently change the active frozen request or global wrapper.

Prove no-incoming-event recovery, response loss, delayed provider completion, repeated agent invocation and expiration of the retry window. Keep signed-event recovery, once-only release, ownership and purchased rights. Retain the separate actual concurrent-preparation one-winner proof. Documentation or manual Dashboard lookup cannot replace missing product behavior.

Ordinary recovery belongs to agents. Escalate to Max only for a genuine strategic/product or authority decision unresolved by existing instructions. A timeout, code defect or unknown payment is not intrinsically human-only. Current owner intervention caused by missing automation is an implementation limit to remove, not the desired operating model.

## Concrete completion delta for the existing review

Runtime probes at22:34UTC: production health200, marketplace MCP path404, finance-agent audit path401 explicitly requiring X-Internal-API-Key, customer checkout verification401 requiring authentication. PUBLIC-RUNTIME-PROBE.json retains complete responses. A404 does not distinguish a disabled flag from failed mount/routing. Candidate main.py993–1006 conditionally mounts marketplace MCP; do not call it enabled. The finance-agent router is unconditionally mounted at990–991 and the runtime401 confirms the protected route is present, not successful authenticated execution.

Marketplace MCP uses aim_ MCPAPIKey identity with user_id/scopes. Its registered initiate_purchase returns browser Checkout; it does not call the agent_api_keys organization/spend attempt service. These key IDs are not interchangeable. Its local idempotency cache is stored after Order/Session work, so lost responses can produce active-Order refusal; no original-attempt recovery guarantee is established there. Retain ordinary buyer browser consent/SCA. Do not add a second customer checkout or silently route these keys into the agent service.

**Recommended operations entry:** reuse POST /api/v1/finance/agent/command, protected by the existing X-Internal-API-Key. Add one narrowly typed automatic command, recover_agent_payment, accepting original transaction_id plus expected organization and optional original provider/event locator for evidence collection. Resolve the original agent API-key identity from the transaction and validate organization and immutable attempt internally; a supplied locator is not authority. This is internal finance-operator recovery, not a new customer purchase endpoint. Existing authenticated finance agents, including the authorized Mars operator, invoke it; no Max approval click is part of ordinary recovery. A real agent invocation must be demonstrated before acceptance. The current run_reconciliation command reconciles financial periods and is not evidence of attempt recovery; preserve that meaning.

Use the existing policy engine automatic category and audit mechanism for this deterministic recovery command. Return bounded structured outcomes (completed, still_pending with retry_after, investigation_required with reason and original IDs) while preserving the existing service's409/503/cancelled422 semantics internally. Do not let submit_command's blanket422 exception handler erase retryability or falsely claim rollback can undo an earlier service commit. The command adapter must respect the existing service-owned commits and independently verify the terminal result. Do not force successful business outcomes from malformed or contradictory records.

**Exact proposed additional target files:** app/routers/finance_agent.py (typed command dispatch/outcome adaptation); app/schemas/finance.py (closed recovery command and result fields); app/services/finance/policy_engine.py (one automatic command entry in existing policy); tests/test_finance_phase2.py (mounted authenticated invocation, policy and outcomes). No new router, registry, table, migration, generic worker or checkout flow. Existing approved transaction_service.py, order_money_service.py, webhooks.py and test_refund_processing_protocol.py carry any required recovery implementation and financial tests. These additions require the existing task's GLM/DeepSeek amendment; no active MP250 file/request mutation has occurred.

### Original-request recovery policy to review

Installed qualification SDK already sets max_network_retries=2. Its RequestsClient default timeout is80seconds, while this candidate abandons its executor await after20seconds. Extra retries are not the fix. Configure a request-local client for this flow with explicit bounded network timeouts and retain the SDK's retry algorithm; do not change global settings. Choose and verify the SDK/network budget so it fits inside the outer operation budget and the retained idempotency window. Capture late original completion using the same saved request identity; cancellation of an asyncio waiter does not prove the network request stopped.

Known PI: use existing read/revalidate/reconcile, never create another PI. Unknown PI with a fully valid durable original request: within a conservative23-hour window from prepared_at, a reviewed recovery call may retry exactly the same original request and idempotency key using standard Stripe idempotency. This explicitly amends the prior one-shot no-retry rule; it is not already approved or implemented. The window is earlier than Stripe's minimum24-hour retention and must include remaining request/retry time. No changed amount, metadata, customer, payment method, account or key is allowed. Same-key concurrent retries may refuse transiently; they must never switch keys. Provider/network calls hold no money locks.

Crash after prepare commit but before dispatch: local prepared state cannot distinguish this from lost response. Inside the window, the same-key request safely starts the original purchase if absent or returns its prior result if present. Outside the window, neither case authorizes another create: retain unresolved state and let the existing finance agent collect original provider/event evidence. Do not infer absence from a failed search or absence of a webhook. Do not release held spend without definitive original no-liability evidence. Terminal, expired or contradictory cases remain agent-investigated via the same protected command and provider-read tooling; no manual DB edits or Max Dashboard work is the normal path.

Acceptance must demonstrate the actual authenticated existing-agent caller, not only direct helper calls. Cover knownPI recovery, no-dispatch crash, lost response, late original thread completion, absent webhook, same-key concurrent retry, changed-parameter refusal, retry-window boundary, missing credentials/provider outage, terminal repeat, and insufficient/foreign liability refusal. Prove the clock/network budget and source SDK version together. Preserve signed-event recovery, all original tests and the separate synchronized original concurrent-preparation winner proof. If no existing agent has a real invocation trigger, expose that remaining integration gap rather than inventing a new scheduler or declaring automation from a callable method.

## Pinned autonomous trigger and timeout design (supersedes open trigger/budget items above)

Source inspection of app/allai/agents/finance/agent.py finds subscriptions=[] and a no-op handle_event. Its run_reconciliation skill concerns accounting periods. There is no existing automatic original-attempt trigger to credit. The existing Celery schedule at app/core/celery_app.py236–239 invokes app.tasks.scheduled.cleanup_stuck_agent_transactions hourly at minute20 on queue scheduled. Reuse this task's existing infrastructure, adding a separate original-attempt recovery pass outside all legacy cleanup transactions. Do not change the legacy cleanup selector/refusal/financial effects or route prepared attempts through generic cleanup. This requires one further existing product target, app/tasks/scheduled.py; the already approved tests/test_cleanup_stuck_agent_transactions_atomicity.py carries scheduler/recovery isolation tests. No new schedule entry, worker, table or registry is proposed.

Discovery selects valid existing canonical_agent_attempt_v1 nonterminal records. Unknown-PI candidates are charging-eligible only while current original authority is valid and at least30seconds remain before prepared_at+23hours. Expired unknown-PI and no-charge investigations cannot consume charging-eligible slots. Known-PI recovery is read-only at the provider and cannot authorize create. Expose expired/revoked investigations separately to the existing protected operator command; age never releases liability.

Order due candidates by COALESCE(last_recovery_at, '-infinity'::timestamptz) ASC, prepared_at ASC, transaction ID ASC using existing agent_audit_log recovery entries. Never-attempted records come first. Pending attempts become due again after one hour. Both scheduled and internal-command recovery record attempt/outcome in this same existing audit log; do not add fields to the closed28-field money attempt. Under the existing Order-then-Transaction preflight locks, re-read the latest shared cursor before admitting provider work and commit the admission record before I/O. A scheduled candidate already advanced by another invocation is skipped. A continuation is enqueued only if this pass admitted due work and due eligible work remains; a no-progress overlapping pass cannot spawn another chain. This bounds redundant overlap without a new claim table or generic queue engine. The protected command uses the same cursor, not FinanceAgentAuditLog as fairness authority. Finish discovery before provider I/O and revalidate each candidate in its own bounded service session. A bookkeeping commit failure before provider dispatch prevents that invocation's creating call.

One recovery pass has a600second wall-clock ceiling: at most19 candidates at30seconds each, reserving30seconds for discovery, bookkeeping and summary. Stop admitting work without the full remaining candidate budget. If due eligible candidates remain, enqueue one recovery-only continuation of this same existing task on the same scheduled queue after60seconds. The recovery-only flag skips legacy cleanup; there is no new task registry, scheduler entry or worker. The existing hourly invocation remains the restart/backstop if continuation enqueue fails. Concurrent invocations must preserve the original idempotency identity and financial refusal/once-only guarantees; fairness timestamps do not grant financial authority.

Report eligible, processed, deferred, expired and investigation counts, oldest due age, continuation outcome and next retry time. Capacity at the worst-case budget is19 candidates per660seconds including continuation delay (about103/hour), not an unlimited guarantee. A500-item initial eligible backlog needs27 passes, at most4hours57minutes at these budget ceilings without new arrivals or outages. Measure actual arrival rate, backlog age, task availability and worker headroom during acceptance; mark an approaching cutoff as capacity degradation and continue allowed recovery, never infer permission for later create. Prove500 eligible items mixed with at least20 expired items across actual continuation invocations, pending backoff, enqueue failure/hourly backstop, and near-cutoff refusal. Source schedule presence is not proof: demonstrate deployed Beat-to-scheduled-worker discovery and completion during human absence.

Construct a request-local StripeClient(api_key, max_network_retries=2, http_client=HTTPXClient(timeout=httpx.Timeout(5.0, connect=2.0, pool=2.0))). Call that client's v1.payment_intents.create_async/retrieve_async and v1.charges.retrieve_async, never module-level global resource methods for this flow. Cleanup explicitly awaits the same HTTPXClient.close_async() under the remaining whole-operation deadline. Retain the existing two SDK retries. The installed package exposes create_async and HTTPXClient.request_async. Configure request-local httpx.Timeout(5.0,connect=2.0,pool=2.0), retain SDK retry policy, and enforce a20second asyncio total provider budget including retries/backoff. Await cancellation and close the request-local async client so no abandoned executor thread continues. Cancellation does not establish that Stripe did not execute; preserve the same original request/key and unknown state. Each local database phase retains5seconds total/2seconds statement/250ms lock wait. The whole candidate limit is30seconds: at most5seconds pre-provider database work,20seconds provider I/O including retries/backoff/cancellation/close, and5seconds post-provider financial/outcome/audit work. All phases share the decreasing30second deadline. Reserve the full30seconds before the conservative23hour retry cutoff. Cancellation cleanup must fit the deadline; expiry remains an unknown provider outcome. No extra cancellation request may extend the20second provider budget or repeat an uncertain cancellation. Do not add retries to the global run_stripe wrapper or change other payment callers. Test actual transport cancellation, socket timeout, retry count and elapsed time together, including provider success concurrent with local cancellation. The numeric values are proposed acceptance requirements, not claims of current runtime configuration.

No-dispatch recovery is permitted only for a valid version1 attempt produced by the reviewed original authorized checkout preparation: existing purchase-scope organization key, same buyer/seller/listing/full money tuple, accepted original terms and configured original payment method/customer, and original confirm=True/off_session=True semantics. These constants come from the saved versioned request's producer, not a new operator instruction. Revalidate current key/org authority explicitly before creating retry: get_api_key is retrieval-only and require_scope checks scope only. Apply the existing validate_key active, not revoked and not-currently-suspended-key predicate plus purchase scope and original organization/key/transaction binding. Require the original terms and producer provenance specified below; a current or later acceptance alone is insufficient. Inactive, revoked, suspended or scope-missing authority cannot initiate an unperformed charge. Trusted internal operators may perform evidence-only original-payment reads without making that a charge permission or weakening customer authentication. Never manufacture a record, upgrade a legacy no-attempt row, substitute a new key or infer customer consent from arbitrary metadata. Legitimate browser Checkout/SCA is unaffected. An unknown attempt cannot prove whether original dispatch happened; inside the window standard same-key/same-parameter idempotency arbitrates those two cases. Outside it, use evidence-only investigation through the existing finance operator command, never another create or synthetic release.

The exact proposed extra target set is now FIVE existing files: app/routers/finance_agent.py; app/schemas/finance.py; app/services/finance/policy_engine.py; app/tasks/scheduled.py; tests/test_finance_phase2.py. Existing approved transaction_service.py/order_money_service.py/webhooks.py/refund protocol tests/cleanup atomicity tests carry the corresponding implementation and regression proof. No additional finance-agent skill is needed for the deterministic scheduled path. The internal command remains the authenticated exception/investigation surface, using the same service. Final review must prove original confirmation context, real scheduled discovery/execution, fairness within the retained window, full original tests, and the separate concurrent-preparation winner proof. GLM and DeepSeek agreement is sufficient; no routine Max approval is requested.



## Truthful outcome and audit failure

The existing atomic attempt/payment/release state and money audit remain authoritative. The shared agent_audit_log supplies recovery fairness and outcome observation. The finance command adapter must not convert a service-owned committed terminal result into a generic422 merely because the subsequent optional policy audit commit failed. After such failure, use a new session to re-read the original financial state and return its actual result with audit_recording_pending. If the re-read is unavailable, return explicit outcome_unknown503; never claim rollback of already committed money. A repeat re-reads state and cannot repeat release. Inject failure after financial commit and before policy audit commit, prove the truthful response, one release and one financial audit. Pre-provider recovery-bookkeeping failure must prevent the creating call.

## Transport evidence and remaining proof

The shared qualification runtime reports Stripe15.4.0 and httpx0.27.2; active requirements.txt instead requires httpx>=0.28.1,<0.29. An isolated owned runtime with Stripe15.4.0/httpx0.28.1 reproduced the same transport assertions. Preserve both receipts; the shared runtime is not an exact production dependency proof. A local HTTP-server experiment used the native request-local StripeClient/HTTPXClient with max_network_retries=2: two500responses then success caused exactly three identical-body/same-key requests. An accelerated0.25second timeout on a separate hanging request closed the socket without another request. This supports API/lifecycle compatibility only; it is not real Stripe financial or full20second deadline proof. Retain source, runtime versions and full receipts. Implementation acceptance still requires real configured socket timeout/backoff, full provider/whole-operation bounds, late-provider completion, post-observation concurrency and deployed invocation evidence.

## Exact no-webhook completion amendment

This explicitly amends the parent rule that succeeded provider observation can only remain bound_pending until a signed event. It preserves every capture, identity, journal, rights and refund invariant. A recovery create response by itself still cannot complete. The shared recovery service must obtain a fresh original PaymentIntent and its owned latest Charge through the request-local authenticated SDK, both inside the same20second provider budget (including any preceding same-key create, SDK backoff and close). If that budget cannot obtain complete evidence, persist only the allowed bounded observation/unknown result and retry later; never fabricate missing Charge or event evidence. No provider I/O occurs while money locks are held.

For active authorized recovery, require succeeded PI, exact original immutable request/context/metadata, amount_received equal to original amount, owned latest Charge linked back to the samePI, paid=True, captured=True, full original gross/currency/customer/account/livemode, and the existing validate_captured_facts checks. Independently refuse current refund/dispute/revocation, inconsistent financial history, changed binding, stale observation/revision, pending liability contradiction, nonzero refunded Charge or contradictory provider flags. Such cases remain evidence/investigation outcomes and use existing refund/dispute paths; do not grant or restore rights by treating a refunded capture as fresh success.

In the bounded post-provider transaction, re-read and lock the original Order then Transaction and the existing money bundle in the established order. Revalidate the exact attempt ID/revision, native PI ownership and all history. Reuse bind_initial_order_payment_intent_sync for the same original pair, transition through bound_pending where required by the existing state graph, and call the existing lock_order_money, ensure_order_capture and OrderService._complete_paid_order(auto_commit=False) helpers. Use the existing capture accounting category payment_event; this is an accounting origin category, not a claim of signed-event provenance. Keep ensure_order_capture and all captured-refund financial functions byte-unchanged. The shared recovery audit records provider_read provenance and no signed event ID. Do not create a StripeEvent row, signature flag or synthetic event ID.

Amend the completed-attempt validation to admit evidence_source=provider_read only through this strictly validated recovery completion; retain the existing signed_event_and_provider_read path. create_response alone remains insufficient. Keep the same closed28fields, version1, state names and terminal immutability; no new evidence enum or attempt field is needed. The transition helper must verify the existing durable completed-capture/money/Order state before accepting completed, and the direct bypass tests must reject provider-read completion without that atomic capture. Commit the original payment, balanced journal/MoneyState, paid Order/Transaction, access snapshot/event, attempt completed and authoritative agent recovery audit together. Captured spend remains held, not released. Dispatch notifications only after this commit through the existing mechanism. Any failure rolls back this entire new financial transaction; a failure of a later optional policy audit cannot undo it.

Late real signed events retain their actual signature/event identity and deduplication behavior. For an already completed matching recovery, they must validate original identity and leave capture, journal, access snapshot, notification count, paid timestamp, spend and attempt revision unchanged while recording the actual event's processing. A contradictory or stale event cannot regress terminal finance. Prove repeated recovery, late signed success, late failure, capture/refund/cancellation races and post-money policy-audit failure with independent sessions and actual snapshots.

requires_action remains investigation-only for later recovery: preserve the held original attempt, report the originalPI and reason, and never start a second cancellation call or a new authentication/charge. The original checkout's existing one-shot cancellation policy is unchanged. Subsequent authoritative canceled/no-liability evidence can use the already reviewed once-only release path. This amendment does not promise an unattended successful purchase where customer action or revoked authority prevents it.

Acceptance for absent-webhook recovery must cover never-dispatched preparation and lost create response returning succeeded, fresh PI+Charge evidence, exactly one original Payment/MoneyState/journal/paid Order and transaction, attempt completed, preserved captured spend, one access snapshot/notification, no second charge/release, and later real signed-event/repeated-task idempotence. Provider-read-only success that stays pending is not completion. Provider-read completion after23hours is allowed only for an already known originalPI with current allowed recovery authority; the23hour cutoff prohibits create, not definitive existing-payment reads. Revoked/inactive/suspended/scope-missing cases remain evidence-only with no financial mutation through this amendment.

## Original terms and producer provenance

The actual terms service is not sufficient proof by its return value: off mode returnsTrue without a row; shadow mode can returnFalse without refusing; get_latest_terms_acceptance joins actor alternatives with OR and normally checks only the current version. Recovery must explicitly resolve the original preparation actor from the original bound Order buyer and organization, not a changed current organization owner. It must find an append-only terms_acceptance record for that actor AND that organization, scope=organization, authority_ack and all acknowledgements true, accepted_at and created_at at or before prepared_at, and terms_published_at at or before acceptance. Verify the exact terms_version and terms_hash_sha256 that were active for the preparation; a matching current version is sufficient only with retained source/configuration evidence that the same version/hash was already active then. Do not use the existing OR/latest-current lookup as this proof. A later acceptance, another actor/org, wrong scope/version/hash, or unverifiable historical configuration refuses unknown-PI create.

For future preparation, write an existing AgentAuditLog preparation-provenance record in the same transaction as the original attempt, using request_payload/response_payload for bounded acceptance ID, actor/org, version/hash/publication timestamp, prepared attempt ID, and reviewed producer source identity. Keep secrets/payment-method details out of this audit. Do not add fields to the closed attempt or invent a new table. Preserve normal first-dispatch terms policy; if the original gate permits shadow/off preparation without genuine acceptance, record that lack honestly and make it ineligible for a later unknown-PI creating retry.

For already prepared version1 records lacking this correlation, use existing immutable acceptance rows plus the exact deployed producer and terms configuration history covering prepared_at. The reviewed version1 producer must prove identical confirm=True/off_session=True constants and the complete original SDK request serialization. Version=1 or today's source alone is not producer proof. If provenance is missing or contradictory, return investigation_required and make no unknown-PI create; never manufacture a historical audit/acceptance or substitute newer consent. A trusted existing-provider evidence read remains separate from permission to initiate a charge.

Tests must cover valid pre-prepare acceptance; missing, later accepted or later inserted/backdated rows; wrong actor/org/scope/version/hash; changed terms configuration; malformed attempt; unverifiable producer; shadow/off return without acceptance; preparation commit versus provenance-audit failure; and exact original request bytes including confirm/off_session against the pinned producer. Original concurrent preparation must still yield one durable Order/attempt/provider dispatch in the actual synchronized race, not a serial fallback.
