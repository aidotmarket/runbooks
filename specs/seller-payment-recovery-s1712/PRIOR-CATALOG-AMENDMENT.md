# Bounded money-readiness extension amendment — S1712 / S1714

Status: proposed correction for GLM and DeepSeek review; neither implementation acceptance nor release approval. Both affirmative reviewer decisions are sufficient under Max's explicit rule; CC is waived for this project. Mars remains sole builder dispatcher and protected-runtime owner. No further user permission is requested.

## Exact parent and scope

This amendment adds exactly one production target, app/services/refund_processing_service.py, to the accepted 31-target backend contract, making 32 targets. Its only authorized changes are the legacy catalog selection and the separately enforced canonical-payment-intent extension readiness. The parent is runbooks commit 6bcb9892c32dcf553bc8ad3fdfa053f447373572, spec SHA256 5f78244affa20ae381e68b2240f36512d7986def19ee18b0bcca6c6d71362508, owner decision SHA256 4b83cdea1c42f431796157aa057915ad221439e850394a0451b327eadef020ff. The complete approved build request SHA256 is 90e39937bffa08b763a6ad4aa13c319977f1b62208b787059a3980dbde0c061e. All original B01–B18 and PI01–PI14 obligations, 1990 original cases plus new cases, exactly six inherited exceptions, financial semantics, environment integration and live-release gates continue unchanged. This is not a defect-only replacement build request.

No other additional file is authorized. Add regression coverage in already authorized tests/test_refund_processing_protocol.py, tests/test_order_money_protocol.py and/or the other existing authorized test modules as appropriate. Preserve all original assertions except the exact previously approved compatibility dispositions. Do not change capture/refund/payout effects, spend release, admission, merchant/chart checks, posting guards, version/entity checks, or their transactional behavior in the added production file.

## Independently reproduced cause

The actual unchanged refund_processing_service.py line41 includes every public constraint whose name matches LIKE '%s1714%' in CATALOG_SQL. Lines145–146 compare its projection against frozen CATALOG_SHA256 3139d7fff567e782c12fc3fb2f27e4592bee7437ddb4b5c2f9529a1f8fdba1b5. Mandatory migration003 constraints uq_s1714_order_payment_intent and uq_s1714_transaction_payment_intent therefore enter the legacy projection, contrary to D5's intended separation. The independently read owner database receipt contains104 rows and digest e339200b1895c686dcf5f8392109c2f173ef0455d85e9e472412763dd0b479b8; excluding exactly the two identified rows leaves102 and restores the frozen digest exactly. The builder separately reproduced readiness passing before003 and refusing after003 on a fresh owned database. Diagnostic Workspace results were290 cases,125 pass,165 fail,0error,0skip; these ran against evolving source and are not final qualification.

## Exact replacement disposition for D5 integration

Keep every legacy projection row and its canonical serialization, ordering, existing narrow normalization and frozen CATALOG_SHA256 unchanged. Change only the constraint arm of CATALOG_SQL to exclude these two exact schema/table/constraint identities from that legacy selection:

- public.orders / uq_s1714_order_payment_intent
- public.transactions / uq_s1714_transaction_payment_intent

Place the exclusion outside the parenthesized legacy inclusion predicate, still inside the constraint arm. Express it as exact tuple equality or equivalent exact conjunctions. Never exclude by name alone, prefix, suffix, wildcard, table alone, all unique constraints, or the entire s1714 family. An object bearing either name on a different public table must remain visible to the legacy selector and cause refusal. Do not remove any other row, change the frozen digest, change digest normalization, or learn expected definitions from the running database. The unchanged-row requirement refers to the retained projection result and digest, not byte identity of the amended SQL source text.

Separately validate both explicit extension objects on every new runtime money-readiness call, including the async path through the existing sync checker, using that caller's database session. Both must exist and match the frozen expected definition. Require the canonical migration ancestry for this new release; absence of003 or either extension object is unavailable, never a fallback to legacy readiness. Qualification of the old pre003 baseline uses the retained old-source query/checker as evidence; it does not justify allowing the new runtime to accept missing003. Keep the original money ancestry, posting guard, merchant, chart and immutable entity checks independently mandatory. Do not import and execute a migration upgrade or perform DDL/repair in readiness. No cache, feature flag, test-only bypass or learned fingerprint.

The extension query must bind each constraint to its exact public base table by OID, its exact stripe_payment_intent_id attribute, and its actual supporting index by conindid; do not trust names without attachment. Verify exactly one expected constraint per identity, contype='u', convalidated=true, condeferrable=false, condeferred=false, exact ordered single-column conkey, and nullable live column. Verify the supporting index belongs to that same base table (indrelid=conrelid), is the actual conindid object, has the expected name in public, and is unique, valid, ready, immediate, one key attribute and one total attribute, with the exact same column key and no expression or predicate. Require ordinary NULL-distinct behavior (indnullsnotdistinct=false), not NULLS NOT DISTINCT, and no included columns. Use explicit stable catalog representations such as contype::text. Missing fields/rows, duplicate matches, wrong attachment or unexpected values fail closed as OrderMoneyProtocolUnavailable through the existing error contract. No exception swallowing or optional extension check.

Freeze the expected extension definitions in reviewed source. The exact native NOT DEFERRABLE nullable UNIQUE constraints and names remain as accepted. Preserve migration002/003 deadlines, lock order, duplicate census, transaction rollback and forward-only policy. This amendment permits no additional migration, DDL object, schema repair, historical data rewrite or renamed object.

## Required actual proof

1. On one fresh owned PostgreSQL database, retain actual version, complete ancestry, pre002/pre003 old projection rows and canonical serialized bytes, and their frozen digest. After both migrations, show the amended legacy selection yields the same ordered rows and serialized bytes with the same3139 digest; retain the independently checked extension rows separately. Explain the old broad selector's two-row delta without accepting its changed digest. No monkeypatch of readiness or source-qualified projection learning.
2. The new runtime must refuse absent003, either missing constraint, wrong schema/table/name or attachment, non-unique or wrong-column definitions, deferrable/deferred variants, invalid/unready/non-immediate index, partial/expression/included-column forms, NOT NULL or NULLS NOT DISTINCT variants. Exercise catalog variants actually constructible in PostgreSQL in disposable owned databases; never corrupt protected/shared catalogs. Where PostgreSQL cannot safely construct a catalog state, retain source-level exact-field validation and explicitly identify the narrower test mechanism and limitation rather than claiming live DDL proof. Ensure same-name objects on wrong tables cannot be hidden by the exclusion.
3. Mutate a separately covered legacy constraint/trigger/function definition in an owned transaction/database and prove old readiness still refuses even when both new constraints are valid. Prove a valid legacy digest cannot mask an invalid new extension and a valid extension cannot mask legacy corruption. Roll back or discard only owned resources.
4. Prove multiple NULL values remain legal and duplicate non-NULL PI values are rejected natively with SQLSTATE23505 in both tables, with complete snapshot equality after a SAVEPOINT refusal. Preserve the original duplicate-PI test node identities and every other approved financial assertion.
5. Run real new-runtime sync and async readiness callers plus normal default-enabled public startup/capture/refund routes against the installed schema. Missing or invalid extension must preserve existing durable failure behavior and must never permit partial money mutation. Complete all original qualification obligations on one frozen final source; no union of evolving diagnostics or added baseline exceptions.
6. Record the final amended production diff and unchanged remainder of refund_processing_service.py, full query/extension source identity, actual final backend commit/tree/runtime, logs/XML and every matrix row. Independent review must assess the complete resulting implementation, including all unfinished MP249 work, not only this correction.

## Continuation and operational diagnosis

MP249 is preserving an incomplete checkpoint, reported as abdb8a5e84ba3844ab5c2bcb4b0d320eeff1d226; the owner must independently verify terminal report, no live descendants, successful normal push, complete durable source/evidence and owned resource cleanup before the next sole builder. Verify the actual checkpoint commit rather than restart from817f or infer completion from a wrapper exit. Never modify the active builder checkout or frozen parent request. After both reviewers approve this amendment, the owner assembles a new immutable complete request and32-target matrix tied to the actual checkpoint, accepted parent plus this exact amendment and every remaining obligation.

If readiness reports Order money schema/guard definition mismatch immediately after003, compare the exact old selector output with these two extension identities. Preserve rows/digests/source before any change; this signature is not permission to update a fingerprint. Once this correction is installed, the same failure can indicate real old-schema drift and must remain fatal. Extension mismatch is diagnosed independently by exact catalog fields, without DDL repair in the runtime. Deployment rollback must retain forward-only native ownership constraints and follow the separately approved cutover/recovery procedure. The protected run remains untouched; production release, AWS S3 and Cloudflare R2 proof are still open.
