# BQ-AIM-DATA-SIGN-IN-WITH-AI-MARKET-S1659 — Gate 2 implementation specification

**Status:** Authored for implementation review; not a build, migration, deployment or enablement approval. **P0; Max directive 2026-09-07. Builder: MP.**
**Frozen authority:** Gate 1 at `1d09d66524c6397013100d6b65c0297535efc18d`, unanimous CC/GLM/DeepSeek per Max. Read verbatim from fetched runbooks origin/main; its blob matches that authority. Historical “awaiting R2” prose in that blob does not reopen the user-confirmed approval. All Gate 1 sections remain binding.
**Delivery:** only this file, branch `spec/bq-aim-data-sign-in-with-ai-market-s1659-gate2`, parent runbooks origin/main below. Source repositories are read-only. New paths below are proposed files, not claimed existing anchors.

## 1. Authoring pins and admission

| Alias / repository | Immutable authoring pin | Verification on 2026-09-07 |
| --- | --- | --- |
| B / ai-market-backend | `505465eee5917f4deb6a86469561b6b17f38e725` | Local origin/main equals live remote refs/heads/main; archived immutable source outside repo. **No new backend SHA: current main is still Gate 1's pin.** |
| F / ai-market-frontend | `abe5ec51706cd32361c5de83ba4ba905d050cb5d` | Local origin/main equals live remote main. |
| C / aim-data | `9f6030563a02e4a23c15c361a50015d9891d50c2` | Local origin/main equals live remote main; Compose v1.22.10. |
| H / e2e-harness | `35c89d39ea93f9b6f1a720c5eb3e785630c0bece` | Local origin/main equals live remote main. |
| R / runbooks | `2db824c411a38fc59b05b55689516f92b51a94e6` | Fetched main; Gate 1 blob equality checked against design authority. |

Citation notation `B:path:line-range` means **ai-market-backend at the full B SHA above**, never working-tree/latest source; likewise F/C/H/R. Every backend citation uses B. All 122 unique Gate 1 ranges resolved; all 15 cited backend files are unchanged from Gate 1 to authoring main. Historical `ce72caaf19fb16b534b592eafad45bcacef83669..B` changes only cited `app/core/config.py` (+1 `CORPUS_SAFETY_CLASSIFIER_ENABLED=True`, B:app/core/config.py:202-207); no OAuth policy drift. Reviewers must have B (`rtk git cat-file -t 505465eee5917f4deb6a86469561b6b17f38e725` → `commit`).

| Admission fact | Binding implementation action / unresolved evidence |
| --- | --- |
| Baseline has owner_id/allowed_scopes; ORM has user_id/scopes; later migration skips an existing table (B:alembic/versions/000_initial.py:7302-7351; B:app/models/oauth_client.py:30-48; B:alembic/versions/20260127_001_oauth_clients.py:24-51). | A0 must inspect target **schema only**, Alembic heads, column types/constraints/indexes and empty-DB replay before migration freeze. No target DB was accessed and no replay executed during document authoring; these remain explicit admission evidence, not presumed passes. Unknown/conflicting shape stops MP; report exact drift for review, no speculative production DDL. |
| Customer release uses Dockerfile.customer → deploy/entrypoint.sh, explicitly one uvicorn worker (C:.github/workflows/aim-data-release.yml:60-65; C:Dockerfile.customer:93-94; C:deploy/entrypoint.sh:80-92). | Choose single-process bounded memory, no new Redis dependency. Existing lifespan enforces env and process lock (C:app/main.py:151-171). C adds WORKERS/VECTORAIZ_WORKERS checks; all launchers must reject >1 rather than silently override. Gate 3 records process tree of exact candidate digest. |
| Harness currently requires buyer-01/password, uses localhost and rejects provider navigation (H:src/e2e_harness/browser.py:126-145; H:src/e2e_harness/browser.py:864-874; H:src/e2e_harness/config.py:85-114). | D2 is an **owner-approved login-entry adaptation**, including narrowly necessary executor guards; charter prose alone cannot satisfy AC11. No general account/domain allowlist relaxation. |

## 2. Ordered, independently deployable chunks

| Order / branch (one repository each) | Build boundary and deployability | Gate 1 AC contribution |
| --- | --- | --- |
| A0 → A1 → A2 → A3; `build/s1659-a-backend` | A0 schema/preflight; A1 additive migration + disabled seed; A2 routed grants/transactions/refresh; A3 routed tests. One backend deployment, **first with flag off and row inactive**. Supports existing clients without F/C; enabled first-party flow waits for B/C. | AC1–3, AC5, AC7–10, AC13–14 backend obligations; AC4 website-cookie baseline. |
| B; `build/s1659-b-frontend` | Mandatory website continuation + confirmation. Deploy after A, independently with UI flag off. No provider OAuth reimplementation and no website auth-storage change. | AC1–3, AC5, AC8, AC13–14 UI obligations. |
| C; `build/s1659-c-aim-data` | Customer login/transactions/completion/refresh adapter/installer; image ships with OAuth off and working password/2FA fallback. Tolerates disabled/old backend. | AC1–11, AC13–14 local obligations. |
| D1; `build/s1659-d1-runbooks` | Canonical operating docs after candidate contracts stabilize; can publish as “pending verification” without activation. | AC4, AC6, AC9, AC11, AC13–14 procedures. |
| D2; `build/s1659-d2-harness` | Harness-owner-approved entry/guards and charter, deployed only to authorized evidence runner; independent of product deploy. | AC11; supplies AC1/AC2 proof support. |
| D3; `build/s1659-d3-release-notes` | AIM Data changelog after C merge; publish notes against released digest, not assumed version. | AC4, AC6, AC13–14 disclosure. |

MP builds every branch; no unrelated refactors. Soft caps (added + deleted lines, including tests): A 1,500; B 700; C 1,200; D1 250; D2 500; D3 100. At cap, report diff/stat and split further by deployable responsibility with owner agreement; never omit tests or compress code to evade cap. New dependencies require justification. Authoring here does not dispatch MP or Council. No change to payments, seller promotion, provider policy, website cookies/lifetimes, agent OAuth semantics, AIM Node/vectorAIz, storage architecture or customer data.

## 3. Exact file manifest

| Chunk | Modify existing files: pinned anchors and intended edit | Create (entire new file; no pre-existing line anchor) |
| --- | --- | --- |
| A routing | B:app/api/v1/router.py:213-213, B:app/api/v1/router.py:257-258; B:app/api/v1/endpoints/oauth_clients.py:122-175, B:app/api/v1/endpoints/oauth_clients.py:182-206; B:app/api/v1/endpoints/oauth.py:107-164, B:app/api/v1/endpoints/oauth.py:268-364, B:app/api/v1/endpoints/oauth.py:485-523. Make oauth.py sole token POST owner; preserve M2M helper/response; oauth_clients.py remains sole client-create POST owner. JSON and legacy form authorize share one POST; escape legacy HTML and fix `/api/v1` form action. | `app/services/aim_data_oauth_service.py` (reserved-client transactions/issuance/refresh); `tests/test_aim_data_oauth_routes.py`. |
| A persistence/trust | B:app/services/oauth_service.py:51-137, B:app/services/oauth_service.py:143-230; B:app/services/oauth_client_service.py:63-100, B:app/services/oauth_client_service.py:192-260; B:app/models/oauth_client.py:30-48; B:app/schemas/oauth.py:24-28. Public inputs reject reserved fields/scope/ID; block revoke/regenerate of reserved row; nullable secret metadata matches additive schema. | `alembic/versions/20260907_001_aim_data_oauth.py`; `scripts/provision_aim_data_oauth.py`; `scripts/inspect_aim_data_oauth_schema.py`; `tests/test_aim_data_oauth_migration.py`; `tests/test_aim_data_oauth_security.py`; `tests/test_aim_data_oauth_refresh.py`. |
| A settings/helpers | B:app/core/config.py:59-64, B:app/core/config.py:320-320; B:.env.example:1-10. Add switch/config documentation. Reuse **read-only** B:app/services/sso.py:84-215; B:app/api/v1/endpoints/auth.py:597-650; B:app/core/auth_cookies.py:16-61; B:app/models/organization.py:94-116. Do not edit website auth.py to accept JSON refresh. | No further files. |
| B | F:lib/redirect.ts:1-37; F:app/login/LoginForm.tsx:32-37, F:app/login/LoginForm.tsx:55-66, F:app/login/LoginForm.tsx:88-92; F:app/auth/oauth/[provider]/callback/page.tsx:21-58; F:app/auth/verify/page.tsx:19-43; F:.env.example:1-9. Exact continuation across every login completion, preserving provider state/nonce. Reuse F:store/auth.ts:59-70 and F:api/client.ts:90-107 without storage edits. | `app/oauth/authorize/page.tsx`; `api/aim-data-oauth.ts`; `lib/aim-data-continuation.ts`; `tests/aim-data-oauth.test.tsx`; `tests/aim-data-continuation.test.ts`. |
| C API/storage | C:app/routers/auth.py:456-623; C:app/config.py:117-120, C:app/config.py:267-302; C:app/main.py:151-171, C:app/main.py:489-493. Extract connected completion; fail `/auth/me` validation instead of returning user=null; add explicit auth-mode transport. Reuse C:app/auth/api_key_auth.py:304-363 and C:app/services/registration_service.py:188-208, no new local JWT/API key. | `app/routers/aim_market_oauth.py`; `app/services/aim_market_oauth.py`; `app/services/connected_login.py`; `tests/test_aim_market_oauth.py`; `tests/test_aim_market_refresh_transport.py`; `tests/test_aim_market_oauth_deployment.py`; `tests/test_aim_market_oauth_call_inventory.py`. |
| C UI | C:frontend/src/pages/LoginPage.tsx:22-63, C:frontend/src/pages/LoginPage.tsx:89-162; C:frontend/src/contexts/AuthContext.tsx:67-93, C:frontend/src/contexts/AuthContext.tsx:130-169; C:frontend/src/lib/api.ts:43-103, C:frontend/src/lib/api.ts:1276-1285; C:frontend/src/App.tsx:113-116. Button, shared completeLogin, one refresh coordinator, mode reset on account change/logout. | `frontend/src/pages/LoginCompletePage.tsx`; `frontend/src/lib/aimMarketAuth.ts`; `frontend/src/pages/LoginPage.oauth.test.tsx`; `frontend/src/pages/LoginCompletePage.test.tsx`; `frontend/src/lib/aimMarketAuth.test.ts`. |
| C deployment | C:docker-compose.aim-data.yml:16-42; C:installers/aim-data/install.sh:58-69, C:installers/aim-data/install.sh:97-105; C:deploy/nginx.conf:14-40; C:deploy/entrypoint.sh:80-92; C:entrypoint.sh:14-30. Numeric-loopback publish, port/flags, callback no-log headers, worker guard and no-access-log on both launchers. | No image-version bump or generated index in this chunk. |
| D1 | R:aim-data.md:59-65 (login/runbook section); R:ai-market-backend.md:26-42 (new OAuth subsection). Document below contracts, readiness, operator seed/disable/revoke procedure, troubleshooting, proof links and retained storage risk. | None. |
| D2 | H:charters/aim-data-fresh-install.json:7-24; H:tests/test_charters.py:28-71; H:src/e2e_harness/browser.py:126-145, H:src/e2e_harness/browser.py:349-368, H:src/e2e_harness/browser.py:864-874; H:src/e2e_harness/config.py:85-114. Exact login-entry mode, approved identity manifest, numeric origin, bounded provider-navigation phase; keep all unrelated charters/guards. | `tests/test_aim_data_oauth_entry.py`; `docs/aim-data-oauth-evidence.md`. |
| D3 | C:CHANGELOG.md:7-20. Record released version/digest, button + fallback adapter, local-host restriction and rollback limitation; no claim of payment/tunnel certification. | None. |

## 4. Chunk A: storage and APIs

A0 inspector is schema-only (`information_schema.columns`, `pg_constraint`, `pg_indexes`, `alembic_version`); prints types/constraints, never account rows/credentials. Migration parent = sole current Alembic head captured by A0; multiple heads stop. Existing table branch must match approved manifest. Baseline shape is canonical for raw OAuth; preserve M2M user_id/scopes/last_used_at as compatibility columns. Add missing columns only after inspector type checks; never overwrite conflicting non-null owners/scopes. Preserve scopes strings for M2M and JSON allowed_scopes for authorization separately. M2M writes populate both owner fields; public creation remains confidential. No inferred first-party status or row conversion.

DDL below is the required resulting additive delta (Alembic inspector guards implement repeat-safe existence/constraint checks; matching existing objects are no-ops, mismatches abort). Compatibility addition applies only if absent; if user_id already exists, drop its NOT NULL to permit ownerless reserved row and enforce confidential ownership in both service entry points. Gate 3 must prove old binary M2M behavior against migrated schema.
```sql
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS user_id uuid REFERENCES users(id);
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS scopes varchar(500) DEFAULT '';
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS last_used_at timestamptz;
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS owner_id uuid REFERENCES users(id);
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS redirect_uris jsonb NOT NULL DEFAULT '[]';
ALTER TABLE oauth_clients ADD COLUMN IF NOT EXISTS allowed_scopes jsonb NOT NULL DEFAULT '["market.read"]';
ALTER TABLE oauth_clients ALTER COLUMN user_id DROP NOT NULL;
UPDATE oauth_clients SET user_id=owner_id WHERE user_id IS NULL AND owner_id IS NOT NULL;
UPDATE oauth_clients SET owner_id=user_id WHERE owner_id IS NULL AND user_id IS NOT NULL;
ALTER TABLE oauth_clients ADD COLUMN token_endpoint_auth_method text NOT NULL DEFAULT 'client_secret_post';
ALTER TABLE oauth_clients ADD COLUMN first_party boolean NOT NULL DEFAULT false;
ALTER TABLE oauth_clients ADD COLUMN allowed_grant_types jsonb; -- NULL preserves existing confidential grant routing
ALTER TABLE oauth_clients ADD COLUMN pkce_required boolean NOT NULL DEFAULT false;
ALTER TABLE oauth_clients ADD COLUMN redirect_policy text NOT NULL DEFAULT 'exact';
ALTER TABLE oauth_clients ALTER COLUMN client_secret_hash DROP NOT NULL;
ALTER TABLE oauth_clients ADD CONSTRAINT aim_data_client_trust CHECK (
 (token_endpoint_auth_method='client_secret_post' AND NOT first_party AND client_secret_hash IS NOT NULL)
 OR (client_id='aim_data_desktop_v1' AND token_endpoint_auth_method='none' AND first_party
 AND client_secret_hash IS NULL AND pkce_required AND redirect_policy='aim_data_loopback_v1'
 AND allowed_grant_types IS NOT NULL AND allowed_grant_types='["authorization_code","refresh_token"]'::jsonb
 AND allowed_scopes='["aim_data.session"]'::jsonb));
ALTER TABLE oauth_authorization_codes ADD COLUMN pkce_challenge varchar(43);
ALTER TABLE oauth_authorization_codes ADD COLUMN pkce_method varchar(4);
ALTER TABLE oauth_authorization_codes ADD COLUMN source_session_id uuid REFERENCES auth_sessions(session_id);
CREATE TABLE aim_data_authorization_transactions (
 request_hash char(64) PRIMARY KEY, binding_hash char(64) NOT NULL, csrf_hash char(64),
 client_id varchar(64) NOT NULL REFERENCES oauth_clients(client_id), redirect_uri text NOT NULL,
 scope text NOT NULL CHECK(scope='aim_data.session'), state varchar(43) NOT NULL,
 pkce_challenge varchar(43) NOT NULL, pkce_method text NOT NULL CHECK(pkce_method='S256'),
 created_at timestamptz NOT NULL DEFAULT now(), expires_at timestamptz NOT NULL);
CREATE INDEX aim_data_authorization_expiry ON aim_data_authorization_transactions(expires_at);
CREATE TABLE aim_data_oauth_grants (
 id uuid PRIMARY KEY DEFAULT gen_random_uuid(), client_id varchar(64) NOT NULL REFERENCES oauth_clients(client_id),
 user_id uuid NOT NULL REFERENCES users(id), source_session_id uuid NOT NULL REFERENCES auth_sessions(session_id),
 current_session_id uuid NOT NULL REFERENCES auth_sessions(session_id), refresh_family uuid UNIQUE NOT NULL,
 current_refresh_jti uuid NOT NULL, created_at timestamptz NOT NULL DEFAULT now(), revoked_at timestamptz);
```
Code-table PKCE columns remain nullable for legacy grants; reserved branch requires all three at issuance/exchange, S256 only. Code/client unique indexes must already exist or be added after duplicate preflight; no destructive deduplication. Seed under advisory transaction lock: insert literal reserved ID/name `AIM Data`, null secret/owners, `is_active=false`, exact metadata above, `redirect_uris=[]`, `scopes='aim_data.session'`. On conflict compare all immutable fields and operator-provisioned provenance; fail on any conflict, never adopt an untrusted existing row. Re-run preserves activation state. Seed returns only `created|unchanged|conflict`, exit 0/0/2. Inspector/seed default dry-run; `--apply` is operator-only, never public API. Activation is separate from provisioning.

| Contract (all marketplace paths prefixed `/api/v1`) | Request → success | Failure/status and invariants |
| --- | --- | --- |
| GET `/oauth/clients/aim_data_desktop_v1/status` | No credentials; 200 `{client_id:"aim_data_desktop_v1",enabled:boolean,protocol_version:1}`; enabled = flag AND exact trusted active row. No-store. | Missing/disabled row returns enabled=false; DB failure 503 `{error:"temporarily_unavailable"}`. No generic registry disclosure. Check at start and recheck every grant, never trust cached status for authorization. |
| GET `/oauth/authorize` | Exactly once: response_type=code, client_id, redirect_uri, scope=aim_data.session, 43-char base64url state/challenge, code_challenge_method=S256. Validate raw fields; create random 32-byte request ID + binding; 303 to fixed FRONTEND_URL `/oauth/authorize?request=<id>`. | Invalid/unknown client/destination: local 400, no Location. Disabled: 400 `{error:"unauthorized_client",error_code:"client_disabled"}` locally. Safe protocol errors use only allowlisted error+state after active-client/URI validation. |
| GET `/oauth/authorize/requests/{id}` | Credentials include API binding cookie; 200 `{request,client_name:"AIM Data",scope:"aim_data.session",expires_at,csrf_nonce}`. New one-use 32-byte CSRF nonce; replace prior nonce hash; no code or user credentials. | 400 invalid_request, 403 binding_failed, 410 transaction_expired; read is non-authorizing. Enforce exact website Origin for fetch. |
| POST `/oauth/authorize` JSON | `{request,csrf_nonce,decision:"continue"\|"cancel"}` plus exact website Origin, binding cookie, in-memory Bearer. 200 `{redirect_url}`; Continue URL has only code+state, Cancel only error=access_denied+state. | 401 login_required/invalid_session; 403 csrf_failed, insufficient_assurance, access_denied; 410 transaction_expired; 400 invalid_request/invalid_scope/unauthorized_client. No echoed arbitrary input. Cancel requires valid browser/session contract too. |
| POST `/oauth/token` form | `grant_type=authorization_code`, public client_id, code, original redirect_uri, code_verifier (43–128 unreserved chars); omit secret. Or `grant_type=refresh_token`, client_id, refresh_token. 200 `{access_token,refresh_token,token_type:"bearer",expires_in,scope:"aim_data.session",onboarding_required,onboarding_step}`. | OAuth 400 invalid_request/invalid_grant/unsupported_grant_type/invalid_scope/unauthorized_client (disabled adds error_code=client_disabled); confidential auth failure 401 invalid_client. Public secret/Basic auth supplied →400 invalid_request. Token form rejects duplicate/unexpected reserved-client fields; refresh cannot request widened scope. 429 slow_down + Retry-After; transient 503 temporarily_unavailable. No token-body/cookie logging; Cache-Control no-store. |
| Existing M2M/client CRUD | Exactly one token POST dispatches client_credentials to existing service with original confidential validation and success shape; legacy code/refresh use original semantics. | Missing secret never makes a confidential client public. Reserved ID/trust/auth method/scope rejected on create/update/regenerate/revoke, 403 reserved_client; ordinary validation errors retain existing contract. No duplicate client-create route. |

Authorization binding cookie: `aim_data_authorization_tx`, host-only API origin, Path=/api/v1/oauth, HttpOnly, Secure, SameSite=Lax, Max-Age=600; clear on consume/cancel. Request ID/state/challenge bounded before storage; 600-second DB transactions, delete expired rows on access and every 60 seconds; distributed rate limiting 10 starts/minute per binding/IP, 100 pending/IP, reject saturation with 429. No client-provided token host. Website/API must remain same-site HTTPS; existing exact credentialed CORS only. Header/body limits 8 KiB; reject duplicate JSON/form/query keys before model parsing.

Redirect parser matches **raw** `http://127.0.0.1:<canonical decimal 1024–65535>/api/auth/aim-market/callback`; reject leading zeros, localhost, IPv6, DNS/alternate numerics, userinfo, query, fragment, backslash/control, percent-encoded path/host. Other clients retain exact registered matching. All success/error destinations revalidated; allowlisted redirect errors only `access_denied`, `invalid_request`, `invalid_scope`, `unsupported_response_type`, `temporarily_unavailable`; never error_description/reflected HTML. Legacy consent escapes names/scopes/action. PKCE challenge is canonical 43-character base64url SHA-256 output; compare decoded digest in constant time and reject malformed verifier/challenge before code consume. Fixed frontend continuation carries only opaque ID.

Continue locks transaction; verifies full current account session (type=access, session live/owned, active user, email cutoff/grandfathering, SSO, current TOTP assurance `2fa` or supported organization assurance); pre-auth/refresh/ID/agent tokens fail. Expired/insufficient sessions return to existing login/step-up. Repeat policy, PKCE and scope/redirect checks; insert hashed random code TTL=60s bound to user/source session/client/scope/URI/challenge, then delete transaction in one commit. Cancel deletes without code. At exchange, lock code, revalidate authorizing account/session, mark used with conditional UPDATE RETURNING (exactly one), create independent session via existing helpers and grant binding, commit together; no helper-internal early commit. Rollback issuance failure leaves no token/session success.

Refresh locks grant by family before session/JTI checks. Require active reserved client, token type/issuer/signature/expiry, matching client/family/user/JTI/current session, active account/SSO and last_login_at floor rounded to whole seconds exactly as website. Use validate_auth_session, refresh_auth_session, rotate_auth_session, issue_token_response_for_session; update grant JTI/session in same commit. Replay cannot mint a second successor; persist applicable family/session revocation **before** returning error, including replay after rotation. Never revoke the independent source website session or update last_login_at during grant issuance. Defaults remain 30-minute access/7-day refresh, independent 24-hour or organization session expiry; rotation creates new session retaining family, per Gate 1 §4.4. Session-aware revocation is not universal immediate access revocation (B:app/api/deps.py:86-133 versus B:app/api/v1/endpoints/auth.py:248-255).

## 5. Chunk B: continuation and confirmation

| Behavior | Build contract |
| --- | --- |
| Page/config | `/oauth/authorize?request=<43 base64url chars>` only; reject extras, duplicate request, encoded bypasses, external URLs or broad `/oauth` prefix. `NEXT_PUBLIC_AIM_DATA_OAUTH_ENABLED=false` default. Off: unavailable message + normal website navigation; no authorization POST. |
| Hydration/login | Existing auth store hydrates before redirect. Save only validated request ID + 600s deadline in `sessionStorage.aim_data_authorization_request`; pass exact internal redirect to login. Provider callback, password, magic-link and 2FA success use bounded continuation; retain provider state/nonce unchanged. Clear expired/consumed continuation. Magic-link in another tab: original tab rehydrates on visibility and fetches metadata; another browser lacks binding and cannot consent. |
| Confirmation | Display **“Continue to AIM Data as [account]”**, authenticated account identity and “AIM Data receives account access equivalent to signing in with your password.” Explicit Continue and Cancel; no generic scope form or auto-submit. Public ID is not binary attestation. Disable double submission. API POST uses credentials + current in-memory Bearer/CSRF; only returned validated loopback URL is navigated top-level. |
| Errors/storage | Map A errors to sign-in, retry/restart or denial text; never render raw error HTML. Do not store tokens/CSRF/verifier in continuation, change website refresh cookie, embed provider UI, or put token in URL. No DB/migration or new credential config in B. |

## 6. Chunk C: local contracts, configuration and fallback

| Local HTTP contract | Success | Failure / binding |
| --- | --- | --- |
| GET `/api/auth/aim-market/bootstrap` | 200 `{enabled,reason,csrf_nonce,loopback_origin}`; host-only `aim_data_oauth_bootstrap_<port>` HttpOnly Lax cookie Path=/api/auth/aim-market, TTL600. JSON readable only same-origin; nonce returned on bootstrap, held in page memory. | enabled=false when local flag off or backend status disabled/unavailable; reason local_disabled/client_disabled/backend_unavailable/backend_unsupported. Never trust Host/proxy headers to select origin. Bind nonce to cookie server-side; limit 10/min/browser/IP, 1024 global entries. |
| POST `/api/auth/aim-market/start` | Same-origin application/json `{csrf_nonce}` and bootstrap cookie; 200 `{authorization_url}` only. Server generates independent 32-byte state/binding/verifier; fixed issuer/client/scope/success=/datasets. | 403 origin_mismatch/csrf_failed; 400 loopback_origin_required; 409 client_disabled; 503 backend_unavailable/backend_unsupported; 429 rate_limited. No navigation or local-auth overwrite on failure. |
| GET `/api/auth/aim-market/callback` | Only code+state OR allowlisted error+state. Verify pending record/state hash/binding, TTL and exact stored issuer/URI; atomically claim before exchange; exchange once, shared connected completion; 303 `/login/complete`. Erase code/verifier immediately. | Duplicate/extra/forged parameters or cookie fail 400 invalid_callback, no exchange; expired/replayed 410 transaction_expired. Valid bound denial/network/disabled failure stores sanitized one-use error, then same fixed 303. Uncertain exchange timeout terminates; fresh start required, never replay. |
| GET `/login/complete`; POST `/api/auth/aim-market/complete` | Public SPA route outside RequireAuth; fresh bootstrap nonce, same-origin JSON `{csrf_nonce}` + binding cookie; 200 existing connected response plus `auth_mode:"oauth"`, `registration_status:"registered"\|"not_ready"`; shared completeLogin then replace `/datasets`. Atomic one-use completed response TTL60. | 403 csrf_failed/binding_failed; 410 completion_expired; sanitized bound error 400 access_denied, 409 client_disabled, 502 upstream_invalid_response, 503 upstream_unavailable, 504 upstream_timeout. Clear binding on terminal response. Identity failure never becomes successful login. |
| POST `/api/auth/aim-market-refresh` | `{refresh_token,auth_mode:"oauth"\|"password"}`; absent legacy marker means password. OAuth form transport to fixed `/api/v1/oauth/token`; password cookie transport below. Validate pair + `/auth/me`, run same token handler/registration. | 400 invalid mode/missing token; upstream OAuth invalid_grant→401; disabled→403 client_disabled; policy→403; 429 preserves Retry-After; network→503/504. Frontend clears auth only 401/403, leaves transient failures retryable without falsely reporting success. Never try a second transport after OAuth failure. |

Local in-memory model: bootstrap nonce hash+binding hash+expiry; pending state hash, binding hash, verifier, exact redirect/issuer/client/scope, created_at, fixed success, status `pending→exchanging→complete→consumed` or terminal error. Async lock guards transitions; new start invalidates old browser attempt; 1024 total entries, 600s pending TTL, 60s complete TTL, cleanup every 30s and every access; no eviction into success. Restart expires attempts. Binding cookie `aim_data_oauth_binding_<port>`, host-only, Path=/api/auth/aim-market, HttpOnly, Lax, Max-Age=600; Secure only on HTTPS, omitted solely for HTTP loopback. Port-specific names do not substitute for state/redirect/issuer checks. No new local SQL DDL; existing token/local-user persistence reused, no verifier/code stored there.

| Key / default | Exact plumbing and behavior |
| --- | --- |
| Backend `AIM_DATA_OAUTH_ENABLED=false` | AND reserved row active; off rejects new authorization/code/refresh, leaves M2M/password/website alone. Existing ACCESS_TOKEN_EXPIRE_MINUTES=30, REFRESH_TOKEN_EXPIRE_DAYS=7, FRONTEND_URL=https://ai.market retained. Public ID/scope/TTLs are protocol constants, not customer input. |
| Website `NEXT_PUBLIC_AIM_DATA_OAUTH_ENABLED=false` | Build-time gate for B; record build switch value; backend remains enforcement authority. |
| Customer `AIM_DATA_OAUTH_ENABLED=false`; `AIM_DATA_OAUTH_LOOPBACK_PORT=8080` | Add explicit Pydantic aliases; canonical integer 1024–65535 only. Installer writes port equal to AIM_DATA_PORT; Compose publishes `127.0.0.1:${AIM_DATA_PORT:-8080}:80` and passes `AIM_DATA_OAUTH_LOOPBACK_PORT=${AIM_DATA_OAUTH_LOOPBACK_PORT:-${AIM_DATA_PORT:-8080}}`. Test resolved config at 8080/8099/18081; no new listener. |
| `AIM_DATA_AI_MARKET_URL=https://api.ai.market` in customer Compose; `AIM_DATA_OAUTH_FRONTEND_ORIGIN=https://ai.market` new | OAuth uses only trusted fixed configuration, no request host/URL override. Existing non-Compose Railway default at C:app/config.py:28-28 remains for legacy clients; OAuth production mode refuses noncanonical issuer until configured explicitly. Password adapter sends configured frontend Origin, never arbitrary browser Origin. |
| `AIM_DATA_OAUTH_TEST_MODE=false` | Isolated test process only, rejects production issuer with test mode on; test API/frontend exact configured same-site HTTPS pair, separate registry. HTTP token exchange allowed only explicitly isolated local test issuer; cookie/browser proof still requires HTTPS same-site pair. No customer CORS addition. |
| `WORKERS`, `WEB_CONCURRENCY`, `UVICORN_WORKERS`, `VECTORAIZ_WORKERS`: absent or 1 | Reject any >1 with actionable startup error and nonzero exit; preserve existing process lock so bypassed CLI multi-worker startup cannot serve. Runtime model/profiling workers are unrelated and unchanged. |

**Exact disabled detection:** local bootstrap/start backchannel GETs A's fixed status endpoint, timeout 5s, redirects disabled, no cache. 200 + exact client_id/protocol_version + enabled=false means client_disabled; start returns 409 `{error_code:"client_disabled"}` and LoginPage keeps password/2FA usable with provider-only limitation text. 404 means backend_unsupported; malformed/timeout/5xx means unavailable, not a false “disabled” assertion. Recheck at start; race to disabled during code/refresh maps A's error_code to same guidance. New image works with A-off and old backend without initiating authorization. Enable C only after A/B readiness and separate rollout authorization.

**Password transport correction only:** preserve form, routes and 2FA challenge; capture only named upstream refresh Set-Cookie after password/2FA success and after refresh. Normalize to nonempty local refresh_token; missing/null pair→502, never storage of null/undefined. Refresh sends that cookie and configured marketplace frontend Origin to fixed `/api/v1/auth/refresh`, no full cookie jar or arbitrary Origin, no upstream redirect following. Keep onboarding flags. Replace the old disable-2FA error advice with retry/support. OAuth/password completion shares `/auth/me` validation + `_handle_ai_market_token` + best-effort registration; failed registration is not_ready, never publish-ready. No connected login may call `_set_jwt_cookie` or mint `vz_` key.

Persist `aim_data_auth_mode` beside existing tokens; legacy absent→password, unknown→clear/error. Centralize all reload/API refresh callers in one in-flight coordinator, use an origin-wide Web Lock across tabs and reread latest token after acquiring; fallback serial coordinator must not run parallel refresh. Write validated new pair and mode in one synchronous storage update, publish auth state only afterward; reset all on logout/account switch. Tokens remain in existing localStorage and 0600 serial file, not S916/encrypted storage. No verifier/provider credentials in those stores.

Exact nginx callback location forwards unchanged to FastAPI, `access_log off`, sanitized error log, Cache-Control no-store, Referrer-Policy no-referrer; app logs scrub query/code/state/verifier/auth/cookie/token bodies and exceptions. Suppress query logging on marketplace authorization/token surface too. Only transient 60-second code may appear in exact loopback callback URL; no callback screenshots, HAR bodies or raw traces. Final `/login/complete` and `/datasets` URLs contain no credentials/state. Header/error/URL/export scans are release checks.

## 7. Named tests and exact builder commands

Commands run from each candidate repository root, using its provisioned dependency environment; `rtk` prefix mandatory. These are **required future commands, not authoring test results**. New tests/inspectors listed here are delivered in their manifest chunk. Set DATABASE_URL to an owner-approved disposable PostgreSQL fixture only; migration test must reject non-test DBs, create fresh databases itself, and tear down only its own resources. Capture command, environment names (not secrets), exit and candidate SHA.

| Chunk / suite | Required test names (parameterize negative matrices) | Commands |
| --- | --- | --- |
| A routes | `test_one_token_route_dispatches_all_grants`, `test_confidential_m2m_contract`, `test_reserved_registration_mutation_rejected`, `test_disabled_status_and_grants`, `test_authorize_json_and_legacy_form` | `rtk proxy .venv/bin/python -m pytest -q tests/test_aim_data_oauth_routes.py tests/test_auth_refresh_cookies.py` |
| A security | `test_pkce_s256_matrix`, `test_loopback_raw_uri_matrix`, `test_error_redirect_allowlist_all_branches`, `test_bound_transaction_csrf_and_origins`, `test_only_full_session_authorizes`, `test_email_role_sso_mfa_matrix`, `test_code_concurrent_single_use`, `test_code_expiry_and_exchange_rollback`, `test_no_secret_or_log_disclosure` | `rtk proxy .venv/bin/python -m pytest -q tests/test_aim_data_oauth_security.py` (real ASGI routes + PostgreSQL, not helper-only or SQLite concurrency). |
| A refresh | `test_refresh_family_jti_client_session_binding`, `test_refresh_rotation_race_and_replay_commit`, `test_refresh_lifetimes_and_last_login_boundary`, `test_website_session_unchanged`, `test_disable_revoke_bound_sessions_only` | `rtk proxy .venv/bin/python -m pytest -q tests/test_aim_data_oauth_refresh.py` |
| A schema | `test_empty_database_upgrade_and_seed_twice`, `test_confidential_fixture_upgrade_and_old_binary`, `test_owner_scope_conflict_aborts`, `test_seed_conflict_refuses_adoption`, `test_schema_preflight_unknown_shape`, `test_no_destructive_downgrade` | `rtk proxy .venv/bin/python -m alembic heads`; `rtk proxy .venv/bin/python scripts/inspect_aim_data_oauth_schema.py --schema-only`; `rtk proxy .venv/bin/python -m pytest -q tests/test_aim_data_oauth_migration.py`; `rtk proxy .venv/bin/python scripts/provision_aim_data_oauth.py --dry-run` |
| B | `exact_continuation_matrix`, `password_provider_magic_link_2fa_resume`, `original_tab_resumes_other_browser_rejected`, `explicit_account_confirm_cancel`, `no_code_before_continue`, `disabled_expired_error_ui`, `website_storage_unchanged` | `rtk npm test -- tests/aim-data-oauth.test.tsx tests/aim-data-continuation.test.ts`; `rtk npm run typecheck`; `rtk npm run build`. Existing scripts F:package.json:6-12. |
| C routes/transport | `test_start_callback_complete_atomic`, `test_state_cookie_nonce_cross_install_matrix`, `test_timeout_never_replays_exchange`, `test_disabled_and_old_backend_fallback`, `test_identity_failure_and_registration_not_ready`, `test_password_2fa_cookie_rotation`, `test_missing_refresh_rejected`, `test_oauth_mode_rotation_and_logout` | `rtk proxy python -m pytest -q tests/test_aim_market_oauth.py tests/test_aim_market_refresh_transport.py tests/test_aim_market_login_2fa.py` |
| C deployment/inventory | `test_ports_8080_8099_18081`, `test_multiworker_startup_rejected`, `test_bootstrap_origin_and_ttl_bounds`, `test_callback_logs_and_final_urls_redacted`, `test_runtime_account_and_install_credential_inventory` | `rtk proxy python -m pytest -q tests/test_aim_market_oauth_deployment.py tests/test_aim_market_oauth_call_inventory.py`; `rtk docker compose -f docker-compose.aim-data.yml config --quiet`; `rtk docker build -f Dockerfile.customer -t aim-data:s1659-c .` |
| C frontend | `button_disabled_preserves_password`, `complete_once_then_datasets`, `refresh_single_flight_across_tabs`, `mode_legacy_account_switch_and_logout`, `transient_refresh_does_not_clear_auth`, `no_credentials_in_navigation` | `rtk npm --prefix frontend test -- src/pages/LoginPage.oauth.test.tsx src/pages/LoginCompletePage.test.tsx src/lib/aimMarketAuth.test.ts`; `rtk npm --prefix frontend run build`. Scripts C:frontend/package.json:6-13. |
| D1/D3 | Docs accurately distinguish authentication/registration/readiness and default-off/unsupported-host states; release reference matches digest. No new runtime/data model/API/config. | D1 `rtk proxy python3 scripts/check.py`; D1/D3 `rtk git diff --check`. |
| D2 | `test_provider_entry_requires_exact_manifest_and_preflight`, `test_unrelated_charters_keep_buyer01_guard`, `test_numeric_origin_and_bounded_provider_navigation`, `test_no_password_prelogin_or_secret_capture`, `test_charter_no_payment_or_destruction` | `rtk proxy python -m pytest -q tests/test_charters.py tests/test_aim_data_oauth_entry.py`; `rtk proxy python -m json.tool charters/aim-data-fresh-install.json`. No DB migration; new entry-mode metadata is runner-only. |

C runtime call/auth inventory must attach method, resolved host/path, credential **class only**, response and account/install ownership assertion for every Gate 1 §3.2 family: auth/me/link/cache, VZ registration, disclosure/version confirmation, signed publish, readiness/status, quote/start/lifecycle and signed-report ingestion, Connect proxy, raw listings/request sync, legacy listing keys, serial bootstrap/activation/status/refresh/checkout/account/metering, credits/deduction/reconciliation, S3 broker, allAI/chat, trust registration/stream and feedback/diagnostics. Unit tests use synthetic transports; live AC11 executes only authorized no-payment subset. Do not invoke paid commands for inventory or “repair” unrelated key/Bearer mismatches; required-journey failure blocks acceptance.

## 8. D2 / provider-only proof and Gate 3 evidence

| Evidence identity | Assignment and constraints |
| --- | --- |
| AC1 `S1659-GOOGLE-ONLY` | Logical fixture label, **not an invented account**. Owner must assign a real controlled Google provider identity and exact existing marketplace UUID with provider=google and password_hash IS NULL; independent clean volume/browser profile. |
| AC2 `S1659-GITHUB-ONLY` | Distinct controlled GitHub identity + exact marketplace UUID with provider=github and password_hash IS NULL; separate lease/profile/volume; include provider cancellation/retry. |
| Synthetic pool admission | No account IDs supplied for those labels. B:e2e/account_pool.json:1-71 lists password credential references, not provider-only attestations; B:app/e2e/account_pool.py:17-33 requires password secret names. Do not relabel buyer-01 or erase pool passwords. Account owner supplies provider-capable synthetic identity membership, exclusion attestation, lease/owner/run scope, current preflight allowlisting and redacted null-password assertion. Real provider email may not match reserved synthetic domain; owner must satisfy existing synthetic exclusion policy explicitly, never infer synthetic status from a label. No Max/customer credentials, guesses, password creation or pool mutation by this authoring task. Unavailable provider identities block live AC1/AC2/AC11, not justify mocks. |

D2 charter uses `start_url=http://127.0.0.1:8099/login`, entry mode `aim_data_oauth`, and exact owner-pinned provider account manifest; intent ≤200 words/2000 chars: fresh install, new button, ai.market provider authentication/confirmation, return, onboarding/version, sample-customers.csv upload, local profiling/PII, metadata approval, publish/readiness. Preserve production_side_effects/requires_mutation and no-payment/no-destruction constraints. Runner refuses missing/expired manifest, wrong preflight account, unavailable provider browser identity or already-authenticated AIM Data shortcut. Existing buyer-01 guards remain for every other charter; OAuth mode must never run `_login_once` password prelogin or silently become anonymous. Owner approves exact adaptation/digest before any run.

For that mode only, runner navigation phases permit exact local origin → pinned marketplace/API origin → exact provider origins from the approved manifest → marketplace callback/confirmation → exact loopback callback → local app; all other domains/paths fail. Never accept a charter-provided wildcard domain, disable global navigation checks, or move tokens/cookies between origins. Use normal authorized Chrome/operator sessions for provider proof; provider interaction can require the authorized operator. Redaction precedes persistence; suspend URL screenshots/raw tracing during callbacks and never record provider credentials. Runner records sanitized transition assertions, not auth URLs with queries.

| Procedure / proof | Required observation |
| --- | --- |
| 1. Before each run | Pin A/B/C/H SHAs, backend/frontend deployment IDs, customer image digest and flags, schema/seed hash, UTC/browser identity, exact synthetic lease. Owner read-only attestation of provider-only linkage and null password before **and after**, without row contents/secrets; pin empty-volume evidence. Confirm no provider secret/client secret/signing key in image, env, build args, browser bundle or host. |
| 2. Real provider authentication | Open numeric-loopback login in authorized Chrome; click new button, then Google (AC1) or GitHub (AC2) at real ai.market. Provider credentials/secrets stay on provider/marketplace backend. Customer container only exchanges ai.market code+PKCE; **no real Google secret on customer host is needed or permitted**. Local provider stubs are only unit/integration tests, not acceptance proof. |
| 3. Confirm and deny | Redacted screenshot/DOM proves correct account + equivalent-password disclosure. No code before explicit Continue; Cancel yields access_denied+state only, no session overwrite. Fresh start after Cancel; Continue returns credential-free `/datasets`. AC2 separately proves provider cancellation and retry. |
| 4. Validate local outcome | `/auth/me` UUID equals attested identity; local link/cache and registration receipt for same install succeed. Record readiness independently; no seller promotion. Reload, expiry/rotated refresh, browser website session still usable. Separate AC3 2FA methods/negative challenges/backup-code and existing-vs-newly-enabled-2FA cases, no secret capture. |
| 5. AC11 | Authorized D2 fresh-install charter, port 8099; synthetic fixture, onboarding/version, profile/PII, metadata approval, publish/readiness receipts. No payment/Stripe/hold/rotation/paid outcome required. Never certify AC11 from an “already logged in” prefix. |

| Gate 3 package per chunk | Required checklist (each item PASS or explicit blocker) |
| --- | --- |
| All | Exact branch/base/candidate SHA + diff, commands/exit/results, no unrelated changes, new dependencies, flags/defaults, reviewer-readable migration/API contracts; unanimous **CC/GLM/DeepSeek** against exact implementation SHAs, MP excluded as reviewer. Missing reviewer/quota/stale vote blocks Gate 3. Deployment and live/browser acceptance remain separate Gate 4 evidence, never inferred from unit tests. |
| A | A0 live schema-only manifest + empty replay + seeded/confidential old-binary fixture; migration/seed idempotency/conflict proof; route table/real ASGI dispatch, PKCE/redirect/CSRF matrix; concurrent code/refresh and persisted replay revocation; 2FA/email/role/SSO checks; cookie/lifetime parity and no-secret scans; default-off and rollback rehearsal. |
| B | Continuation across all login methods/tabs; no silent code, account disclosure/Cancel; origin/cookie attribute assertions on same-site HTTPS test stack, adversarial origin refusal; S916 unchanged; build/test output; live AC1/AC2 planned identity prerequisites clearly marked. |
| C | Actual one-worker process tree and rejection of multiworker startup; Docker 8080/8099/18081 origin/port routes, occupied/wrong port and remote/local-host failures; atomic state/complete TTL/CSRF/replay; disabled/old-backend password/2FA/reload/rotated-refresh; `/auth/me`/registration failure behavior; runtime credential inventory; image/layer/bundle/log/export scans + 0600 store. |
| D1/D2/D3 | Runbook checks, exact charter and owner approval, provider leases/preflight and no-payment scope, source guard regression tests, release digest/version reconciliation; AC-to-proof manifest retaining missing live proofs rather than false completion. |

## 9. Rollout, rollback and retained risks

| Chunk | Rollout / compatibility | Rollback |
| --- | --- | --- |
| A | After authorized migration admission/review: deploy first, AIM_DATA_OAUTH_ENABLED=false + inactive seed. Verify M2M and website cookie/password baseline before B/C. Enable only after B/C, review and explicit rollout authority. | Switch off; revoke only reserved grant refresh families/current AIM Data sessions, expire pending transactions/codes. Preserve additive schema/other clients/website session. Never destructive downgrade or secret rotation. Record residual access bound (configured 30m default; some consumers do not check session revocation). |
| B | Deploy continuation with UI switch off; test isolated same-site issuer, then approved enablement. | Disable UI build flag and redeploy previous frontend after A off; clear continuation metadata. Existing login remains. |
| C | Deploy exact image with local flag off; disabled/status fallback tests pass before enabling new primary action. No change to default password UI/2FA. | Turn local flag off, expire attempts and clear OAuth mode/tokens when invalidated. Retain corrected password adapter in candidate image; **do not blindly downgrade to v1.22.10**. Provider-only users lose this path: honest outage guidance, no forced password/2FA change. |
| D1/D2/D3 | Documentation/runner/notes deploy separately; never mark live verification without evidence. | Revert only erroneous docs/charter/runner/notes, keep historical proof and outage guidance; no resetting accounts, deleting volumes or reversing listings. |

Retained risks: public client ID cannot attest binary; scope grants full account-session authority; existing AIM Data localStorage/file exposure remains; schema admission/provider pool/owner-authorized runner evidence may block build freeze or live release. Flags do not instantly invalidate every issued access token. Remote-host button use unsupported; password fallback cannot serve passwordless accounts. **POST-P0-PAID (S1656 owner) and AC12/POST-P0-REMOTE (this BQ evidence owner) remain OPEN, not P0 gates.** No S1656 17-criterion/paid proof, owner origin amendment or tunnel execution is required for this P0; no tunnel-free relay introduced.

## 10. Acceptance coverage ledger

Each row retains Gate 1's complete criterion; chunk tests discharge only their contribution until the specified integrated/live proof exists.

| Gate 1 criterion | Implementation chunks | Required closing evidence |
| --- | --- | --- |
| AC1 Google-only fresh install | A+B+C, D2 support | Independent provider/null-password/confirmation/identity/registration bundle. |
| AC2 GitHub-only fresh install | A+B+C, D2 support | Separate volume/identity/bundle, provider Cancel and retry. |
| AC3 2FA across login methods | A+B+C | Real website challenge + wrong/expired/pre-auth/backup-code tests and existing/newly-enabled assurance cases. |
| AC4 password/2FA/local operator fallback | C, A baseline; D1/D3 | Disabled-OAuth fresh/upgrade/reload/rotation evidence, local role/key/cookie parity and no null refresh storage. |
| AC5 email/role/SSO/account rules | A+B+C | Current cutoff/grandfathered, provider/magic-link, buyer/pending/inactive/SSO assertions; no authority promotion. |
| AC6 local loopback ports | C; D1 | 8080/8099/18081 Docker/browser chain, wrong/occupied port, localhost/mismatched-origin/remote-browser failures. |
| AC7 public client/PKCE/routes | A+C | ASGI M2M parity, one token route, malicious registration and code/client/URI/port/scope/verifier matrix. |
| AC8 state/cookies/CSRF/redirects | A+B+C | Adversarial origin, browser/install/issuer mix-up, duplicate/encoded parameters, exact cookies through provider return, no overwritten auth. |
| AC9 refresh family/session policy | A+C; D1 | Clock/race/replay/revocation persistence, website-cookie comparison, 30m/7d plus session expiry, reload/transient/rate-limit proof. |
| AC10 shared completion/credential parity | A+C | Local HTTP integration + complete runtime call/auth inventory, registration not-ready and unchanged signed/key paths. |
| AC11 no-payment charter | A+B+C+D2; D1 | Owner-approved entry adaptation and exact fresh-install login/profile/PII/metadata/publish/readiness bundle. |
| AC12 remote-host/tunnel | None in P0; D1 tracking | POST-P0-REMOTE OPEN; not a P0 blocker or claimed pass. |
| AC13 secrets/storage/disclosure | A+B+C+D1+D2+D3 | Image/layer/bundle/config/log/export/URL scans, TTL deletion/0600 permission and current-storage disclosure. |
| AC14 kill switch/rollback | A+B+C+D1+D3 | Rehearsed disable/revoke + password/2FA/reload, other-client/website continuity and residual access expiry bound. |

**Authoring validation boundary:** Gate 1 verbatim/authority equality and 122 ranges checked; remote source pins verified; cited backend drift inspected. This document specifies future commands, not fabricated runtime, target-schema, Council or provider results. Sole-file diff and line/citation validation are required before push.
