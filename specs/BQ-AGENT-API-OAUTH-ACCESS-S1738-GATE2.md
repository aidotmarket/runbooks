# BQ-AGENT-API-OAUTH-ACCESS-S1738 — Gate 2 detailed build

**Status:** Proposed Gate 2 build contract. Gate 1 passed R3 3/3 APPROVE_WITH_NITS and merged at `e78070a3`; this document does not assert implementation, deployment or Gate 4 acceptance.

**Authority and source:** Gate 1 `specs/BQ-AGENT-API-OAUTH-ACCESS-S1738-GATE1.md` limits this work to credentialed Agent API OAuth `client_credentials` admission alongside existing agent keys. Anonymous catalogue discovery is deferred. Baseline citations are to immutable `aidotmarket/ai-market-backend@d9b02a5c`; Round 1 party-source citations below are to `origin/main@9617e23a7fd16030b49b2aac07f90052f312f0af`. The builder must rebase this manifest against its exact candidate and report any changed seam before implementation. This is a product and S1656 test-environment build plan, not permission to flip production traffic.

## 1. Outcome and decisions

A buyer-owned, explicitly agent-enabled OAuth client exchanges its own secret for one `type=oauth_access`, `aud=oauth_agent` bearer and calls protected `/api/v1/agent/*` routes without an agent key. The actor is the client owner/buyer, bound organization and current server-side policy. The same selected actor controls scope, rate budget, spend reservation, legal acceptance, transaction attribution and audit. An existing agent key continues through its existing path. No generic web JWT, refresh token, AIM Data token or legacy `type=agent/aud=agent` token gains Agent API authority.

Ship `client_credentials` only. Remove authorization-code and refresh-grant claims from this Agent API quick start and the mounted token endpoint description; their implementation remains outside this release. The mounted `/oauth/token` delegates non-AIM grants to the confidential client-credentials handler today, while its prose advertises unsupported grants [backend@d9b02a5c:app/api/v1/endpoints/oauth.py:356-399]. Current client-credentials tokens carry `type=agent/aud=agent`, whereas the wallet verifier requires `oauth_access/oauth_agent` [backend@d9b02a5c:app/services/oauth_client_service.py:147-181; backend@d9b02a5c:app/services/oauth_service.py:340-379]. Gate the new token shape and its admission with the same flag. Old token shape remains valid only for its existing consumers and never becomes an Agent API bearer.

**Settled owner cap rule (Max decision Event `e90b3bb4`, rule A):** initial cap and administrative ceiling are zero. An administrator grants paid scope and sets the ceiling; the buyer sets their own cap within that ceiling; only an administrator raises the ceiling. An owner cannot enable paid scope or transfer client ownership. The admin operation requires existing admin authorization and an audit reason.

## 2. Exact baseline and build manifest

At the pinned tree, `AgentAuthMiddleware` uses a key-only gate, prefix-based public matching and a process-local key RPM deque [backend@d9b02a5c:app/middleware/agent_auth.py:21-30,52-108]. `app/main.py` exempts four Agent API route prefixes [backend@d9b02a5c:app/main.py:795-815]. The client model has owner/user IDs, scopes and active state but no agent policy [backend@d9b02a5c:app/models/oauth_client.py:23-54]. `agent_api_keys` stores key cap, used amount, RPM and suspension/revocation [backend@d9b02a5c:app/models/agent_api_key.py:13-34]. Transactions have `api_key_id` and globally unique `idempotency_key`; audit has only `api_key_id` [backend@d9b02a5c:app/models/transaction.py:133-163; backend@d9b02a5c:app/models/agent_audit_log.py:16-34].

Product chunk A, admission and documentation:

- EDIT `app/core/config.py`: `AGENT_API_OAUTH_ACCESS_ENABLED: bool = False`; add the nonsecret maximum allowable owner-set cap setting, default zero. No environment implicitly enables admission.
- EDIT `app/middleware/agent_auth.py` and `app/main.py`: keep the flag-off branch byte-for-byte equivalent for protected requests; flag-on branch selects exactly one credential, attaches `request.state.agent_actor`, classifies Agent public exceptions by exact method plus `routed_path`, and invokes the shared OAuth RPM gate before route dependencies. Keep legacy `request.state.agent_api_key`/`key_data` for key actors.
- NEW `app/services/agent_oauth_access.py`: immutable `AgentActor`, strict authorization-header parsing and signature verification, DB principal and policy resolution, scope checks, revocation checks, selected-credential namespace and Redis RPM decision. Request body, query parameters and unverified claims cannot define an actor.
- EDIT `app/api/v1/endpoints/oauth_clients.py`, `app/services/oauth_client_service.py`, `app/api/v1/endpoints/oauth.py`: authenticated client creation/control, explicit agent opt-in and grant checks, flag-gated canonical token issuance, exact `scope` response, secret-rotation epoch, strict interactive-token check on management, truthful grant documentation. Client management currently decodes a subject without type/audience check [backend@d9b02a5c:app/api/v1/endpoints/oauth_clients.py:94-123].
- EDIT `app/api/v1/agent/router.py`: replace flag-on independent bearer lookups with the selected actor in all protected handlers; require route scopes in code; preserve flag-off route dependencies. Current wallet/order handlers independently resolve `OAuthService.verify_access_token` [backend@d9b02a5c:app/api/v1/agent/router.py:548-604,717-739,795-860,932-964,1080-1100]. Update generated Agent OpenAPI and quick start [backend@d9b02a5c:app/api/v1/agent/router.py:280-333] and the direct `/agent/llms.txt` redirect description [backend@d9b02a5c:app/api/v1/agent/router.py:340-350].
- EDIT the Agent API text at the root `/llms.txt` generator only where it describes Agent API credentials; preserve its separate root discovery policy [backend@d9b02a5c:app/api/v1/endpoints/llms_txt.py:217-249].

Product chunk B, money and records:

- NEW `app/models/agent_oauth_client_policy.py`; EDIT `app/models/oauth_client.py`, `app/models/transaction.py`, `app/models/agent_audit_log.py`; NEW additive Alembic revision `s1738_agent_oauth_access`, parent **the actual ScriptDirectory head at build time** (the pinned tree includes `s1749_gateway_buyer` [backend@d9b02a5c:alembic/versions/20260924_001_s1749_gateway_buyer.py:1-10]). Refuse to guess a sole head or create an unrelated merge.
- NEW `app/services/agent_spend_policy.py`: one actor-aware paid guard and idempotent reservation/release API, invoked by both wallet endpoints and canonical agent checkout. EDIT `app/services/agent_service.py`, `app/services/transaction_service.py`, `app/services/order_money_service.py`, `app/api/v1/endpoints/webhooks.py` for selected actor, compensation and reconciliation. Wallet purchase currently calls `execute_purchase(user_id, dataset_id, ...)` without a key [backend@d9b02a5c:app/api/v1/agent/router.py:717-739,795-860; backend@d9b02a5c:app/services/agent_service.py:458-465]; canonical `initiate_agent` is key-specific [backend@d9b02a5c:app/services/transaction_service.py:344-375,406-430], and payment failure currently releases only key spend [backend@d9b02a5c:app/api/v1/endpoints/webhooks.py:1768-1809].
- EDIT the existing order and licence acceptance call sites only as necessary to pass selected buyer/credential; preserve S1735 document, seller, signer and issuance checks. Current licence acceptance stores `principal_ref` and `credential_id` [backend@d9b02a5c:app/models/license_acceptance.py:14-37,60-80].
- EDIT `app/routers/mcp_sse.py` and narrowly extend `app/middleware/rate_limiter.py` with a strict public-MCP decision. The current helper admits when Redis is absent or throws [backend@d9b02a5c:app/middleware/rate_limiter.py:80-98,163-170]; `/sse` and `/tools/call` call it [backend@d9b02a5c:app/routers/mcp_sse.py:375-425].

Product tests: NEW `tests/test_agent_oauth_access.py`, `tests/test_agent_oauth_access_db.py`, `tests/test_agent_oauth_access_redis.py`; extend existing agent purchase/licence/refund tests at their present seams. S1656 environment chunk: edit its account seed and acceptance journey to provision buyer-02 client/policy, inject new feature flag only into the isolated backend, and record two-replica/outage and money-path evidence. Secrets are read from Infisical only; no new committed secret fixture.

## 3. Schema and policy control

Add `agent_oauth_client_policies` with `oauth_client_id UUID PRIMARY KEY REFERENCES oauth_clients(id) ON DELETE RESTRICT`, `owner_user_id UUID NOT NULL REFERENCES users(id)`, `org_id UUID NOT NULL REFERENCES party(id)`, `approved_scopes TEXT[] NOT NULL DEFAULT '{}'`, `spend_cap_cents BIGINT NOT NULL DEFAULT 0`, `spend_used_cents BIGINT NOT NULL DEFAULT 0`, `admin_ceiling_cents BIGINT NOT NULL DEFAULT 0`, `rate_limit_rpm INTEGER NOT NULL DEFAULT 0`, `is_active BOOLEAN NOT NULL DEFAULT false`, `suspended_until TIMESTAMPTZ`, `revoked_at TIMESTAMPTZ`, `created_at/updated_at TIMESTAMPTZ NOT NULL`. CHECK nonnegative monetary values, `spend_used_cents <= spend_cap_cents <= admin_ceiling_cents`, RPM between 0 and 600; no paid operation when cap or RPM is zero. Policy creation is simultaneous with explicit agent opt-in, owned by the already authenticated client owner. Existing clients are not auto-enrolled; no backfill grants paid scope. Bind `org_id` to the current validated buyer party and never accept an arbitrary organization ID from an OAuth caller.

**Authoritative party source and check:** implement one `resolve_agent_buyer_party(db, owner_user_id)` query over `party_identity` joined to `party`. Require `provider='auth_user'`, `external_id=str(owner_user_id)`, `is_primary=true`, `party.status='active'`, `party.deleted_at IS NULL`, and exactly one result; require its `party.id` to equal `policy.org_id` at client opt-in, token mint, each protected admission, and again under the paid policy row lock before side effects. The same resolved ID supplies legal/order/transaction attribution. Missing, duplicate, inactive, deleted, removed or stale bindings fail closed. This is the single buyer-party authority for this release: `party_identity` binds the authenticated user to a party [backend `origin/main@9617e23a7`:app/domains/crm/core/models.py:7-45]; the pay-in resolver already joins active/nondeleted parties and requires exactly one match [backend `origin/main@9617e23a7`:app/services/data_verification_payin_service.py:353-369], while the terms resolver omits that live-party check [backend `origin/main@9617e23a7`:app/services/terms_acceptance_service.py:66-80]. `organization_memberships.organization_id` references the separate `organizations` namespace [backend `origin/main@9617e23a7`:app/models/organization.py:50-58]; do not infer a `party.id` mapping or delegated organization authority from it. A client binds to one directly resolved buyer party; delegated organization membership is outside this release.

On `oauth_clients`, add `agent_enabled BOOLEAN NOT NULL DEFAULT false` and the sole writable `agent_token_epoch INTEGER NOT NULL DEFAULT 0`. Require `user_id=owner_id=policy.owner_user_id`, a nonnull owner and active user/client, `client_credentials` in approved grant types, and the live buyer-party check above before an agent token is minted or an actor is admitted. Creation defaults agent-disabled, `read write` remains unrelated to `market.read` and `market.purchase`, and agent scopes are granted only through the policy control path. Secret rotation increments `oauth_clients.agent_token_epoch` and invalidates old agent tokens; the policy has no duplicate epoch. Client suspension, revocation or scope/cap lowering takes effect on next request; no cache may delay denial beyond five seconds. The management API uses verified interactive user `type=access` and its correct web audience, not an agent bearer; authenticated owners can read/lower their own policy and set a cap up to the admin ceiling; only admin can grant `market.purchase`, raise ceiling, raise RPM, or unsuspend/re-enable. Persist and audit every change.

Add `transactions.oauth_client_id UUID NULL REFERENCES oauth_clients(id) ON DELETE RESTRICT`, index it, retain historical `api_key_id`, and add `agent_credential_version SMALLINT NULL`. The migration installs a `BEFORE INSERT OR UPDATE OF surface, agent_credential_version, api_key_id, oauth_client_id` trigger: for every inserted `surface='agent_api'` row, and every attribution-changing update to such a row, reject unless `agent_credential_version=1` and exactly one of `api_key_id`/`oauth_client_id` is nonnull. SQL `NULL` cannot bypass this predicate. Existing rows with null version remain readable and may receive ordinary status updates unchanged; historical attribution changes must first be reconciled explicitly, never silently exempted. All new agent transaction writers, including legacy-key writers, set version 1 while preserving their existing key idempotency. Store immutable `buyer_id`, `party_id`, selected credential and the policy snapshot (cap, approved scopes and owner) in existing transaction metadata; never derive authorization from the snapshot. Add `agent_audit_log.oauth_client_id UUID NULL REFERENCES oauth_clients(id)`, `auth_kind VARCHAR(16) NULL`, `buyer_user_id UUID NULL`, `org_id UUID NULL`, `correlation_id VARCHAR(64) NULL`, and indexes for client and correlation. Historical nulls remain valid; new selected-actor events require one credential ID and `auth_kind IN ('oauth_client','agent_key')`. Keep request/response payload redacted.

Migration sequence: add nullable columns/indexes and policy table with flag false; inventory every `surface='agent_api'` writer; update each writer to supply version 1 and exactly one FK; backfill or reconcile any historical agent row with invalid attribution and record counts without rewriting its idempotency key; deploy the compatible writers homogeneously; then install the trigger before any OAuth flag flip. Test raw inserts after trigger installation, not just ORM writers. Rollback flips the flag false, retains the trigger and additive columns, and uses a compatibility build whose key writers still supply version 1. A schema downgrade may drop the trigger and additive objects only in an empty disposable database with no OAuth policy/transaction/audit rows and no new versioned agent rows; otherwise refuse downgrade and preserve history. Verify clean install, upgrade from exact baseline, repeated upgrade, old-row preservation, writer inventory and trigger enforcement.

For idempotency, keep the existing global unique `transactions.idempotency_key` [backend@d9b02a5c:app/models/transaction.py:155-163]. **Legacy key actors retain today's exact identity and lookup:** the raw caller key plus `api_key_id`, with no hash, route namespace, migration or changed retry behavior [backend@d9b02a5c:app/services/transaction_service.py:357-369,411-434]. Only OAuth-credential requests store a deterministic server-side hash of `oauth_client:client_id:route:caller_key`, with a distinct OAuth namespace; they look up by selected client and that hash. Same actor/request returns the same result; OAuth cross-client or cross-route calls get distinct identities. Store a unique reservation identity on the transaction/order attempt so a retry, webhook, refund or reconciliation releases at most once. A concurrent cap update must lock the same policy row and cannot set the cap below already used cents. Use `SELECT ... FOR UPDATE` and one transaction for reservation plus money/order/acceptance state where possible; for external Stripe uncertainty, retain the current recovery state machine and release only after a confirmed failed/cancelled/refunded outcome. No speculative release on timeout.

## 4. Canonical credential and route matrix

| Flag / request headers | Selected actor and result | Budget and records |
| --- | --- | --- |
| Off / neither or OAuth only | Today's key gate returns 401. | No new policy lookup, reservation or OAuth record. |
| Off / key only or both | Existing key middleware and existing downstream route checks. | Existing key in-process RPM, key cap/records. |
| On / neither | 401 before handler. | No budget or record. |
| On / key only | Existing validated key path and downstream behavior. | Key RPM and `api_key_id` only. |
| On / valid canonical bearer only | OAuth client owner/buyer actor. | Shared client RPM, OAuth cap, `oauth_client_id` only. |
| On / canonical bearer plus any key | Bearer actor exclusively; key is not parsed or charged. | OAuth budget and records only. |
| On / malformed or invalid bearer plus valid key | 401; no key fallback. | No reservation or transaction. |

Bearer syntax is exactly one case-insensitive `Bearer` scheme and one nonempty token, without comma, whitespace inside the token or duplicate `Authorization` fields. Verify configured algorithm, signature, `exp`, `iat`, exact `aud=oauth_agent`, exact `type=oauth_access`, UUID `sub`, nonempty `client_id`, current epoch and issuer if minted; reject `alg=none`, wrong audience/type, refresh, web access, AIM Data, old `agent/agent`, forged subject/client, expired, wrong scope and inactive owner. Parse and select before consulting `X-API-Key`. Re-read client/policy for every OAuth protected request and again under the paid policy row lock before side effects. Effective scope is intersection of token scope, client grant, policy approved scope and route requirement; no wildcard from `read write`.

Exact routed method/path (never a text prefix) determines flag-on scope:

| Route | Required OAuth scope and behavior |
| --- | --- |
| `GET /agent/search`, `/agent/datasets/{id}`, `/agent/datasets/{id}/eval`, `/agent/datasets/{id}/preview`, `/agent/listings/{id}/synthetic_preview` | `market.read`; published/preview eligibility and synthetic labels remain. |
| `GET /agent/wallet/balance`, `POST /agent/wallet/deposit` | `wallet.read` and `wallet.deposit` respectively; existing Stripe/user checks remain. Gate 1 Q7 is settled: retain the deposit route for this release. |
| `POST /agent/purchase`, `POST /agent/orders/instant` | `market.purchase`, active nonzero paid policy and all money/licence guards. |
| `GET /agent/orders`, `GET /agent/orders/{id}/data`, `POST /agent/orders/{id}/request_access`, `GET /agent/orders/{id}/artifact` | `orders.read` for list/data/artifact, `orders.write` for request_access; enforce owner, selected client/org and active licence before issuing access. |
| `GET /agent/openapi.json`, `GET /agent/llms.txt` | Public metadata, no actor and no limiter dependency. |
| `GET /agent/sse`, `POST /agent/tools/call` | Existing public MCP transport/tool policy, independent of this flag; strict Redis outage rule in section 6. |
| Any other `/agent/*` method/path | Deny by default until explicitly inventoried and approved. |

The table uses `/agent` as shorthand for `/api/v1/agent`. Generate and review the actual mounted method/path list, including HEAD/OPTIONS semantics, against [backend@d9b02a5c:app/api/v1/agent/router.py:272-276,340-350,357-490,607-641,703-795,904-932,1055-1117; backend@d9b02a5c:app/routers/mcp_sse.py:375-397]. A sibling path such as `/search/private` or wrong method never inherits an exception. Flag off retains today's route behavior; no-credential search/details/eval remain denied before and after enablement. Anonymous catalogue access is deferred.

## 5. Paid-operation order and attribution

Both wallet entry points and canonical `TransactionService.initiate_agent`/checkout call `AgentSpendPolicy.guard_and_reserve(actor, listing, amount, idempotency)` before wallet debit, Stripe session, order, transaction or access grant. It reuses `resolve_agent_buyer_party` under the paid policy lock and verifies current owner, exact policy party, seller and listing state, route scope, nonzero cap, user wallet/daily/monthly limits, organization ceiling, terms, S1735 acceptance intent and synthetic buyer/seller guard. Existing wallet synthetic refusal occurs before charging [backend@d9b02a5c:app/services/agent_service.py:482-515]; canonical initiation has the same guard [backend@d9b02a5c:app/services/transaction_service.py:374-401]. Retain the current S1735 constructor and immutable document hash; write `principal_ref=user:<buyer UUID>` (or the directly resolved legal party where supported) and stable `credential_id=oauth_client:<UUID>` or `agent_key:<UUID>`. Never use the bearer, client secret or JWT `jti` as legal credential identity. Under flag off, avoid the new guard and preserve existing paid-path behavior, including existing independent bearer checks.

One selected policy owns the cap and one matching FK on transaction/audit/order evidence. OAuth paid transactions use `oauth_client_id`, null `api_key_id`; key transactions use the inverse. Reconciliation, payment failure, cancellation, partial/full refund and retry resolve the tagged credential, lock its policy and release only the original reserved cents once, with an append-only audit/transaction event. Preserve order-money state transitions and unknown-provider outcomes; the present failure writer checks `api_key_id` only [backend@d9b02a5c:app/api/v1/endpoints/webhooks.py:1768-1809]. No dummy key row. Reject another client's access to an owned order even when its owner user is the same; owner user, bound organization and selected credential must all agree for new OAuth orders. Historical key orders use their original key ownership rules.

## 6. Shared limiter and four public exceptions

Every admitted protected OAuth request consumes one per-client RPM unit before route work, including reads and rejected paid attempts after authentication. Redis key is versioned `agent-oauth-rpm:v1:<oauth_client UUID>` with a 60-second sliding window; never key by raw token or replica. Use a single atomic Redis script (prune, count, conditional add, expire, oldest timestamp) so concurrent replicas cannot both admit the last slot. The current strict helper uses `pipeline(transaction=True)` and returns `(False,0,reset_epoch)` on over-limit, `(False,0,0)` on no client/exception; current callers discard the epoch [backend@d9b02a5c:app/core/redis_cache.py:300-343]. Implement a typed result or narrow wrapper that distinguishes **quota 429 plus positive `Retry-After`** from **no Redis client/exception 503**. Missing/zero RPM policy denies admission as policy invalid (403). Legacy key RPM remains process-local [backend@d9b02a5c:app/middleware/agent_auth.py:61-78,98-108].

Choose fail-closed 503 for both public MCP routes when Redis is unavailable, before SSE stream creation or tool JSON parsing/dispatch, **only while this flag is on**. Flag off retains today's fail-open behavior and rollback restores it; this flag-on tightening is an explicit Gate 1 requirement. Keep their existing 429 shape for actual quota exhaustion. Their current limiter fails open [backend@d9b02a5c:app/middleware/rate_limiter.py:90-98,163-170; backend@d9b02a5c:app/routers/mcp_sse.py:375-425]. `GET /agent/openapi.json` has no limiter dependency and serves generated route metadata with cache header [backend@d9b02a5c:app/api/v1/agent/router.py:280-333]. `GET /agent/llms.txt` has no limiter dependency and returns a direct 301 to root `/llms.txt` [backend@d9b02a5c:app/api/v1/agent/router.py:340-350]. The direct request remains available with Redis down. **Do not follow that redirect for the Agent exception acceptance test**; record the 301 and Location. Separately record the normal redirect-following client result as observation under root discovery's own policy: root `/llms.txt` is dynamic, `no-store` and DB-backed [backend@d9b02a5c:app/api/v1/endpoints/llms_txt.py:217-249]. Do not claim the redirect chain is static or cacheable.

## 7. Tests and S1656 environment

Unit tests: all eight credential-matrix rows under flag off/on; malformed/multiple headers and bearer priority; signature/algorithm/expiry/issuer/subject/client/epoch/type/audience/token-scope confusion; explicit rejection of web access, refresh, AIM Data and old agent tokens; management token cannot be an agent token; `market.read` on all five catalogue reads; all wallet/order scopes; route exactness, wrong methods, HEAD/OPTIONS, rollback and public exception method/path matching; OpenAPI and llms text/examples match issued token/grant; ignored key never supplies authority or spend; OAuth 429/503 distinction and positive `Retry-After`; public MCP 429/503 and direct metadata behavior without DB/tool work.

PostgreSQL tests: clean migration, baseline upgrade, repeat upgrade and downgrade refusal with data; policy defaults and CHECKs; concurrent cap reservations and cap updates; zero/missing policy, inactive owner/client, revoked/suspended client, no/duplicate/inactive/deleted party, removed identity, stale policy binding, rotated secret and grant removal before/after a minted token; assert the same party resolver at opt-in, mint, admission, paid lock and legal/order attribution. Cover two buyer accounts and two clients under one buyer; both wallet purchase routes and canonical checkout; no side effect before cap/scope/synthetic/legal denial; same-actor retry, cross-actor OAuth idempotency isolation, refund/payment-failure/reconciliation exactly-one release and unknown Stripe outcome retained. **Legacy parity test:** seed a historical key transaction with raw caller idempotency key; retry with the same key and caller value under flag on and after rollback; assert the original result and no second transaction, charge, reservation or release. Raw SQL insert tests preserve historical null-version rows, reject every new `agent_api` row with null/wrong version or neither/both FKs, and accept each valid key-only/OAuth-only shape. Check licence/order/audit principal agreement, real S1735 signed acceptance and issuance, and synthetic pair guard with flag off/on.

Two-replica Redis tests: run two API processes with separate in-memory state and one Redis; distribute the same client across them and prove total admitted count never exceeds RPM, then kill Redis and prove both return 503 before protected route work. With Redis down, call direct `GET /agent/openapi.json` (200), direct `GET /agent/llms.txt` without `-L` (301), `GET /agent/sse` (503 before stream) and `POST /agent/tools/call` (503 before tool work) on **each** replica. Record the redirect-following root outcome separately. Restore Redis; prove client quota 429 with positive `Retry-After`, no stale outage status, and revocation visible on both replicas in under five seconds. No test may substitute one-process mocks for this acceptance.

S1656: extend the disposable environment's seed to create or locate `buyer-02` under its existing synthetic-account rules, then create one buyer-owned OAuth client through the same service/control path, set agent opt-in, `market.read`/`market.purchase`/required wallet/order scopes, a small positive admin ceiling and cap, and RPM. Use a unique test-only client; do not copy production client data or mint an agent key. Generate the client secret once into the existing `ai-market-backend/test-env` Infisical environment and inject it at runtime; no `.env`, Compose plaintext, fixture, log or PR body may contain it. The seed is idempotent by client/buyer binding, refuses mismatched existing ownership, and rotates rather than prints a lost secret. Its acceptance journey obtains a real `client_credentials` bearer, proves read, purchase, legal acceptance, delivery and refund/failure attribution against the disposable money path, and captures the two-replica Redis and synthetic-pair results. S1656 must run from reviewed backend/environment image SHAs, with test Stripe keys and its existing isolated guards. The existing S1656 secret boundary is `test-env` in `ai-market-backend`, with no Railway sync [runbooks:specs/BQ-MONEY-PATH-TEST-ENV-S1656-GATE1.md:166-180].

## 8. Rollout, rollback and Gate 4 evidence

1. Before rollout, read only privacy-safe production counts of total/active `agent_api_keys` and recent route usage; expose no key, owner or token. If nonzero active use, pause flag flip and review caller migration/compatibility plan. Gate 1's zero-row expectation is a hypothesis, not a production fact.
2. Merge reviewed additive migration and code with flag false; run the exact migration on disposable DB, then approved environment. Verify old key and public-route behavior and canonical token issuance stays old-shape or denied while false. Deploy schema before any flag true. Keep Stripe, licence, synthetic and canonical money guards enabled.
3. Complete S1656 seed and acceptance, including two replicas, real test-mode money path, Redis outage and recovery, token confusion and rollback. Record backend SHA, environment SHA, image digests, migration revision, sanitized policy/client identifiers, test outcomes and signed provider receipts. A test stub is not Gate 4 evidence.
4. For a bounded production cohort, verify **every serving API replica** has the same reviewed backend SHA, migrated schema, attribution trigger, flag value and active route version via deployment inventory plus per-replica readback before traffic. Flip `AGENT_API_OAUTH_ACCESS_ENABLED=true` uniformly; record the authorized operator and timestamp. Do not admit a cohort during a mixed flag/deploy state. Provision only reviewed cohort clients with explicit owner, org, scopes, ceiling and cap; obtain secrets through Infisical. Capture live key-only compatibility, OAuth read and bounded paid-path outcomes, denied anonymous search, denied web/refresh/old agent tokens, 429/503 distinctions and audit/transaction/licence links without revealing secrets or customer data.
5. Rollback is a homogeneous flag false readback across all replicas. It immediately stops canonical token issuance and OAuth admission, retains existing key behavior, and **does not** delete policy, transaction, order, audit, licence or refund history. Reconcile any in-flight payment outcome under its recorded credential; do not downgrade data-bearing schema or double release reserved spend. If a release code rollback is needed, use a compatibility build that reads the additive columns while flag false; prove its exact SHA and all replicas.

Gate 4 may say accepted only with: reviewed product and S1656 exact SHAs plus a fresh backend rebase/seam report; DB migration, attribution-trigger and policy-default proof; matrix and token-confusion results; DB concurrency/idempotency/refund/legal/synthetic results including historical-key retry under flag on and rollback; two-replica Redis-down results for all four exceptions and protected OAuth; real S1656 buyer-02 OAuth money path and Infisical-only secret provenance; privacy-safe production legacy-key count and any required migration decision; homogeneous production flag/deploy proof before and after flip with operator/timestamp; bounded live bearer/key and denial receipts; rollback drill and reconciliation evidence. Process health, a single replica, synthetic-only API response or PR merge alone is insufficient.

## 9. Gate 1 R3 nits

DeepSeek's route-scope LOW is closed by explicit `market.read` on search/details/eval. Its limiter nit is closed by typed quota/outage results. Its metadata wording nit is closed by stating those routes have **no limiter dependency**. GLM's `/agent/llms.txt` redirect nit is closed by direct no-follow 301 acceptance plus a separately observed root redirect chain. Review-controller repository/branch metadata and the historical runbook count are operator metadata, not product schema or lint work; future review dispatch should name `aidotmarket/runbooks` and its actual branch. Do not add runbook tooling, schema or lint rules.

## 10. Round 1 disposition

| Review finding | Disposition in this build contract |
| --- | --- |
| GLM MEDIUM / DeepSeek F3: legacy-key idempotency | Resolved in §3 and §7: raw key identity and `api_key_id` lookup remain exact; OAuth alone gains a namespace; historical-key retry test covers flag on and rollback. |
| GLM MEDIUM / DeepSeek F2: new-row attribution | Resolved in §3: insert/update trigger enforces version 1 and one FK; staged migration, rollback and raw-insert tests cover null bypass. |
| GLM MEDIUM / DeepSeek F6: party authority | Resolved in §3 and §5: one active, nondeleted primary `party_identity`/`party` resolver, exact policy match and fail-closed checks at all enforcement points; delegated organization membership deferred. |
| GLM LOW: duplicate epoch | Resolved in §3: `oauth_clients.agent_token_epoch` is sole writable epoch. |
| DeepSeek F1, F4, F5 | Corrected Redis helper baseline in §6; explicitly retained wallet deposit in §4; added flag-flip operator/timestamp to §8. |
| Gemini nits | Backend `origin/main` drift identified in header and fresh rebase report required at Gate 4; controller dispatch should name `aidotmarket/runbooks`, PR #283 and `build/spec-agent-api-oauth-access-s1738-gate2`. |
| DeepSeek and Gemini verdicts | Both `APPROVE_WITH_NITS`; their nits are folded above. GLM `REQUEST_CHANGES` is addressed by this revision, pending review. |

Max decision Event `e90b3bb4` settles spend-cap rule A in §1. It is no longer an open question. No runbook tooling, schema or lint-rule work is part of this fold.
