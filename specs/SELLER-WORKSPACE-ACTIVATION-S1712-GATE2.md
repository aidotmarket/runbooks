# Workspace implementation and recovery contract — S1712

Prepared September 12, 2026. This is a concrete implementation proposal, not a claim that these commands exist or have passed. Keep the Gate1 review checkout frozen at 1ff494acc79245e8481ece2e985eda902fbf5207. MP implements after the exact dependency and Gate2 acceptance are bound. The operator guide remains the published system runbook; this document supplies the next implementation-specific appendix.

## Authority and remaining integration inputs

Max's existing authorization, relayed by Mars4096 and event99af61fd-db46-42fd-a18d-1d0698e457d1, covers the required release corrections. The cumulative project cap is100USD. Current scoped work continues under that approval. The original handoff is valid until Mars returns Tuesday2026-09-15; Mars4383 has separately asked Max once for an extension through the later genuine holds and production verification. No response is yet recorded. This future boundary does not pause currently authorized work and must not be treated as an approved extension. Mars owns general refund/settlement, acceptance redesign and protected runtime transitions. Vulcan owns Workspace fixture activation, narrow TEST admission and final release. Product code is authored by MP and reviewed by CC/GLM/DeepSeek.

The accepted Mars payment design includes the original Gate2 decision `baac7653f93df83fd4ddf9c56e3fc2b68ac2351a`, F1/F2/F3, and consolidated F4 candidate `53753f127045d863256425b1365ae0b15b5a173c`, accepted by the full CC/GLM/DeepSeek panel with implementation clarifications at `af1de97030d090414fd7f09aafb5d005eeb9434a` (PR166 section17). F4 covers the actual refresh request, WebSocket callback, persistence, token issue/redemption, raw/reference/agent/MCP surfaces and listener refund races. Workspace retains its shared authorization regression obligation without widening first-delivery eligibility. VC alternate callback correlation and external raw-HMAC consumption remain explicit transport proof limits; neither is certified by this design.

Current verified product candidate is terminal MP244 `c3a62af8ccf60929a8e7cec3d62c9d30e0adefd2`, rooted at `d766b8e4e66d7803ebe48f8a3132521abbebe957`. Accepted consolidated scope is decision `26588d3571efc3a19d1a9b591fb51991690e3f62`, spec SHA256 `1e3c9783575db778301d0b7ec8a66f1dadca0b855ce59cf0cff9bdaf8cfd6806`, sections1–20.5. This supersedes MP241 as the dependency candidate. The final manifest `/var/tmp/s1714-readiness-6fc522aa/manifest.json`, SHA256 `e23c753c6da7451fcbd0d1c5beb1d6ae0dc8f098aab7881749d9d1d6f2410287`, reconciles42source/321migration/2protected hashes,52current and291retained artifacts,84requirement rows and732node references. There are1024actual executions,1010unique nodes,1004unique passes and six exact inherited failures; zero errors/skips. All prior1010nodes,142F7,20F6 and19original posting positives are retained. Core665pass/4baseline, adjacent284pass/2baseline, gold14pass/11intentional deselections and canonical55pass remain separately classified. The canonical temporary schema lacks refund_phase; migrated PG17 core supplies the separate new-refund evidence.

The named-database startup race is corrected by authenticated final TCP readiness with bounded probes and a total deadline. Actual delayed-bootstrap refusal/success, all three owned-container removals and hosted run34688843645 attempt1 at the exact candidate passed. Detailed diagnosis, callback/session support and upgrade procedures are published in runbooks PR183/184. Historical failed runs and retries remain preserved. Full product review covers all42targets against the root base and is unanimously APPROVE_WITH_NITS from CC/GLM/DeepSeek, with no HIGH/MEDIUM findings. Exact owner decision `/Users/max/koskadeux-state/s1656/s1714-product-gate3-c3a62af8-unanimous-decision.json` SHA256 `ed425ff091bebc41a879f3b4167a7816e0f23c47d94211a17426e5b5fd9b190f` and all three full responses were independently read and rehashed. The decision accepts product implementation only; the first Workspace Gate2 panel required revision; that panel and its disagreement are retained below.

API-cost's actual caller/chart produces independent progress rather than shared blocking. No chart or entity relationship may be manufactured to satisfy the old wording. All three product reviewers support disposition A and finite disposition B; their declared coverage limits and the independent evidence remain separate. The expanded delivery child-FK and order-first cleanup evidence is finite, not whole-graph certification. The unchanged unmapped BillingEntity.doppler_key_prefix field remains a real inherited defect: bounded source inspection found no dependency in the planned merchant readiness/posting/report paths, but does not certify dynamic consumers or deployed behavior. The same-class wallet loader remains outside this build, and no wallet-route certification is claimed. Bind final accepted product and environment identities before Workspace Gate2 review/build. The last retained production observation is d766; refresh actual production source/image identity at cutover. Actual scheduled Beat execution, old-writer quiescence, provider reconciliation and external transport proof remain release gates.

The accepted environment implementation is candidate `606267af80470b1d61c07d91cda30a0dc5200224`, root `c6b8770e69514035b2c78f0433f86209b4cefe0f`, merged by environment PR36 as `43d67264ab7a3a7e47d6cc6911737273ce1c94cc`. Candidate and merge have identical tree `e553476a38e0948d3a57da60872e406048a0d391`; the current remote main and GitHub merge object were independently verified. Full14-file CC/GLM/DeepSeek review is unanimously APPROVE_WITH_NITS with no HIGH/MEDIUM findings. Owner decision `/Users/max/koskadeux-state/s1656/s1714-environment-606267a-unanimous-decision.json` SHA256 `b3e7880bd6d574cd3ce9abba87990fa872017879b7ec8bf2e896158046ed6f2b`, all three full responses, manifest14 source hashes and six final logs were independently read/rehashed. Final135refund/110delivery/72seed/10journey/21nightly =348 contracts plus2 static placement tests pass. Four unaffected suite logs are retained; no claim that every reviewer reran every test.

The predecessor b006 fixed merchant UUID defect was reproduced against two fresh fully migrated PostgreSQL17.11 databases, which generated different valid IDs; both disposable resources were removed and independently confirmed absent. The final environment resolves the unique strict observed AIM_WY_LLC/US/USD merchant and binds both P/N descriptors, every financial row, all four archived journals, fresh cleanup and preserved settled-P proof to it. Strict boolean checks and guarded first-read v2 browser evidence are present; raw original/browser bytes retain their format. Runbooks PR186 documents diagnosis and recovery. W must resolve this same observed merchant through readiness; no fixture constant or synthetic journal may replace real financial authority.

Retain the accepted environment advisories: three canonical recovery files parse before later immutable write revalidation; the role archive lacks an independent revoke timestamp; one `/refresh` route has the exact documented generic refusal; duplicate acknowledgement uses logs while money nonduplication is independently checked; broad4xx and literal summary flags require their underlying corroborating evidence. The actual browser confirmation is `/transactions/{id}/confirm` → OrderService.confirm_order, not DeliveryService.confirm_delivery under `/deliveries`; the canonical path permits confirmation after download exhaustion while refusing independent money revocation/refund restrictions. Seller revoke records the same locked money-state restriction before commit. This is source evidence, not a live ordering proof. Preserve reviewer coverage limits and corrected route analysis, and do not turn advisory changes into unreviewed scope.

W inherits accepted environment merge43d67264 and productc3 interfaces, the authentic signed TEST seam, fixed P/N authority and protected run25. The final source/spec recording is runbooks PR188, reviewed candidate `ef015581768b8c5bf0af7aaac6c4bcc0525de82c`, merged as `0ec6bac7afa66def9a5f51385443ff215b51fc3f` with the identical reviewed tree. S1656 A2 item37 and S1681 G14.23 record the accepted environment43d67264/backendc3/spec26588 and all three votes. Both accepted money-dependency S1656_SPEC_A2_SHA and S1681_SPEC_SHA name this recording merge. Final W runtime will require the later exact source recording defined below after its implementation exists. Vulcan independently read all four recording-file diffs, verified the corrected immutable spec links, source identity and G9 ancestry. This completes the design dependency binding; runtime installation of these pins remains deferred until the protected lifecycle permits it. Protected run25 remains on original03d3f30/pins519b1d5 and must complete its actual ordered lifecycle; environment merge does not release the owner window. This revision selects exact persisted window/order authority and retained custody without DELETE; the complete revised contract requires a fresh full Gate2 panel. No duplicate product/harness implementation is authorized.

The September12 bounded production read confirms the two authorized historical paid S1707 AWS/R2 orders use Workspace direct provider delivery, outside alternate VC/raw-HMAC paths. Both connections remain revoked; retained file hashes are verified, fresh availability/refusal and production-device transport are not. Mars4255 independently accepted only that bounded evidence. Keep the §17.3 transport gap and final paid acceptance distinct from this historical observation. The cumulative real cost cap remains100USD, with84.8369454546 retained exposure; nominal Stripe TEST ledger amounts do not create real spending or fresh headroom.

Accepted readiness interface: `app.services.refund_processing_service.ORDER_MONEY_PROTOCOL_VERSION=1`; async `assert_order_money_protocol_ready(db, *, required_version=1)` and sync `assert_order_money_protocol_ready_sync(db, *, required_version=1)` raise `OrderMoneyProtocolUnavailable`. Migration `s1714_order_money_v1` has parent `f0e47ebd81ee`. Readiness verifies ancestry, actual columns/constraints, exact merchant chart, bound financial authority and the accepted F2 corrected journal function plus exact sole enabled attachment and all preserved guards; a flag or cached previous success is insufficient. Both synchronous signed-admission and asynchronous processor checks must reject an old/unknown function or trigger drift before money work.

The normal payment capture/refund processor is the sole financial-effects writer. Its `ensure_order_capture(db, *, order, transaction, provider_payment, provider_charge, origin, locked=None, _prepare_only=False)` flushes without committing and establishes a real original order-bound Payment and balanced gross capture journal from canonical order_id/transaction_id checkout metadata. It is also used for refund-before-payment-event ordering. No fixture/backfill finance rows may conceal missing capture. The migration corrects the real order-status trigger: partial refunds do not transition the transaction; full refunds require applied real Refund records and cumulative gross equality, with exactly one transition/event, retaining actual prior status and existing settled facts.

Merchant authority is exactly one active US/USD merchant_of_record entity with code `AIM_WY_LLC`, matching BILLING_ENTITY_CODE. Existing2100 remains Customer Credit Liability. Accepted accounts are1000 Cash - Stripe Clearing,2110 Seller Payouts Payable,4000 Marketplace Commission Revenue,5100 Unrecovered Seller Payouts, with exact types/normal balances and active/system-managed/manual-posting-disabled attributes. Once financial authority is present, billing_entity_id cannot be null, changed or cleared. W relies on readiness and normal financial processing; it never seeds chart or financial authority.

The authentic signed TEST admission seam follows signature/scope and generic dedup validation, at the existing synthetic-suppression decision before durable admission. Mars owns `admits_s1681_negative_refund(event, db, *, normalized_stripe_account)` and the separate `admits_s1681_failed_run_refund` predicate. Vulcan adds a distinct W alternative at this same seam after Mars's accepted product candidate. W cannot satisfy either Mars predicate and neither can satisfy W. An exact claimed TEST authority with incompatible readiness produces retryable failure, not acknowledged suppression. The old BillingService charge.refunded writer is skipped, including replay. Durable stripe_events admission commits and releases synchronous locks before provider reads; async normal effects and completion commit atomically before HTTP2xx. There is no fire-and-forget processing.

The accepted full lock order is event mutex → order → transaction → order_money_states → Payment → Refund rows sorted by real ID → agent_api_keys → period/GL/journal; implicit UPDATE and foreign-key trigger edges count. Non-event paths start with order and cannot acquire an event lock afterward. GL locks sort by (entity_id, code, id), including existing FinanceEngine template posting. The W predicate performs reads and acquires only existing order-money row locks in this chain; it performs no INSERT/UPDATE/DELETE or provider mutation. It must not call a helper that creates missing OrderMoneyState. Registration and cleanup are separate mutations following this chain. The private opening/binding/terminal-cardinality table lock described below is acquired NOWAIT before row locks; it is never acquired by the webhook predicate. Unknown payout dispatches retain their original provider identity and must be reconciled without a new Transfer key or age-based redispatch.

Cross-repository manifest names exact backend source, built image digest and source label, environment source, both acceptance specifications and runtime versions.env. Mars owns the repin/rebuild transition. A clean checkout is insufficient evidence of the running image. Shared webhooks.py edits are serialized on his accepted product candidate. Production GitHub triggers for all four services must be frozen and verified absent BEFORE product merge; current main triggers all have checkSuites=false. Actual stop, old-writer absence, provider reconciliation, migration and restored adoption evidence are separate execution gates.

Mars4145 found the canonical HTTP checkout refusal behind the delivered-order fixture conflict. At c3 checkout.py97–111/204–215, it refuses completed/delivered/pending_delivery for the same buyer/listing but permits created/unpaid; this is not all-status uniqueness or concurrency exclusion. DS second-round MCP-only provenance was disproved by that actual HTTP source. Mars4151 freezes fixture amendment9ab2e7cfef6272b6c708093d28c135d61f7b93c9 (section14; later unanimously accepted in decisionf34cb198): retain P seller01/buyer01, use existing packaged buyer02 (`4b10c456-5936-49d7-be3f-d9c68c9a263e`) for N on the same listing, and retain W seller02/buyer01. F1 now has unanimous acceptance recorded atf34cb198d2415acf82c637b807618245025b6a5e. Vulcan seed implementation1ac556c13238a8b74f1bced9c8af870babcb2221 was integrated by Mars as1db2ecc with70tests passing. Final complete environment Gate3 and the owner runtime window still precede live fixture activation. The protected live environment retains its original three fixtures; only the next accepted clean lifecycle may apply the implemented fourth-fixture seed/transport changes. N still confirms before independent revocation, waits its genuine48-hour hold, proves bare-revoked settlement refusal, then normal full-refund refusal. Failed-run cleanup is limited to exact FAILED v2 authority and cannot include protected v1 run25. W authority remains separate from table `s1681_refund_test_authorities` and its N/failed_run purposes.

## Revised panel disposition and explicit scope decision

The first Gate2 candidate e138f16a6068ebc0214f7f11baa936f1dc4e689c was not accepted: DeepSeek REVISE, GLM REQUEST_CHANGES, CC APPROVE_WITH_NITS. Preserve all three full responses and the unchanged predecessor packet. CC treated cross-window admission as proof orchestration and assumed enforceable exclusive credential custody; this revision does not adopt those assumptions. A qualifying old W order can reach the signed seam without the host's new-order check, and an independent writer can replace a name-addressed secret after GET. Neither a local lock nor observed-drift preservation closes those cases. Select separate exact W registration and remove DELETE from the lifecycle. All three voters must review this complete revision; no prior vote is carried forward.

Source corrections retained: c3 account_pool.json seller02 has email/password names but no totp_secret_name; add that exact field below. The Mars admission module's no-provider-mutation statement does not mean no database locks: _admits invokes lock_order_money_sync. Its N/failed_run table keeps its P/N role and negative/failed_run CHECK constraints unchanged. Do not silently extend those values or route W through the existing CLI's failed-run fallthrough. Reject the suggested table-drop rollback because paid authority/history must remain recoverable. Incorporate CC's optional capture parameters and actual payout eligibility path, DS's nonsecret PYTHONPATH exception and documentation dispositions, and GLM's actual local-image identity.

The second candidate793e79153d9eac5c2d57536982d3f793b04d2c19 also requires a complete correction before implementation: CC/DS APPROVE_WITH_NITS, GLM APPROVE_WITH_MANDATES with a blocking MEDIUM. Later detection of seller01/buyer02 reset damage does not preserve paid P/N history. This third revision instead unconditionally refuses backend SurgicalReset for exactly the four protected fixture UUIDs, including before any owner file/window/schema exists. This closes the host-file-publication race without a new lineage table, distributed lock or destructive guarded path. Mars4483 verified that the accepted money environment uses its separately guarded bin/reset lifecycle, not the surgical-reset endpoint. Unrelated account targets retain existing behavior. All three full second-round responses and mandate/nit dispositions accompany the new full review; no approval is carried over.

## Command and transport proposal

Public interface: `bin/workspace-fixture open-window`, `activate`, `bind-purchase`, `status`, `close-window`, or `deactivate`, with no extra arguments. These are private operator commands, not public API endpoints. The shell wrapper prefixes new subprocess commands with rtk. Status is read-only and never creates audit, lock, token or credentials. The host selects the operation only through ROOT/.state/workspace-authority/current.json, never by listing directories or choosing latest. This owner-written nonsecret canonical envelope has receipt keys schema_version2,kind=owner_pointer,operation_id,owner_window_id,owner_authorization_sha256,runtime_sha256,protected_evidence_sha256. It uses guarded regular/current-owner/single-link/no-symlink reads, max16384bytes, mode0400/private parent. Only Mars as lifecycle owner publishes/replaces this pointer by fsynced same-directory atomic rename after verifying the prior operation is fully closed/restored; commands never create or advance it. All referenced operation-directory files must already exist with matching hashes before publication. Missing/conflicting pointer/audit/artifact refuses; first activation can select its issued operation without an existing audit. An unresolved prior audit blocks a replacement pointer. The receiver compares mounted current.json to stdin operation_id and fixed artifact hashes for lifecycle commands; historical webhook convergence uses its persisted authority, not the newest pointer. Status only reports the actual pointer/audit mismatch without repairing it. Every mutation enters the existing absolute-command check-settlement --lock path, then unchanged --guard seed under inherited lock ownership. Both exits1/2 refuse before the first write; retain ancestor/token/flock checks and do not create a second lock. A successful lock does not authorize a window: the separately issued owner artifact and exact protected lifecycle evidence below are also required. Commands never issue that owner artifact themselves.

Select seed/workspace-host.py as host implementation and seed/seed.py --workspace-activate-stdin as the fixed container receiver for all listed actions. The existing read-only mount resolves /s1656/seed.py. Use the freshly inspected exact backend container, docker exec -i without TTY, /app working directory and the existing Python interpreter. Permit the one fixed nonsecret assignment PYTHONPATH=/app; no credential-bearing environment assignment, argv, temporary secret file, shell interpolation or Compose credential mapping. Send one UTF-8 JSON object on stdin, then EOF. Read at most16385 bytes; limit16384. Reject duplicate keys, trailing/non-object JSON, unknown keys at every level, wrong scalar types and unsupported versions before fixture-writing imports.

The host clears stale inherited values for exact Workspace custody names and retrieves only the reviewed test-env credentials. Do not copy broad host-seed injection, serial enrollment, S3 upload or seller01 helpers. Tokens remain in private host process memory. The actual child-boundary canary uses a unique synthetic Workspace TOTP/new custody secret and must show none of that plaintext in argv, output, audit, exception text or inherited/added environment. Existing accepted Compose application credentials (Stripe, database and prior fixture credentials) remain governed by their existing contracts; this is not an assertion that the whole established application environment contains no secrets. No new Workspace credential is added through Compose, argv or a temporary file. Nonsecret custody IDs/digests remain permitted only in the explicitly allowlisted stdin/audit/receipts; do not add them to environment as a transport workaround. Responses are bounded allowlisted nonsecret JSON; raw provider bodies and traceback/input fragments never escape.

## Owner issuance, runtime pins and trusted opening artifact

The authenticated local operator with authority to run docker exec and maintain the environment is the trust boundary. A hash, actor string, UID check or flock does not authenticate a malicious operator or exclude an external provider writer. No public route invokes this receiver and no new signing key/capability is invented. Mars issues the owner authorization only after independently validating completion of the original protected run25 online/offline lifecycle, corrected P/N phases with their genuine48-hour holds, settled-P/negative-refund results and preserved device/install/identity evidence. Time passing, a wrapper success or a caller's boolean cannot substitute for those receipts. No owner window is issued by this design document.

Concrete private host directory: ROOT/.state/workspace-authority, existing mode0700, current operator owner, no symlink components. New explicit compose.yaml directory bind mounts it read-only at /s1712-workspace-authority; no implicit file availability is assumed. This mount contains only nonsecret authority/runtime records. Before a supported clean lifecycle starts the stack, bin/up creates the empty private directory; it never manufactures an authorization. Protected run25 is not recreated to add this mount. Verify actual container mount source/destination/read-only mode and readable UID mapping during isolated qualification; do not weaken host permissions or fall back to another path.

Each operation has an append-only directory ROOT/.state/workspace-authority/<operationUUID>, mode0700. Only a canonical UUID operation_id selects this relative subdirectory, never an arbitrary path. Old directories remain immutable; a later authorized window receives a new directory rather than overwriting a prior authorization/runtime. Mars's future owner-issued file is <operationUUID>/owner-authorization.json, canonical envelope {receipt,sha256}, schema_version2, kind=owner_authorization, namespace=workspace_s1712. Its exact receipt keys are schema_version,kind,namespace,operation_id(UUID),owner_window_id(UUID),owner_authorization_ref(nonempty ASCII reference to the real owner transition),pins(the exact set below),protected_evidence_sha256(SHA256),issued_at(UTC RFC3339). It contains no future order/PaymentIntent/charge IDs. The protected-evidence.json envelope at the same fixed directory carries the actual phase result artifacts and before-W P/N/device snapshot manifest, with explicit original-run and corrected online/offline dispositions; owner verification must bind the complete actual files and results, not a hand-authored success flag. The selected design uses authenticated owner attestation of the exact evidence set, not an invented existing readiness CLI. protected-evidence.json receipt has exact keys schema_version2,kind=protected_evidence,namespace=workspace_s1712,operation_id,owner_window_id,owner_authorization_ref,attested_at,records. records is a list of exact {category,identity,path,sha256} objects with unique category+identity, absolute owner-controlled regular-file paths and SHA256. Required categories are original_online,original_offline,original_summary,original_wrapper,corrected_online,corrected_offline,corrected_summary,corrected_wrapper,scheduled_success,launchd_provenance,installed_plists,stable_release,money_gate4_bq,protected_before. Each phase category includes purchase-confirmation.json,browser-evidence.json,settlement-check.json,cleanup.json and every transitively referenced artifact; corrected phases additionally include test-negative-pair.json,test-negative-registration.json and settlement-stage artifacts. Run source/spec identities remain separately bound by each phase's actual receipts; the old protected run's pins are not substituted for current deployment pins. Each required category must be nonempty, identity unique, no path traversal/symlink, size/hash checked with guarded reads. The owner attests semantic completion and actual authoritative release/BQ/scheduler provenance; host validation checks completeness of the declared categories, exact bytes/hashes and operation/reference binding, not a fictional independent proof of operator honesty. No bare success boolean is accepted in place of those actual source artifacts. The existing owner with Docker/custody authority is trusted; a compromised operator is outside this interface's authentication guarantee.

Exact protected evidence root is /Users/max/koskadeux-state/s1656/acceptance-evidence and shared lock /Users/max/koskadeux-state/s1656/environment.lock. Original run is20260911T233041Z with historical environment03d3f30acf854537edd76fdb19e1cf521b577c9a/specs519b1d5893dc410392cc66051dad0902572dcef9. Its current online pending active-run.json hash cbbfe3278d4ed6933a254c2c4a24f345a84bbb1a3e44ec5045892509f49889a5 is an observation, not completed authority. Future completed <root>/<R>/online and offline supply the named phase artifacts; <root>/<R>/summary.json and wrapper-contract.json supply run records. Future corrected R must be resolved from the actual distinct v2 run, never fabricated here. Active-run.json is mutable ownership and never substitutes for historical archives.

Actual bin/check-settlement load_pair_phase_archive_v2 (candidate606267 lines2552ff) and resume_v2 (2715ff) define the corrected hash/cleanup/summary semantics the owner must verify. bin/check-settlement and bin/verify --from-clean-seed execute mutations, so neither is called by a Workspace opening/status verifier; --pins checks only source. No existing read-only CLI attests the complete owner window. bin/nightly and bin/nightly --settlement-check produce <R>/scheduled-<UTCstamp>.json for exit0 or scheduled-pending-<UTCstamp>.json for exit2, linked through command_evidence_sha256. Exit2 or a manually run wrapper is not actual scheduled success. The owner includes real launchd execution provenance, installed/active versions of config/com.aimarket.s1681-settlement.plist and config/com.aimarket.s1681-money-path.plist, actual AIM Data1.23.3 stable tag/release evidence and the completed money Gate4/BQ record. These authoritative records are resolved at issuance; none is invented in this draft. protected_before binds settled-P/N and device/install/identity snapshots immediately before W. Receiver reads only the mounted manifest, not host historical files outside its mount; backend separately captures its own complete order baseline. Source interface retained from Mars4453 at /Users/max/koskadeux-state/s1656/s1714-workspace-protected-evidence-interface.md SHA2560476faa68652a9eabbb1809df4a4e4df859a6131c6f61f8a5ca61e344e491bb4. Both files are written once, fsynced, mode0400, max1MiB, regular/current-owner/single-link/no-symlink, with private parent. The host reads through O_NOFOLLOW|O_NONBLOCK and stable fd/path metadata and validates canonical bytes/hash. No overwrite or silent repair on retry; conflicting preexisting bytes refuse.

runtime.json in that same operation directory is the same guarded canonical, write-once format, issued for this accepted deployment/operation by the owner, with exact receipt keys kind=runtime_manifest, schema_version2, namespace,operation_id,owner_window_id,pins,runtime_settings. runtime_settings has exactly E2E_TEST_ROUTES_ENABLED(boolean true), E2E_RESET_ALLOWED_ACCOUNT_IDS(empty JSON array), E2E_TEARDOWN_ALLOWED_ACCOUNT_IDS(empty JSON array). These nonsecret settings are bound by the complete persisted runtime_json/runtime_sha256 and owner-issued runtime artifact; the source pin set remains exactly eight keys. Host verifies it against direct current Docker discovery and independent repository/version sources on every mutating invocation before sending input. The backend compares stdin pins, mounted runtime/owner artifact bytes and their persisted window copies, plus actual ENVIRONMENT=test, STRIPE_TEST_MODE=true, S1681_ENVIRONMENT_SHA and S1681_SPEC_SHA. It checks the real read-only mount and guarded file types. The receiver cannot independently call Docker and must not claim that comparing two caller strings measures its image; actual Docker identity is the trusted host's separate verification, retained as evidence. The mounted files are not accepted from stdin, and bind-purchase never accepts replacement authority or baseline bytes.

Synthetic public checkout arming is explicit in the final reviewed TEST Workspace environment only. compose.yaml backend.environment sets E2E_TEST_ROUTES_ENABLED: "true", E2E_RESET_ALLOWED_ACCOUNT_IDS: "[]" and E2E_TEARDOWN_ALLOWED_ACCOUNT_IDS: "[]" as literal values, not caller interpolation. The unchanged synthetic checkout guard requires the first flag; the same flag imports both account cleanup routes, so the unconditional shared four-UUID service refusal remains mandatory even if an isolated test deliberately populates the allowlists. Empty allowlists are the accepted actual Workspace runtime state, not a substitute for service protection. Do not add a new checkout flag or modify production configuration. The protected run25 is not recreated or armed; only the later owner-approved final TEST runtime uses this reviewed configuration.

On every mutating wrapper invocation the host independently checks effective running-container Config.Env for exactly these three nonsecret keys against the reviewed literals (reject missing/duplicate/conflicting values); do not print or retain unrelated environment entries or credentials. The receiver and qualifying W predicate compare actual application settings to the exact runtime_settings and persisted runtime_json/hash, in addition to existing TEST/pin checks; wrong/unknown flag or nonempty allowlist refuses W mutation/admission through the existing retryable authority-readiness path, never silently suppressing an already registered refund. Perform the cheap definitely-non-W rejection before any optional W/runtime artifact check. Host discovery of configuration is trusted operator evidence, not backend Docker self-measurement. The actual application process startup/router import must be exercised by tests; changing a settings object after router import is not proof that routes are present/absent.

Required configuration/ASGI tests use the exact reviewed Compose representation and isolated fresh application startup: flag=false rejects synthetic public checkout with403 synthetic_purchase_requires_e2e_guard; flag=true permits the actual valid fixed-pair purchase through normal processing with all financial/readiness prerequisites, without stubbing the checkout guard or claiming a later unrelated error is success. Verify both destructive routes are registered when armed and each fixed-identity service guard still refuses with valid isolated tokens/allowlists, including missing Workspace lineage. Verify actual runtime_settings matches host environment and application values and wrong/missing/duplicate flags, changed allowlist, altered manifest/preimage/hash refuse before W writes. Exercise ordinary non-W/production-shaped traffic unchanged. Existing runtime_settings tests belong to tests/test-workspace-fixture.py and tests/test_workspace_test_refund_admission.py; no new production route or settings file target is needed.

Exact pins keys: backend_sha(SHA40),backend_local_image_id(sha256:64hex),backend_source_label(SHA40),environment_sha(SHA40),s1656_spec_a2_sha(SHA40),s1681_spec_sha(SHA40),workspace_spec_sha(SHA40),versions_env_sha256(SHA256). backend_local_image_id is running container.Image, equal to docker image inspect .Id for ai-market-backend:$BACKEND_RC_SHA and .runtime/image-digests.json backend.digest. backend_sha independently equals versions.env BACKEND_RC_SHA and runtime-record backend.source_sha. backend_source_label is actual Config.Labels[ai.market.s1656.backend-sha], independently compared to backend_sha. Re-discover exactly one project/service container directly; no cached snapshot or Compose polling. A registry/OCI manifest or index digest, including AIM Data's index, is never a substitute for this local image ID. Any optional registry digest is separate informational evidence and does not enter pins. environment_sha is the exact clean deployed environment Git commit selected in the final owner-reviewed runtime manifest, equal to actual checkout HEAD and deployed S1681_ENVIRONMENT_SHA. Equal trees do not make different commit IDs interchangeable. For dependency preparation, the selected start is merge43d67264ab7a3a7e47d6cc6911737273ce1c94cc, treee553476a38e0948d3a57da60872e406048a0d391; candidate606267af80470b1d61c07d91cda30a0dc5200224 is retained review provenance and is never substituted for that dependency pin. After MP adds Workspace code/mounts, the runtime pin must instead name the actual final reviewed and merged W environment commit and tree;43d67264 cannot falsely claim to contain those new files. No future SHA is invented here. Before runtime issuance, update the accepted source/spec recording to bind the actual W backend/environment combination and run the real existing --pins source check against that exact record. Both money spec pins name that final recording commit; current0ec6 is the accepted pre-W dependency recording only. Tests reject the alternate same-tree commit and any changed tree. Re-hash actual versions.env and verify both spec pins and accepted Workspace commit against owner-issued files. Pin/hash/label mismatch refuses before provider, credential, audit or database writes.

## Persisted two-stage W authority and concurrency

Use two new tables, separate from s1681_refund_test_authorities. New backend migration s1712_workspace_authority_v1 has down_revision=s1714_order_money_v1; file alembic/versions/20260912_002_workspace_test_authority.py. Confirm a single accepted parent at build time; a changed parent is a reviewed integration change. New models in app/models/workspace_test_authority.py; explicit import in app/models/__init__.py. New service app/services/workspace_refund_test_admission.py owns opening, binding, closure, readiness and the predicate. seed.py invokes that service through the private receiver; no new order_money_protocol.py command or W fallthrough is used. Ordinary capture/refund/payout services remain the sole financial writers.

workspace_test_windows columns: id UUID PK (=owner_window_id), operation_id UUID NOT NULL UNIQUE, namespace TEXT CHECK workspace_s1712, role TEXT CHECK W, seller_id/buyer_id UUID FKs users with fixed-pair CHECKs, owner_authorization_json TEXT and sha256 CHAR64, runtime_json TEXT and sha256 CHAR64, baseline_json TEXT and sha256 CHAR64, opening_json TEXT and sha256 CHAR64, opened_at timestamptz NOT NULL. Fixed seller02=961bd48a-3018-43f4-a7ad-6871720fd806, buyer01=7d662d0e-22f2-4b87-bbc5-80984ba808ac. The canonical JSON fields store the exact receipt preimage bytes as UTF-8 text, never reserialized JSONB; their lowercase hashes must verify on every use. Add state TEXT CHECK(open,bound,closing,recovery,closed), revision BIGINT>=0, closed_at nullable timestamptz and terminal_json/terminal_sha256 nullable paired TEXT/CHAR64. A partial unique index on namespace where state<>closed enforces one nonclosed W window. Immutable trigger rejects DELETE and changes to every column except the explicitly mutable lifecycle columns state,revision,closed_at,terminal_json,terminal_sha256. Explicitly immutable columns are id,operation_id,namespace,role,seller_id,buyer_id,owner_authorization_json,owner_authorization_sha256,runtime_json,runtime_sha256,baseline_json,baseline_sha256,opening_json,opening_sha256,opened_at. Direct SQL tests independently mutate each owner/runtime JSON preimage and hash and require refusal; valid lifecycle transitions remain allowed. The earlier shorthand "and sha256" in each column pair means exactly the corresponding prefixed *_sha256 column, never a shared ambiguous sha256 column. Only forward state transitions open→bound/closing/recovery, bound→closing/recovery, closing→closed/recovery, recovery→closing are permitted with revision increment exactly1; closed cannot reopen. Recovery→closing requires the service's complete reconciliation; closed requires terminal evidence, with database checks on paired terminal fields and nonnull closed_at. Every nonclosed state has null terminal fields/closed_at; the close transition sets them together, and closed is entirely immutable. Inserts require state=open,revision=0 and null terminal fields. Immutable authority is never erased by a lifecycle update.

workspace_refund_test_authorities columns: order_id UUID PK FK orders RESTRICT; window_id UUID UNIQUE NOT NULL FK workspace_test_windows RESTRICT; transaction_id UUID UNIQUE FK transactions; payment_id UUID UNIQUE FK payments; payment_intent_id TEXT UNIQUE; charge_id TEXT UNIQUE; seller_id/buyer_id UUID FKs users; listing_id UUID FK listings, purchased_version_id UUID FK listing_versions, publication_id UUID FK seller_listing_publications, approval_id UUID FK seller_listing_approvals, delivery_authority_id UUID FK delivery_authorities, connection_id UUID FK cloud_connections; amount_cents BIGINT positive safe integer; currency TEXT CHECK usd; role TEXT CHECK W; protocol_version integer CHECK1; registration_json TEXT and registration_sha256 CHAR64; registered_at timestamptz. All columns NOT NULL. Bind the exact immutable scope/approval/version hashes and complete pins in registration_json, and compare all scalar columns to that canonical receipt on each use. Fixed actors checked at DB level; all FKs RESTRICT, no cascade. Immutable trigger rejects every UPDATE/DELETE. Unique order/window/payment/provider IDs prevent another window or charge adopting the same authority. No refund outcome field is updated in this row; outcomes stay in canonical financial/event tables and separate terminal receipts. Readiness verifies actual tables/columns/FKs/checks/unique indexes and exact enabled immutability/transition trigger definitions, alongside accepted order-money readiness. Missing/disabled/drifted schema refuses W admission; existing normal and P/N behavior is unchanged.

Stage A open-window: require all prior windows closed, owned readiness fully restored and retained-custody/provider lineage verified before opening another; a closed window alone cannot bypass an incomplete deactivation. Under owner guard and verified mounted artifacts, private receiver starts a short READ COMMITTED transaction and acquires LOCK TABLE orders IN SHARE MODE NOWAIT before any order row lock or authority write, with the same explicit timeout/deadline limits as Stage B. There is no provider IO while held. If lock unavailable, rollback and return window_busy with zero writes. Capture the complete fixed-pair set of existing order UUIDs in every state, sorted; query with explicit4097 sentinel and refuse if more than4096 rather than truncate. Baseline receipt keys schema_version2,kind=window_baseline,namespace,operation_id,owner_window_id,seller_id,buyer_id,order_ids(sorted unique complete UUID list),observed_at(database UTC). Its identity is the window UUID. Persist baseline canonical bytes/hash plus verified owner/runtime preimages and a separate opening receipt atomically in workspace_test_windows with state=open. No payment IDs are invented at this stage. An identical retry reads/revalidates that same row and returns its original bytes; it never replaces the baseline. A conflicting operation/window/pin/evidence refuses without writes.

Opening receipt keys schema_version2,kind=window_opened,namespace,operation_id,owner_window_id,role=W,seller_id,buyer_id,pins,owner_authorization_sha256,protected_evidence_sha256,baseline_sha256,observed_at. The host retains backend-produced baseline.json and opening.json under ROOT/.state/workspace-windows/<windowUUID>/ using the same private/write-once/canonical/max1MiB/fsync rules. They are returned by the receiver, not generated by host snapshot substitution, and are not an input source of backend authority. If backend commits but host file persistence fails, inspect retrieves the exact stored bytes. A digest cannot prove completeness by itself: completeness comes from this backend query under the stated table lock.

Stage B bind-purchase follows activation and a real normal public W purchase; caller supplies no order ID, snapshot or candidate list. Receiver first performs read-only discovery from its persisted baseline and obtains authenticated TEST PaymentIntent/charge facts through the existing provider client before locks. Then it rolls back the preflight transaction, begins a fresh transaction, acquires orders SHARE NOWAIT before any row lock, locks the window row and all relevant order rows in sorted order followed by the accepted transaction→money-state→Payment→Refund chain. A changed discovery/provider binding causes rollback and a whole-operation retry, not a provider lookup under lock. Recompute the complete fixed-pair order set using the persisted baseline; all baseline IDs must still exist in the fixed-pair set and the entire new-order delta, across all states/listings, must contain exactly one order. That order must be actually paid with unique canonical order/transaction/original capture, fixed actors, usd positive full capture and no pending payout, dispute, ambiguous authority or existing unregistered refund. Never select latest order or silently filter an extra unpaid order out of the delta.

This short table SHARE lock conflicts with public order INSERT/UPDATE/DELETE ROW EXCLUSIVE locks, so an already active writer makes binding refuse immediately and a writer starting later cannot change order cardinality before commit. It is deliberately limited to the isolated TEST environment, statement/lock bounded, and is never a production-wide new lock or a webhook lock. NOWAIT precedes row locks to avoid waiting on existing table writers while holding product locks; subsequent row-lock/deadlock/statement timeout rolls back with zero authority effects and requires a complete retry. Set LOCAL lock_timeout=250ms, statement_timeout=2000ms and idle_in_transaction_session_timeout=2000ms, with a host-enforced5second whole transaction deadline/connection cancellation. Every explicit window/order/money/Payment/Refund row lock is NOWAIT; unique/FK/trigger waits remain bounded by lock_timeout. SHARE permits another SHARE and preexisting ROW SHARE/FOR UPDATE, so same-window SELECT FOR UPDATE NOWAIT plus unique constraints serializes duplicate binders. Never UPDATE orders or call a helper that creates/updates money state inside this binding transaction; no table-lock upgrade is allowed. SQLSTATE55P03,40P01,57014 or deadline/connection failure rolls back before any whole-operation retry, and uncertain commit is reconciled by exact immutable identity. This briefly blocks unrelated order writers in this TEST database as well as the fixed pair. These conflict semantics follow [PostgreSQL17 explicit locking](https://www.postgresql.org/docs/17/explicit-locking.html); actual elapsed bounds require the PostgreSQL tests below. This is not a claim that flock serializes public buyers. A purchase after successful binding may exist, but cannot gain authority (unique window/order binding) and makes closing/deactivation final cardinality proof refuse. It cannot retroactively turn the registered original order into another one. Retain and reconcile all extra paid orders; no silent deletion or synthetic refund.

Validate order→purchased_version→SellerListingPublication→DeliveryAuthority→seller-owned CloudConnection, unique persisted PI/charge/Payment, publication listing/seller/version equality, authority_kind=workspace_connection, legacy_serial_id=null, matching connection owner and immutable approval/version/scope hashes. Reject agent/device-backed transactions and synthetic lookalikes. Validate retained immutable authority even if access/connection is revoked; no active credential requirement merely to refund. The accepted real capture/journal readiness must already hold; this service never manufactures capture. Insert one exact canonical registration row and change window open→bound in one commit. Identical retry returns existing original bytes only after comparing all persisted bindings; conflict refuses. Existing bound row recovery does not produce a second authority. No provider mutation happens during registration.

The operator must obtain the successful committed bind-purchase registration receipt before issuing any normal TEST refund request. With available schema, an unregistered W refund is not admitted and the current synthetic seam acknowledges it as completed/e2e_scope_suppressed; this is terminal suppression, not retryable authority readiness failure. Explicitly test a pre-registration signed charge.refunded event and preserve its actual provider/event disposition as an incident rather than manufacture registration after the fact. Signed webhook exception calls admits_workspace_refund(event,db,*,normalized_stripe_account), called at the seam as admits_workspace_refund(event,db,normalized_stripe_account=stripe_account), a narrow imported disjunct at the authentic TEST suppression seam after real signature/scope/dedup validation. First restrict to the fixed W TEST actor/provider shape; unrelated synthetic or real production traffic must not depend on this optional W schema/mount. Require explicit TEST event/PI/charge mode, exact full amount/currency and deployment pins matching retained authority. A definitely unrelated/non-W event returns false without consulting optional W tables/mount. For a qualifying fixed-pair immutable Workspace charge or an existing claimed/registered W charge, missing/incompatible readiness, mismatched current runtime pins or uncertain authority lookup raises a stable WorkspaceTestAuthorityUnavailable exception inside the existing critical-event try/except. It must use the actual critical charge.refunded failure path: rollback, durable failed/retryable event and non2xx response, never completed/e2e_scope_suppressed or a false success. Once readiness is restored the same signed event ID converges normally. A fully available schema with no exact registration does not admit the order. Normal P/N/production routing remains unchanged; test all of these distinctions against the real ASGI endpoint. Locate the unique authority by provider IDs, lock the existing order/transaction/money-state/Payment/Refund rows under the accepted event→order chain, and independently compare all immutable authority/purchase/capture bindings and canonical hashes. Implement explicit existing-row SELECT FOR UPDATE NOWAIT in order→transaction→order_money_states→Payment→Refund(sorted actual IDs) order inside the new service. Do not invoke the creating lock_order_money_sync helper; a missing row refuses without db.add/INSERT or repair. Retain the existing event mutex before these locks and do not acquire a window-table lock in the predicate. This predicate performs no DML, no provider calls, no window table lock and does not require window state=open. A closed/recovery window retains authority only for its originally registered charge/order and normal exact event/refund convergence; it cannot bind a second order. Existing dedup and normal processor own event/refund identity immutability and financial effects. An old same-pair unregistered order, P/N, another window's replacement order, changed scope or mismatched pins never reaches W processing.

close-window first stops new binding by forward transition to closing; uncertain outcomes may retain recovery. It does not revoke an already registered refund's authority. Final closed rechecks under the same short orders SHARE NOWAIT and existing-money row-lock transaction, with no provider IO/order UPDATE; it requires unchanged complete baseline-plus-exact-registered-order cardinality, actual normal full refund and cannot-settle financial state, no pending/unknown provider result, exact canonical receipt hashes, and preserved P/N/device comparisons. A never-purchased window may close only when its complete delta is empty and no uncertain provider/custody step is hidden; opening/activation resource uncertainty remains independently recorded. Paid rows/history remain. Deactivate restores only owned readiness after the same terminal checks and retains custody/account/history as described below. No automatic timeout opens/closes a window. Workspace preservation incidents have two distinct dispositions. For an extra order after registration: stop new W purchases/binds/unregistered refund requests; retain exact immutable window/baseline/registration, complete current delta, provider IDs/outcomes, event status, protected before/after and custody lineage; keep nonzero recovery_required_extra_order and continue only the originally registered refund's normal compatible convergence; report evidence to the existing owner for a separately reviewed reconciliation/archival correction. For a provider refund before registration: signed event suppression is terminal and acknowledged, no registered authority exists and there is no compatible convergence path; retain the same evidence and nonzero recovery_required_unregistered_refund, stop new W purchases/binds/refund requests and escalate to the existing owner. Do not retry the suppressed event to manufacture registration, bind retroactively, clear the incident or close the window. Only the separately reviewed owner reconciliation/archival correction can permit closure. In both cases the one-nonclosed-window refusal remains until the reviewed correction is completed; no automatic closure escape, extra registration, new authority, row deletion or baseline rewrite exists. Distinct anomaly tests assert extra-order original registered convergence versus terminal unregistered suppression and un-closeable incident state, never false closed/success.

## Surgical reset preservation and fresh protected-state comparison

Both supported backend account cleanup services, SurgicalResetService.reset and TeardownService.teardown, unconditionally refuse exactly seller01=32f8e9c1-a779-459c-b49b-d6db464133b5, buyer01=7d662d0e-22f2-4b87-bbc5-80984ba808ac, buyer02=4b10c456-5936-49d7-be3f-d9c68c9a263e and seller02=961bd48a-3018-43f4-a7ad-6871720fd806. Add one pure shared helper is_protected_workspace_fixture(target_user_id) in app/e2e/workspace_fixture_protection.py with exactly these fixed UUID constants. Normalize a valid UUID before membership comparison; malformed UUID refuses through normal validation and never becomes an unprotected result. No caller list, configuration override, file/DB/provider lookup, all-synthetic blanket or role-only match is allowed. Tests bind the shared constants to the exact packaged identities. In SurgicalResetService the guard follows existing authentication/allowlist/request/apply-review validation and precedes _execute, _reset_database_rows and object operations. In TeardownService insert after build_guard_context and claims/target_email assignment, before validate_inventory, _collect_hitlist and _execute_deletes; the preceding catalog snapshot is read-only and need not be redesigned. Direct service calls as well as routes must receive the protection. Both dry-run and apply refuse with the exact existing exception message: ResetRefusal("workspace_protected_fixture") or TeardownRefusal("workspace_protected_fixture") with ordinary nonincident semantics. Existing str(exc), HTTP detail and audit reason are the carrier; do not invent a category attribute or new audit schema. Existing authentication/allowlist/review failures remain their normal failures. Normal sanitized refusal audit INSERT/commit is permitted; every protected refusal performs zero destructive DELETEs and zero object-deleter calls, not zero SQL or zero audit writes.

The canonical reset/account route returns HTTP400 with detail workspace_protected_fixture. The existing teardown/account route returns202 after authentication and starts its retained background task; that task catches TeardownRefusal and its existing durable status endpoint reports outcome=refusal, reason=workspace_protected_fixture and empty executed_counts. Preserve this actual interface;202 is request acceptance, not successful deletion. Do not add an immediate400 route rewrite, alter nonce replay protection or bypass the engine by testing only a route stub. Actual signed ASGI tests must drive the background task to terminal durable status.

The refusal does not depend on presence or readability of a W table, mount, owner pointer, audit, custody record or any window state. Thus these two supported backend account cleanup services cannot delete the four targeted identities/history before issuance, during issuance, after closing/no-purchase or when Workspace lineage evidence is missing or damaged. This is not a claim of protection against privileged arbitrary SQL, future unguarded routes or authorized Docker volume destruction; the separately guarded environment lifecycle remains distinct. Upgrade review must inventory new account cleanup entrypoints and require the shared guard regression. A reset that inspects no lineage and then races a new host pointer still refuses by target identity; no claim that a database advisory lock serializes a host file writer is needed. Remove the superseded reset/open pg_try_advisory_xact_lock and strict destructive fixed-party branch from the proposal: they are unnecessary because the four-target path cannot reach deletion at all. The existing _safe_scalar/_safe_execute rollback-and-continue behavior remains unchanged for genuinely unrelated targets; it is not used to implement a new protected destructive path. Stage A's short orders SHARE transaction and unique window constraints remain; no reset advisory lock is required for it.

The separately authorized environment bin/reset still follows its existing settlement guard and new pre-W lineage guard; no protection is weakened there. Accepted environment source606267/merge43d67264 invokes that clean lifecycle and has no surgical-reset endpoint dependency. Once W lineage exists, environment seed/reset/up refuse as already specified; any archival reset requires a separately reviewed preservation/reconciliation procedure. A later need to surgically reset one of the four fixed identities is outside this implementation, rather than an undocumented allow-if-no-row escape. This is a bounded conservative refusal on four known fixture accounts, not a generic reset redesign.

The owner evidence includes protected_before identity=backend-protected-state.json in its immutable operation directory. This is a canonical schema2,kind=protected_state receipt with operation_id,owner_window_id,records; records are sorted unique {collection,id,sha256}, with hashes of explicit PostgreSQL17 UTF-8 jsonb text preimages, excluding only the observation timestamp. New service capture_protected_state performs the same fixed read-only queries during opening, before binding and before terminal close/deactivation; compare complete record sets and exact hashes, never only counts or caller IDs. The owner obtains the initial expected record set from the same reviewed query definition when issuing evidence. Host historical file presence alone is insufficient for this fresh comparison.

Protected collections: (a) seller01,buyer01,buyer02 User identity/profile/readiness projection: id,email,is_test,role,status,first_name,last_name,company_name,display_name,totp_enabled,totp_verified_at,totp_secret_enc,backup_codes_hash and four Stripe fields; plaintext/encrypted values never leave the database, only row digests. Normal buyer login last_login_at is deliberately excluded; fixture code still may not modify it. (b) complete auth_user PartyIdentity rows for those three fixed users, preserving exact Party ownership. (c) all currently retained orders whose seller is seller01 and buyer buyer01/buyer02, with complete order, linked transaction, order_money_states, Payment, Refund, order_event and transaction_event rows; row digests cover complete jsonb row values and ordered collection membership, not a latest-row query. Historical absent N rows after approved cleanup are represented by the actual complete empty result and independently retained terminal archive, never recreated. (d) seller01 devices and trust_sessions with stable identity/state fields devices.id/device_id/user_id/is_active and trust_sessions.id/device_id/is_active; exclude routine heartbeat timestamps/counters. Existing owner device/install/serial receipts remain separate required evidence; this finite DB projection does not certify an uninspected whole graph. Queries are bounded to4096rows per collection plus sentinel refusal, and all emitted IDs/digests are nonsecret. Exact source table/column and FK mapping must be bound in implementation tests; no optional-table omission on SQL errors.

For open/bind/terminal comparisons, unconditional four-identity refusal excludes the supported backend reset path and orders SHARE excludes order mutations; obtain the relevant existing protected order/transaction/money rows in sorted accepted order, NOWAIT and bounded, before comparing. Protected user/identity/device reads are finite read-only snapshots; the host must compare again after each owned mutation and before final success. External administrative writers remain outside the private cooperating-operator boundary and any observed drift refuses; no claim of universal external-write exclusion. No provider IO occurs while the short opening/binding/terminal locks are held. A reset-before-open changed/missing protected collection therefore cannot become an accepted new baseline merely because the old owner manifest still hashes correctly.

Required actual signed-route and PostgreSQL tests in tests/test_e2e_surgical_reset.py: every protected UUID, canonical/alternate UUID representation, existing authentication failures, dry-run/apply, no record/pre-open issued pointer/active/closed/no-purchase lineage, missing W schema/mount and damaged lineage. Assert zero destructive SQL and zero object-deleter calls for all four. Use a synchronization barrier for reset-start-before-owner-publication and publication-before-reset; neither ordering may reach deletion. Verify unrelated allowlisted targets without W lineage preserve the existing reset behavior and existing token/review constraints. No protected strict-deletion branch, rollback-and-reacquire or lineage lookup is required. Tests use isolated DB/object doubles and no live fixtures. Add teardown tests in tests/test_e2e_teardown_t2.py and tests/test_e2e_teardown_t2_real_pg.py for allfour protected UUIDs and alternate valid formats, dry-run/apply with valid allowlist/token, preserved users and protected history, exact durable refusal reason and empty executed_counts. Instrument actual SQL DELETE effects and object calls; assert the protected early refusal never calls inventory/hitlist/delete work, while allowing preceding ordinary guard/catalog reads and refusal audit writes. Test both direct TeardownService and actual signed ASGI202/background/status flow through terminal audit, not just stubs or initial response. Missing W schema/mount/lineage, preopen/active/closed and host-pointer publication interleavings cannot alter the fixed-target refusal. Unrelated valid synthetic targets keep existing teardown semantics, and invalid UUID/token/allowlist/synthetic/live-linkage failures retain existing safeguards. Use isolated real PostgreSQL and owned fixtures only; no live reset or teardown. Existing nonce/replay/concurrent-task and rollback/audit regressions remain required.

## Canonical receipts and fixed registration envelope

Every authority receipt uses {receipt,sha256}; hash only the receipt's canonical bytes: recursively sorted ASCII keys, compact separators, ASCII strings, safe integers with absolute value<=9007199254740991 (booleans not integers), booleans/null, arrays/objects only, no trailing newline. Reuse accepted Mars canonical_digest and actual JavaScript/Python golden preimages, including numeric-looking keys; JavaScript must emit sorted key/value pairs directly rather than rely on JSON.stringify object enumeration. Canonical envelope bytes themselves are also retained exactly. Source SHA40 and content SHA25664 are distinct types.

Registration receipt exact keys: schema_version2,kind=paid_registration,namespace=workspace_s1712,operation_id,owner_window_id,role=W,pins,owner_authorization_sha256,opening_sha256,baseline_sha256,protected_evidence_sha256,seller_id,buyer_id,order_id,transaction_id,listing_id,purchased_version_id,publication_id,delivery_authority_id,connection_id,immutable_scope_hash,approval_id,approval_review_hash,listing_manifest_hash,payment_id,payment_intent_id,charge_id,capture_amount_cents,currency=usd,protocol_version=1,observed_at. UUIDs/provider IDs and content hashes are obtained from actual persisted canonical fields; immutable_scope_hash is DeliveryAuthority.immutable_scope_hash; approval_id is SellerListingPublication.approval_id joining SellerListingApproval.id; approval_review_hash is SellerListingApproval.review_hash and must equal SellerListingPublication.review_hash; listing_manifest_hash is ListingVersion.manifest_hash for Order.purchased_version_id. A registration PostgreSQL test independently changes AgentProposal.manifest_hash and ListingVersion.manifest_hash and proves only the ListingVersion joined through Order.purchased_version_id supplies this binding; no ambiguous model-column shortcut. Also require approval seller/connection equal authority seller/connection and publication.authority_id/listing_version_id/listing_id equal the same joined records. Never invent a replacement hash or substitute request_hash for review_hash. Validate the accepted hash shape, preserving its actual canonical value. Terminal receipt is a distinct kind=window_terminal with schema_version2,namespace,operation_id,owner_window_id,registration_sha256(or null for proved no-purchase),refund_id/refund_event_id(or both null for no-purchase),financial_evidence_sha256,protected_after_sha256,disposition(refunded|no_purchase),observed_at. It is produced only from observed canonical outcomes. No preparation/opening receipt can masquerade as paid registration or successful release.

## Fixed receiver nested schema

Schema version2 is a new unimplemented contract; no issued version1 runtime record is silently upgraded. Top-level keys exactly schema_version,operation_id,action,pins and the action-specific activation or restoration only. action is open-window,activate,bind-purchase,inspect,close-window,deactivate. Only activate has activation; only deactivate has restoration; the other actions have neither. Runtime pins have exactly the eight keys above, not an undefined owner-window hash. Operation/window identity comes from the fixed mounted owner artifact and its persisted row. Reject wrong keys/scalars, noncanonical UUIDs, wrong hash length and boolean-as-integer before writes. Output cap1MiB accommodates complete opening baseline; overflow refuses without truncation and committed output is recoverable from inspect.

Exact custody/marker encoding: totp_secret is uppercase canonical RFC4648 base32 without padding, exactly32ASCII characters decoding20bytes; decoding then canonical re-encoding must equal the input. custody_sha256 is SHA256 of those32ASCII bytes, not decoded bytes, JSON quoting, lowercase text or a newline. For a test-only golden input AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA, the digest is 22a48051594c1949deed7040850c1f0f8764537f5191be56732d16a54c1d8153; this is synthetic test data, never a live credential. Scope/name/UUID/version/generation remain separately compared and are not smuggled into that digest.

The nonsecret Infisical create marker is secretComment exactly workspace_s1712:totp:<seller02UUID>:operation:<originalOperationUUID>:generation:<custodyGenerationUUID>, all literal ASCII punctuation/prefixes and canonical lowercase UUIDs. No alternate metadata marker format is accepted. connect_marker is exactly workspace_s1712:connect:<seller02UUID>:operation:<originalProviderOperationUUID>, stored as the value of Stripe account metadata key workspace_s1712_marker. Original provider operation means the durable first-creation operation, retained unchanged for later-window reuse. The account-create idempotency key is exactly workspace-s1712-connect-<originalProviderOperationUUID>; provider_request_sha256 hashes the accepted canonical full creation payload including this marker and the durably fixed ToS timestamp. Host/backend golden tests use fixed synthetic operation/generation UUIDs and verify exact bytes, not just matching prefixes; mutate each encoding/marker/name/scope/ID/version independently and require refusal. No real account or secret identity is generated to repair missing evidence.

activation keys exactly totp_secret(base32 ASCII32characters decoding20bytes),custody_id(UUID),custody_version(integer>=1),custody_sha256(SHA256),custody_generation_id(UUID),connect_account_id(valid acct_ ID),connect_marker(exact derivation from retained provider operation and fixed seller02),provider_request_sha256(SHA256). A reuse refers to the original immutable custody/provider generation, not the new window operation. Receiver validates digest and actual fixed-party/account ownership and uses existing TOTPService encryption. It returns only ciphertext SHA256, owned Connect PartyIdentity UUID and allowlisted readiness. No plaintext response. Host custody proof is trusted private-operator input; it is not falsely described as independent backend Infisical authentication.

restoration keys exactly before_state,expected_state,owned_connect_identity_id(UUID),custody_id,custody_version,custody_sha256,custody_generation_id. before_state has totp_enabled(boolean),totp_verified_at(null),totp_secret_enc(null),backup_codes_hash(null),stripe_account_id(null),stripe_onboarding_complete(string not_started/pending/complete),stripe_payouts_enabled(string false/true),stripe_details_submitted(string false/true). Activation requires false/not_started/false/false and the empty credential/account baseline. expected_state has the same nonsecret fields, except totp_verified_at is the actual nonnull activated UTC RFC3339 timestamp, totp_secret_enc is replaced by totp_ciphertext_sha256 and backup_codes_hash by backup_codes_sha256=null. The activation transaction invokes only TOTPService._encrypt_secret, sets totp_enabled=true, totp_verified_at to the transaction database UTC time, leaves backup_codes_hash=null and sets the four reviewed Stripe projection fields to the authenticated account readiness values. It never calls committing verify_setup, setup_2fa or backup generation. Return the actual stored timestamp/ciphertext discriminator after flush, and restore the exact baseline false/null values in the owned restoration transaction. c3 User has no mapped updated_at field; do not assign or fabricate it. created_at,last_login_at,profile and authentication identity fields are not fixture mutation targets. Qualification inspects actual database triggers; an additional implicit changed field requires an explicit reviewed before/expected extension rather than silent loss. Compare exact current state before restoring; no arbitrary patch map, SQL, user ID, DB URL, secret name or account target. Any additional modified source field must be explicitly reviewed in before/expected schema before build acceptance.

In e2e/account_pool.json add seller02 totp_secret_name exactly E2E_SYNTHETIC_SELLER_02_TOTP; preserve every other packaged identity. Validate exact unique seller02 and buyer01 UUIDs and secret-name fields. Missing, renamed, foreign or duplicate bindings fail before mutation; no inline secret value. Do not recreate the historical three-fixture baseline: accepted F1 has protected buyer02 and four actually seeded fixtures.

## Custody protocol and verified API limits

The established self-hosted authority is https://secrets.ai.market, backend project bd272d48-c5a1-4b52-9d24-12066ae4403c, environment test-env, path /, shared type, fixed name E2E_SYNTHETIC_SELLER_02_TOTP. Current environment source supplies test-env; older generic local-secops.md allows only prod/staging and is not an executable adapter for this fixture. Do not change or use its broad upsert/argv transport to work around this difference. The fixed fixture host adapter must use the already-reviewed private HTTP transport to the exact test-env scope with no redirects and bounded responses.

The published Infisical v3 raw API supports create/read and name-addressed DELETE but no documented atomic expected-version/UUID DELETE. This design performs no DELETE, PATCH or upsert, including recovery. A local lock does not exclude external writers. Retain the exact fixed-name TOTP in test-env after database restoration, with explicit inactive_with_retained_custody state. A successful deactivation means owned readiness restored, financial/resource disposition proved and custody retained; it never means the secret is absent.

First-ever custody creation requires authenticated404 at the fixed scope, no prior retained generation and no uncertain create. Generate20random bytes only in private process memory. Before POST durably record generation UUID, original operation UUID, exact fixed name, value digest and nonsecret generation marker. Create once; conflict/uncertain response reconciles exact GET scope/name/marker/digest/ID/version. Mismatch refuses, never overwrites. If an unposted generation lost its plaintext and authenticated absence is established, record it abandoned before a new generation; an uncertain POST requires exact reconciliation and cannot use absence alone to erase that uncertainty. Timeout/403/empty parse never means absence. Account creation has its separate stricter no-recreate rule.

After creation record actual immutable ID/version/generation marker/digest, re-read exact custody immediately before private stdin activation and again after projection before declaring active. A detected external change means recovery_required; retain all evidence and restore only exact owned database projection when safe. Concurrent provider writers remain possible: no claim that those reads provide exclusion. Since no delete/upsert/patch occurs, an unseen post-read writer cannot have its credential erased by cleanup. Future activation reuses the same retained generation only when original custody ledger and current exact UUID/version/digest/marker/scope/name agree and no prior operation is unresolved. Do not require or create a fresh secret per window. A deleted/recreated, missing or changed retained item refuses and preserves original lineage; do not silently enroll a new generation to repair drift.

Cleanup always leaves custody untouched. After exact DB restoration and normal financial terminal proof, matching retained custody yields inactive_with_retained_custody; missing/changed/unverifiable custody yields deactivation_incomplete with DB-restored status separately recorded. A provider GET denial does not undo the completed restoration or make a false full-success receipt. Interrupted retry continues remaining verification only, never re-enrolls or restores active just to match the audit. Tests prove zero DELETE/PATCH/upsert calls across success, failure, observed drift and an external write after the last GET, plus create-response loss and authenticated404 versus403.

Sources inspected2026-09-12: [Infisical create](https://infisical.com/docs/api-reference/endpoints/deprecated/secrets/create), [Infisical delete](https://infisical.com/docs/api-reference/endpoints/deprecated/secrets/delete), plus repository seed/host-seed.py and established infisical-secrets.md/local-secops.md. These document/API contracts are not proof of the installed provider response shape or write permission; read-only metadata validation and actual owned-operation receipts remain required.

## Durable audit and crash boundaries

Use private `.state/workspace-fixture.json`, schema_version2, mode0600 and a private parent directory. Reject symlinks, malformed/unknown records and mismatched ownership. Write a same-directory temporary file, flush/fsync, atomically replace and fsync its directory. Persist only under the inherited successful guard. Do not rewrite a corrupt record to unblock a run.

Required audit groups: operation UUID, phase, exact pins, owner-window reference, fixed fixture IDs, protected before-state hashes, enumerated restorable nonsecret seller02 state, secret ownership UUID/version/digest, stable provider marker/request identity and payload digest, exact owned Connect identity row, W resource/order references, last completed step and sanitized failure category. Values of credentials and provider response bodies are excluded. `preparing`, `active`, `deactivating`, `recovery_required`, `inactive_with_retained_custody`, `deactivation_incomplete` are the only audit phases. Archive every completed operation under .state/workspace-windows/<windowUUID>/; the active pointer cannot overwrite retained custody lineage or prior operation receipts. A new operation is allowed only after a closed prior window, fully restored readiness and exact retained-custody/provider reconciliation. A step records a durably completed operation, not an intention misrepresented as success.

First activation requires empty seller02 credential/Connect baseline and exactly one expected active auth_user PartyIdentity. It must not call `ensure_local_users()` or `establish_parties_and_assertions()`. Those broad seed helpers reset or assert unrelated fixture state. Capture all readiness fields actually changed, including timestamps: totp_enabled, totp_verified_at, totp_secret_enc (known null baseline), backup_codes_hash (known null baseline), stripe_account_id, stripe_onboarding_complete, stripe_payouts_enabled, stripe_details_submitted. Preserve Party, auth_user identity, company/profile fields and all seller01/buyer01 protected state.

Persist preparing and a stable provider creation payload, ToS timestamp and idempotency identity before provider creation. Enumerate the complete account result set for the exact fixture marker. Zero matches permits creation only when the durable record proves no uncertain previous create; one owned match permits validated reuse; multiple matches refuse. An interrupted or expired idempotency key never authorizes a fresh account. Reconcile the original owned account, retaining uncertainty when provider evidence is insufficient.

Accounts v1 has no documented livemode field. Bind TEST provenance to the exact approved test credential/version and authenticated account retrieval; reject explicit true when returned. Verify Custom type, exact marker, distinctness from seller01/platform, transfers capability and payout readiness independently. Do not repair a preexisting incomplete account by assumption. Events/charge/payment objects must explicitly assert TEST mode where supported by their schema.

Before Connect projection, validate and lock the exact Party/auth_user binding and prove the account has no foreign Connect identity. Existing upsert can reassign party ownership and must never be called before this proof. Projection and owned identity creation belong to one reviewed database transaction. The host records the returned owned row and ciphertext discriminator. If the database commit succeeds but host receipt persistence fails, resume by inspecting exact expected state; never enroll a new secret or move an account to make the audit fit.

Credential create/retrieve/retention recovery must bind the provider's actual UUID/version semantics in the final implementation. An uncertain create response must be reconciled against the same named operation and expected value digest. Only an exactly proved retained generation may be reused; never overwrite a preexisting or changed credential. Retain a recovery-required state when custody cannot prove ownership; a timeout does not prove absence.

## Deactivation and support procedure

1. Read status, operation identity, pins and the precise failed step. Retrieve the named evidence and compare current state. Missing audit plus gained readiness is drift, not a fresh activation opportunity.
2. Require the same persisted operation/window (including closed-window restoration recovery), successful shared guard and identical accepted runtime identities before a mutating retry. A hold deadline alone does not open the window.
3. Verify every W resource/access grant has the reviewed cleanup disposition and every new W order has a supported terminal refund/release state that cannot settle. Uncertain payout dispatch, provider refund or financial aggregate preserves all recovery evidence and refuses destructive cleanup.
4. Enter deactivating durably. Under the accepted database lock order, compare current seller02 and owned Connect identity to the audit. Restore only enumerated captured fields and remove only the exact newly owned Connect row. Do not call a broad identity removal helper unless exact-single-row ownership is proven in that transaction. Never delete Party/auth_user identity.
5. Never delete, patch or upsert TOTP custody. Verify and retain the original generation; matching custody permits inactive_with_retained_custody, while drift/uncertainty yields deactivation_incomplete with exact restored steps recorded. Retry does not re-enroll.
6. Retain TEST provider accounts with financial history and record their explicit reuse/disposition. Record inactive_with_retained_custody only after all required comparisons, closed-window evidence and owned readiness restoration pass. Preserve the audit and receipts for later diagnosis.

Default bin/seed, bin/reset and bin/up must refuse any present Workspace authority/audit/custody lineage, including inactive_with_retained_custody (an empty prepared directory alone is not an authority record), under their existing lock before secret/provider writes, S3 upload/cleanup, source replacement or container destruction. This conservative guard preserves the durable DB/file evidence required for future reuse; no automatic purge/reset is in scope. A later reviewed archival reset procedure is required before destructive reuse of this environment. Ordinary Workspace reactivation can reuse retained resources without invoking seed/reset/up. Placing the check only in seed.py is too late. Status must not silently reconcile, repair or normalize drift.

## Concrete revised implementation scope

Environment targets: bin/workspace-fixture(new),seed/workspace-host.py(new),seed/seed.py,bin/reset,bin/seed,bin/up,compose.yaml,tests/test-workspace-fixture.py(new),tests/test-seed-contract.py,workspace/README.md. bin/up and compose.yaml are explicit additions for the private read-only authority directory and early preservation guard; they do not repin/rebuild protected run25. No generic provider adapter/framework or modifications to the accepted money proof are implied.

Backend targets: e2e/account_pool.json,app/e2e/workspace_fixture_protection.py(new pure shared fixed-UUID helper),app/e2e/surgical_reset.py(exact four-identity unconditional refusal),app/e2e/teardown.py(same refusal before inventory/hitlist),app/api/v1/endpoints/webhooks.py(narrow imported W disjunct),app/services/workspace_refund_test_admission.py(new),app/models/workspace_test_authority.py(new),app/models/__init__.py(explicit model import),alembic/versions/20260912_002_workspace_test_authority.py(new),tests/test_workspace_test_refund_admission.py(new),tests/test_workspace_test_authority.py(new),tests/test_e2e_surgical_reset.py,tests/test_e2e_teardown_t2.py,tests/test_e2e_teardown_t2_real_pg.py. New service/readiness/model/migration/concurrency tests are the concrete reviewed extension replacing the previous no-table option. Keep normal financial processor and S1681 admission/table/CLI untouched. MP may not invent additional schema or silently alter shared locks beyond this stated design.

Documentation targets carried/dispositioned from Gate1: specs/BQ-MONEY-PATH-TEST-ENV-S1656-GATE1.md records the required Workspace preservation/mount change without changing the genuine money phase contract; specs/SELLER-WORKSPACE-ACTIVATION-S1712-GATE1.md retains historical acceptance and appends this superseding implementation decision; runbooks/seller-workspace-operator-guide.md documents exact commands, artifacts, failures, retained custody and upgrade/recovery; runbooks/seller-workspace-live-release.md documents ordered runtime gates and evidence. The active operator guide's stale environment-review-open sentence is corrected with this substantive guide update. Historical authority/hash nits already corrected at0ec6 are retained rather than reintroduced. This Gate2 specification is the governing design appendix. Generated INDEX changes are limited to normal index output.

Mars owns overlapping F1 environment work and runtime repin/transition. Start from accepted environment43d67264 and backendc3, preserve all P/N seed/test changes and buyer02, and serialize shared webhooks.py integration. No copying old c6b fixtures over F1. Runtime installation remains separate after accepted build/Gate3 and actual owner transition.

Revised review request embeds exact repo/path/base/candidate/spec blob+SHA256, full revised text, all three Gate1 responses, all three rejected-round responses with this explicit disagreement disposition, accepted product/environment/authority records and actual final CI run tuples. Request full review of both new two-stage authority and retained lifecycle, not only a delta or prior-vote confirmation. No MP dispatch before all three revised reviews accept the exact candidate and scoped clarifications.

## Required implementation verification

Use isolated evidence, audit and lock roots; no test may fall back to live S1681 defaults. Test the actual wrapper with guard exits1/2, foreign lock, wrong token/ancestor and invalid pins and assert zero provider, credential and DB writes. Verify status creates no files. Exercise malformed/oversized/duplicate-key/trailing stdin and secret canary transport through the actual child invocation.

Use meaningful PostgreSQL tests for unique packaged fixture identity, foreign/duplicate Connect binding refusal, changed-secret/ciphertext refusal, locked before-state equality, first activation, repeat activation and interrupted commit/receipt recovery. Inject failure before and after each durable step. Provider stubs must model complete pagination, duplicate marked accounts, uncertain creation and aged-out idempotency without a new resource. Do not claim mocked capability success is real provider proof.

The narrow webhook allowance reuses existing signature, scope and idempotency admission and the accepted Mars processor. Test invalid signature, production shape, live mode, wrong/missing actor, device/agent binding, partial/zero/noninteger/mismatched amount, currency mismatch, ambiguous finance authority and missing/incompatible readiness. Verify intended rejection causes, not zero Transfer calls caused by unrelated missing prerequisites. Duplicate and failure/retry cases must converge to one normal effect.

Require direct settle() and actual scheduled caller regression through order_money_service.order_payout_eligibility and dispatch_order_payout for a still-confirmed transaction whose linked order is refunded/revoked; verify those actual order exclusions were read, not merely a stub in settlement_service.py. Use the accepted normal refund flow for transaction/payment/order/event/journal consistency. Never invoke a private handler or write financial statuses from seed to manufacture proof.


### Required acceptance cases for this revised authority

tests/test_workspace_test_authority.py must exercise actual PostgreSQL17 full-ancestry and repeat migration, exact constraints/indexes/triggers/readiness and wrong/disabled-trigger refusal; direct SQL UPDATE/DELETE of immutable opening/baseline/registration; one-nonclosed-window uniqueness; all invalid lifecycle transitions; genuine table/row lock waits and rollback. Use two real database sessions with synchronization barriers for writer already active, writer arriving during SHARE, preexisting SELECT FOR UPDATE, two same-window binders, timeout/deadlock recovery and an extra order after bind. Verify zero partial authority/financial writes on refusal and original registered refund recovery after the final proof is invalidated by that extra order. Tests must measure the actual bounds rather than sleep and assume ordering.

tests/test_workspace_test_refund_admission.py must prove old same-pair order, wrong/unissued/closed-for-new-binding window, substituted host baseline, changed canonical bytes/hash, wrong actor/pin/source/local-image/label/version/spec, P/N crossover and changed approval/publication/scope cannot gain admission. Include exact repeated registration, commit-success/host-receipt-loss, missing money state without auto-creation, revoked connection with intact registered authority, and real signed same-ID convergence after closing/recovery. Independently mutate local image ID and an OCI index with matching source labels to prove neither can substitute. Distinguish source/static schema tests from actual public signature/processor integration. Add real ASGI unchanged-status/response regressions for S1656 payin/purchase, S1681 negative/failed_run, unrelated synthetic refunds and real production-shaped traffic; assert zero optional W schema/mount lookups or W exceptions before the cheap TEST/event/actor rejection. Predicates are computed before the final synthetic filter, so that filter alone is not W isolation.

tests/test-workspace-fixture.py must exercise all actual command entrypoints, private read-only mount presence/absence/permissions, canonical operation-directory selection and traversal refusal, missing/forged/unverified owner evidence, guard exits1/2, retained-custody reuse/drift, all audit phases and restart/seed/reset/up preservation before any destructive call. Drive actual secret-canary stdin, fixed PYTHONPATH exception, package TOTP binding and response caps. Prove no DELETE/PATCH/upsert even when a separate provider stub writes after final GET. Tests never touch live S1681 directories, containers or provider credentials.

## Maintainer diagnosis map

These categories are the proposed stable wrapper/receiver errors; final implementation must bind exact exit codes and tests. Mutating commands return nonzero on every unresolved category, and no failure text includes input/provider body values. Record the category and the last durably completed step in the private audit only after guard success.

| Symptom/category | Evidence to read | Recovery action | Prohibited shortcut |
|---|---|---|---|
| owner_window_closed / guard_refused | Existing settlement guard result, Mars window receipt, protected run state | Continue read-only preparation; owner performs supported phase transition before same command retry | Editing the hold timestamp, ignoring exit2 or creating a second lock |
| runtime_pin_mismatch | Actual container local image ID/source label, versions.env and both accepted spec pins | Owner rebuilds/redeploys exact reviewed combination and verifies source/image equality | Hot source mount, trusting checkout HEAD or caller-supplied digest |
| protocol_unavailable | Accepted readiness exception category, Alembic ancestry, actual constraints and merchant chart | Diagnose the missing exact schema/chart requirement; apply reviewed product migration/cutover | Flipping a flag, manufacturing Payment or seeding finance from Workspace |
| audit_missing_with_readiness / audit_invalid | Read-only seller02 state and private audit file type/schema/hash | Preserve evidence; reconstruct only from real owned-operation receipts through reviewed recovery | Resetting seller02 or overwriting audit to make a fresh activation |
| custody_create_uncertain | Fixed-name GET status, operation marker, expected digest, actual UUID/version | Reconcile exact same generation; distinguish authenticated404 from denied/unavailable | Upsert, copying seller01 secret, or exposing value in a support ticket |
| custody_changed | Current UUID/version/digest/marker versus audit | Leave custody untouched, record exact restored DB steps and deactivation_incomplete; owner investigates external change | Name-only delete or pretending GET+DELETE is atomic compare-and-delete |
| provider_create_uncertain | Stable request/idempotency identity and complete marker search | Reconcile original owned account; retain uncertainty if no unique supported result | New account after idempotency expiry or first-match pagination |
| provider_not_ready / foreign_connect_binding | Authenticated TEST account fields and exact PartyIdentity ownership | Refuse projection; diagnose actual ownership/capability cause | Borrowing seller01/platform account or upsert that reassigns a party |
| db_commit_receipt_uncertain | Exact locked seller02/Connect state versus expected result and prior audit step | Inspect/recover the committed step, persist receipt, resume same operation | Creating another secret/account or repeating broad seed |
| w_cardinality_mismatch | Persisted backend opening baseline and complete current fixed-pair order delta under short table SHARE lock | Use Workspace preservation incident procedure; retain nonzero recovery_required_extra_order, original refund convergence and owner reconciliation/archival correction | Selecting the latest order or silently excluding an extra paid order |
| refund_pending / payout_unknown | Durable stripe_events state, order_money_states, real provider IDs and normal processor logs | Follow accepted normal retry/reconciliation; preserve financial and fixture evidence | Fabricating event, deleting pending authority, new Transfer key or treating timeout as absence |
| restore_state_mismatch | Locked current versus exact recorded owned readiness/ciphertext/identity | Preserve differences for support; apply only reviewed exact recovery | Broad identity delete, Party removal or overwriting unrelated fields |

Support bundle: include operation/window IDs; exact source/image/spec/version manifest; phase and last completed step; sanitized category; guard result; nonsecret custody/provider object discriminators; W receipt; protected before/after snapshot hashes; canonical product event/Payment/Refund/journal/order/transaction IDs; test/review/merge/deployment receipts. Do not include bearer download URLs, TOTP/passwords, raw Stripe/Infisical responses, database URLs, provider credentials or source data. A support engineer should be able to map every object to its owned operation and distinguish provider outcome from application commit.

Upgrade qualification is independent of an ordinary retry. Test loading all supported audit phases from the previous version, including provider response loss and DB-commit/host-receipt loss; unknown versions refuse without rewriting. Verify an unchanged operation cannot acquire a new resource or credential generation by upgrade. Validate public command arguments, byte cap and nested schema against archived golden inputs. Recheck backend encryption coupling, packaged account identities, provider API/SDK shape and exact normal refund admission/lock/readiness interfaces. Re-run real subprocess secret-canary and isolated PostgreSQL integration tests. Retain the prior compatible binary and manifest, but never use an old payment binary against newly written authority unless the accepted cutover explicitly proves compatibility.

## Release and upgrade sequence

Production stop/reconciliation/restore preparation is retained in SELLER-PRODUCTION-QUIESCENCE-RUNBOOK-S1712.md with current Railway inventory and authenticated API-schema receipts. Existing flags cannot quiesce old binaries. Current configuration does not establish a warm drain, and interrupted external payment outcomes require reconciliation. Incorporate the accepted exact cutover and source/image/adoption evidence before deployment; this read-only preflight did not stop or change production.

Preserve run25 environment03d3f30acf854537edd76fdb19e1cf521b577c9a and backendd766b8e4e66d7803ebe48f8a3132521abbebe957. Its genuine hold ends2026-09-13T23:41:11.491739Z, subject to Mars's supported lifecycle. Corrected money phases retain exactly P/N and genuine48-hour holds. W starts in its separate evidence namespace only after required phases and an explicit owner transition. Snapshot settled P and installation/identity state before W; compare after exact W purchase/refund/cleanup.

After accepted implementation, bind reviews, test results, source/image/spec identities and deployment receipts in one manifest. Refresh actual production identity rather than relying on September11 observations. Run the normal public product paths, buyer bytes/hash and entitlement denials; record outside verification. A seed fixture, test account, health check or unanimous design review does not prove full AWS/R2 production release.

For upgrades, inspect schema/custody/audit compatibility before replacing the helper. Refuse unknown audit versions instead of silently migrating active state. Review and test any migration separately on a copied isolated audit/database. Do not deploy old refund code over new financial states or downgrade purchased-rights schemas blindly. Rollback first closes affected new admission, preserves existing paid rights and uncertain operations, then uses the reviewed compatible source/image/configuration combination and reconciliation procedure.

## Verified historical source-hash recipe

At backendd766b8e4, read exact git object bytes for webhooks.py, split with keepends=True and join indices1699:1807. This includes the terminating newline of lines1700–1807 and produces SHA2569c29e6e300116aa5e2cdba8f8b0fd61cb3666450b6019a4980034b81cecee567. Removing that final newline produces881bd14b4d29e27a72b6b1a24c5f00f017dd881179514745e02ffd4238475a69. Earlier wording saying no trailing newline was incorrect. See SELLER-WORKSPACE-REFUND-HASH-METHOD-S1712.json. This is historical baseline verification, not a hash for Mars's future product implementation.

Installed custody read-only shape verification: SELLER-CUSTODY-EXISTING-SHAPE-S1712.json confirms an existing seller01 TOTP metadata response200, canonical UUID, integer version1, test-env and comment/metadata fields; no value retained and no mutation. SELLER-FIXTURE-CUSTODY-METADATA-S1712.json records authenticated lookups returning404 for buyer02 email/password and seller02 TOTP. This does not prove create behavior; DELETE is excluded by this revision.

## Historical MP235 dependency increment and F2

This paragraph retains the earlier diagnostic checkpoint only. Its pending implementation language is superseded by the accepted c3 product and environment dependencies at the start of this specification.

MP235 produced incomplete candidate88d6cdc7019cf2d7c99345fc7e7f7ef58bcdeb6a: money/refund services exist, but writer/dispatch/webhook integration and complete tests remain open. Mars4163 continues MP task2dfe810cb018, TS236, from that head with accepted F1. The installed journal entry trigger currently discards draft posting updates by returning OLD; F2 is unanimously accepted with its evidence mandate fulfilled at8b4a315ddfc5325959190ca6ae1b5e07fc915b35 (PR166 section15), preserving posted immutability while returning NEW for allowed UPDATE and OLD for DELETE. Vulcan independently confirmed production definition through BEGIN READ ONLY and tested exact old/proposed bodies in rolled-back session-temporary PostgreSQL objects. Require MP implementation of accepted F2, exact metadata/body/sole-attachment recognition, all preserved guards enabled O, real full-ancestry/repeat migration/readiness and posted-journal tests in the final product base. Reinstalling the defective old body is not an authorized rollback. The explicitly identified production receipt at03:50:26UTC confirms the installed old function; the separate03:51:37UTC aggregate found zero current journal entries, without establishing historical accounting completeness. This diagnostic proof is not implementation approval. The precise assessment and receipts are in outputs/SELLER-JOURNAL-TRIGGER-ASSESSMENT-S1712.md.


## Complete R3 panel disposition

R3 candidate87bc6d09b6551b2d0b0c381ffa8c78a4ba988658 was not implementation accepted. CC APPROVE_WITH_NITS; DeepSeek and GLM APPROVE_WITH_MANDATES with blocking MEDIUM findings. DS M1 and GLM M1 source-confirmed sibling teardown recursive deletion are corrected by the same shared pure UUID guard at both actual service entrypoints, with explicit targets/tests. GLM M1 also requires explicit synthetic public checkout arming: selected existing flag true and literal empty destructive allowlists only in the final reviewed TEST Compose/runtime manifest, independently checked at host/application boundaries and covered by actual fresh-start ASGI tests. A new narrower checkout flag is advisory and not selected; no unreviewed production flag change is implied.

DS LOW terminal unregistered-refund recovery is separated from registered extra-order convergence. DS NIT exact refusal carrier uses existing exception text/audit; the next review request includes runnable diff and exact-head actual CI tuples. GLM NIT all owner/runtime preimage/hash columns are explicitly immutable except the enumerated lifecycle fields. CC LOW unchanged ASGI non-W isolation remains mandatory; CC NIT signature now matches the S1681 event,db,normalized_stripe_account shape and ListingVersion binding has an explicit regression. CC operator-visible permanent refusal is documented for both account services, with separately reviewed archival reconciliation required for any future change.

Do not adopt CC's advisory guard-before-auth ordering: preserve ordinary authorization failure behavior and guard before destructive work after current validation. CC's claim of all reset vectors closed missed the actual sibling service; retain its full response unchanged but do not count its approval to override the two concrete MEDIUM findings. CC's suggestion that row triggers block Docker volume destruction is not technically supported; the environment clean lifecycle removes the volume, bypassing row triggers. No generic DB-trigger backstop is selected: it adds a broader table-protection policy beyond the concrete supported-path correction. No claim of immunity against arbitrary privileged SQL is made. GLM's advisory removing the W disjunct as rollback is not accepted while registered refunds remain outstanding; retain compatible admission/convergence or a separately reviewed reconciliation. All twelve historical full responses remain evidence; no prior vote carries forward. This complete revised contract requires fresh full acceptance before MP implementation.
