## Current project authority — 12 September 2026

Max explicitly authorized continuation without CC. Owner event e4c7e041-4eee-4217-8be5-6591157274ca records this exception for the current S1714 ai.market production objective including Workspace only. Max further clarified: “If GLM and Deepseek agree that is approval.” Agreement by GLM and DeepSeek approving the exact candidate is sufficient approval; no additional CC or user approval is required. This supersedes older prospective CC requirements for this objective. CC is USER-WAIVED and unavailable, never approved. Existing CC findings, complete raw responses and terminal quota failure remain evidence. Historical three-voter receipts below are preserved as history; their CC requirement does not govern current acceptance. No prior vote carries. Sole Mars MP dispatch, full 1990+new testing and source freeze, environment integration, combined review, genuine phases, cutover and enabled AWS S3/Cloudflare R2 proof remain required within the cumulative USD100 authorization. No global Council policy is changed.

# Workspace implementation and recovery contract — S1712
## Current proposed successor — durable agent attempts and complete compatibility correction

This successor retains the complete e3b8b088 specification below and adds the final review dispositions and agent-attempt protocol. Its effective proposed scope is31 named backend/test targets: the prior27 plus tests/test_agent_tx.py, tests/test_bq_biz_agent_beta_m2.py, tests/test_refund_processing_protocol.py and tests/test_cleanup_stuck_agent_transactions_atomicity.py. The final compatibility correction below explicitly supersedes the two webhook call assertions and two impossible post-migration duplicate-PI fixture stages while preserving their business invariants. The final protocol expressly supersedes the earlier preserve-agent-external-call-lifecycle/no-new-pre-provider-lock instruction only for the durable prepare/one-shot provider/reconciliation sequence; it does not authorize unrelated money changes. D1 names the earlier scope clauses it supersedes, with the forged-N200 non-waiver intact. The final exact attempt schema/state/API/signed-event section resolves earlier prose shorthand; apply the complete protocol together. No old vote carries forward to this semantic successor. Backend817f remains partial/unqualified. Fresh full review, full implementation qualification, environment integration, combined review, genuine phases and enabled AWS S3/R2 release proof remain required. This is preparation, not accepted implementation authority.

## Effective successor scope and mode authority

The following31-file list is the complete proposed implementation scope. It replaces the prior27-target membership, not the original qualification or non-waiver obligations. The retained three modes checkout_completion/direct_payment_completion/signed_capture_recovery remain restricted as originally specified; the final agent_attempt_completion section adds exactly one fourth mode with its own durable-attempt/no-Session authority. Earlier statements that list only three modes,24targets or preserve the agent external-call lifecycle are historical and explicitly superseded only by these final named changes and the provider-status policy. No preserved historical sentence can authorize the old rollback-only agent mechanism or narrow the required complete implementation.

1. e2e/account_pool.json
2. app/e2e/workspace_fixture_protection.py
3. app/e2e/surgical_reset.py
4. app/e2e/teardown.py
5. app/api/v1/endpoints/webhooks.py
6. app/services/workspace_refund_test_admission.py
7. app/models/workspace_test_authority.py
8. app/models/__init__.py
9. alembic/versions/20260912_002_workspace_test_authority.py
10. tests/test_workspace_test_refund_admission.py
11. tests/test_workspace_test_authority.py
12. tests/test_e2e_surgical_reset.py
13. tests/test_e2e_teardown_t2.py
14. tests/test_e2e_teardown_t2_real_pg.py
15. tests/test_webhook_audit.py
16. tests/test_e2e_stripe_isolation.py
17. tests/test_webhooks.py
18. app/services/order_money_service.py
19. app/services/order_service.py
20. app/services/transaction_service.py
21. app/services/billing_service.py
22. tests/test_order_money_protocol.py
23. tests/test_refund_webhook_protocol.py
24. alembic/versions/20260912_003_canonical_payment_intent_binding.py
25. tests/integration/test_webhook_canonical_payment.py
26. tests/test_notification_events.py
27. tests/test_s1097_order_fulfillment_scoping.py
28. tests/test_agent_tx.py
29. tests/test_bq_biz_agent_beta_m2.py
30. tests/test_refund_processing_protocol.py
31. tests/test_cleanup_stuck_agent_transactions_atomicity.py

## Retained e3b8b088 contract (historical candidate status below)

## Current proposed successor status — canonical PI binding

This full successor retains every55690c7 obligation below. The final canonical-PI amendment explicitly supersedes prior17-target/no-normal-money-change limits only for its named24-target scope, new child migration, and PI producer/recovery rules. Backend base817f7331a07d07929a67198b28a82ed9b98baa56 is partial/unqualified. No old vote carries forward. Full fresh review and all original money/W/environment/provider release gates remain mandatory. Historical declarations elsewhere in the retained contract do not authorize implementation of this amendment.



Prepared September 12, 2026. This is a concrete implementation proposal, not a claim that these commands exist or have passed. Keep the Gate1 review checkout frozen at 1ff494acc79245e8481ece2e985eda902fbf5207. MP implements after the exact dependency and Gate2 acceptance are bound. The operator guide remains the published system runbook; this document supplies the next implementation-specific appendix.

## Authority and remaining integration inputs

Max's existing authorization, relayed by Mars4096 and event99af61fd-db46-42fd-a18d-1d0698e457d1, covers the required release corrections. The cumulative project cap is100USD. Current scoped work continues under that approval. The original handoff is valid until Mars returns Tuesday2026-09-15; Mars4383 has separately asked Max once for an extension through the later genuine holds and production verification. No response is yet recorded. This future boundary does not pause currently authorized work and must not be treated as an approved extension. Mars owns general refund/settlement, acceptance redesign and protected runtime transitions. Vulcan owns Workspace fixture activation, narrow TEST admission and final release. Product code is authored by MP and reviewed by CC/GLM/DeepSeek.

The accepted Mars payment design includes the original Gate2 decision `baac7653f93df83fd4ddf9c56e3fc2b68ac2351a`, F1/F2/F3, and consolidated F4 candidate `53753f127045d863256425b1365ae0b15b5a173c`, accepted by the full CC/GLM/DeepSeek panel with implementation clarifications at `af1de97030d090414fd7f09aafb5d005eeb9434a` (PR166 section17). F4 covers the actual refresh request, WebSocket callback, persistence, token issue/redemption, raw/reference/agent/MCP surfaces and listener refund races. Workspace retains its shared authorization regression obligation without widening first-delivery eligibility. VC alternate callback correlation and external raw-HMAC consumption remain explicit transport proof limits; neither is certified by this design.

Current verified product candidate is terminal MP244 `c3a62af8ccf60929a8e7cec3d62c9d30e0adefd2`, rooted at `d766b8e4e66d7803ebe48f8a3132521abbebe957`. Accepted consolidated scope is decision `26588d3571efc3a19d1a9b591fb51991690e3f62`, spec SHA256 `1e3c9783575db778301d0b7ec8a66f1dadca0b855ce59cf0cff9bdaf8cfd6806`, sections1–20.5. This supersedes MP241 as the dependency candidate. The final manifest `/var/tmp/s1714-readiness-6fc522aa/manifest.json`, SHA256 `e23c753c6da7451fcbd0d1c5beb1d6ae0dc8f098aab7881749d9d1d6f2410287`, reconciles42source/321migration/2protected hashes,52current and291retained artifacts,84requirement rows and732node references. There are1024actual executions,1010unique nodes,1004unique passes and six exact inherited failures; zero errors/skips. All prior1010nodes,142F7,20F6 and19original posting positives are retained. Core665pass/4baseline, adjacent284pass/2baseline, gold14pass/11intentional deselections and canonical55pass remain separately classified. The canonical temporary schema lacks refund_phase; migrated PG17 core supplies the separate new-refund evidence.

The named-database startup race is corrected by authenticated final TCP readiness with bounded probes and a total deadline. Actual delayed-bootstrap refusal/success, all three owned-container removals and hosted run34688843645 attempt1 at the exact candidate passed. Detailed diagnosis, callback/session support and upgrade procedures are published in runbooks PR183/184. Historical failed runs and retries remain preserved. Full product review covers all42targets against the root base and is unanimously APPROVE_WITH_NITS from CC/GLM/DeepSeek, with no HIGH/MEDIUM findings. Exact owner decision `/Users/max/koskadeux-state/s1656/s1714-product-gate3-c3a62af8-unanimous-decision.json` SHA256 `ed425ff091bebc41a879f3b4167a7816e0f23c47d94211a17426e5b5fd9b190f` and all three full responses were independently read and rehashed. The decision accepts product implementation only; the first Workspace Gate2 panel required revision; that panel and its disagreement are retained below.

API-cost's actual caller/chart produces independent progress rather than shared blocking. No chart or entity relationship may be manufactured to satisfy the old wording. All three product reviewers support disposition A and finite disposition B; their declared coverage limits and the independent evidence remain separate. The expanded delivery child-FK and order-first cleanup evidence is finite, not whole-graph certification. The unchanged unmapped BillingEntity.doppler_key_prefix field remains a real inherited defect: bounded source inspection found no dependency in the planned merchant readiness/posting/report paths, but does not certify dynamic consumers or deployed behavior. The same-class wallet loader remains outside this build, and no wallet-route certification is claimed. Bind final accepted product and environment identities before Workspace Gate2 review/build. The last retained production observation is d766; refresh actual production source/image identity at cutover. Actual scheduled Beat execution, old-writer quiescence, provider reconciliation and external transport proof remain release gates.

The accepted environment implementation is candidate `606267af80470b1d61c07d91cda30a0dc5200224`, root `c6b8770e69514035b2c78f0433f86209b4cefe0f`, merged by environment PR36 as `43d67264ab7a3a7e47d6cc6911737273ce1c94cc`. Candidate and merge have identical tree `e553476a38e0948d3a57da60872e406048a0d391`; the current remote main and GitHub merge object were independently verified. Full14-file CC/GLM/DeepSeek review is unanimously APPROVE_WITH_NITS with no HIGH/MEDIUM findings. Owner decision `/Users/max/koskadeux-state/s1656/s1714-environment-606267a-unanimous-decision.json` SHA256 `b3e7880bd6d574cd3ce9abba87990fa872017879b7ec8bf2e896158046ed6f2b`, all three full responses, manifest14 source hashes and six final logs were independently read/rehashed. Final135refund/110delivery/72seed/10journey/21nightly =348 contracts plus2 static placement tests pass. Four unaffected suite logs are retained; no claim that every reviewer reran every test.

The predecessor b006 fixed merchant UUID defect was reproduced against two fresh fully migrated PostgreSQL17.11 databases, which generated different valid IDs; both disposable resources were removed and independently confirmed absent. The final environment resolves the unique strict observed AIM_WY_LLC/US/USD merchant and binds both P/N descriptors, every financial row, all four archived journals, fresh cleanup and preserved settled-P proof to it. Strict boolean checks and guarded first-read v2 browser evidence are present; raw original/browser bytes retain their format. Runbooks PR186 documents diagnosis and recovery. W must resolve this same observed merchant through readiness; no fixture constant or synthetic journal may replace real financial authority.

Retain the accepted environment advisories: three canonical recovery files parse before later immutable write revalidation; the role archive lacks an independent revoke timestamp; one `/refresh` route has the exact documented generic refusal; duplicate acknowledgement uses logs while money nonduplication is independently checked; broad4xx and literal summary flags require their underlying corroborating evidence. The actual browser confirmation is `/transactions/{id}/confirm` → OrderService.confirm_order, not DeliveryService.confirm_delivery under `/deliveries`; the canonical path permits confirmation after download exhaustion while refusing independent money revocation/refund restrictions. Seller revoke records the same locked money-state restriction before commit. This is source evidence, not a live ordering proof. Preserve reviewer coverage limits and corrected route analysis, and do not turn advisory changes into unreviewed scope.

W inherits accepted environment merge43d67264 and productc3 interfaces, the authentic signed TEST seam, fixed P/N authority and protected run25. The final source/spec recording is runbooks PR188, reviewed candidate `ef015581768b8c5bf0af7aaac6c4bcc0525de82c`, merged as `0ec6bac7afa66def9a5f51385443ff215b51fc3f` with the identical reviewed tree. S1656 A2 item37 and S1681 G14.23 record the accepted environment43d67264/backendc3/spec26588 and all three votes. Both accepted money-dependency S1656_SPEC_A2_SHA and S1681_SPEC_SHA name this recording merge. Final W runtime will require the later exact source recording defined below after its implementation exists. Vulcan independently read all four recording-file diffs, verified the corrected immutable spec links, source identity and G9 ancestry. This completes the design dependency binding; runtime installation of these pins remains deferred until the protected lifecycle permits it. Protected run25 remains on original03d3f30/pins519b1d5 and must complete its actual ordered lifecycle; environment merge does not release the owner window. This revision selects exact persisted window/order authority and retained custody without DELETE; the complete revised contract requires a fresh full Gate2 panel. No duplicate product/harness implementation is authorized.

The September12 bounded production read confirms the two authorized historical paid S1707 AWS/R2 orders use Workspace direct provider delivery, outside alternate VC/raw-HMAC paths. Both connections remain revoked; retained file hashes are verified, fresh availability/refusal and production-device transport are not. Mars4255 independently accepted only that bounded evidence. Keep the §17.3 transport gap and final paid acceptance distinct from this historical observation. The cumulative real cost cap remains100USD, with84.8369454546 retained exposure; nominal Stripe TEST ledger amounts do not create real spending or fresh headroom.

Accepted readiness interface: `app.services.refund_processing_service.ORDER_MONEY_PROTOCOL_VERSION=1`; async `assert_order_money_protocol_ready(db, *, required_version=1)` and sync `assert_order_money_protocol_ready_sync(db, *, required_version=1)` raise `OrderMoneyProtocolUnavailable`. Migration `s1714_order_money_v1` has parent `f0e47ebd81ee`. Readiness verifies ancestry, actual columns/constraints, exact merchant chart, bound financial authority and the accepted F2 corrected journal function plus exact sole enabled attachment and all preserved guards; a flag or cached previous success is insufficient. Both synchronous signed-admission and asynchronous processor checks must reject an old/unknown function or trigger drift before money work.

The normal payment capture/refund processor is the sole financial-effects writer. Its `ensure_order_capture(db, *, order, transaction, provider_payment, provider_charge, origin, locked=None, _prepare_only=False)` flushes without committing and establishes a real original order-bound Payment and balanced gross capture journal from canonical order_id/transaction_id checkout metadata. It is also used for refund-before-payment-event ordering. No fixture/backfill finance rows may conceal missing capture. The migration corrects the real order-status trigger: partial refunds do not transition the transaction; full refunds require applied real Refund records and cumulative gross equality, with exactly one transition/event, retaining actual prior status and existing settled facts.

Merchant authority is exactly one active US/USD merchant_of_record entity with code `AIM_WY_LLC`, matching BILLING_ENTITY_CODE. Existing2100 remains Customer Credit Liability. Accepted accounts are1000 Cash - Stripe Clearing,2110 Seller Payouts Payable,4000 Marketplace Commission Revenue,5100 Unrecovered Seller Payouts, with exact types/normal balances and active/system-managed/manual-posting-disabled attributes. Once financial authority is present, billing_entity_id cannot be null, changed or cleared. W relies on readiness and normal financial processing; it never seeds chart or financial authority.

The authentic signed TEST admission seam follows signature/scope and generic dedup validation, at the existing synthetic-suppression decision before durable admission. Mars owns `admits_s1681_negative_refund(event, db, *, normalized_stripe_account)` and the separate `admits_s1681_failed_run_refund` predicate. Vulcan adds a distinct W alternative at this same seam after Mars's accepted product candidate. W cannot satisfy either Mars predicate and neither can satisfy W. An exact claimed TEST authority with incompatible readiness produces retryable failure, not acknowledged suppression. The old BillingService charge.refunded writer is skipped, including replay. Durable stripe_events admission commits and releases synchronous locks before provider reads; async normal effects and completion commit atomically before HTTP2xx. There is no fire-and-forget processing.

The accepted full lock order is event mutex → order → transaction → order_money_states → Payment → Refund rows sorted by real ID → agent_api_keys → period/GL/journal; implicit UPDATE and foreign-key trigger edges count. Non-event paths start with order and cannot acquire an event lock afterward. GL locks sort by (entity_id, code, id), including existing FinanceEngine template posting. The W predicate performs reads and acquires only existing order-money row locks in this chain; it performs no INSERT/UPDATE/DELETE or provider mutation. It must not call a helper that creates missing OrderMoneyState. Registration and cleanup are separate mutations following this chain. The private opening/binding/terminal-cardinality table lock described below is acquired NOWAIT before row locks; it is never acquired by the webhook predicate. Unknown payout dispatches retain their original provider identity and must be reconciled without a new Transfer key or age-based redispatch.

Cross-repository manifest names exact backend source, built image digest and source label, environment source, both acceptance specifications and runtime versions.env. Mars owns the repin/rebuild transition. A clean checkout is insufficient evidence of the running image. Shared webhooks.py edits are serialized on his accepted product candidate. Production GitHub triggers for all four services must be frozen and verified absent BEFORE product merge; current main triggers all have checkSuites=false. Actual stop, old-writer absence, provider reconciliation, migration and restored adoption evidence are separate execution gates.

Mars4145 found the canonical HTTP checkout refusal behind the delivered-order fixture conflict. At c3 checkout.py97–111/204–215, it refuses completed/delivered/pending_delivery for the same buyer/listing but permits created/unpaid; this is not all-status uniqueness or concurrency exclusion. DS second-round MCP-only provenance was disproved by that actual HTTP source. Mars4151 freezes fixture amendment9ab2e7cfef6272b6c708093d28c135d61f7b93c9 (section14; later unanimously accepted in decisionf34cb198): retain P seller01/buyer01, use existing packaged buyer02 (`4b10c456-5936-49d7-be3f-d9c68c9a263e`) for N on the same listing, and retain W seller02/buyer01. F1 now has unanimous acceptance recorded atf34cb198d2415acf82c637b807618245025b6a5e. Vulcan seed implementation1ac556c13238a8b74f1bced9c8af870babcb2221 was integrated by Mars as1db2ecc with70tests passing. Final complete environment Gate3 and the owner runtime window still precede live fixture activation. The protected live environment retains its original three fixtures; only the next accepted clean lifecycle may apply the implemented fourth-fixture seed/transport changes. N still confirms before independent revocation, waits its genuine48-hour hold, proves bare-revoked settlement refusal, then normal full-refund refusal. Failed-run cleanup is limited to exact FAILED v2 authority and cannot include protected v1 run25. W authority remains separate from table `s1681_refund_test_authorities` and its N/failed_run purposes.

## Revised panel disposition and explicit scope decision

The first Gate2 candidate e138f16a6068ebc0214f7f11baa936f1dc4e689c was not accepted: DeepSeek REVISE, GLM REQUEST_CHANGES, CC APPROVE_WITH_NITS. Preserve all three full responses and the unchanged predecessor packet. CC treated cross-window admission as proof orchestration and assumed enforceable exclusive credential custody; this revision does not adopt those assumptions. A qualifying old W order can reach the signed seam without the host's new-order check, and an independent writer can replace a name-addressed secret after GET. Neither a local lock nor observed-drift preservation closes those cases. Select separate exact W registration and remove DELETE from the lifecycle. All three voters must review this complete revision; no prior vote is carried forward.

Source corrections retained: c3 account_pool.json seller02 has email/password names but no totp_secret_name; add that exact field below. The Mars admission module's no-provider-mutation statement does not mean no database locks: _admits invokes lock_order_money_sync. Its N/failed_run table keeps its P/N role and negative/failed_run CHECK constraints unchanged. Do not silently extend those values or route W through the existing CLI's failed-run fallthrough. Reject the suggested table-drop rollback because paid authority/history must remain recoverable. Incorporate CC's optional capture parameters and actual payout eligibility path, DS's nonsecret PYTHONPATH exception and documentation dispositions, and GLM's actual local-image identity.

The second candidate793e79153d9eac5c2d57536982d3f793b04d2c19 also requires a complete correction before implementation: CC/DS APPROVE_WITH_NITS, GLM APPROVE_WITH_MANDATES with a blocking MEDIUM. Later detection of seller01/buyer02 reset damage does not preserve paid P/N history. This third revision instead unconditionally refuses backend SurgicalReset for exactly the four protected fixture UUIDs, including before any owner file/window/schema exists. This closes the host-file-publication race without a new lineage table, distributed lock or destructive guarded path. Mars4483 verified that the accepted money environment uses its separately guarded bin/reset lifecycle, not the surgical-reset endpoint. Unrelated account targets retain existing behavior. All three full second-round responses and mandate/nit dispositions accompany the new full review; no approval is carried over.

## Command and transport proposal

Public interface: `bin/workspace-fixture open-window`, `activate`, `bind-purchase`, `status`, `close-window`, or `deactivate`, with no extra arguments. These are private operator commands, not public API endpoints. The shell wrapper prefixes new subprocess commands with rtk. Status is read-only and never creates audit, lock, token or credentials. The host selects the operation only through ROOT/.state/workspace-authority/current.json, never by listing directories or choosing latest. This owner-written nonsecret canonical envelope has receipt keys schema_version2,kind=owner_pointer,operation_id,owner_window_id,owner_authorization_sha256,runtime_sha256,protected_evidence_sha256. It uses guarded regular/current-owner/single-link/no-symlink reads, max16384bytes, mode0400/private parent. Only Mars as lifecycle owner publishes/replaces this pointer by fsynced same-directory atomic rename after verifying the prior operation is fully closed/restored; commands never create or advance it. All referenced operation-directory files must already exist with matching hashes before publication. Missing/conflicting pointer/audit/artifact refuses; first activation can select its issued operation without an existing audit. An unresolved prior audit blocks a replacement pointer. The receiver compares mounted current.json to stdin operation_id and fixed artifact hashes for lifecycle commands; historical webhook convergence uses its persisted authority, not the newest pointer. Status only reports the actual pointer/audit mismatch without repairing it. Every mutation enters the existing absolute-command check-settlement --lock path, then unchanged --guard seed under inherited lock ownership. Both exits1/2 refuse before the first write; retain ancestor/token/flock checks and do not create a second lock. A successful lock does not authorize a window: the separately issued owner artifact and exact protected lifecycle evidence below are also required. Commands never issue that owner artifact themselves.

Select seed/workspace-host.py as host implementation and seed/seed.py --workspace-activate-stdin as the fixed container receiver for all listed actions. The existing read-only mount resolves /s1656/seed.py. Use the freshly inspected exact backend container, docker exec -i without TTY, /app working directory and the existing Python interpreter. Permit the one fixed nonsecret assignment PYTHONPATH=/app; no credential-bearing environment assignment, argv, temporary secret file, shell interpolation or Compose credential mapping. Send one UTF-8 JSON object on stdin, then EOF. Read at most16385 bytes; limit16384. Reject duplicate keys, trailing/non-object JSON, unknown keys at every level, wrong scalar types and unsupported versions before fixture-writing imports.

The host clears stale inherited values for exact Workspace custody names and retrieves only the reviewed test-env credentials. Do not copy broad host-seed injection, serial enrollment, S3 upload or seller01 helpers. Tokens remain in private host process memory. The actual child-boundary canary uses a unique synthetic Workspace TOTP/new custody secret and must show none of that plaintext in argv, output, audit, exception text or inherited/added environment. Existing accepted Compose application credentials (Stripe, database and prior fixture credentials) remain governed by their existing contracts; this is not an assertion that the whole established application environment contains no secrets. No new Workspace credential is added through Compose, argv or a temporary file. Nonsecret custody IDs/digests remain permitted only in the explicitly allowlisted stdin/audit/receipts; do not add them to environment as a transport workaround. Responses are bounded allowlisted nonsecret JSON; raw provider bodies and traceback/input fragments never escape.

## Owner issuance, runtime pins and trusted opening artifact

The authenticated local operator with authority to run docker exec and maintain the environment is the trust boundary. A hash, actor string, UID check or flock does not authenticate a malicious operator or exclude an external provider writer. No public route invokes this receiver and no new signing key/capability is invented. Mars issues the owner authorization only after independently validating completion of the original protected run25 online/offline lifecycle, corrected P/N phases with their genuine48-hour holds, settled-P/negative-refund results and preserved device/install/identity evidence. Time passing, a wrapper success or a caller's boolean cannot substitute for those receipts. No owner window is issued by this design document.

Concrete private host directory: ROOT/.state/workspace-authority, existing mode0700, current operator owner, no symlink components. New explicit compose.yaml directory bind mounts it read-only at /s1712-workspace-authority; no implicit file availability is assumed. This mount contains only nonsecret authority/runtime records. Before a supported clean lifecycle starts the stack, bin/up creates the empty private directory; it never manufactures an authorization. Protected run25 is not recreated to add this mount. Verify actual container mount source/destination/read-only mode and readable UID mapping during isolated qualification; do not weaken host permissions or fall back to another path.

Each operation has an append-only directory ROOT/.state/workspace-authority/<operationUUID>, mode0700. Only a canonical UUID operation_id selects this relative subdirectory, never an arbitrary path. Old directories remain immutable; a later authorized window receives a new directory rather than overwriting a prior authorization/runtime. Mars's future owner-issued file is <operationUUID>/owner-authorization.json, canonical envelope {receipt,sha256}, schema_version2, kind=owner_authorization, namespace=workspace_s1712. Its exact receipt keys are schema_version,kind,namespace,operation_id(UUID),owner_window_id(UUID),owner_authorization_ref(nonempty ASCII reference to the real owner transition),pins(the exact set below),protected_evidence_sha256(SHA256),issued_at(UTC RFC3339). It contains no future order/PaymentIntent/charge IDs. The protected-evidence.json envelope at the same fixed directory carries the actual phase result artifacts and before-W P/N/device snapshot manifest, with explicit original-run and corrected online/offline dispositions; owner verification must bind the complete actual files and results, not a hand-authored success flag. The selected design uses authenticated owner attestation of the exact evidence set, not an invented existing readiness CLI. protected-evidence.json receipt has exact keys schema_version2,kind=protected_evidence,namespace=workspace_s1712,operation_id,owner_window_id,owner_authorization_ref,attested_at,records. records is a list of exact {category,identity,path,sha256} objects with unique category+identity, absolute owner-controlled regular-file paths and SHA256. Required categories are original_online,original_offline,original_summary,original_wrapper,corrected_online,corrected_offline,corrected_summary,corrected_wrapper,scheduled_success,launchd_provenance,installed_plists,stable_release,money_gate4_bq,protected_before. Each phase category includes purchase-confirmation.json,browser-evidence.json,settlement-check.json,cleanup.json and every transitively referenced artifact; corrected phases additionally include test-negative-pair.json,test-negative-registration.json and settlement-stage artifacts. Run source/spec identities remain separately bound by each phase's actual receipts; the old protected run's pins are not substituted for current deployment pins. Each required category must be nonempty, identity unique, no path traversal/symlink, size/hash checked with guarded reads. The owner attests semantic completion and actual authoritative release/BQ/scheduler provenance; host validation checks completeness of the declared categories, exact bytes/hashes and operation/reference binding, not a fictional independent proof of operator honesty. No bare success boolean is accepted in place of those actual source artifacts. The existing owner with Docker/custody authority is trusted; a compromised operator is outside this interface's authentication guarantee.

Exact protected evidence root is /Users/max/koskadeux-state/s1656/acceptance-evidence and shared lock /Users/max/koskadeux-state/s1656/environment.lock. Original run is20260911T233041Z with historical environment03d3f30acf854537edd76fdb19e1cf521b577c9a/specs519b1d5893dc410392cc66051dad0902572dcef9. Its current online pending active-run.json hash cbbfe3278d4ed6933a254c2c4a24f345a84bbb1a3e44ec5045892509f49889a5 is an observation, not completed authority. Future completed <root>/<R>/online and offline supply the named phase artifacts; <root>/<R>/summary.json and wrapper-contract.json supply run records. Future corrected R must be resolved from the actual distinct v2 run, never fabricated here. Active-run.json is mutable ownership and never substitutes for historical archives.

Actual bin/check-settlement load_pair_phase_archive_v2 (candidate606267 lines2552ff) and resume_v2 (2715ff) define the corrected hash/cleanup/summary semantics the owner must verify. bin/check-settlement and bin/verify --from-clean-seed execute mutations, so neither is called by a Workspace opening/status verifier; --pins checks only source. No existing read-only CLI attests the complete owner window. bin/nightly and bin/nightly --settlement-check produce <R>/scheduled-<UTCstamp>.json for exit0 or scheduled-pending-<UTCstamp>.json for exit2, linked through command_evidence_sha256. Exit2 or a manually run wrapper is not actual scheduled success. The owner includes real launchd execution provenance, installed/active versions of config/com.aimarket.s1681-settlement.plist and config/com.aimarket.s1681-money-path.plist, actual AIM Data1.23.3 stable tag/release evidence and the completed money Gate4/BQ record. These authoritative records are resolved at issuance; none is invented in this draft. protected_before binds settled-P/N and device/install/identity snapshots immediately before W. Receiver reads only the mounted manifest, not host historical files outside its mount; backend separately captures its own complete order baseline. Source interface retained from Mars4453 at /Users/max/koskadeux-state/s1656/s1714-workspace-protected-evidence-interface.md SHA2560476faa68652a9eabbb1809df4a4e4df859a6131c6f61f8a5ca61e344e491bb4. Both files are written once, fsynced, mode0400, max1MiB, regular/current-owner/single-link/no-symlink, with private parent. The host reads through O_NOFOLLOW|O_NONBLOCK and stable fd/path metadata and validates canonical bytes/hash. No overwrite or silent repair on retry; conflicting preexisting bytes refuse.

runtime.json in that same operation directory is the same guarded canonical, write-once format, issued for this accepted deployment/operation by the owner, with exact receipt keys kind=runtime_manifest, schema_version2, namespace,operation_id,owner_window_id,pins,runtime_settings. runtime_settings has exactly E2E_TEST_ROUTES_ENABLED(boolean true), E2E_RESET_ALLOWED_ACCOUNT_IDS(empty JSON array), E2E_TEARDOWN_ALLOWED_ACCOUNT_IDS(empty JSON array). These nonsecret settings are bound by the complete persisted runtime_json/runtime_sha256 and owner-issued runtime artifact; the source pin set remains exactly eight keys. Host verifies it against direct current Docker discovery and independent repository/version sources on every mutating invocation before sending input. The backend compares stdin pins, mounted runtime/owner artifact bytes and their persisted window copies, plus actual ENVIRONMENT=test, STRIPE_TEST_MODE=true, S1681_ENVIRONMENT_SHA and S1681_SPEC_SHA. It checks the real read-only mount and guarded file types. The receiver cannot independently call Docker and must not claim that comparing two caller strings measures its image; actual Docker identity is the trusted host's separate verification, retained as evidence. The mounted files are not accepted from stdin, and bind-purchase never accepts replacement authority or baseline bytes.

Workspace TEST runtime configuration is explicit only in the final reviewed environment. compose.yaml backend.environment sets E2E_TEST_ROUTES_ENABLED: "true", E2E_RESET_ALLOWED_ACCOUNT_IDS: "[]" and E2E_TEARDOWN_ALLOWED_ACCOUNT_IDS: "[]" as literal values, not caller interpolation. The first flag gates only the existing legacy synthetic checkout branch; default ENABLE_CANONICAL_TX=true returns through the canonical transaction branch before that guard. It must not be represented as a canonical checkout admission or purchase-stop switch. The same flag imports both account cleanup routes, so the unconditional shared four-UUID service refusal remains mandatory even if an isolated test deliberately populates the allowlists. Empty allowlists remain the selected actual Workspace runtime state, not a substitute for service protection. Preserve default canonical routing and the three exact immutable runtime_settings; do not add a checkout flag, a canonical override in final Compose, or a production configuration change. The protected run25 is not recreated or armed; only the later owner-approved final TEST runtime uses this configuration. The proposed source-grounded correction and its outstanding review/verification are recorded in the final section below.

On every mutating wrapper invocation the host independently checks effective running-container Config.Env for exactly these three nonsecret keys against the reviewed literals (reject missing/duplicate/conflicting values); do not print or retain unrelated environment entries or credentials. The receiver and qualifying W predicate compare actual application settings to the exact runtime_settings and persisted runtime_json/hash, in addition to existing TEST/pin checks; wrong/unknown flag or nonempty allowlist refuses W mutation/admission through the existing retryable authority-readiness path, never silently suppressing an already registered refund. Perform the cheap definitely-non-W rejection before any optional W/runtime artifact check. Host discovery of configuration is trusted operator evidence, not backend Docker self-measurement. The actual application process startup/router import must be exercised by tests; changing a settings object after router import is not proof that routes are present/absent.

Required configuration/ASGI tests use the exact reviewed Compose representation and isolated fresh application startup/lifespan. Prove ENABLE_CANONICAL_TX is absent from Compose and test-process overrides and resolves to the actual default true. With complete valid fixed-pair listing/readiness prerequisites and controlled providers, exercise the real public canonical checkout with E2E_TEST_ROUTES_ENABLED false and true: both create an order and transaction through normal processing. Preserve the measured default/flags, actual response, created database identities and side effects; do not stub checkout/transaction logic or treat an unrelated error as success. This new requirement explicitly corrects tests/test_workspace_test_refund_admission.py::test_fresh_main_lifespan_public_checkout_reviewed_settings[False], whose original default false403 assertion and failed run remain retained rather than relabeled passed. Separately exercise the legacy branch under a clearly labeled auxiliary ENABLE_CANONICAL_TX=false test: flag=false must return403 synthetic_purchase_requires_e2e_guard before checkout/provider/financial effects; flag=true must reach the actual legacy branch, with its real result and effects retained. Do not claim legacy synthetic mock-payment behavior is canonical real paid capture, manufacture its finance prerequisites, or use the auxiliary override as final Compose proof. The final selected canonical/flagtrue path must additionally prove the actual public checkout→normal signed capture→committed Workspace registration→normal signed full refund/replay lifecycle with all financial/readiness prerequisites; merely obtaining a checkout URL is not this paid proof. Verify both destructive routes are registered when armed and both absent when unarmed, each from fresh startup; each fixed-identity service guard still refuses with valid isolated tokens/allowlists, including missing Workspace lineage. Verify actual runtime_settings matches host environment and application values and wrong/missing/duplicate flags, changed allowlist, altered manifest/preimage/hash refuse before W writes. Retain the flagfalse current-runtime mismatch retry behavior for qualifying registered W, independent of canonical checkout success. Exercise ordinary non-W/production-shaped traffic unchanged through real signed ASGI paths with zero optional W lookups before rejection. Separately exercise both canonical mixed-synthetic directions, synthetic buyer/real-seller listing and real buyer/synthetic-seller listing, retaining actual responses and database/provider-double effects. The default canonical branch bypasses the legacy _refuse_mixed_synthetic_purchase call; do not assume production authentication excludes either pair. Any discovered production exposure remains an explicit release dependency requiring exact disposition, not silently accepted behavior or authority to alter checkout code. Existing runtime_settings tests belong to tests/test-workspace-fixture.py and tests/test_workspace_test_refund_admission.py; no new product route, normal money writer or settings file target is selected.

Exact pins keys: backend_sha(SHA40),backend_local_image_id(sha256:64hex),backend_source_label(SHA40),environment_sha(SHA40),s1656_spec_a2_sha(SHA40),s1681_spec_sha(SHA40),workspace_spec_sha(SHA40),versions_env_sha256(SHA256). backend_local_image_id is running container.Image, equal to docker image inspect .Id for ai-market-backend:$BACKEND_RC_SHA and .runtime/image-digests.json backend.digest. backend_sha independently equals versions.env BACKEND_RC_SHA and runtime-record backend.source_sha. backend_source_label is actual Config.Labels[ai.market.s1656.backend-sha], independently compared to backend_sha. Re-discover exactly one project/service container directly; no cached snapshot or Compose polling. A registry/OCI manifest or index digest, including AIM Data's index, is never a substitute for this local image ID. Any optional registry digest is separate informational evidence and does not enter pins. environment_sha is the exact clean deployed environment Git commit selected in the final owner-reviewed runtime manifest, equal to actual checkout HEAD and deployed S1681_ENVIRONMENT_SHA. Equal trees do not make different commit IDs interchangeable. For dependency preparation, the selected start is merge43d67264ab7a3a7e47d6cc6911737273ce1c94cc, treee553476a38e0948d3a57da60872e406048a0d391; candidate606267af80470b1d61c07d91cda30a0dc5200224 is retained review provenance and is never substituted for that dependency pin. After MP adds Workspace code/mounts, the runtime pin must instead name the actual final reviewed and merged W environment commit and tree;43d67264 cannot falsely claim to contain those new files. No future SHA is invented here. Before runtime issuance, update the accepted source/spec recording to bind the actual W backend/environment combination and run the real existing --pins source check against that exact record. Both money spec pins name that final recording commit; current0ec6 is the accepted pre-W dependency recording only. Tests reject the alternate same-tree commit and any changed tree. Re-hash actual versions.env and verify both spec pins and accepted Workspace commit against owner-issued files. Pin/hash/label mismatch refuses before provider, credential, audit or database writes.

## Persisted two-stage W authority and concurrency

Use two new tables, separate from s1681_refund_test_authorities. New backend migration s1712_workspace_authority_v1 has down_revision=s1714_order_money_v1; file alembic/versions/20260912_002_workspace_test_authority.py. Confirm a single accepted parent at build time; a changed parent is a reviewed integration change. New models in app/models/workspace_test_authority.py; explicit import in app/models/__init__.py. New service app/services/workspace_refund_test_admission.py owns opening, binding, closure, readiness and the predicate. seed.py invokes that service through the private receiver; no new order_money_protocol.py command or W fallthrough is used. Ordinary capture/refund/payout services remain the sole financial writers.

workspace_test_windows columns: id UUID PK (=owner_window_id), operation_id UUID NOT NULL UNIQUE, namespace TEXT CHECK workspace_s1712, role TEXT CHECK W, seller_id/buyer_id UUID FKs users with fixed-pair CHECKs, owner_authorization_json TEXT and sha256 CHAR64, runtime_json TEXT and sha256 CHAR64, baseline_json TEXT and sha256 CHAR64, opening_json TEXT and sha256 CHAR64, opened_at timestamptz NOT NULL. Fixed seller02=961bd48a-3018-43f4-a7ad-6871720fd806, buyer01=7d662d0e-22f2-4b87-bbc5-80984ba808ac. The canonical JSON fields store the exact receipt preimage bytes as UTF-8 text, never reserialized JSONB; their lowercase hashes must verify on every use. Add state TEXT CHECK(open,bound,closing,recovery,closed), revision BIGINT>=0, closed_at nullable timestamptz and terminal_json/terminal_sha256 nullable paired TEXT/CHAR64. A partial unique index on namespace where state<>closed enforces one nonclosed W window. Immutable trigger rejects DELETE and changes to every column except the explicitly mutable lifecycle columns state,revision,closed_at,terminal_json,terminal_sha256. Explicitly immutable columns are id,operation_id,namespace,role,seller_id,buyer_id,owner_authorization_json,owner_authorization_sha256,runtime_json,runtime_sha256,baseline_json,baseline_sha256,opening_json,opening_sha256,opened_at. Direct SQL tests independently mutate each owner/runtime JSON preimage and hash and require refusal; valid lifecycle transitions remain allowed. The earlier shorthand "and sha256" in each column pair means exactly the corresponding prefixed *_sha256 column, never a shared ambiguous sha256 column. Only forward state transitions open→bound/closing/recovery, bound→closing/recovery, closing→closed/recovery, recovery→closing are permitted with revision increment exactly1; closed cannot reopen. Recovery→closing requires the service's complete reconciliation; closed requires terminal evidence, with database checks on paired terminal fields and nonnull closed_at. Every nonclosed state has null terminal fields/closed_at; the close transition sets them together, and closed is entirely immutable. Inserts require state=open,revision=0 and null terminal fields. Immutable authority is never erased by a lifecycle update.

workspace_refund_test_authorities columns: order_id UUID PK FK orders RESTRICT; window_id UUID UNIQUE NOT NULL FK workspace_test_windows RESTRICT; transaction_id UUID UNIQUE FK transactions; payment_id UUID UNIQUE FK payments; payment_intent_id TEXT UNIQUE; charge_id TEXT UNIQUE; seller_id/buyer_id UUID FKs users; listing_id UUID FK listings, purchased_version_id UUID FK listing_versions, publication_id UUID FK seller_listing_publications, approval_id UUID FK seller_listing_approvals, delivery_authority_id UUID FK delivery_authorities, connection_id UUID FK cloud_connections; amount_cents BIGINT positive safe integer; currency TEXT CHECK usd; role TEXT CHECK W; protocol_version integer CHECK1; registration_json TEXT and registration_sha256 CHAR64; registered_at timestamptz. All columns NOT NULL. Bind the exact immutable scope/approval/version hashes and complete pins in registration_json, and compare all scalar columns to that canonical receipt on each use. Fixed actors checked at DB level; all FKs RESTRICT, no cascade. Immutable trigger rejects every UPDATE/DELETE. Unique order/window/payment/provider IDs prevent another window or charge adopting the same authority. No refund outcome field is updated in this row; outcomes stay in canonical financial/event tables and separate terminal receipts. Readiness verifies actual tables/columns/FKs/checks/unique indexes and exact enabled immutability/transition trigger definitions, alongside accepted order-money readiness. Missing/disabled/drifted schema refuses W admission; existing normal and P/N behavior is unchanged.

Stage A open-window: require all prior windows closed, owned readiness fully restored and retained-custody/provider lineage verified before opening another; a closed window alone cannot bypass an incomplete deactivation. Under owner guard and verified mounted artifacts, private receiver starts a short READ COMMITTED transaction and acquires LOCK TABLE orders IN SHARE MODE NOWAIT before any order row lock or authority write, with the same explicit timeout/deadline limits as Stage B. There is no provider IO while held. If lock unavailable, rollback and return window_busy with zero writes. Capture the complete fixed-pair set of existing order UUIDs in every state, sorted; query with explicit4097 sentinel and refuse if more than4096 rather than truncate. Baseline receipt keys schema_version2,kind=window_baseline,namespace,operation_id,owner_window_id,seller_id,buyer_id,order_ids(sorted unique complete UUID list),observed_at(database UTC). Its identity is the window UUID. Persist baseline canonical bytes/hash plus verified owner/runtime preimages and a separate opening receipt atomically in workspace_test_windows with state=open. No payment IDs are invented at this stage. An identical retry reads/revalidates that same row and returns its original bytes; it never replaces the baseline. A conflicting operation/window/pin/evidence refuses without writes.

Opening receipt keys schema_version2,kind=window_opened,namespace,operation_id,owner_window_id,role=W,seller_id,buyer_id,pins,owner_authorization_sha256,protected_evidence_sha256,baseline_sha256,observed_at. The host retains backend-produced baseline.json and opening.json under ROOT/.state/workspace-windows/<windowUUID>/ using the same private/write-once/canonical/max1MiB/fsync rules. They are returned by the receiver, not generated by host snapshot substitution, and are not an input source of backend authority. If backend commits but host file persistence fails, inspect retrieves the exact stored bytes. A digest cannot prove completeness by itself: completeness comes from this backend query under the stated table lock.

Stage B bind-purchase follows activation and a real normal public W purchase; caller supplies no order ID, snapshot or candidate list. Receiver first performs read-only discovery from its persisted baseline and obtains authenticated TEST PaymentIntent/charge facts through the existing provider client before locks. Then it rolls back the preflight transaction, begins a fresh transaction, acquires orders SHARE NOWAIT before any row lock, locks the window row and all relevant order rows in sorted order followed by the accepted transaction→money-state→Payment→Refund chain. A changed discovery/provider binding causes rollback and a whole-operation retry, not a provider lookup under lock. Recompute the complete fixed-pair order set using the persisted baseline; all baseline IDs must still exist in the fixed-pair set and the entire new-order delta, across all states/listings, must contain exactly one order. That order must be actually paid with unique canonical order/transaction/original capture, fixed actors, usd positive full capture and no pending payout, dispute, ambiguous authority or existing unregistered refund. Never select latest order or silently filter an extra unpaid order out of the delta.

This short table SHARE lock conflicts with public order INSERT/UPDATE/DELETE ROW EXCLUSIVE locks, so an already active writer makes binding refuse immediately and a writer starting later cannot change order cardinality before commit. It is deliberately limited to the isolated TEST environment, statement/lock bounded, and is never a production-wide new lock or a webhook lock. NOWAIT precedes row locks to avoid waiting on existing table writers while holding product locks; subsequent row-lock/deadlock/statement timeout rolls back with zero authority effects and requires a complete retry. Set LOCAL lock_timeout=250ms, statement_timeout=2000ms and idle_in_transaction_session_timeout=2000ms, with a host-enforced5second whole transaction deadline/connection cancellation. Every explicit window/order/money/Payment/Refund row lock is NOWAIT; unique/FK/trigger waits remain bounded by lock_timeout. SHARE permits another SHARE and preexisting ROW SHARE/FOR UPDATE, so same-window SELECT FOR UPDATE NOWAIT plus unique constraints serializes duplicate binders. Never UPDATE orders or call a helper that creates/updates money state inside this binding transaction; no table-lock upgrade is allowed. SQLSTATE55P03,40P01,57014 or deadline/connection failure rolls back before any whole-operation retry, and uncertain commit is reconciled by exact immutable identity. This briefly blocks unrelated order writers in this TEST database as well as the fixed pair. These conflict semantics follow [PostgreSQL17 explicit locking](https://www.postgresql.org/docs/17/explicit-locking.html); actual elapsed bounds require the PostgreSQL tests below. This is not a claim that flock serializes public buyers. A purchase after successful binding may exist, but cannot gain authority (unique window/order binding) and makes closing/deactivation final cardinality proof refuse. It cannot retroactively turn the registered original order into another one. Retain and reconcile all extra paid orders; no silent deletion or synthetic refund.

Validate order→purchased_version→SellerListingPublication→DeliveryAuthority→seller-owned CloudConnection, unique persisted PI/charge/Payment, publication listing/seller/version equality, authority_kind=workspace_connection, legacy_serial_id=null, matching connection owner and immutable approval/version/scope hashes. Reject agent/device-backed transactions and synthetic lookalikes. Validate retained immutable authority even if access/connection is revoked; no active credential requirement merely to refund. The accepted real capture/journal readiness must already hold; this service never manufactures capture. Insert one exact canonical registration row and change window open→bound in one commit. Identical retry returns existing original bytes only after comparing all persisted bindings; conflict refuses. Existing bound row recovery does not produce a second authority. No provider mutation happens during registration.

The operator must obtain the successful committed bind-purchase registration receipt before issuing any normal TEST refund request. With available schema, an unregistered W refund is not admitted and the current synthetic seam acknowledges it as completed/e2e_scope_suppressed; this is terminal suppression, not retryable authority readiness failure. Explicitly test a pre-registration signed charge.refunded event and preserve its actual provider/event disposition as an incident rather than manufacture registration after the fact. Signed webhook exception calls admits_workspace_refund(event,db,*,normalized_stripe_account), called at the seam as admits_workspace_refund(event,db,normalized_stripe_account=stripe_account), a narrow imported disjunct at the authentic TEST suppression seam after real signature/scope/dedup validation. First restrict to the fixed W TEST actor/provider shape; unrelated synthetic or real production traffic must not depend on this optional W schema/mount. Require explicit TEST event/PI/charge mode, exact full amount/currency and deployment pins matching retained authority. A definitely unrelated/non-W event returns false without consulting optional W tables/mount. For a qualifying fixed-pair immutable Workspace charge or an existing claimed/registered W charge, missing/incompatible readiness, mismatched current runtime pins or uncertain authority lookup raises a stable WorkspaceTestAuthorityUnavailable exception inside the existing critical-event try/except. It must use the actual critical charge.refunded failure path: rollback, durable failed/retryable event and non2xx response, never completed/e2e_scope_suppressed or a false success. Once readiness is restored the same signed event ID converges normally. A fully available schema with no exact registration does not admit the order. Normal P/N/production routing remains unchanged; test all of these distinctions against the real ASGI endpoint. Locate the unique authority by provider IDs, lock the existing order/transaction/money-state/Payment/Refund rows under the accepted event→order chain, and independently compare all immutable authority/purchase/capture bindings and canonical hashes. Implement explicit existing-row SELECT FOR UPDATE NOWAIT in order→transaction→order_money_states→Payment→Refund(sorted actual IDs) order inside the new service. Do not invoke the creating lock_order_money_sync helper; a missing row refuses without db.add/INSERT or repair. Retain the existing event mutex before these locks and do not acquire a window-table lock in the predicate. This predicate performs no DML, no provider calls, no window table lock and does not require window state=open. A closed/recovery window retains authority only for its originally registered charge/order and normal exact event/refund convergence; it cannot bind a second order. Existing dedup and normal processor own event/refund identity immutability and financial effects. An old same-pair unregistered order, P/N, another window's replacement order, changed scope or mismatched pins never reaches W processing.

close-window first stops new binding by forward transition to closing; uncertain outcomes may retain recovery. It does not revoke an already registered refund's authority. Final closed rechecks under the same short orders SHARE NOWAIT and existing-money row-lock transaction, with no provider IO/order UPDATE; it requires unchanged complete baseline-plus-exact-registered-order cardinality, actual normal full refund and cannot-settle financial state, no pending/unknown provider result, exact canonical receipt hashes, and preserved P/N/device comparisons. A never-purchased window may close only when its complete delta is empty and no uncertain provider/custody step is hidden; opening/activation resource uncertainty remains independently recorded. Paid rows/history remain. Deactivate restores only owned readiness after the same terminal checks and retains custody/account/history as described below. No automatic timeout opens/closes a window. Workspace preservation incidents have two distinct dispositions. For an extra order after registration: stop new W purchases/binds/unregistered refund requests; retain exact immutable window/baseline/registration, complete current delta, provider IDs/outcomes, event status, protected before/after and custody lineage; keep nonzero recovery_required_extra_order and continue only the originally registered refund's normal compatible convergence; report evidence to the existing owner for a separately reviewed reconciliation/archival correction. For a provider refund before registration: signed event suppression is terminal and acknowledged, no registered authority exists and there is no compatible convergence path; retain the same evidence and nonzero recovery_required_unregistered_refund, stop new W purchases/binds/refund requests and escalate to the existing owner. Do not retry the suppressed event to manufacture registration, bind retroactively, clear the incident or close the window. Only the separately reviewed owner reconciliation/archival correction can permit closure. In both cases the one-nonclosed-window refusal remains until the reviewed correction is completed; no automatic closure escape, extra registration, new authority, row deletion or baseline rewrite exists. Distinct anomaly tests assert extra-order original registered convergence versus terminal unregistered suppression and un-closeable incident state, never false closed/success.

## Surgical reset preservation and fresh protected-state comparison

Both supported backend account cleanup services, SurgicalResetService.reset and TeardownService.teardown, unconditionally refuse exactly seller01=32f8e9c1-a779-459c-b49b-d6db464133b5, buyer01=7d662d0e-22f2-4b87-bbc5-80984ba808ac, buyer02=4b10c456-5936-49d7-be3f-d9c68c9a263e and seller02=961bd48a-3018-43f4-a7ad-6871720fd806. Add one pure shared helper is_protected_workspace_fixture(target_user_id) in app/e2e/workspace_fixture_protection.py with exactly these fixed UUID constants. Normalize a valid UUID before membership comparison; malformed UUID refuses through normal validation and never becomes an unprotected result. No caller list, configuration override, file/DB/provider lookup, all-synthetic blanket or role-only match is allowed. Tests bind the shared constants to the exact packaged identities. In SurgicalResetService the guard follows existing authentication/allowlist/request/apply-review validation and precedes _execute, _reset_database_rows and object operations. In TeardownService insert after build_guard_context and claims/target_email assignment, before validate_inventory, _collect_hitlist and _execute_deletes; the preceding catalog snapshot is read-only and need not be redesigned. Direct service calls as well as routes must receive the protection. Both dry-run and apply refuse with the exact existing exception message: ResetRefusal("workspace_protected_fixture") or TeardownRefusal("workspace_protected_fixture") with ordinary nonincident semantics. Existing str(exc), HTTP detail and audit reason are the carrier; do not invent a category attribute or new audit schema. Existing authentication/allowlist/review failures remain their normal failures. Normal sanitized refusal audit INSERT/commit is permitted; every protected refusal performs zero destructive DELETEs and zero object-deleter calls, not zero SQL or zero audit writes.

The canonical reset/account route returns HTTP400 with detail workspace_protected_fixture. The existing teardown/account route returns202 after authentication and starts its retained background task; that task catches TeardownRefusal and its existing durable status endpoint reports outcome=refusal, reason=workspace_protected_fixture and empty executed_counts. Preserve this actual interface;202 is request acceptance, not successful deletion. Do not add an immediate400 route rewrite, alter nonce replay protection or bypass the engine by testing only a route stub. Actual signed ASGI tests must drive the background task to terminal durable status.

The refusal does not depend on presence or readability of a W table, mount, owner pointer, audit, custody record or any window state. Thus these two supported backend account cleanup services cannot delete the four targeted identities/history before issuance, during issuance, after closing/no-purchase or when Workspace lineage evidence is missing or damaged. This is not a claim of protection against privileged arbitrary SQL, future unguarded routes or authorized Docker volume destruction; the separately guarded environment lifecycle remains distinct. Upgrade review must inventory new account cleanup entrypoints and require the shared guard regression. A reset that inspects no lineage and then races a new host pointer still refuses by target identity; no claim that a database advisory lock serializes a host file writer is needed. Remove the superseded reset/open pg_try_advisory_xact_lock and strict destructive fixed-party branch from the proposal: they are unnecessary because the four-target path cannot reach deletion at all. The existing _safe_scalar/_safe_execute rollback-and-continue behavior remains unchanged for genuinely unrelated targets; it is not used to implement a new protected destructive path. Stage A's short orders SHARE transaction and unique window constraints remain; no reset advisory lock is required for it.

The separately authorized environment bin/reset still follows its existing settlement guard and new pre-W lineage guard; no protection is weakened there. Accepted environment source606267/merge43d67264 invokes that clean lifecycle and has no surgical-reset endpoint dependency. Once W lineage exists, environment seed/reset/up refuse as already specified; any archival reset requires a separately reviewed preservation/reconciliation procedure. A later need to surgically reset one of the four fixed identities is outside this implementation, rather than an undocumented allow-if-no-row escape. This is a bounded conservative refusal on four known fixture accounts, not a generic reset redesign.

The owner evidence includes protected_before identity=backend-protected-state.json in its immutable operation directory. This is a canonical schema2,kind=protected_state receipt with operation_id,owner_window_id,records; records are sorted unique {collection,id,sha256}, with hashes of explicit PostgreSQL17 UTF-8 jsonb text preimages, excluding only the observation timestamp. New service capture_protected_state performs the same fixed read-only queries during opening, before binding and before terminal close/deactivation; compare complete record sets and exact hashes, never only counts or caller IDs. The owner obtains the initial expected record set from the same reviewed query definition when issuing evidence. Host historical file presence alone is insufficient for this fresh comparison.

Protected collections: (a) seller01,buyer01,buyer02 User identity/profile/readiness projection: id,email,is_test,role,status,first_name,last_name,company_name,display_name,totp_enabled,totp_verified_at,totp_secret_enc,backup_codes_hash and four Stripe fields; plaintext/encrypted values never leave the database, only row digests. Normal buyer login last_login_at is deliberately excluded; fixture code still may not modify it. (b) complete auth_user PartyIdentity rows for those three fixed users, preserving exact Party ownership. (c) all currently retained orders whose seller is seller01 and buyer buyer01/buyer02, with complete order, linked transaction, order_money_states, Payment, Refund, order_event and transaction_event rows; row digests cover complete jsonb row values and ordered collection membership, not a latest-row query. Historical absent N rows after approved cleanup are represented by the actual complete empty result and independently retained terminal archive, never recreated. (d) seller01 devices and trust_sessions with stable identity/state fields devices.id/device_id/user_id/is_active and trust_sessions.id/device_id/is_active; exclude routine heartbeat timestamps/counters. Existing owner device/install/serial receipts remain separate required evidence; this finite DB projection does not certify an uninspected whole graph. Queries are bounded to4096rows per collection plus sentinel refusal, and all emitted IDs/digests are nonsecret. Exact source table/column and FK mapping must be bound in implementation tests; no optional-table omission on SQL errors.

For open/bind/terminal comparisons, unconditional four-identity refusal excludes the supported backend reset path and orders SHARE excludes order mutations; obtain the relevant existing protected order/transaction/money rows in sorted accepted order, NOWAIT and bounded, before comparing. Protected user/identity/device reads are finite read-only snapshots; the host must compare again after each owned mutation and before final success. External administrative writers remain outside the private cooperating-operator boundary and any observed drift refuses; no claim of universal external-write exclusion. No provider IO occurs while the short opening/binding/terminal locks are held. A reset-before-open changed/missing protected collection therefore cannot become an accepted new baseline merely because the old owner manifest still hashes correctly.

Required actual signed-route and PostgreSQL tests in tests/test_e2e_surgical_reset.py: every protected UUID, canonical/alternate UUID representation, existing authentication failures, dry-run/apply, no record/pre-open issued pointer/active/closed/no-purchase lineage, missing W schema/mount and damaged lineage. Assert zero destructive SQL and zero object-deleter calls for all four. Use a synchronization barrier for reset-start-before-owner-publication and publication-before-reset; neither ordering may reach deletion. Verify unrelated allowlisted targets without W lineage preserve the existing reset behavior and existing token/review constraints. No protected strict-deletion branch, rollback-and-reacquire or lineage lookup is required. Tests use isolated DB/object doubles and no live fixtures. Add teardown tests in tests/test_e2e_teardown_t2.py and tests/test_e2e_teardown_t2_real_pg.py for allfour protected UUIDs and alternate valid formats, dry-run/apply with valid allowlist/token, preserved users and protected history, exact durable refusal reason and empty executed_counts. Instrument actual SQL DELETE effects and object calls; assert the protected early refusal never calls inventory/hitlist/delete work, while allowing preceding ordinary guard/catalog reads and refusal audit writes. Test both direct TeardownService and actual signed ASGI202/background/status flow through terminal audit, not just stubs or initial response. Missing W schema/mount/lineage, preopen/active/closed and host-pointer publication interleavings cannot alter the fixed-target refusal. Unrelated valid synthetic targets keep existing teardown semantics, and invalid UUID/token/allowlist/synthetic/live-linkage failures retain existing safeguards. Use isolated real PostgreSQL and owned fixtures only; no live reset or teardown. Existing nonce/replay/concurrent-task and rollback/audit regressions remain required.

## Canonical receipts and fixed registration envelope

Every authority receipt uses {receipt,sha256}; hash only the receipt's canonical bytes: recursively sorted ASCII keys, compact separators, ASCII strings, safe integers with absolute value<=9007199254740991 (booleans not integers), booleans/null, arrays/objects only, no trailing newline. Reuse accepted Mars canonical_digest and actual JavaScript/Python golden preimages, including numeric-looking keys; JavaScript must emit sorted key/value pairs directly rather than rely on JSON.stringify object enumeration. Canonical envelope bytes themselves are also retained exactly. Source SHA40 and content SHA25664 are distinct types.

Registration receipt exact keys: schema_version2,kind=paid_registration,namespace=workspace_s1712,operation_id,owner_window_id,role=W,pins,owner_authorization_sha256,opening_sha256,baseline_sha256,protected_evidence_sha256,seller_id,buyer_id,order_id,transaction_id,listing_id,purchased_version_id,publication_id,delivery_authority_id,connection_id,immutable_scope_hash,approval_id,approval_review_hash,listing_manifest_hash,payment_id,payment_intent_id,charge_id,capture_amount_cents,currency=usd,protocol_version=1,observed_at. UUIDs/provider IDs and content hashes are obtained from actual persisted canonical fields; immutable_scope_hash is DeliveryAuthority.immutable_scope_hash; approval_id is SellerListingPublication.approval_id joining SellerListingApproval.id; approval_review_hash is SellerListingApproval.review_hash and must equal SellerListingPublication.review_hash; listing_manifest_hash is ListingVersion.manifest_hash for Order.purchased_version_id. A registration PostgreSQL test independently changes AgentProposal.manifest_hash and ListingVersion.manifest_hash and proves only the ListingVersion joined through Order.purchased_version_id supplies this binding; no ambiguous model-column shortcut. Also require approval seller/connection equal authority seller/connection and publication.authority_id/listing_version_id/listing_id equal the same joined records. Never invent a replacement hash or substitute request_hash for review_hash. Validate the accepted hash shape, preserving its actual canonical value. Terminal receipt is a distinct kind=window_terminal with schema_version2,namespace,operation_id,owner_window_id,registration_sha256(or null for proved no-purchase),refund_id/refund_event_id(or both null for no-purchase),financial_evidence_sha256,protected_after_sha256,disposition(refunded|no_purchase),observed_at. It is produced only from observed canonical outcomes. No preparation/opening receipt can masquerade as paid registration or successful release.

## Fixed receiver nested schema

Schema version2 is a new unimplemented contract; no issued version1 runtime record is silently upgraded. Top-level keys exactly schema_version,operation_id,action,pins and the action-specific activation or restoration only. action is open-window,activate,bind-purchase,inspect,close-window,deactivate. Only activate has activation; only deactivate has restoration; the other actions have neither. Runtime pins have exactly the eight keys above, not an undefined owner-window hash. Operation/window identity comes from the fixed mounted owner artifact and its persisted row. Reject wrong keys/scalars, noncanonical UUIDs, wrong hash length and boolean-as-integer before writes. Output cap1MiB accommodates complete opening baseline; overflow refuses without truncation and committed output is recoverable from inspect.

Exact custody/marker encoding: totp_secret is uppercase canonical RFC4648 base32 without padding, exactly32ASCII characters decoding20bytes; decoding then canonical re-encoding must equal the input. custody_sha256 is SHA256 of those32ASCII bytes, not decoded bytes, JSON quoting, lowercase text or a newline. For a test-only golden input AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA, the digest is 22a48051594c1949deed7040850c1f0f8764537f5191be56732d16a54c1d8153; this is synthetic test data, never a live credential. Scope/name/UUID/version/generation remain separately compared and are not smuggled into that digest.

The nonsecret Infisical create marker is secretComment exactly workspace_s1712:totp:<seller02UUID>:operation:<originalOperationUUID>:generation:<custodyGenerationUUID>, all literal ASCII punctuation/prefixes and canonical lowercase UUIDs. No alternate metadata marker format is accepted. connect_marker is exactly workspace_s1712:connect:<seller02UUID>:operation:<originalProviderOperationUUID>, stored as the value of Stripe account metadata key workspace_s1712_marker. Original provider operation means the durable first-creation operation, retained unchanged for later-window reuse. The account-create idempotency key is exactly workspace-s1712-connect-<originalProviderOperationUUID>; provider_request_sha256 hashes the accepted canonical full creation payload including this marker and the durably fixed ToS timestamp. Host/backend golden tests use fixed synthetic operation/generation UUIDs and verify exact bytes, not just matching prefixes; mutate each encoding/marker/name/scope/ID/version independently and require refusal. No real account or secret identity is generated to repair missing evidence.

activation keys exactly totp_secret(base32 ASCII32characters decoding20bytes),custody_id(UUID),custody_version(integer>=1),custody_sha256(SHA256),custody_generation_id(UUID),connect_account_id(valid acct_ ID),connect_marker(exact derivation from retained provider operation and fixed seller02),provider_request_sha256(SHA256). A reuse refers to the original immutable custody/provider generation, not the new window operation. Receiver validates digest and actual fixed-party/account ownership and uses existing TOTPService encryption. It returns only ciphertext SHA256, owned Connect PartyIdentity UUID and allowlisted readiness. No plaintext response. Host custody proof is trusted private-operator input; it is not falsely described as independent backend Infisical authentication.

restoration keys exactly before_state,expected_state,owned_connect_identity_id(UUID),custody_id,custody_version,custody_sha256,custody_generation_id. before_state has totp_enabled(boolean),totp_verified_at(null),totp_secret_enc(null),backup_codes_hash(null),stripe_account_id(null),stripe_onboarding_complete(string not_started/pending/complete),stripe_payouts_enabled(string false/true),stripe_details_submitted(string false/true). Activation requires false/not_started/false/false and the empty credential/account baseline. expected_state has the same nonsecret fields, except totp_verified_at is the actual nonnull activated UTC RFC3339 timestamp, totp_secret_enc is replaced by totp_ciphertext_sha256 and backup_codes_hash by backup_codes_sha256=null. The activation transaction invokes only TOTPService._encrypt_secret, sets totp_enabled=true, totp_verified_at to the transaction database UTC time, leaves backup_codes_hash=null and sets the four reviewed Stripe projection fields to the authenticated account readiness values. It never calls committing verify_setup, setup_2fa or backup generation. Return the actual stored timestamp/ciphertext discriminator after flush, and restore the exact baseline false/null values in the owned restoration transaction. c3 User has no mapped updated_at field; do not assign or fabricate it. created_at,last_login_at,profile and authentication identity fields are not fixture mutation targets. Qualification inspects actual database triggers; an additional implicit changed field requires an explicit reviewed before/expected extension rather than silent loss. Compare exact current state before restoring; no arbitrary patch map, SQL, user ID, DB URL, secret name or account target. Any additional modified source field must be explicitly reviewed in before/expected schema before build acceptance.

In e2e/account_pool.json add seller02 totp_secret_name exactly E2E_SYNTHETIC_SELLER_02_TOTP; preserve every other packaged identity. Validate exact unique seller02 and buyer01 UUIDs and secret-name fields. Missing, renamed, foreign or duplicate bindings fail before mutation; no inline secret value. Do not recreate the historical three-fixture baseline: accepted F1 has protected buyer02 and four actually seeded fixtures.

## Custody protocol and verified API limits

The established self-hosted authority is https://secrets.ai.market, backend project bd272d48-c5a1-4b52-9d24-12066ae4403c, environment test-env, path /, shared type, fixed name E2E_SYNTHETIC_SELLER_02_TOTP. Current environment source supplies test-env; older generic local-secops.md allows only prod/staging and is not an executable adapter for this fixture. Do not change or use its broad upsert/argv transport to work around this difference. The fixed fixture host adapter must use the already-reviewed private HTTP transport to the exact test-env scope with no redirects and bounded responses.

The published Infisical v3 raw API supports create/read and name-addressed DELETE but no documented atomic expected-version/UUID DELETE. This design performs no DELETE, PATCH or upsert, including recovery. A local lock does not exclude external writers. Retain the exact fixed-name TOTP in test-env after database restoration, with explicit inactive_with_retained_custody state. A successful deactivation means owned readiness restored, financial/resource disposition proved and custody retained; it never means the secret is absent.

First-ever custody creation requires authenticated404 at the fixed scope, no prior retained generation and no uncertain create. Generate20random bytes only in private process memory. Before POST durably record generation UUID, original operation UUID, exact fixed name, value digest and nonsecret generation marker. Create once; conflict/uncertain response reconciles exact GET scope/name/marker/digest/ID/version. Mismatch refuses, never overwrites. If an unposted generation lost its plaintext and authenticated absence is established, record it abandoned before a new generation; an uncertain POST requires exact reconciliation and cannot use absence alone to erase that uncertainty. Timeout/403/empty parse never means absence. Account creation has its separate stricter no-recreate rule.

After creation record actual immutable ID/version/generation marker/digest, re-read exact custody immediately before private stdin activation and again after projection before declaring active. A detected external change means recovery_required; retain all evidence and restore only exact owned database projection when safe. Concurrent provider writers remain possible: no claim that those reads provide exclusion. Since no delete/upsert/patch occurs, an unseen post-read writer cannot have its credential erased by cleanup. Future activation reuses the same retained generation only when original custody ledger and current exact UUID/version/digest/marker/scope/name agree and no prior operation is unresolved. Do not require or create a fresh secret per window. A deleted/recreated, missing or changed retained item refuses and preserves original lineage; do not silently enroll a new generation to repair drift.

Cleanup always leaves custody untouched. After exact DB restoration and normal financial terminal proof, matching retained custody yields inactive_with_retained_custody; missing/changed/unverifiable custody yields deactivation_incomplete with DB-restored status separately recorded. A provider GET denial does not undo the completed restoration or make a false full-success receipt. Interrupted retry continues remaining verification only, never re-enrolls or restores active just to match the audit. Tests prove zero DELETE/PATCH/upsert calls across success, failure, observed drift and an external write after the last GET, plus create-response loss and authenticated404 versus403.

Sources inspected2026-09-12: [Infisical create](https://infisical.com/docs/api-reference/endpoints/deprecated/secrets/create), [Infisical delete](https://infisical.com/docs/api-reference/endpoints/deprecated/secrets/delete), plus repository seed/host-seed.py and established infisical-secrets.md/local-secops.md. These document/API contracts are not proof of the installed provider response shape or write permission; read-only metadata validation and actual owned-operation receipts remain required.

## Durable audit and crash boundaries

Use private `.state/workspace-fixture.json`, schema_version2, mode0600 and a private parent directory. Reject symlinks, malformed/unknown records and mismatched ownership. Write a same-directory temporary file, flush/fsync, atomically replace and fsync its directory. Persist only under the inherited successful guard. Do not rewrite a corrupt record to unblock a run.

Required audit groups: operation UUID, phase, exact pins, owner-window reference, fixed fixture IDs, protected before-state hashes, enumerated restorable nonsecret seller02 state, secret ownership UUID/version/digest, stable provider marker/request identity and payload digest, exact owned Connect identity row, W resource/order references, last completed step and sanitized failure category. Values of credentials and provider response bodies are excluded. `preparing`, `active`, `deactivating`, `recovery_required`, `inactive_with_retained_custody`, `deactivation_incomplete` are the only audit phases. Archive every completed operation under .state/workspace-windows/<windowUUID>/; the active pointer cannot overwrite retained custody lineage or prior operation receipts. A new operation is allowed only after a closed prior window, fully restored readiness and exact retained-custody/provider reconciliation. A step records a durably completed operation, not an intention misrepresented as success.

First activation requires empty seller02 credential/Connect baseline and exactly one expected active auth_user PartyIdentity. It must not call `ensure_local_users()` or `establish_parties_and_assertions()`. Those broad seed helpers reset or assert unrelated fixture state. Capture all readiness fields actually changed, including timestamps: totp_enabled, totp_verified_at, totp_secret_enc (known null baseline), backup_codes_hash (known null baseline), stripe_account_id, stripe_onboarding_complete, stripe_payouts_enabled, stripe_details_submitted. Preserve Party, auth_user identity, company/profile fields and all seller01/buyer01 protected state.

Persist preparing and a stable provider creation payload, ToS timestamp and idempotency identity before provider creation. Enumerate the complete account result set for the exact fixture marker. Zero matches permits creation only when the durable record proves no uncertain previous create; one owned match permits validated reuse; multiple matches refuse. An interrupted or expired idempotency key never authorizes a fresh account. Reconcile the original owned account, retaining uncertainty when provider evidence is insufficient.

Accounts v1 has no documented livemode field. Bind TEST provenance to the exact approved test credential/version and authenticated account retrieval; reject explicit true when returned. Verify Custom type, exact marker, distinctness from seller01/platform, transfers capability and payout readiness independently. Do not repair a preexisting incomplete account by assumption. Events/charge/payment objects must explicitly assert TEST mode where supported by their schema.

Before Connect projection, validate and lock the exact Party/auth_user binding and prove the account has no foreign Connect identity. Existing upsert can reassign party ownership and must never be called before this proof. Projection and owned identity creation belong to one reviewed database transaction. The host records the returned owned row and ciphertext discriminator. If the database commit succeeds but host receipt persistence fails, resume by inspecting exact expected state; never enroll a new secret or move an account to make the audit fit.

Credential create/retrieve/retention recovery must bind the provider's actual UUID/version semantics in the final implementation. An uncertain create response must be reconciled against the same named operation and expected value digest. Only an exactly proved retained generation may be reused; never overwrite a preexisting or changed credential. Retain a recovery-required state when custody cannot prove ownership; a timeout does not prove absence.

## Deactivation and support procedure

1. Read status, operation identity, pins and the precise failed step. Retrieve the named evidence and compare current state. Missing audit plus gained readiness is drift, not a fresh activation opportunity.
2. Require the same persisted operation/window (including closed-window restoration recovery), successful shared guard and identical accepted runtime identities before a mutating retry. A hold deadline alone does not open the window.
3. Verify every W resource/access grant has the reviewed cleanup disposition and every new W order has a supported terminal refund/release state that cannot settle. Uncertain payout dispatch, provider refund or financial aggregate preserves all recovery evidence and refuses destructive cleanup.
4. Enter deactivating durably. Under the accepted database lock order, compare current seller02 and owned Connect identity to the audit. Restore only enumerated captured fields and remove only the exact newly owned Connect row. Do not call a broad identity removal helper unless exact-single-row ownership is proven in that transaction. Never delete Party/auth_user identity.
5. Never delete, patch or upsert TOTP custody. Verify and retain the original generation; matching custody permits inactive_with_retained_custody, while drift/uncertainty yields deactivation_incomplete with exact restored steps recorded. Retry does not re-enroll.
6. Retain TEST provider accounts with financial history and record their explicit reuse/disposition. Record inactive_with_retained_custody only after all required comparisons, closed-window evidence and owned readiness restoration pass. Preserve the audit and receipts for later diagnosis.

Default bin/seed, bin/reset and bin/up must refuse any present Workspace authority/audit/custody lineage, including inactive_with_retained_custody (an empty prepared directory alone is not an authority record), under their existing lock before secret/provider writes, S3 upload/cleanup, source replacement or container destruction. This conservative guard preserves the durable DB/file evidence required for future reuse; no automatic purge/reset is in scope. A later reviewed archival reset procedure is required before destructive reuse of this environment. Ordinary Workspace reactivation can reuse retained resources without invoking seed/reset/up. Placing the check only in seed.py is too late. Status must not silently reconcile, repair or normalize drift.

## Concrete revised implementation scope

Environment targets: bin/workspace-fixture(new),seed/workspace-host.py(new),seed/seed.py,bin/reset,bin/seed,bin/up,compose.yaml,tests/test-workspace-fixture.py(new),tests/test-seed-contract.py,workspace/README.md. bin/up and compose.yaml are explicit additions for the private read-only authority directory and early preservation guard; they do not repin/rebuild protected run25. No generic provider adapter/framework or modifications to the accepted money proof are implied.

Backend targets: e2e/account_pool.json,app/e2e/workspace_fixture_protection.py(new pure shared fixed-UUID helper),app/e2e/surgical_reset.py(exact four-identity unconditional refusal),app/e2e/teardown.py(same refusal before inventory/hitlist),app/api/v1/endpoints/webhooks.py(narrow imported W disjunct),app/services/workspace_refund_test_admission.py(new),app/models/workspace_test_authority.py(new),app/models/__init__.py(explicit model import),alembic/versions/20260912_002_workspace_test_authority.py(new),tests/test_workspace_test_refund_admission.py(new),tests/test_workspace_test_authority.py(new),tests/test_e2e_surgical_reset.py,tests/test_e2e_teardown_t2.py,tests/test_e2e_teardown_t2_real_pg.py,tests/test_webhook_audit.py(narrow dedup fixture compatibility),tests/test_e2e_stripe_isolation.py(narrow dedup sentinel compatibility),tests/test_webhooks.py(exact retained-checkpoint SQL expectation). This proposed amendment makes exactly seventeen backend targets; the additional three test files and the concrete discovery-reference schema amendment below require fresh unanimous review. New service/readiness/model/migration/concurrency tests are the concrete reviewed extension replacing the previous no-table option. Keep normal financial processor and S1681 admission/table/CLI untouched. MP may not invent additional schema or silently alter shared locks beyond this stated design.

Documentation targets carried/dispositioned from Gate1: specs/BQ-MONEY-PATH-TEST-ENV-S1656-GATE1.md records the required Workspace preservation/mount change without changing the genuine money phase contract; specs/SELLER-WORKSPACE-ACTIVATION-S1712-GATE1.md retains historical acceptance and appends this superseding implementation decision; runbooks/seller-workspace-operator-guide.md documents exact commands, artifacts, failures, retained custody and upgrade/recovery; runbooks/seller-workspace-live-release.md documents ordered runtime gates and evidence. The active operator guide's stale environment-review-open sentence is corrected with this substantive guide update. Historical authority/hash nits already corrected at0ec6 are retained rather than reintroduced. This Gate2 specification is the governing design appendix. Generated INDEX changes are limited to normal index output.

Mars owns overlapping F1 environment work and runtime repin/transition. Start from accepted environment43d67264 and backendc3, preserve all P/N seed/test changes and buyer02, and serialize shared webhooks.py integration. No copying old c6b fixtures over F1. Runtime installation remains separate after accepted build/Gate3 and actual owner transition.

Revised review request embeds exact repo/path/base/candidate/spec blob+SHA256, full revised text, all three Gate1 responses, all three rejected-round responses with this explicit disagreement disposition, accepted product/environment/authority records and actual final CI run tuples. Request full review of both new two-stage authority and retained lifecycle, not only a delta or prior-vote confirmation. No MP dispatch before all three revised reviews accept the exact candidate and scoped clarifications.

## Required implementation verification

Use isolated evidence, audit and lock roots; no test may fall back to live S1681 defaults. Test the actual wrapper with guard exits1/2, foreign lock, wrong token/ancestor and invalid pins and assert zero provider, credential and DB writes. Verify status creates no files. Exercise malformed/oversized/duplicate-key/trailing stdin and secret canary transport through the actual child invocation.

Use meaningful PostgreSQL tests for unique packaged fixture identity, foreign/duplicate Connect binding refusal, changed-secret/ciphertext refusal, locked before-state equality, first activation, repeat activation and interrupted commit/receipt recovery. Inject failure before and after each durable step. Provider stubs must model complete pagination, duplicate marked accounts, uncertain creation and aged-out idempotency without a new resource. Do not claim mocked capability success is real provider proof.

The narrow webhook allowance reuses existing signature, scope and idempotency admission and the accepted Mars processor. Test invalid signature, production shape, live mode, wrong/missing actor, device/agent binding, partial/zero/noninteger/mismatched amount, currency mismatch, ambiguous finance authority and missing/incompatible readiness. Verify intended rejection causes, not zero Transfer calls caused by unrelated missing prerequisites. Duplicate and failure/retry cases must converge to one normal effect.

Require direct settle() and actual scheduled caller regression through order_money_service.order_payout_eligibility and dispatch_order_payout for a still-confirmed transaction whose linked order is refunded/revoked; verify those actual order exclusions were read, not merely a stub in settlement_service.py. Use the accepted normal refund flow for transaction/payment/order/event/journal consistency. Never invoke a private handler or write financial statuses from seed to manufacture proof.


### Required acceptance cases for this revised authority

tests/test_workspace_test_authority.py must exercise actual PostgreSQL17 full-ancestry and repeat migration, exact constraints/indexes/triggers/readiness and wrong/disabled-trigger refusal; direct SQL UPDATE/DELETE of immutable opening/baseline/registration; one-nonclosed-window uniqueness; all invalid lifecycle transitions; genuine table/row lock waits and rollback. Use two real database sessions with synchronization barriers for writer already active, writer arriving during SHARE, preexisting SELECT FOR UPDATE, two same-window binders, timeout/deadlock recovery and an extra order after bind. Verify zero partial authority/financial writes on refusal and original registered refund recovery after the final proof is invalidated by that extra order. Tests must measure the actual bounds rather than sleep and assume ordering.

tests/test_workspace_test_refund_admission.py must prove old same-pair order, wrong/unissued/closed-for-new-binding window, substituted host baseline, changed canonical bytes/hash, wrong actor/pin/source/local-image/label/version/spec, P/N crossover and changed approval/publication/scope cannot gain admission. Include exact repeated registration, commit-success/host-receipt-loss, missing money state without auto-creation, revoked connection with intact registered authority, and real signed same-ID convergence after closing/recovery. Independently mutate local image ID and an OCI index with matching source labels to prove neither can substitute. Distinguish source/static schema tests from actual public signature/processor integration. Add real ASGI unchanged-status/response regressions for S1656 payin/purchase, S1681 negative/failed_run, unrelated synthetic refunds and real production-shaped traffic; assert zero optional W schema/mount lookups or W exceptions before the cheap TEST/event/actor rejection. Predicates are computed before the final synthetic filter, so that filter alone is not W isolation.

tests/test-workspace-fixture.py must exercise all actual command entrypoints, private read-only mount presence/absence/permissions, canonical operation-directory selection and traversal refusal, missing/forged/unverified owner evidence, guard exits1/2, retained-custody reuse/drift, all audit phases and restart/seed/reset/up preservation before any destructive call. Drive actual secret-canary stdin, fixed PYTHONPATH exception, package TOTP binding and response caps. Prove no DELETE/PATCH/upsert even when a separate provider stub writes after final GET. Tests never touch live S1681 directories, containers or provider credentials.

## Maintainer diagnosis map

These categories are the proposed stable wrapper/receiver errors; final implementation must bind exact exit codes and tests. Mutating commands return nonzero on every unresolved category, and no failure text includes input/provider body values. Record the category and the last durably completed step in the private audit only after guard success.

| Symptom/category | Evidence to read | Recovery action | Prohibited shortcut |
|---|---|---|---|
| owner_window_closed / guard_refused | Existing settlement guard result, Mars window receipt, protected run state | Continue read-only preparation; owner performs supported phase transition before same command retry | Editing the hold timestamp, ignoring exit2 or creating a second lock |
| runtime_pin_mismatch | Actual container local image ID/source label, versions.env and both accepted spec pins | Owner rebuilds/redeploys exact reviewed combination and verifies source/image equality | Hot source mount, trusting checkout HEAD or caller-supplied digest |
| protocol_unavailable | Accepted readiness exception category, Alembic ancestry, actual constraints and merchant chart | Diagnose the missing exact schema/chart requirement; apply reviewed product migration/cutover | Flipping a flag, manufacturing Payment or seeding finance from Workspace |
| audit_missing_with_readiness / audit_invalid | Read-only seller02 state and private audit file type/schema/hash | Preserve evidence; reconstruct only from real owned-operation receipts through reviewed recovery | Resetting seller02 or overwriting audit to make a fresh activation |
| custody_create_uncertain | Fixed-name GET status, operation marker, expected digest, actual UUID/version | Reconcile exact same generation; distinguish authenticated404 from denied/unavailable | Upsert, copying seller01 secret, or exposing value in a support ticket |
| custody_changed | Current UUID/version/digest/marker versus audit | Leave custody untouched, record exact restored DB steps and deactivation_incomplete; owner investigates external change | Name-only delete or pretending GET+DELETE is atomic compare-and-delete |
| provider_create_uncertain | Stable request/idempotency identity and complete marker search | Reconcile original owned account; retain uncertainty if no unique supported result | New account after idempotency expiry or first-match pagination |
| provider_not_ready / foreign_connect_binding | Authenticated TEST account fields and exact PartyIdentity ownership | Refuse projection; diagnose actual ownership/capability cause | Borrowing seller01/platform account or upsert that reassigns a party |
| db_commit_receipt_uncertain | Exact locked seller02/Connect state versus expected result and prior audit step | Inspect/recover the committed step, persist receipt, resume same operation | Creating another secret/account or repeating broad seed |
| w_cardinality_mismatch | Persisted backend opening baseline and complete current fixed-pair order delta under short table SHARE lock | Use Workspace preservation incident procedure; retain nonzero recovery_required_extra_order, original refund convergence and owner reconciliation/archival correction | Selecting the latest order or silently excluding an extra paid order |
| refund_pending / payout_unknown | Durable stripe_events state, order_money_states, real provider IDs and normal processor logs | Follow accepted normal retry/reconciliation; preserve financial and fixture evidence | Fabricating event, deleting pending authority, new Transfer key or treating timeout as absence |
| restore_state_mismatch | Locked current versus exact recorded owned readiness/ciphertext/identity | Preserve differences for support; apply only reviewed exact recovery | Broad identity delete, Party removal or overwriting unrelated fields |

Support bundle: include operation/window IDs; exact source/image/spec/version manifest; phase and last completed step; sanitized category; guard result; nonsecret custody/provider object discriminators; W receipt; protected before/after snapshot hashes; canonical product event/Payment/Refund/journal/order/transaction IDs; test/review/merge/deployment receipts. Do not include bearer download URLs, TOTP/passwords, raw Stripe/Infisical responses, database URLs, provider credentials or source data. A support engineer should be able to map every object to its owned operation and distinguish provider outcome from application commit.

Upgrade qualification is independent of an ordinary retry. Test loading all supported audit phases from the previous version, including provider response loss and DB-commit/host-receipt loss; unknown versions refuse without rewriting. Verify an unchanged operation cannot acquire a new resource or credential generation by upgrade. Validate public command arguments, byte cap and nested schema against archived golden inputs. Recheck backend encryption coupling, packaged account identities, provider API/SDK shape and exact normal refund admission/lock/readiness interfaces. Re-run real subprocess secret-canary and isolated PostgreSQL integration tests. Retain the prior compatible binary and manifest, but never use an old payment binary against newly written authority unless the accepted cutover explicitly proves compatibility.

## Release and upgrade sequence

Production stop/reconciliation/restore preparation is retained in SELLER-PRODUCTION-QUIESCENCE-RUNBOOK-S1712.md with current Railway inventory and authenticated API-schema receipts. Existing flags cannot quiesce old binaries. Current configuration does not establish a warm drain, and interrupted external payment outcomes require reconciliation. Incorporate the accepted exact cutover and source/image/adoption evidence before deployment; this read-only preflight did not stop or change production.

Preserve run25 environment03d3f30acf854537edd76fdb19e1cf521b577c9a and backendd766b8e4e66d7803ebe48f8a3132521abbebe957. Its genuine hold ends2026-09-13T23:41:11.491739Z, subject to Mars's supported lifecycle. Corrected money phases retain exactly P/N and genuine48-hour holds. W starts in its separate evidence namespace only after required phases and an explicit owner transition. Snapshot settled P and installation/identity state before W; compare after exact W purchase/refund/cleanup.

After accepted implementation, bind reviews, test results, source/image/spec identities and deployment receipts in one manifest. Refresh actual production identity rather than relying on September11 observations. Run the normal public product paths, buyer bytes/hash and entitlement denials; record outside verification. A seed fixture, test account, health check or unanimous design review does not prove full AWS/R2 production release.

For upgrades, inspect schema/custody/audit compatibility before replacing the helper. Refuse unknown audit versions instead of silently migrating active state. Review and test any migration separately on a copied isolated audit/database. Do not deploy old refund code over new financial states or downgrade purchased-rights schemas blindly. Rollback first closes affected new admission, preserves existing paid rights and uncertain operations, then uses the reviewed compatible source/image/configuration combination and reconciliation procedure.

## Verified historical source-hash recipe

At backendd766b8e4, read exact git object bytes for webhooks.py, split with keepends=True and join indices1699:1807. This includes the terminating newline of lines1700–1807 and produces SHA2569c29e6e300116aa5e2cdba8f8b0fd61cb3666450b6019a4980034b81cecee567. Removing that final newline produces881bd14b4d29e27a72b6b1a24c5f00f017dd881179514745e02ffd4238475a69. Earlier wording saying no trailing newline was incorrect. See SELLER-WORKSPACE-REFUND-HASH-METHOD-S1712.json. This is historical baseline verification, not a hash for Mars's future product implementation.

Installed custody read-only shape verification: SELLER-CUSTODY-EXISTING-SHAPE-S1712.json confirms an existing seller01 TOTP metadata response200, canonical UUID, integer version1, test-env and comment/metadata fields; no value retained and no mutation. SELLER-FIXTURE-CUSTODY-METADATA-S1712.json records authenticated lookups returning404 for buyer02 email/password and seller02 TOTP. This does not prove create behavior; DELETE is excluded by this revision.

## Historical MP235 dependency increment and F2

This paragraph retains the earlier diagnostic checkpoint only. Its pending implementation language is superseded by the accepted c3 product and environment dependencies at the start of this specification.

MP235 produced incomplete candidate88d6cdc7019cf2d7c99345fc7e7f7ef58bcdeb6a: money/refund services exist, but writer/dispatch/webhook integration and complete tests remain open. Mars4163 continues MP task2dfe810cb018, TS236, from that head with accepted F1. The installed journal entry trigger currently discards draft posting updates by returning OLD; F2 is unanimously accepted with its evidence mandate fulfilled at8b4a315ddfc5325959190ca6ae1b5e07fc915b35 (PR166 section15), preserving posted immutability while returning NEW for allowed UPDATE and OLD for DELETE. Vulcan independently confirmed production definition through BEGIN READ ONLY and tested exact old/proposed bodies in rolled-back session-temporary PostgreSQL objects. Require MP implementation of accepted F2, exact metadata/body/sole-attachment recognition, all preserved guards enabled O, real full-ancestry/repeat migration/readiness and posted-journal tests in the final product base. Reinstalling the defective old body is not an authorized rollback. The explicitly identified production receipt at03:50:26UTC confirms the installed old function; the separate03:51:37UTC aggregate found zero current journal entries, without establishing historical accounting completeness. This diagnostic proof is not implementation approval. The precise assessment and receipts are in outputs/SELLER-JOURNAL-TRIGGER-ASSESSMENT-S1712.md.


## Complete R3 panel disposition

R3 candidate87bc6d09b6551b2d0b0c381ffa8c78a4ba988658 was not implementation accepted. CC APPROVE_WITH_NITS; DeepSeek and GLM APPROVE_WITH_MANDATES with blocking MEDIUM findings. DS M1 and GLM M1 source-confirmed sibling teardown recursive deletion are corrected by the same shared pure UUID guard at both actual service entrypoints, with explicit targets/tests. GLM M1 also requires explicit synthetic public checkout arming: selected existing flag true and literal empty destructive allowlists only in the final reviewed TEST Compose/runtime manifest, independently checked at host/application boundaries and covered by actual fresh-start ASGI tests. A new narrower checkout flag is advisory and not selected; no unreviewed production flag change is implied.

DS LOW terminal unregistered-refund recovery is separated from registered extra-order convergence. DS NIT exact refusal carrier uses existing exception text/audit; the next review request includes runnable diff and exact-head actual CI tuples. GLM NIT all owner/runtime preimage/hash columns are explicitly immutable except the enumerated lifecycle fields. CC LOW unchanged ASGI non-W isolation remains mandatory; CC NIT signature now matches the S1681 event,db,normalized_stripe_account shape and ListingVersion binding has an explicit regression. CC operator-visible permanent refusal is documented for both account services, with separately reviewed archival reconciliation required for any future change.

Do not adopt CC's advisory guard-before-auth ordering: preserve ordinary authorization failure behavior and guard before destructive work after current validation. CC's claim of all reset vectors closed missed the actual sibling service; retain its full response unchanged but do not count its approval to override the two concrete MEDIUM findings. CC's suggestion that row triggers block Docker volume destruction is not technically supported; the environment clean lifecycle removes the volume, bypassing row triggers. No generic DB-trigger backstop is selected: it adds a broader table-protection policy beyond the concrete supported-path correction. No claim of immunity against arbitrary privileged SQL is made. GLM's advisory removing the W disjunct as rollback is not accepted while registered refunds remain outstanding; retain compatible admission/convergence or a separately reviewed reconciliation. All twelve historical full responses remain evidence; no prior vote carries forward. This complete revised contract requires fresh full acceptance before MP implementation.


## Final R4 acceptance and implementation dispositions — September12

The exact reviewed candidate is b444332af4ec86c4258ae62d7c0c1e12f3f2645d, base8f58ec24153beb4c7f467e4ce30e7363305b3380, reviewed spec SHA25638bc8a84c1458336300e32535243a1703fad08faa613eca0d0595ccbb46e386f (101555bytes). CC, GLM and DeepSeek each completed a fresh full APPROVE_WITH_NITS review with no HIGH/MEDIUM findings. The [exact owner decision](../runbooks/evidence/workspace-gate2-s1712/R4-OWNER-DECISION.json) SHA2568bb18a5a19e1fd10de5564b2a3b0cff4fd86b321817868f47a917eb816077136 binds all three full responses; [retained response index](../runbooks/evidence/workspace-gate2-s1712/R4-REVIEW-INDEX.json) supplies their exact bytes/hashes. This appended documentation records the accepted decision and selected nonblocking dispositions; its later recording commit/hash is distinct from the original reviewed candidate. No earlier rejected vote is carried forward.

Append admits_workspace_refund(event,db,normalized_stripe_account=stripe_account) after both unchanged S1681 disjuncts: negative, then failed_run, then Workspace. Preserve actual short-circuit behavior and cheap definitely-non-W rejection before optional W schema/mount lookup. Actual ASGI tests must prove unchanged S1681/S1656/unrelated-synthetic/production-shaped behavior and proper W readiness retry versus terminal unregistered suppression.

Retain exactly the three reviewed runtime_settings. Under this isolated TEST configuration, invoking e2e_armed_window_status with E2E_ARMED_AT absent produces ok=false,status=alert,failure_class=e2e_routes_over_armed,severity=P1. Explicitly retain and attribute this expected observable result to the owner-issued TEST operation; no suppression, relabeling healthy, production exception, widened arming window, added runtime setting or monitor code change is selected. A fresh-start exact-Compose configuration test must invoke the actual monitor and assert that result. This observation is not proof that the TEST runtime schedules/pages the monitor; do not invent that runtime evidence. Broad sysadmin health is never Workspace readiness or release-success proof. Production occurrences remain actionable under the existing production procedure.

Upgrade review inventories all existing E2E_TEST_ROUTES_ENABLED effects: checkout.py192, router.py148, synthetic_exclusion.py121 authenticated harness-bypass helper, and allai/agents/sysadmin/monitors.py718. No application caller of the bypass helper was found at c3, which is bounded source evidence and must be rechecked at upgrade. Never remove W admission or disarm runtime settings mid-window merely to clear the alert while original registered refunds or unknown provider outcomes remain; preserve normal compatible convergence and separately reviewed recovery.

Owner authorizes backend MP first from c3a62af8ccf60929a8e7cec3d62c9d30e0adefd2 under the existing Mars umbrella and sole Vulcan Workspace component ownership. Environment implementation follows accepted43d67264 against the actual resulting backend interface. The actual built Workspace BACKEND_RC_SHA change in versions.env is explicitly part of later environment source recording/Gate3, with final A2/G14/headchain pins; no guessed SHA, same-tree alias, oldc3 substitution or dirty protected-runtime repin. Full unanimous implementation Gate3, runtime owner transition, genuine phases and complete AWS S3/Cloudflare R2 release evidence remain required. Design acceptance is not implementation or release completion.


## Proposed canonical checkout correction — review required

This candidate corrects the two operative paragraphs identified in CHECKOUT-CORRECTION-INDEX.json. It requires fresh unanimous CC/GLM/DeepSeek acceptance before replacing the b444332/38bc instructions. R4 acceptance and its full responses remain historical evidence for the original exact bytes; they do not approve this change. MP247 continues independent work and preserves the original failed assertion. No implementation or environment acceptance is claimed.

### Actual evidence and affected dispositions

The supported FastAPI0.110.1/Starlette0.37.2 fresh main lifespan probe ran with ENABLE_CANONICAL_TX=true. Its probe code supplies no explicit canonical override, but the historical output does not independently attest absence of an inherited environment override; the revised final matrix must explicitly assert that absence. Both false and true E2E flags returned200 with a created order/transaction and controlled Stripe Checkout response. The false case fails the original403 assertion: one failed, one passed, full logs retained without rewriting. Checkout source is byte-identical at d766b8e4, acceptedc3 and MP246aca73: SHA2567bdfd4821db7043465d1f6f51893376147086557e4136f839c3789f58fd90ddf. Canonical early return at checkout.py149–171 precedes legacy190–195; config.py101 defaults canonicaltrue, accepted Compose does not override. The probe controls KMS/provider/telemetry and authenticated-user resolution, sets SKIP_SERVICES=1 to exclude background integrations, and forbids non-owned network access. Full raw logs retain lifecycle warnings as well as the assertions; this is not fleet startup, provider readiness, production configuration or an external paid purchase.

Original operative paragraphs at b444332 lines71/75 and their complete text are retained in the evidence index. The b444332 R3 historical disposition at line259 and the later56665b4 R4 flag-effect inventory at line274 must be read with this explicit correction: legacy flag gating was incorrectly generalized to default canonical checkout. The original reviews are not silently reinterpreted. Runtime literal true/[]/[], host/application comparisons, both account service guards, expected monitor alert and W-last short-circuit disposition are unchanged. The current candidate replaces only the erroneous branch rationale and its contradictory checkout test expectation, with an explicit branch/configuration/paid-flow evidence matrix.

### TEST-only authority and production isolation assessment

Proposed decision: preserve inherited canonical checkout behavior; the successor amendment below explicitly expands only the test targets from fourteen to seventeen and adds the named W discovery-reference schema invariant within existing W implementation files. Creating a canonical checkout/order is not issuing a Workspace owner window, binding an immutable paid registration, or admitting a TEST refund. W mutation/admission independently requires ENVIRONMENT=test, strict STRIPE_TEST_MODE=true, exact fixed pair and immutable purchased source, exact owner/runtime/protected/pin state and the required readiness. The existing webhook shape helper rejects prod/production environment, FRONTEND_URL=https://ai.market, RAILWAY_ENVIRONMENT or PRODUCTION markers, live event, connected-account scope and normalized Stripe account before optional W access. Signature/scope/dedup and both existing S1681 predicates precede W. This candidate changes none of those product checks.

A canonical order created with the flagfalse does not satisfy the selected Workspace runtime manifest: qualifying W operations still refuse mismatched settings and registered refunds remain retryable until the identical accepted runtime is restored. The E2E flag is neither a canonical purchase stop nor a universal production emergency switch. Owner-controlled TEST purchasing must stop operationally on closure/incident; extra orders invalidate complete cardinality and terminal success instead of receiving new authority. Existing production cutover/quiescence remains a separate procedure. Do not disarm mid-window to clear the monitor or use an extra order, retrospective binding or a forged baseline to manufacture success.

This reasoning depends on executed unchanged-behavior tests, not only source shape. Final integration must prove valid signed production-shaped and unrelated events take their existing responses/financial paths with no W schema/mount lookup or W exception; validate each production marker, live/account scope, S1656 payin/purchase and both S1681 negative/failed_run cases. Prove genuine W false-settings failure/restoration with same-event convergence and no added effects, and the full true-settings canonical paid lifecycle. This proposal does not certify all inherited synthetic checkout behavior safe in every production configuration; a discovered independent regression remains a reported dependency. If reviewers conclude the intended boundary requires a newly enforced canonical checkout gate, reject this narrow proposal and require an explicit reviewed target/behavior amendment before product edits. Do not merely change an assertion to obtain a green suite.

### Monitor, upgrade and continuation

Retain exactly E2E_TEST_ROUTES_ENABLED=true and empty reset/teardown allowlists. Invoke the real monitor in the qualified TEST startup and assert missing E2E_ARMED_AT produces ok=false/status=alert/failure_class=e2e_routes_over_armed/severity=P1. It remains expected and attributed in isolated TEST, actionable in production, never suppressed/relabelled healthy or treated as real scheduled/paged proof. Inventory canonical-versus-legacy checkout branching, router registration, authenticated harness-bypass helper and monitor on upgrade. No new runtime settings, widened arming window, default canonical change, protected-run mutation or production exception.

After unanimous acceptance, the owner binds the exact correction commit/spec hash/decision into continuation instructions and the affected B12 checklist. The failing original case remains retained as discovered evidence; a revised passing matrix receives its own exact source/log/node/runtime bindings. Backend/environment source and all other B01–B18 requirements, genuine holds, final owner/source/image transition and full AWS S3/Cloudflare R2 enabled release remain open. This documentary candidate does not authorize a duplicate MP, an environment dispatch, a merge to backend main or a live action.


### Adjacent N-to-W lookalike regression: no expectation waiver

The complete money regression run exposed `test_signed_n_authority_cannot_admit_other_scope[W]`: actual500 versus unchanged expected200. This is not one of the six inherited baseline failures. Its f1_pair retains a seller01-owned generic listing; the mutation changes only the N order/transaction actors to seller02/buyer01 and does not establish a purchased Workspace version/publication/authority. Exact fixed actor IDs alone do not prove a qualifying immutable W charge. Read ADJACENT-CLASSIFICATION-RECONCILIATION.json, the complete failed node/log section and immutable fixture source context.

The initially considered alternative was to declare every fixed-actor TEST charge ambiguous W and change this old test to500 when W schema is unavailable. That would deliberately broaden the optional-W dependency to non-W lookalikes. It is not selected here. The builder instead changed only the already authorized Workspace service's core discovery, checking listing seller ownership and matching human/browser transaction binding before optional W reads. The same unchanged adjacent node now passes in money-core-final; the complete run still has other failures and is not accepted. Existing publication immutability guards seller ownership for published Workspace listings; final tests must also ensure classification cannot silently discard a previously registered W charge when a later binding is damaged.

The c6bae proposal retained fourteen backend targets. The successor below explicitly selects three different legacy webhook test compatibility targets, for seventeen total; tests/test_refund_webhook_protocol.py remains outside the target list and its old200 suppression expectation is not waived. Full final qualification must retain that exact node and prove zero optional-W schema/mount lookups for this forged non-W case, alongside genuine immutable-W and existing registered/claimed-W unavailable500 with durable failed event and same-ID recovery after readiness restoration. A fully available authority store with exact absence of registration remains terminal200 suppression; missing readiness or binding uncertainty for a genuine/registered W charge must not be converted to completed suppression. N authority never authorizes W. These distinctions are tested within the existing Workspace test targets and unchanged adjacent suite, not by monkeypatching away the W predicate.

This observed repair is factual context for the scope assessment, not Gate3 acceptance of evolving MP247 source or a newly accepted classifier shortcut. Reject any final implementation that makes a retained registered refund disappear into terminal suppression, broadens unrelated/production dependencies, or claims all failures inherited. The complete raw initial and rerun commands/logs/XML are durably retained at the paths and hashes in the evidence receipt; the full relevant testcase elements and failed log section are byte-exact in this candidate. That earlier c6bae reconciliation proposed no product writer, S1681 authority table/CLI or additional test-file scope. The successor below explicitly adds the three named compatibility test files and the W discovery-reference schema behavior; normal product writers and S1681 authority remain unchanged.


## Successor revision after the complete c6bae panel — proposed, not accepted

The complete c6bae734aa7faf5645f3cd724ab3460c7ac2f49f panel is not unanimous: CC and GLM APPROVE_WITH_NITS, DeepSeek REVISE with one MEDIUM for registered W binding drift before optional authority lookup. The exact owner decision CHECKOUT-R2-OWNER-DECISION.json (SHA2565aa327617dfea7fe1381d8a8c1032266fc4d6951290d45c67813688ff00939c8) and all three complete responses are retained with this successor. CC/GLM treat the drift as a final implementation obligation; DeepSeek requires a concrete design invariant first. This revision resolves the shared factual defect with the explicit reference below, not majority vote, a source-only assumption or a waiver. All eighteen historical full responses must accompany fresh full CC/GLM/DeepSeek review; no prior vote accepts the successor bytes.

The prior candidate remains historical evidence. Its early Transaction/Listing join is not an accepted final implementation: mutable shape must not discard a registered refund. The actor-only authority-first recommendation is also not selected because it alone breaks the unchanged forged-N zero-optional-dependency case. The concrete protected Order tuple reference below preserves both obligations. The wording in the preceding adjacent assessment describing the MP247 repair is an observation of partial code, superseded as implementation design by this section.

MP247 ended normally at e621c760509bde085d5ef784bb14ffa90810d92b and remains incomplete:1990 unique cases,1968passed22failed. S1712 and Mars separately verified final identity, retained source/evidence and disposable resource absence. The next backend build starts from that exact head, retaining all B01–B18 and missing old/other-window/coherent identity/private-file-race requirements. Detailed mandatory continuation and diagnostic runbook supplements are retained with this revision. No environment dispatch or Gate3 acceptance follows from terminal clean_exit.

Review precision: the c6bae exact base-to-candidate diff--check exits2 with nine trailing-whitespace findings in two byte-exact captured output files; an unqualified clean-working-tree check exits0 and was not the review-range result. Preserve raw bytes and report exact commands/results. GLM said eight findings; the actual output contains nine. CC's explicit inherited canonical-env claim is not established by the derived command receipt, which has no environment field: retain measured true versus absent-override proof distinction. The final configuration matrix now names the superseded false403 node, both fresh route-import states and both canonical mixed-synthetic directions. No production exclusion is assumed from these documentary findings.

### Protected registered discovery tuple and required qualification

This is a concrete successor design proposal, not an accepted implementation instruction. The c6bae734 panel is complete: CC and GLM approve with nits; DeepSeek requests revision with one MEDIUM. Do not count votes or carry a prior approval to these bytes. All three agree that genuine registered refund binding drift cannot become terminal suppression, and all three retain the forged N zero-optional-dependency requirement. This proposal resolves that shared requirement with an explicit database invariant rather than treating the current mutable join as correct.

## Minimal stable discovery tuple

The cheap TEST classifier will query only Orders joined to Listings, using the order's fixed seller02/buyer01 pair and exact PaymentIntent, and the listing's seller02 ownership. It must not filter on Transaction seller/buyer, api_key_id, buyer_type, origin, surface, current credential state, version/approval/source content or other mutable validation fields before authority lookup. Those are compared after exact provider-ID registration resolution and must raise the existing retryable authority exception on mismatch.

Protect the fields that remain in early discovery using an explicit new composite reference. In the existing W migration create a named nondeferrable UNIQUE constraint on orders(id,seller_id,buyer_id,listing_id,stripe_payment_intent_id), named uq_s1712_order_discovery. The existing orders.id primary key makes this uniqueness redundant for ordinary rows; its purpose is an exact PostgreSQL referenced key. Add fk_s1712_registered_order_discovery, a validated MATCH SIMPLE, NONDEFERRABLE composite foreign key from workspace_refund_test_authorities(order_id,seller_id,buyer_id,listing_id,payment_intent_id) to that orders tuple, ON UPDATE RESTRICT ON DELETE RESTRICT. All five registration fields are already required non-null and registration UPDATE/DELETE is already forbidden. Preserve existing independent FKs; do not replace or loosen them.

This prevents changing any referenced Order discovery value, including setting one to NULL or deleting the order, after registration. It does not rewrite Order rows at bind, create an extra authority, or change ordinary status/payment processing. Once a W registration exists, its discovery tuple remains protected through bound/closing/recovery/closed states; no release of the reference at terminal close. W bind already requires a valid immutable SellerListingPublication for the exact listing and purchased version. The existing publication guard prevents that published Listing seller from changing; the registration's Listing FK preserves its referenced row. Both guarantees are load-bearing and must be verified from actual catalog/source plus SQL mutation tests. Do not infer Order immutability from the publication guard alone.

Keep the existing short StageB orders SHARE NOWAIT before row locks. Inserting the registration acquires the normal referenced-key protection within that same bounded transaction, atomically with bound state. Never UPDATE orders, upgrade to a new table lock, call a creating money helper or perform provider IO in that transaction. Composite FK concurrency may add normal referential-integrity waits; retain lock_timeout250ms, statement/idle2s and host5s bound, zero partial effects and exact retry. Test concurrent discovery-field writer versus binder, including the attempted update after bind commits.

No generic hand-written core mutation trigger is selected. No normal money writer, Order model source file or S1681 authority table is added to implementation scope. The existing W migration/model/service/tests implement the named schema/reference and catalog recognition. SQLAlchemy W metadata may declare its composite reference to existing Order columns; the parent unique key is installed by the migration. Full actual migrations, not metadata.create_all on an unqualified parent schema, govern this protocol. If implementation requires another file or any behavior beyond this exact schema amendment, return the concrete additional scope for review before editing it.

## Authority precedence after stable classification

Production/live/account context and definitely unrelated core traffic still reject before optional W schema or mount lookups. The unchanged forged N test has a seller01 generic listing even though Order/Transaction actors were changed: the core Listing-owner discriminator rejects it without W access, preserving HTTP200 and its original test expectation.

A matching fixed-pair, seller02-listing charge reaches W readiness and exact provider-ID authority resolution before mutable Transaction/product validation. If readiness, current runtime, mounted authority or bindings are unavailable/mismatched, raise WorkspaceTestAuthorityUnavailable through the existing signed critical-event rollback/durable-failed HTTP500 path. If the compatible current authority is fully available and no exact registration exists, retain terminal200 suppression. If registration exists, validate every retained binding and preserve same-event refund convergence. Do not use transaction shape to turn a retained registered charge into unrelated traffic. Normal signed amount/scope rejection and existing financial ownership remain unchanged.

Schema readiness must recognize the exact new parent constraint and referencing FK: relation/column order, referenced table/key, update/delete action, nondeferrability, validation, uniqueness and enabled internal referential enforcement, together with the existing W registration and publication guards. Do not accept an unknown same-name object or hash learned from live schema. Update the frozen expected catalog contract and fresh/repeat migration tests. This is an explicit core constraint/index behavior extension, not a claim the earlier14-target design already implemented it. Even though files remain within the W migration/model/service/tests, full fresh design review is required before implementation.

Ordinary application SQL cannot mutate the protected tuple after registration. Privileged disabling/dropping of enforcement is schema corruption, not a supported binding mutation or sanctioned recovery method. Readiness must refuse a qualifying charge under every individually tested missing/disabled/misbound invariant. Tests that deliberately bypass constraints to manufacture corruption must label that setup and restore/verify the guards before ordinary admission evidence. Do not claim immunity to arbitrary superuser destruction or accept silent registered loss as a recovery strategy. Owner diagnosis preserves all records and restores only through reviewed schema/reconciliation procedures.

## Required decisive execution matrix

- Actual PostgreSQL17 registration succeeds against exactly matching five values. Each mismatching child value refuses, without partial bound state. Registration UPDATE/DELETE and terminal receipt immutability remain unchanged.
- For each of the five referenced Order values, attempt a real SQL update to another value and to NULL, plus deletion, after valid registration. The database refuses and original values/authority remain; the subsequent original signed refund still converges exactly once. Repeat relevant state transitions through closed to prove no release of protection.
- Attempt Listing seller change and deletion on the genuinely published registered listing: existing guards/FKs refuse. Do not rely on a generic seller01 listing without publication to demonstrate this invariant.
- For every mutable Transaction discovery field previously used in the early join (seller_id,buyer_id,api_key_id,buyer_type,origin,surface), perform actual database mutation after valid registration; the signed refund must produce durable failed500, no premature financial effect, and the same event ID must converge exactly once after restoration. Other canonical version/approval/publication/connection/Payment bindings retain their full required mismatch/refusal matrix, with SQL-level refusal where their immutable guards apply.
- The unchanged seller01 generic forged-N case remains200 with zero optional W schema/mount calls in a legacy database with no W objects. Production-shaped, ordinary non-W and both existing S1681 paths retain zero optional dependency before rejection. Normal unrelated order identity/status writes and normal financial workflows remain executable with the W migration installed, and legacy source does not suddenly require W objects.
- Fresh full ancestry and repeat migration, exact parent/reference/index/trigger catalogs, missing/disabled/wrong-binding readiness, duplicate binders and concurrent identity-writer ordering must execute on supported runtime and owned PostgreSQL17. Every failure retains raw evidence. No live/protected/runtime migration is authorized by this proposal.

## Additional closure requirement, unchanged intent made explicit

B13/B14 already require no pending/unknown result before immutable close. Final source must not infer this from one succeeded Refund and one completed/applied event after filtering. Under existing bounded money/order locking, enumerate and classify all relevant retained refund and event records, refusing any unresolved/pending/ambiguous related work before terminal receipt/state. Test an otherwise valid full refund plus a coexisting pending Refund, and an otherwise completed event plus unresolved related event. Refusal retains original authority/history and no closed_at/terminal receipt; valid explicit reconciliation permits normal close and exact repeat. If a database invariant prevents a combination, prove its exact SQL refusal rather than assume it. Normal processors remain sole financial writers.


## Exact catalog enforcement and finite closure observation

The existing e621 CATALOG_SQL covers W tables and excludes internal triggers; merely changing its expected hash is insufficient. Extend its fixed projection to the named public.orders parent UNIQUE constraint and supporting index, validating exact column order, index uniqueness, indisvalid/indisready and immediate enforcement. Include the actual named composite FK on W registration, convalidated, condeferrable=false, condeferred=false, MATCH SIMPLE, RESTRICT update/delete actions, and the corresponding referenced/referencing internal RI triggers with enabled state and exact attached constraint/relation/function identity. Normalize unstable object OIDs and generated internal trigger names through the constrained relation/constraint identity rather than hardcoding one database's OIDs. Include the exact existing published-Listing seller enforcement and immutable publication attachment/body in the fixed required catalog; preserve all existing W/money invariants. Expected facts and canonical projection are frozen from reviewed isolated PostgreSQL17 qualification, never learned at runtime. Test disabled RI enforcement, unvalidated or wrong-action FK, invalid/not-ready parent index, altered relation/column order and missing/disabled publication guard. Prove accepted money readiness fingerprint remains unchanged by this s1712-named parent key.

Bound privileged simultaneous removal of referential/publication enforcement plus rewrite/destruction of the identifying core evidence explicitly: normal SQL cannot cause that state under the accepted invariant, and the classifier does not claim to reconstruct destroyed evidence after superuser schema tampering. Single missing/disabled enforcement under an otherwise identifying qualifying W charge must refuse readiness. Actual SQL mutation-refusal and signed mutable-field restoration tests remain mandatory; this limitation is not permission to silently skip a feasible application mutation.

For terminal closure, relevant stripe_events are event_type=charge.refunded records where refund_order_id equals the registered order, refund_payment_intent_id equals its PI, refund_charge_id equals its charge, OR signature_valid=true and the retained signed payload_json.data.object.payment_intent or .id equals the corresponding registered provider identity. Use OR inclusion so a one-sided or contradictory match cannot disappear from scrutiny. The payload relation is required even when all normal refund linkage columns are NULL: a signed failure before normal admission already retains payload_json. A contradictory pair or malformed matching signed event is ambiguous and refuses closure. Do not consult arbitrary unsigned metadata or require provider IO to discover this set.

Under the existing bounded Order/financial row locks and READ COMMITTED transaction, perform a final read-only observation of every related committed event, sorted by stripe_event_id, bounded to4096 plus a sentinel refusal. Take no event-row lock after Order/money locks; preserve normal event-to-money lock order. All relevant retained Refund rows must have resolved known outcomes, exactly one normal full applied Refund must account for the registered amount, and every related event must be completed/applied with consistent canonical binding and no unresolved/reconciliation state. Multiple valid applied replay event IDs may refer to that same one Refund; they cannot create additional financial effects. For the initial terminal refund_event_id select the smallest verified stripe_event_id under PostgreSQL COLLATE "C" ordering from the complete resolved observed set, and retain it unchanged on every retry even if later replay IDs sort earlier. This is a deterministic evidence representative, not an invented originating-event relationship: the Refund model has no event_id. Each selected/related applied record must resolve consistently to the same registered order/PI/charge and the same sole applied Refund.stripe_refund_id and financial outcome; a contradictory or incompletely proven association refuses as ambiguous. Qualification retains the complete observed set and proves multiple valid separately delivered events still produce one financial effect and one unchanged closed receipt. A positive filtered subset is not proof that every related record is resolved.

The closure observation boundary is exactly the rows visible to that final READ COMMITTED statement, while financial-effect writers are excluded by the held money/order locks. An uncommitted or later inserted dedup event is a late delivery, not a claim that all future events are prevented; closed retained authority must still permit its normal exact replay convergence. Capture the statement's observed nonsecret event IDs, binding/status/phase facts and timing in qualification evidence alongside terminal receipt; no new root receipt key or claimed timeless event-exhaustiveness is implied. Tests must use synchronization barriers for: an applied original plus a failed-pre-admission signed sibling committed before final observation with NULL linkage (refuse), a pending Refund plus applied original (refuse), concurrent normal admission/effects (bounded refusal or verified resolved observation), restoration/reconciliation with same-event success, and a valid late replay after closed (converge once, immutable terminal receipt). Any stronger terminal claim requires a separately reviewed coordination mechanism, never reverse-order event locks or optimistic assumption.


### Explicit three-test compatibility scope amendment

#### Source-grounded exact repair and evidence

PROPOSED ONLY. Not applied, dispatched, accepted or added to any inherited exception. Keep the separately dispatched c6bae734 checkout candidate and7b353083 packet immutable. Review this explicit additional test scope before MP changes any file outside the original14 targets. Mars owns dispatch and next builder.

## Decision requested

Permit exactly three additional backend test targets at the verified e621c760509bde085d5ef784bb14ffa90810d92b continuation base: tests/test_webhook_audit.py, tests/test_e2e_stripe_isolation.py and tests/test_webhooks.py. This changes total backend target scope from14 to17 only if explicitly accepted. Existing14 obligations remain unchanged. No app, normal money processor, webhook implementation, migration, configuration, provider or environment target is added by this amendment.

The complete proposed three-file diff and originals/proposals are retained here. It repairs stale database-result doubles and a stale expected SQL string while preserving the existing tests and every existing behavioral assertion. It is a concrete minimum patch for review, not a claim of executed success. If executing it exposes further stale fixtures or a real defect, retain the failure and supply exact source/intent disposition; do not broaden scope or weaken expected behavior automatically.

## Exact changes and rationale

In tests/test_webhook_audit.py, change the11 explicit accepted dedup tuples from (event_id,) to (event_id, None), retaining their original event IDs. The shared helper contributes to more than one of13 failing cases in this module. None represents no prior error_message. Keep duplicate fetchone=None branches unchanged; do not alter mapping/handler query results or the general _result helper. The accepted immutable c3 and current e621 webhook both RETURN stripe_event_id,error_message and inspect index1 for fulfillment_pending before any W branch. A one-column result cannot represent that query.

In tests/test_e2e_stripe_isolation.py, change only the _Result dedup sentinel tuple to ("evt_ok", None); preserve all other result behavior. Keep public HTTP200 suppression, exact suppression reason, no handler call, durable commit and e2e_scope_suppressed assertions. Do not stub Workspace admission to force a result or convert a synthetic event into ordinary processing.

In tests/test_webhooks.py, update only the exact whitespace-normalized SQL expectation in test_payment_succeeded_billing_failure_leaves_failed_checkpoint to include payload_json, signature_valid, cast(:signed_payload as jsonb), :signature_valid and the conflict-update condition protecting completed/applied rows. Keep full exact-string comparison and all existing HTTP500, finance_pending, event ID/error, commit/rollback, no delivery, subsequent successful same-event retry and completion assertions. Do not remove the SQL assertion, replace it with a permissive substring or derive expected SQL by calling the implementation.

Signed-payload retention and applied-refund protection are accepted money semantics. No handler/dedup/source rollback is permitted to satisfy the old expected SQL. Normal signature/scope ordering, duplicate behavior, retries and refund effects retain their independently required actual-database protocol evidence. These mocked tests do not replace actual signed PostgreSQL tests.

## Exact evidence and limits

S1712 executed the exact15 newly observed failing nodes at immutable c3a62af8ccf60929a8e7cec3d62c9d30e0adefd2. All15 node identities and messages match the MP247 webhook-regressions-final result:14 index1 tuple errors and1 SQL mismatch. All three test files are byte-equal across that base and e621. The process denied all DNS and socket connections, used FastAPI0.110.1/Starlette0.37.2/SQLAlchemy2.0.25, no database/container resources, and verified source hashes unchanged. Existing tokenizer data with pinned SHA was copied to the isolated audit directory. The first collection-only network-guard failure remains separately retained; it is not represented as executed tests.

Full baseline log, XML, receipt,15-node identity/message reconciliation and reference XML are retained beside this package, with hashes in manifest. Full MP247 regression log/XML and complete original baseline evidence are also referenced. This proves pre-W provenance; it is not a release waiver or an addition to the six accepted historical exceptions. Broad regression qualification remains incomplete until this explicitly accepted repair is actually executed and any newly reachable failure resolved.

## Required validation after acceptance

Use the existing supported runtime and network-isolated harness. Run the three complete test modules, preserve all collected node identities and complete outputs, and explicitly confirm all15 prior failures now execute their original behavior assertions. Re-run relevant actual signed money/refund/dedup and W regression cohorts against exact final source, with fresh owned PostgreSQL17 resources where those tests require them. Preserve signature failure refusal, duplicates processed once, failure checkpoint/retry convergence, payload hashes, handler order, critical500/noncritical200, synthetic suppression and applied-refund immutability. Preserve the unchanged forged N-to-W test and genuine registered W recovery matrix. No xfail/skip/deselection or exception expansion is authorized.

Record original and final hashes, exact17-target diff only if amended, all command/runtime/source/node identities, original failures, final results and resource absence. Full combined backend/environment Gate3 remains required. Neither approval of this scope proposal nor passing mocked tests permits runtime changes or release.


## Full a4 panel disposition and execution-grounded clarification

This successor replaces the a4e06c08 documentary proposal only after a fresh full panel accepts this complete contract. The a4 panel ended CC/GLM APPROVE_WITH_NITS and DeepSeek REVISE; the owner decision is CHECKOUT-R3-OWNER-DECISION.json and all three complete responses are retained. No old vote carries forward. Backend remains e621c760 partial and unaccepted. The original minimum three-file patch remains historical evidence, not a sufficient repair: its exploratory execution exposed eight further failures. The exact scope below supersedes its claim that only13 mechanical edits will suffice. All original14 targets plus exactly three named test modules remain proposed17 targets; the migration already belongs to the original14 and its new core key/FK behavior remains expressly proposed. No new normal money writer or product/configuration target is added.

### Post-authority Transaction comparison and exception routing

The cheap classifier queries only Order and Listing as previously specified. After exact PI/charge registration resolution, lock existing rows through lock_existing_workspace_money_sync, then run _validate_registration including _purchase_binding. No mutable Transaction comparison may return terminal False before or after registration resolution. This is an explicit implementation requirement, not a claim e621 already removed its defective early Transaction join.

| Transaction field | Exact expected comparison after registration | Existing e621 validation and failure |
| --- | --- | --- |
| seller_id | Equal to locked Order.seller_id, which must equal fixed seller02 and retained registration.seller_id | lock_existing_workspace_money_sync calls validate_order_binding; inequality raises OrderMoneyConflict |
| buyer_id | Equal to locked Order.buyer_id, which must equal fixed buyer01 and retained registration.buyer_id | Same validate_order_binding comparison and OrderMoneyConflict |
| api_key_id | is None | _purchase_binding raises WorkspaceTestAuthorityConflict(workspace_actor_binding) |
| buyer_type | exact human | Same _purchase_binding conflict |
| origin | exact listing_purchase | Same _purchase_binding conflict |
| surface | exact browser | Same _purchase_binding conflict |

These existing source comparisons are independently visible in order_money_service.py validate_order_binding and workspace_refund_test_admission.py lock_existing_workspace_money_sync/_purchase_binding/_validate_registration. _validate_registration compares exact retained receipt bindings and registration scalar columns; validate_order_binding also checks reverse order_id, listing_id, amounts, fees, PI and currency. Do not replace these with permissive string coercion or merely compare two equally changed fields. The fixed Order pair and immutable registration remain independently checked.

The enclosing admits_workspace_refund try block preserves WorkspaceTestAuthorityUnavailable and wraps OrderMoneyConflict and WorkspaceTestAuthorityConflict as WorkspaceTestAuthorityUnavailable(workspace_authority_unavailable). The webhook must retain a durable failed event and return500 with no financial effects; it must not convert that exception into ordinary synthetic suppression. Prove each of the six real Transaction mutations separately in PostgreSQL, including NULL where allowed and otherwise-valid alternate values, using a genuinely paid registered published W purchase. Preserve failed row/effects snapshot, restore only the mutated value, resend the same signed event ID and prove exactly-one normal refund convergence. No test stub replacing the comparator/authority exception is proof. All other retained mismatch and immutable-guard matrices remain mandatory.

DeepSeek F1 correctly requires this behavior but its claim that the design names no post-resolution comparison conflicts with the a4 text and existing source helpers. The explicit table removes the ambiguity; it does not waive the vote or the actual drift tests.

### Exact native-FK NULL semantics and fresh/repeat preflight

PostgreSQL MATCH SIMPLE exempts a referencing child key containing NULL. All five W child columns are NOT NULL, so that exemption is unavailable. The parent Orders PI remains nullable for unrelated unpaid orders. A registered non-NULL tuple prevents changing its referenced parent to NULL through ON UPDATE RESTRICT. Do not add global Orders PI NOT NULL. A proposed child with non-NULL PI cannot reference a parent whose PI is NULL; a child with NULL PI is itself refused by NOT NULL.

The owned PostgreSQL17.11 rollback-only toy probe retained as PG17-FK-PROBE-* records22/22 expected outcomes: all five parent-value changes23503; parent-to-NULL23503 except primary-key id23502; all five child-NULL23502; parent-delete23503; NULL parent/non-NULL child23503; NULL parent/NULL child23502; valid registration and unrelated nullable/identity/status writes00000. SQL SHA748bbb7782975033296ad141cbe0b786c6799f0f653c26e896acdf8378e8f1cb; log SHA1108cbc59a545cbbb8c3da5dcdd476bdb20558ec7f067c9fd26239fde248ff28. It used an owned network-none container without published ports or volumes, subsequently removed and independently absent. The first startup readiness race is retained separately and cleaned. These are native semantics, not actual product migration, registered signed refund, catalog qualification or Gate3 proof. Full actual-schema tests remain required. DeepSeek F2's parent-NULL escape premise is disproved; no new global constraint is justified by it.

Select fail-closed named-object preflight for both uq_s1712_order_discovery and fk_s1712_registered_order_discovery inside the existing W migration. Before creation, query catalog by exact schema/relation/name; an absent object is created with the already specified definition. An existing object is a no-op only after exact canonical definition, column order, referenced/referencing identities, validation/nondeferrability, RESTRICT/MATCH SIMPLE actions, supporting unique index validity/readiness/immediacy, and enabled RI trigger attachment are verified. A same-name wrong object or conflicting index name aborts the transaction without dropping, overwriting, silently repairing, or accepting it. Do not assume ALTER TABLE ADD CONSTRAINT IF NOT EXISTS exists. Do not use exception-swallowing duplicate-object handling. After creation verify the same exact facts. Preserve the independent existing FKs and immutable guards. Run actual fresh ancestry and a direct repeat of the W upgrade body, compare full frozen catalogs and retained data, and individually test name/type/column/action/index/enforcement collisions. The second successful upgrade must be a no-op; an Alembic head no-op alone is not a repeat-body test.

### Migration availability, closure status and final qualification

The selected parent UNIQUE is ordinary transactional ALTER TABLE ADD CONSTRAINT, not a concurrent-index variant. Name its ACCESS EXCLUSIVE orders lock and the child/reference locking DDL in the owner cutover maintenance sequence. Quiesce normal writers first under the existing separately authorized cutover; use bounded lock/statement waits and transactional abort on failure, never migrate the protected run to collect this evidence. Qualification records exact PostgreSQL version, representative table row count, index size, lock modes/wait/build duration, actual timeout refusal and rollback, and normal unrelated nullable-order identity/status/financial writes after installation. Retain the before/after accepted money-readiness canonical projection and digest and prove byte equality; a changed money fingerprint is a blocker requiring reviewed disposition. Prove the actual published-Listing seller update and referenced-row delete refusals. These gates are mandatory before implementation acceptance and separately before live cutover; the toy22-case probe cannot substitute.

For every relevant applied StripeEvent, closure requires the conjunction status='completed' AND refund_phase='applied' AND processed_status='processed', plus all previously specified exact identity, signature, sole Refund, resolved outcome and no-reconciliation checks. These three fields match finance.StripeEvent and the accepted refund_processing_service success writes. A relevant event with completed/applied but processed_status=failed, received or ignored refuses; test each contradictory field independently and restoration. Do not filter such rows out of the relevant-event union. Preserve the finite READ COMMITTED cutoff,4096+sentinel, same Refund across replay IDs, no event lock after money locks, and immutable deterministic terminal representative.

Preserve all1990 prior unique node identities in the final reconciliation inventory; map the explicitly corrected false403 node to its retained failing evidence and new canonical200 node rather than demand the obsolete expectation pass. An inventory does not replace missing required tests. Prior direct/scheduled delivery tests prove revoked-order exclusion only. Add actual normal full-refund through the accepted processor and status-sync trigger, verify the linked Transaction becomes refunded, and prove direct and actual scheduled exclusion. Also exercise normal partial-refund behavior: the accepted trigger deliberately preserves the prior Transaction state, so measure exclusion where required by the delivery contract rather than assume full-refund state propagation. Where the original matrix requires a still-confirmed/full-refunded combination, identify it explicitly as an adversarial database fixture and preserve separate normal-flow evidence. Check actual trigger catalog/body and fresh database outcomes. Do not relabel revoked proof as refunded proof, manufacture still-confirmed after normal full refund and call it live proof, or change the normal money writers.

### Eight newly reachable legacy failures: exact scope and behavior disposition

The retained WEBHOOK-MINIMUM-PATCH-EXPLORATION files contain the exploratory exact15-node execution against c3 with proposed test copies, supported runtime, denied network, unchanged source, and7pass/8fail. It ran external copied files, so it does not establish repository conftest or complete-module qualification. No failure becomes an inherited exception. The following concrete repair scope is proposed within the same three test modules; original node identities and product outcomes remain required.

| Existing failing node | Observed result after minimum patch; source-grounded cause | Permitted test repair and required preserved outcome |
| --- | --- | --- |
| test_payment_succeeded_billing_failure_leaves_failed_checkpoint | Retry reaches a second stale exact SET processing assertion; accepted dedup uses CASE preserving completed charge.refunded rows and returns two fields | Update the independent exact normalized retry SQL expectation to the accepted CASE, signed payload and WHERE failed OR charge.refunded statement, with explicit two-field RETURNING. Retain500/finance_pending/error/commit/rollback/no-delivery and successful same-event retry completion; no implementation-derived expected string |
| test_09_checkout_completed_order_paid |500 RuntimeError after paid flow; _async_db_gen is a single generator returned repeatedly while webhook uses separate paid and fulfillment acquisitions | Supply a fresh async session generator on every get_async_db call and explicit async fulfillment service result. Assert OrderService.mark_paid awaited exactly once with original Order/PI, fulfillment once after paid checkpoint, no inline pending_delivery write, HTTP200 and completed checkpoint. Retain failure/retry coverage; a mock does not prove real capture |
| test_10_dispute_created_flagged_revoked |500 OrderMoneyConflict; old fixture supplies only SELECT id,status while handler now locks validated money state | Supply coherent typed locked Order/Transaction/money fixtures or use the actual owned PostgreSQL fixture. Preserve actual handler execution, disputed UPDATE, revocation and event assertions; assert reservation cancellation/revision or reconciliation as applicable. Do not mock the dispute handler or treat MagicMock truthiness as money state |
| test_11_dispute_closed_won_restored |500 OrderMoneyConflict for the same incomplete fixture; current handler refuses restore for refund, pending, independent revocation or reconciliation | Supply coherent typed money state and explicit false pending result for the won eligible case. Retain completed UPDATE and revoked=false assertions. Add/retain negative cases for pending/refunded/independent revocation/reconciliation that prove no restoration. No removal of the current refusal guard |
| test_12_refund_full_revoked |400 before old inline refund SQL assertions; minimal charge lacks its own livemode matching the signed event and current durable refund fixture | Use complete platform signed event and captured provider/refund facts, actual normal admit_order_refund_sync/process_order_refund path with owned PostgreSQL and provider boundary doubles only. Assert full refunded state, access revoked, exact Refund/journal/effect identity and same-event replay once. Replace obsolete inline UPDATE/is_full implementation assertions with stronger persisted outcome assertions; do not stub the refund processor or weaken expected200 |
| test_13_refund_partial_access_kept |400 under the same incomplete event; normal refund path replaced the obsolete inline writer | Same complete genuine local processor fixture with a partial amount. Assert partially_refunded, retained access where otherwise eligible, exact cumulative amount/journal and replay-once. Keep full versus partial business distinction; no handler shortcut or obsolete _handle_refund call |
| test_18_dedup_row_inserted_before_processing |500 RuntimeError in checkout flow with single-use async generator | Use a fresh generator per acquisition and explicit fulfillment result as node09. Preserve ordering assertion that dedup write precedes processing; instrument paid/fulfillment callbacks and durable checkpoint ordering rather than discard the ordering check. Assert200/completion and required service calls |
| test_20_checkout_completed_calls_handle_payment_when_canonical_tx |HTTP200 after early binding refusal: metadata contains transaction_id but mocked stored Order lacks it; reverse canonical SELECT also lacks a matching fixture. Actual e621 webhooks693-726 still executes UPDATE transactions paid_at and transaction_events INSERT after valid checkout | Supply coherent stored Order.transaction_id, matching PI/session and reverse transaction-to-order SELECT result. Return a fresh async generator on every acquisition. Preserve BOTH original raw UPDATE transactions/paid_at and transaction_events INSERT assertions plus exactly-once OrderService.mark_paid; they remain valid accepted behavior. Add actual owned-PG canonical payment outcome companion for persisted paid_at, state/bindings and once-only journal/capture. Do not remove or weaken a valid SQL assertion or change the source to make this pass |

The checkout RuntimeError logs record the exception class, not its full internal message; the single-generator exhaustion is a source-grounded fixture diagnosis that the corrected test must verify. Preserve the original log as observed evidence. The dispute fixtures must exercise the real handlers and preserve their SQL/state assertions; only the existing money-lock boundary may be represented by a coherent typed unit fixture, with the independent actual-database protocol tests retained. Refund and canonical financial outcomes require actual PostgreSQL, not mocks claiming business effects.

The original13 mechanical edits still apply where not superseded by these exact stronger test reconstructions. tests/test_e2e_stripe_isolation.py remains limited to its two-column sentinel correction and original suppression assertions. Scope permits necessary imports/local fixtures and explicit linked companion tests inside the same three modules, not conftest or source expansion. Run all three complete modules under repository fixture scope and supported runtime; record every collected node and original-to-final identity mapping, without skips/xfails/deselection or expansion of the six historical exceptions. Re-run actual signed money/W/signature/dedup and all remaining qualification cohorts at the exact final head. Any real accepted-source defect or additional target need returns to owner review with exact evidence rather than test weakening.

### Raw evidence precision and support instructions

CHECKOUT-R3-WHITESPACE-COUNTS.json records the exact base56665b4e-to-a4e06c08 per-file14243 trailing-whitespace findings; its three authored prose files pass. Prior c6 nine findings apply only to that older range. DeepSeek F4's additional10408 claim for ADJACENT-CLASSIFICATION-SOURCE.txt does not reconcile with the actual count/file size; consult the retained exact counts rather than repeating it. Retain all raw captures, generated source extracts and proposed patch bytes unchanged. New successor range checks must be recorded independently, not reuse a4 totals.

For diagnosis start from the exact checkpoint, owner decision, current candidate spec and operator runbook, then inspect the named raw artifact for the failed gate. Distinguish collection/setup failure, toy SQL semantics, mocked handler tests, actual product PostgreSQL/ASGI, source review, hosted CI, deployed identity, scheduled path and real provider release. Preserve startup failures, all failed and passing runs, original test identities, runtime/source hashes and cleanup receipts. Never infer completed release from a review, candidate dispatch, health response, unit pass or database probe. The full enabled AWS S3 and Cloudflare R2 objective, budget and protected-run authority boundaries are unchanged.

The node20 diagnosis in undispatched candidate46752d04 was incorrect and is superseded here. Exact accepted source still performs the payment metadata UPDATE and transaction event INSERT after valid binding. The retained owner finding CHECKOUT-R4-NODE20-OWNER-FINDING.json and full source identify the omitted stored transaction_id/reverse lookup fixture. The prior candidate/packet remain historical evidence and were never dispatched or accepted.


---

# Canonical browser payment-intent binding amendment proposal

Status: proposed for joint owner disposition and fresh review, 2026-09-12. This is not an accepted contract, implementation instruction to the running MP248 job, or release approval. Preserve frozen runbooks55690c7a1fb48c87849a387f7fef79d7008385a9 and MP248 request SHA192beb94361b0bf01f30d8da4b5e5572f870d936d126b56a6d546a1e4a79419c. Mars request4667 and independently acknowledged4664 motivate this document. The objective remains complete public checkout, real canonical capture, committed Workspace binding, full refund/replay/close, environment integration and enabled AWS S3/R2 production release.

## Exact demonstrated problem

MP248 public-lifecycle-first failed on an incorrect paid-only assertion; normal processing advances to pending_delivery/fulfilling. R2 corrected that assertion but omitted the separate signed payment_intent.succeeded event. R3 and public-lifecycle-binding then sent the event and received HTTP500 with durable failed/error_message=Order/transaction reverse binding mismatch. The actual diagnostic tuple shows a populated Order.stripe_payment_intent_id and NULL Transaction.stripe_payment_intent_id. Reverse IDs and buyer, seller, listing, amount, platform fee, seller amount and currency match. No later Workspace bind/refund/close was reached.

Evidence: SELLER-WORKSPACE-MP248-CAPTURE-BINDING-FINDING-S1712.json and SELLER-WORKSPACE-MP248-PUBLIC-PRODUCER-TRACE-S1712.json in this directory bind source and raw failed receipts. Builder originals reside at /Users/max/koskadeux-state/s1656/s1714-workspace-final-67392781. Retain all failed attempts, including diagnostic output; no rewriting failure history.

Real public checkout invokes TransactionService.initiate/create_checkout. create_checkout consumes only the returned Stripe Session id/url, stores the Session ID on Order and commits the Order/Transaction pair. It does not read Session.payment_intent, so adding that attribute to the provider double cannot populate the missing column. OrderService.mark_paid writes Order PI; checkout webhook and TransactionService.handle_payment write Transaction JSON metadata PI, not the dedicated column. Other dedicated Transaction PI writes in TransactionService belong to agent checkout. The signed_post helper uses actual HMAC validation, webhook routing and independent DB sessions; it does not replace money processing. This is a product-path omission in the exercised browser flow.

## Concrete successor design selected after Mars4675

This complete amendment supersedes the earlier provisional proposal. It remains proposed until fresh joint review. Baseline is817f7331a07d07929a67198b28a82ed9b98baa56. Choose database uniqueness, not application advisory serialization. An advisory lock would require every existing and future writer to share one hashing/key convention before money locks, including legacy and agent paths; a missed writer would evade the guarantee, and adding it after existing locks risks inversion. Native unique constraints arbitrate conflicting writes from all database clients without introducing a new application lock tier. They preserve multiple NULLs for unpaid Orders/Transactions. They do not, alone, establish cross-table identity; the existing exact reverse-pair and money-field validation remains mandatory.

### Exact schema and target addition

Add one migration: alembic/versions/20260912_003_canonical_payment_intent_binding.py, revision s1714_canonical_pi_binding_v1, down_revision s1712_workspace_authority_v1. It creates public.orders constraint uq_s1714_order_payment_intent UNIQUE(stripe_payment_intent_id) NOT DEFERRABLE and public.transactions constraint uq_s1714_transaction_payment_intent UNIQUE(stripe_payment_intent_id) NOT DEFERRABLE. Both columns remain nullable; do not use NULLS NOT DISTINCT, change column types, update rows, backfill NULLs, deduplicate records or drop/rename existing objects. No cross-table PI FK is claimed. Payments existing unique PI constraint remains unchanged.

Preflight under the deployment's existing writer-quiescence procedure, not during an active protected run. Ordinary transactional DDL locks tables in Order→Transaction order with ACCESS EXCLUSIVE, lock_timeout250ms, statement_timeout2000ms and a decreasing five-second body deadline; failure rolls back both additions. Before DDL, inspect non-NULL duplicates in each table and report counts plus bounded nonsecret row identities; any duplicates abort, never choose winners. Missing objects are created; exact named existing constraints/indexes are a no-op; same-name mismatches abort. Verify exact relation/column attnums, contype=u, validated, nondeferrable/nondeferred, index unique/valid/ready/immediate, no predicate/expression, and actual column nullability. Existing money/FK/publication fingerprint components remain equal; new uniqueness objects are an explicitly separate expected extension. PostgreSQL17 fresh/full ancestry, repeat body and collision/rollback cases are mandatory. Retained forward-only downgrade refusal; removal requires a separately reviewed migration.

The exact proposed allowed target set is the existing17 plus six additions: app/services/order_money_service.py, app/services/order_service.py, app/services/transaction_service.py, app/services/billing_service.py, tests/test_order_money_protocol.py and tests/test_refund_webhook_protocol.py plus this migration:24 files. No Order/Transaction model edit, agent endpoint, checkout endpoint, config, environment or provider-setting edit is required by this selected design. Existing models can represent nullable PI values; migration/catalog proof is authoritative. No runtime create_all substitutes for migrated database tests.

### Exhaustive production call-site disposition at817f

- Browser API checkout.create calls real TransactionService.initiate/create_checkout. Session creation stores only Order session ID; PI is legitimately unavailable at this stage. Leave Session creation unchanged. Do not depend on Session.payment_intent being returned.
- OrderService.mark_paid is called by the signed checkout webhook and the guarded legacy mock-checkout branch in checkout.py. Integrate the new initial-binding helper before its normal status write while holding Order then exact reverse Transaction locks. For a canonical pair, write both dedicated PIs in the same transaction. For a legitimate legacy Order with no Transaction in either direction, write only Order PI and preserve legacy behavior. The guarded mock branch uses its existing local synthetic PI authority; it does not gain a new provider bypass.
- webhooks._handle_checkout_completed has a separate inline synthetic Order update. For that existing admitted branch, call the same sync helper before its paid transition. The normal branch still delegates to OrderService; do not make an extra sync write that holds locks across the async-session boundary. Preserve metadata/paid_at/event assertions on the canonical seam.
- webhooks payment_intent.succeeded retains its failed/finance_pending checkpoint and BillingService call. Its backup created-Order path must not call general lock_order_money on a missing pair and then repair it: the narrow binding producer runs only when the same validated Order PI and exact ownership are already established. If no Order PI exists, fail retryably and wait for legitimate checkout completion. No metadata fallback into generic finance.
- BillingService.handle_stripe_event canonical PI branch owns completed-checkout/failed-capture recovery. It resolves an existing Order by stored PI, fetches authoritative PI and Charge before event/money locks, then locks event→Order→Transaction, applies the narrow allowed initial completion below, and runs unchanged strict lock_order_money/ensure_order_capture. The helper must re-use the already owned exact rows and must not take an upstream event lock. A completely equal pair simply follows current processing.
- TransactionService.handle_payment is an exposed service method but the app source caller search finds no current production call site. Keep direct method compatibility and test it: integrate the same initial binding for a coherent linked Order before paid transition or idempotent return. It cannot bind an Order-less transaction or invent Order PI ownership; refuse that new binding request. Do not use its absence from current routing as a reason to leave inconsistent writes.
- TransactionService.create_agent_checkout is the active agent PI producer, called by app/services/mcp_tools.py. Its requires_action/cancellation and normal agent-payment branches already set both dedicated PIs with the same returned provider PI in one transaction. Preserve these branches, stable existing provider idempotency key, spend ownership and failure behavior. Native uniqueness applies automatically to both branches; verify their existing paired writes and rollback on duplicate PI without adding an advisory lock or new provider call. This amendment introduces no new lock before agent provider I/O. It does not claim to redesign the pre-existing agent creation transaction or its external-call lifecycle; original money/provider-unknown gates remain required independently.
- order_service_patch.py and order_service_updated.py are unimported replacement fragments (app import/caller search has no references); they are not active producers and are not changed. Preserve the caller-search evidence. Finance Payment constructors write Payments, not Order/Transaction PI; keep their existing uniqueness and capture logic. No other direct Order/Transaction PI assignment was found in app source outside the listed paths. Tests, migration fixtures and operational SQL are not accepted production provenance; native unique constraints still cover their duplicate writes.

### Shared helper API, transaction ownership and state table

Place bind_initial_order_payment_intent_sync(db, *, order_id, payment_intent_id, expected_transaction_id, mode) and its async run_sync adapter in order_money_service.py. mode is a closed internal enum: checkout_completion, direct_payment_completion, signed_capture_recovery. It is never user input, a setting or a bypass flag. The function returns the locked Order and optional exact linked Transaction; it does not commit, create money authority or invoke providers. The caller owns one bounded transaction and must not enter with unrelated staged writes that would be discarded. Use the existing reverse-Transaction query ordered by Transaction.id, reject cardinality greater than one, and compare Order.transaction_id exactly. Never acquire another Order after those Transaction locks.

Read all identity/money fields again under locks. Reject all differences other than the specifically permitted absent dedicated PI fields. Existing non-NULL Order PI or Transaction PI must equal the supplied PI. Nonempty metadata PI must also match. Existing Session/event/provider identity is checked at its owning validated caller; the helper cannot turn untrusted metadata into permission. No broad OR allowing an arbitrary matching field.

State decisions:
- Both equal supplied PI: no binding DML, preserve existing processing/replay policy. Any unrelated identity/money mismatch still refuses.
- Order created, both PI NULL, coherent canonical pair, validated checkout completion: atomically establish both PIs, then normal paid transition. Conflicting other-row PI uniqueness produces rollback and retryable conflict, not success.
- Order created with matching PI and Transaction NULL, coherent pair and validated completion: establish Transaction PI; do not overwrite Order identity.
- Order NULL and Transaction already populated: refuse automatic binding, even if supplied PI matches Transaction; this unexplained asymmetric history is outside the demonstrated producer defect.
- Already paid/pending_delivery Order with matching PI, linked Transaction paid/fulfilling and NULL PI, original signed capture event failed before finance, no retained W registration, no Payment/refund/payout/captured authority and no independent revocation/dispute/reconciliation: allow only signed_capture_recovery. Require actual matching stored Session, fresh provider metadata for both IDs, exact amount/currency/platform scope and original persisted signed event/payload. Complete Transaction PI, then capture in the same transaction. No status or paid_at rewrite. If a pre-existing empty OrderMoneyState is present, refuse this recovery pending explicit adjudication; absence is required for this narrow case.
- Both NULL in already-paid history, any conflicting non-NULL PI, unknown/ambiguous authority, captured/refunded/settled/disputed/revoked state, or retained W registration with missing PI: refuse. No deletion, automatic backfill or reactivation.
- Legitimate legacy Order without any reverse Transaction: normal checkout completion may set compatible Order PI only; signed capture retains existing legacy rules. No creation of a Transaction.

These rules explicitly recover the demonstrated state without redelivering the already-completed checkout event. The original failed payment_intent.succeeded ID is retried directly after deployment; the capture branch performs the narrow initial completion. Same completed-checkout dedup remains completed and is not reopened. Original event payload/signature mismatch still refuses. Any newly bound PI and new finance roll back together on capture failure; an uncertain commit is reconciled by original-ID replay and exact stored finance, never a second transaction or provider purchase.

### Exact concurrency and deadline behavior

Use event→Order→Transaction→money authority→Payment→sorted Refund→agent key. PI unique-index checks happen during the permitted paired writes; they are not a new advisory-lock tier. Each operation modifies only its one validated Order/Transaction pair. A same-PI competing Order write cannot both commit because the Order unique constraint arbitrates it; competing Transaction PI writes cannot both commit because the Transaction unique constraint arbitrates it. The loser must roll back; it cannot retain a one-sided binding. A raw cross-table mismatch is not prevented by uniqueness alone, and continues to be refused by strict pair validation. No assertion that unrelated raw SQL gains valid ownership merely by satisfying unique constraints.

The helper transaction sets lock_timeout250ms and statement_timeout2000ms; every statement uses remaining time from a five-second monotonic deadline, and async callers also have a five-second cancellation boundary followed by rollback. Sync timeout/cancellation must propagate as retryable failure after transaction rollback. SQLSTATE23505 for these named PI constraints is a binding conflict; 55P03/40P01/57014 and connection/commit uncertainty are not success. Do not swallow other integrity failures. Repeated exact equal state does no DML and cannot recreate paid events.

Capture provider preflight has at most10seconds per read and20seconds total, outside event/money locks. Cancel/timeout retains the original failed event and does no binding DML. No provider write is introduced. Checkout completion uses its existing authenticated signed event facts and does not add network reads inside mark_paid. Existing agent external creation receives no new binding/event/money lock before its provider call; database uniqueness is checked when persisting its returned PI. Full original provider-unknown/retry/cutover obligations are not waived by this amendment.

### Concrete additional tests

In tests/test_order_money_protocol.py, add migratedPG17 matrix coverage for the state decisions above, both unique constraints, multiple NULL preservation, duplicate preflight/no rewrite, exact catalog collision/repeat-body, per-pair atomic rollback and five-second/250ms contention bounds. Test two independent sessions assigning the same PI to different coherent pairs: exactly one commit, one failed attempt with unchanged losing pair, then no creation of duplicate finance. Also test cross-table mismatch remains invalid despite satisfying each table's individual unique constraint.

In tests/test_refund_webhook_protocol.py, add signed checkout-first and PI-first/retry orderings, same/different event-ID races, completed-checkout+failed-capture original-ID recovery, payload/account/signature/provider mismatch, capture exception rollback after initial binding and commit-uncertainty replay. Assert actual dedicated columns, preserved paid history, one Payment and balanced capture journal, strict refusal without additional effects on established money/registration history. Exercise actual OrderService/TransactionService direct entrypoints and both existing agent response branches with external provider doubles; unique collision must not silently consume/release spend twice. Retain original agent idempotency and failed-action assertions.

In existing tests/test_workspace_test_refund_admission.py, keep the complete real public checkout→signed completion→signed capture→committed bind→normal full refund/replay→close test, all six distinct Transaction drift checks, full lifecycle/recovery and all existing W obligations. Its failed historical captures remain in the review packet. Run full final-source inventories, all original money gates and three required fresh reviewers. No review or implementation dispatch is authorized by this proposed text alone.


### Direct-mode state clarification and actual caller transaction ownership

The direct_payment_completion mode has only two allowed cases: (1) an already equal, coherent pair is a no-op before existing direct method semantics; (2) Order is created with its dedicated PI already equal to the supplied PI, the exact reverse Transaction is checkout_pending with NULL dedicated PI, and there is no existing money/Payment/refund/payout/registration history, revocation, dispute or reconciliation. In case2 it may complete Transaction PI inside the caller-owned transaction and then follow the existing direct method's normal transition. Both-NULL direct calls refuse: an internal method argument alone cannot establish the Order's PI ownership. Paid-history NULL completion is exclusively signed_capture_recovery, never direct mode or checkout dedup repair. No other direct state is allowed. Tests assert the complete allowed/refused table using actual service calls.

Actual webhooks source510-545 keeps the synchronous dedup claim's event lock through dispatch. The normal checkout path reads Order/Transaction without money row locks in _handle_checkout_completed, then calls an independent async OrderService session at683. Therefore this amendment must NOT perform synchronous PI binding or acquire Order/Transaction locks before entering that async session. The async mark_paid owns Order→Transaction locks, initial binding and paid transition and commits together under its existing auto_commit=True behavior. Its helper cannot read or lock the event row owned by the synchronous caller. After the async session commits/releases its locks, the synchronous caller may perform its existing metadata/paid-at/event work. Crash after async commit but before event completion must be tested: failed original event replay observes a coherent bound pair and does not duplicate paid transitions or capture.

For the existing inline synthetic branch, no async mark_paid is entered (marked_paid_inline=True). Its synchronous session owns event→Order→Transaction, binds and changes status/events in that one transaction; no second session can re-enter money locks. Legacy mock checkout calls async mark_paid(auto_commit=False) in its existing request transaction; the helper must neither commit nor discard caller writes. Any validation failure rolls back the full request through the existing endpoint boundary.

The existing _repair_canonical_payment starts with a Transaction FOR UPDATE. Since this branch now participates in the explicitly reviewed checkout lock protocol, change it to acquire and verify Order first, then its exact reverse Transaction, without taking any new event lock. Preserve its current paid_at/event dedup purpose; it must not bind missing dedicated PI columns or reopen a completed checkout event. It is not the recovery route for the demonstrated capture defect. Paid-history initial PI completion remains owned by the original failed signed capture branch only.

The PI-success webhook writes and commits its retryable finance_pending checkpoint before the independent async BillingService call. Fresh authoritative provider reads occur outside event/money locks. The async capture session then owns event→Order→Transaction and initial completion/capture together. The completed checkout event is not locked/reopened. The failed capture ID and exact original payload are retained; unrelated failed events cannot acquire ownership. A completed capture replay must not re-enter paid transitions.

### Mandatory next-build source freeze

The next MP request must finish edits, record the exact candidate tree and all allowed file hashes, then freeze that source before any qualifying migration/test run. Any subsequent source edit invalidates affected qualification; rerun against a new recorded tree. Bind collection/execution/phase results, full logs and XML to that one final candidate. MP248's earlier-migration final runs and its44 setup errors remain retained counterevidence, not accepted coverage. Restore or explicitly reconcile both renamed prior checkout nodes, including the previously passing True case. Do not invent failed evidence for it. All1990 original nodes plus new required cases must be accounted for without skip/xfail/selection substitutes.


### Final MP248 test isolation diagnosis and required fixture correction

Mars4684 supplies a source/log-supported explanation for the44 setup errors, retained in s1714-mp248-template-session-source-diagnosis.json. OrderService.mark_paid schedules a real post-commit notification. notification_service imports a module-bound AsyncSessionLocal using the initial template database; the case fixture changes environment but does not replace that already imported factory. The log records a notification FK error for the cloned seller immediately before subsequent template clones fail with one connected session. This is a strong inference, not identification of the transient backend PID or a reproduced final fix. Preserve that uncertainty and original logs.

Within tests/test_workspace_test_refund_admission.py and tests/test_refund_webhook_protocol.py, bind the actual notification session factory to the current owned per-case database. Keep the real notification service, post-commit scheduling and payment handlers. Track only this fixture's notification coroutines, allow its queued after-commit callbacks to start, await their completion with a bounded5seconds, and dispose every owned pool in finally on both success and assertion/error paths. On timeout cancel/await only the explicitly tracked fixture tasks and report the failure; do not enumerate/cancel unrelated event-loop tasks, kill database sessions, or disable notifications/money processing to mask isolation errors. Restore original factories after owned tasks complete. Wrap admin engine creation/CREATE DATABASE and yield in one try/finally so clone failure also disposes the admin engine; conditionally drop only the successfully created owned clone. Test the actual forced checkout assertion/notification failure path followed by successful next clone, and observe zero retained template-client sessions for this run before final owned cleanup. No production notification_service.py or database.py edit is proposed.

The normal backup _handle_payment_succeeded branch must not create an OrderMoneyState to make NULL binding recovery possible. Its existing lock_order_money_sync call is permitted only on an already fully coherent binding (including the existing legacy/agent cases). A created canonical Order with missing Transaction PI fails retryably before that creating helper; it is not eligible for paid-history signed_capture_recovery. In the demonstrated public failure the Order is already pending_delivery, so the backup branch does not run and has not created money authority. Verify this exact absence in the original-event recovery test. Existing empty authority remains a refusal for the narrow recovery mode, not an invitation to delete/reset it. A successful signed recovery establishes the missing initial PI and then invokes normal strict money/capture processing in its own transaction.


### Lock-owning validation facts across the normal checkout session boundary

Mars4685 identifies a real pre-lock validation boundary. Extend the normal webhook's validated checkout result and OrderService.mark_paid with a keyword-only expected_binding argument for canonical checkout. The closed immutable binding value carries order_id, transaction_id, stored_checkout_session_id observed by the validator, signed event Session ID, signed event PI, signed amount/currency, buyer_id, seller_id, listing_id, platform_fee_cents and seller_amount_cents. Reject missing/extra fields and mismatching types at the internal boundary. Pass it directly from _handle_checkout_completed to the independent async mark_paid session; do not reconstruct trusted event facts from caller-supplied IDs after the boundary. The sync synthetic path passes the same validated value into its sync lock owner. The helper's exact API is extended with expected_binding in addition to the previously listed keyword arguments; it is not a user-configurable override.

After event→Order→Transaction ownership is established, compare all supplied identity/money/Session facts with the locked rows and exact reverse pair. An originally NULL stored Session may remain NULL only where the existing producer contract explicitly permits it; a changed non-NULL Session must match the signed Session exactly or refuse. Previously observed dedicated PI may remain NULL or advance only to the same signed PI as an idempotent concurrent winner; never accept a different PI. Concurrent transition from created to already-paid is a no-op only for an already fully equal binding and preserved normal paid history; it cannot trigger another paid event or enter paid-history NULL recovery. Recheck revocation, disputes, refunds and reconciliation under locks. No unlocked validation result substitutes for these comparisons.

Legacy mock checkout with no Transaction retains the original mark_paid signature compatibility through expected_binding=None only when both directions confirm no canonical Transaction. Canonical callers missing expected_binding refuse. For TransactionService.handle_payment, the tx_id argument identifies the row but is not PI authority: under Order→Transaction locks obtain the persisted Order PI/Session and coherent full tuple, compare the event PI to that already established Order PI, then build the direct-mode internal facts from locked state. Both-NULL direct mode remains refused. Signed_capture_recovery carries fresh provider/event facts into its lock-owning helper and repeats all comparisons under locks. Tests use barriers between sync validation and async lock acquisition to mutate Session, reverse IDs, buyer/seller/listing, amount/fees/currency, PI and revocation state; each conflict must refuse with no partial binding or finance. The legitimate concurrent identical completion must remain idempotent.


# Full successor review dispositions

## D1 — explicit retained-clause supersession and non-waiver

The retained line189/420/315 references in this paragraph refer to base e3b8b08813f1476910422c4233fee92360409cb7, not current-file numbering. The final31-target scope above and D3 durable-agent lifecycle below are the current proposed authority; retained24/27-target memberships are historical. Quoted clause content identifies the precise supersession; all other obligations remain.

The canonical PI amendment supersedes the normal-money scope restriction in retained line189 and the no-new-normal-money-writer restriction in retained line420 solely for the named24 targets and the expressly described canonical PI producer, original-event recovery, native uniqueness and necessary collision refusal changes. The S1681 admission/table/CLI restriction in line189 remains unchanged. At line315 only the statement that tests/test_refund_webhook_protocol.py is outside the target list is superseded. Its original forged non-W terminal200 suppression expectation, exact original node, and zero optional-W schema/mount lookup proof remain mandatory. No historical scope sentence can narrow the accepted24-target amendment; no target membership alone authorizes an unrelated behavioral change. All remaining authority, refund, provider, environment, genuine-hold, cutover and enabled AWS S3/R2 release gates remain binding.

## D2 — duplicate census and separately reviewed remediation

Before qualification applies either new canonical PI unique constraint, retain a read-only duplicate census of the owned database at backend base817f7331a07d07929a67198b28a82ed9b98baa56, before the new migration, and another immediately before production DDL under the separately authorized cutover quiescence. Record source/migration/database identity, observation time, query, row counts and bounded nonsecret conflicting row identities separately for Orders and Transactions; exclude NULL from duplicate groups. An empty synthetic census proves only that database, never production cleanliness. Each census must count all duplicate groups even when displayed examples are bounded. The transactional migration must independently reject duplicates; a prior census cannot replace enforcement against a race.

Any duplicate, census uncertainty or object collision stops that migration without choosing a winner or mutating payment/history rows. The named remediation path is a separate canonical-PI duplicate reconciliation design: preserve original Order/Transaction/Payment/provider references and spend/refund/payout history; determine authoritative ownership with read-only evidence; obtain full independent review of the exact proposed forward remediation before any mutation; then repeat census and qualification. No automatic deduplication, PI reassignment, deletion, backfill or claim-table addition is authorized by this disposition. Census and stop evidence are implementation/cutover requirements, not an assertion that duplicates currently exist.

## D3 — durable agent attempt protocol

The complete protocol in the final section below replaces the earlier rollback-only agent collision disposition and expressly changes the old external-call lifecycle. Its exact schema, state transitions and signed-before-binding recovery are required together.

## D4 — zero-unmigrated-caller gate

On the final frozen backend tree, enumerate all definitions, direct/qualified calls, imports/aliases and test uses of mark_paid, the initial-binding helper and the affected completion entry points across the entire repository, including modules outside the embedded review packet. Retain the exact rtk-prefixed search commands and output, and inspect aliases/dynamic dispatch exposed by those results. Classify every caller as canonical with complete expected_binding, strict direct mode, signed capture recovery, agent_attempt_completion with exact durable attempt correlation and provider evidence, or independently proven no-Transaction legacy compatibility. Require zero unclassified/unmigrated callers; do not infer completeness from the packet's known webhook and mock callers. Existing money assertions and duplicate None test branches remain. Record actual source identity and final full-module execution evidence. Also inventory every transactions.metadata writer and every spend_used mutation and its callers, including shared primitives, direct SQL and ORM writes. Classify metadata writes as preserving the exact attempt/revision, an authorized locked attempt transition, or refusal; prove real delivery and settlement metadata updates preserve the attempt. Explicitly classify the original requires_action rollback at backend817f transaction_service.py:427 as superseded by the terminal-proof release, and refund_processing_service.py:492–493 as a distinct captured-payment refund flow whose spend decrement cannot coexist with no-capture held-to-released attempt cancellation. Preserve that refund flow unchanged; no new production target is added. Require zero unclassified writers, not merely zero unclassified mark_paid callers.

## D5 — migration bounds and projection extension

Both the002 W parent-key body and003 canonical-PI body must use lock_timeout250ms, statement_timeout2000ms and a decreasing five-second whole-body database deadline, with rollback/refusal on timeout, cancellation, deadlock, uncertain connection or failure. The five seconds applies to each entire body, not to each DDL statement. For003 acquire the required shared-table locks in Order→Transaction order; retain002's reviewed ordering and dependency rules. Record actual lock waits/held duration, row counts, concurrent ordinary-writer outcome and transactional rollback evidence for uq_s1712_order_discovery, uq_s1714_order_payment_intent and uq_s1714_transaction_payment_intent. No concurrent DDL substitution or destructive downgrade is authorized.

The accepted pre-existing money-readiness projection is unchanged and must compare byte-identically before and after both bodies. The two new objects uq_s1714_order_payment_intent and uq_s1714_transaction_payment_intent are outside that retained projection and are separately enumerated in an explicit expected extension. Verify their exact table/column order, nullable-column behavior, native unique nondeferrable constraint/index validity/readiness/immediacy, lack of predicate/expression and normal NULL-distinct semantics. The combined qualification consists of unchanged old projection plus this exact extension, never a hash learned from the migrated live schema. Any unexpected old projection delta or different/missing extension is a blocker for disposition, not an automatic update of the accepted fingerprint. Existing W parent/FK/internal-trigger readiness remains independently mandatory.

## D6 — closed12-field expected_binding schema

The internal value is immutable and contains exactly the following12 fields. Canonical names below make the formerly prose-named signed fields concrete. Use an immutable value object with explicit strict validation; no mutable nested mappings and no coercion of bool, float, Decimal, arbitrary object or numeric/string lookalikes. The normal webhook validator constructs it once from validated signed facts; the lock owner validates the type and all fields again against locked rows. UUID-valued internal fields use actual uuid.UUID values, not a mixture of strings and UUIDs. Explicit parsing of trusted boundary strings into UUID happens before constructing this internal value; UUID input must round-trip to the canonical lowercase hyphenated representation. Internal validation never silently converts a mismatching type.

| Field | Exact internal representation | Nullability / provenance |
| --- | --- | --- |
| order_id | uuid.UUID | Never NULL; exact validated or locked Order |
| transaction_id | uuid.UUID | Never NULL for this canonical value; exact reverse pair |
| stored_checkout_session_id | str, length1–255, exact case, no whitespace/control characters | May be None only if the observed stored value was NULL and the existing producer/mode permits it |
| signed_checkout_session_id | str, same rules as stored Session | Required for checkout_completion; None for direct_payment_completion, which has no signed Session; for signed_capture_recovery use the authoritative matching Session ID and refuse absence |
| signed_payment_intent_id | str, length1–255, exact case, no whitespace/control characters | Never NULL; exact validated signed/provider PI or already established locked Order PI in direct mode |
| signed_amount_cents | exact built-in int, 0–2147483647 inclusive | Never NULL; bool explicitly rejected; equal locked amount |
| signed_currency | exact built-in str, three uppercase ASCII letters | Never NULL; normalize validated signed/provider lowercase ASCII currency explicitly before value construction; compare locked currency using the existing canonical money normalization |
| buyer_id | uuid.UUID | Never NULL; exact actor |
| seller_id | uuid.UUID | Never NULL; exact actor |
| listing_id | uuid.UUID | Never NULL; exact listing |
| platform_fee_cents | exact built-in int, 0–2147483647 inclusive | Never NULL; bool explicitly rejected; equal locked fee |
| seller_amount_cents | exact built-in int, 0–2147483647 inclusive | Never NULL; bool explicitly rejected; equal locked seller amount |

Preserve existing stricter financial equations and admission checks, including fee/seller total, actor/reverse linkage, signed metadata and provider scope. The integer upper bound matches the existing PostgreSQL Integer storage; it does not change the product's accepted price/fee rules. Identifier strings are opaque identity values, not credentials; no W-receipt-only alphanumeric regex is imported here. In particular, already admitted legacy synthetic pi_e2e_mock_<UUID> with hyphens remains valid in its existing guarded context, as do valid provider Session identifiers. This schema does not authorize synthetic identities in normal provider admission. The no-Transaction legacy path uses expected_binding=None only after both reverse directions prove absence; it does not manufacture a canonical value with a NULL transaction_id.

Direct mode has no signed Session event: signed_checkout_session_id=None, stored_checkout_session_id reflects the locked Order (including permitted None), and PI authority comes only from the existing non-NULL Order PI. Both-NULL direct binding still refuses. signed_capture_recovery must carry fresh signed/provider facts and the matching stored Session; missing stored or authoritative Session refuses recovery. Ordinary canonical checkout preserves only the existing explicit NULL-stored-Session allowance; a NULL value is never a general bypass of Session matching. All twelve fields, extra/missing fields, UUID/string swaps, bool-as-int, integer bounds, malformed currency/identifiers, nullable-mode violations and post-validation row mutations require refusal tests with zero partial binding or finance. Legitimate concurrent identical completion must remain idempotent.

## D7 — canonical continuation status and support responsibility

Designate runbooks/seller-workspace-operator-guide.md's continuation status as the single operator-facing authority for current candidate, implementation qualification and open release gates. runbooks/seller-workspace-live-release.md must link to that section and summarize the release action without maintaining a second verbatim candidate/result paragraph. The specification remains the design/acceptance contract; immutable evidence remains the proof. Local checkpoint/bus records are operational coordination and must not override the documented accepted contract or imply completion. Apply this documentation correction only after the current frozen reviews finish, through the exact jointly selected record/update procedure; keep this draft outside the frozen checkout meanwhile.

Future diagnosis starts from the canonical status, exact source/image/spec identity and raw failure/qualification receipts. Preserve all failed attempts, test phase/source vectors, reviewer coverage limits and cleanup evidence. Upgrade instructions must require the complete final backend, then environment integration and combined review, followed by genuine protected phases, production cutover and live AWS S3/R2 proof. None of these low-mandate dispositions waives a release gate or constitutes implementation evidence.

## D8 — CC premise and historical evidence provenance

CC204046-397140 was independently read in full (21898 bytes); its vote is provisional because it explicitly read the delta and only five historical responses. Its source verification remains useful, but full retained-contract/history completion is required. Its numbered specification references are diff-output positions rather than the frozen588-line file; final references must be re-anchored, not copied as real specification line numbers.

State the native-uniqueness premise explicitly: one non-NULL PaymentIntent belongs to at most one Order and at most one Transaction; an admitted canonical Order/Transaction pair must share that PI with exact reverse identity and money validation. No legitimate split-order/multi-Transaction use of one PI is authorized by this design. The duplicate census verifies this premise on the actual database; a found duplicate stops for the D2 separate reconciliation path, never an arbitrary winner. Existing Payment PI uniqueness is corroborating evidence, not a replacement for these per-table checks.

Preserve the raw historical SELLER-WORKSPACE-CANONICAL-PI-AMENDMENT-R3-S1712.txt bytes/hash as evidence. In the final evidence index and canonical operator guide, label that84-line file an earlier R3 snapshot superseded by the full frozen specification including notification isolation and expected_binding sections. Do not rewrite the raw historical artifact or let it act as implementation authority. If a separately generated current amendment extract is useful, give it a new explicit filename and source/hash identity. The authoritative full specification and joint decision remain required reading.

Retain the selected migration revision names. The cross-session naming is cosmetic; prove the actual linear ancestry s1714_order_money_v1 → s1712_workspace_authority_v1 → s1714_canonical_pi_binding_v1. No rename or duplicate migration is needed for naming consistency.

## D9 — completed GLM table attribution and closure mapping

GLM205453-683019 was independently read in full (14952 bytes). It completes all26 historical response bodies and retains the prior full588-line specification review; this is an eligible full-scope design-only APPROVE_WITH_NITS response on e3b8b088, not a vote on the later semantic successor proposal. Its two additional wording findings are folded here.

Attribute status=completed AND refund_phase=applied AND processed_status=processed to the stripe_events table. refund_phase and processed_status are mapped on finance.StripeEvent; status is the raw table/SQL field, not a currently available ORM attribute. Do not implement a nonexistent ORM attribute or weaken the three-field conjunction.

For the closure inclusion predicate, signed payload data.object.payment_intent maps to the registered PaymentIntent ID, and signed data.object.id maps to the registered Charge ID. Include either matching signed identity, not only both, inside the event_type=charge.refunded predicate. Keep the existing linkage-column ORs. Do not prefilter successful states before reading related siblings. NULL-linkage signed siblings must be included; contradictory one-sided matches included then refused; a different event_type excluded even when IDs match. The already prepared next-build predicate is the exact parenthesized transcription; GLM's historical payload-AND example is not accepted. Preserve all original closure observation, cardinality, status and immutable terminal requirements.


# Durable original agent payment attempt and caller compatibility contract

## Why the existing instruction cannot finish the work

At817f TransactionService.create_agent_checkout creates a new uncommitted Order before confirmed provider creation; its requires_action branch cancels the returned PI before native uniqueness can reject a reused identity. A rollback loses that Order but cannot undo provider spend. A later call can generate a different Order with the same provider idempotency key. The generic failure helper unconditionally decrements reserved spend. app/services/mcp_tools.py commits the initial transaction and spend reservation before checkout, so that reservation survives the failed checkout transaction. There is no identified existing collision reconciliation or once-only release operation. A draft saying preserve lifecycle and use existing reconciliation therefore leaves a known implementation hole.

The minimal selected successor uses an internal durable attempt inside the existing transactions.metadata JSONB and persists the original Order/request before the one provider creation. This deliberately supersedes the amendment's preserve-agent-external-call-lifecycle sentence; it does not introduce a claim table, new schema column, advisory lock, new endpoint or new provider purchase. All current actor/terms/published-listing/payout checks and exact money/ownership validation remain. This is a meaningful design change requiring full review, not a mere file-membership permission.

## Durable identity and state

Reserve the internal metadata key canonical_agent_attempt_v1. Preserve every unrelated metadata key. The value is a closed versioned record with: version=1; immutable attempt_id UUID; transaction_id, original_order_id, api_key_id, org/actor/listing identity; original provider idempotency key api_key_id:transaction_id; exact amount/currency/fee/seller amount; original customer/payment-method identifiers and provider account/test context; original provider request metadata; state; bounded observed provider PI/status and evidence timestamps when available; spend_release_state=held|released; and a once-only release evidence identity. Never store credentials, signed secrets, card data or complete provider payloads. Store identifiers needed for exact request equality in the same access-controlled internal data as the existing transaction; redact them from public response/logs. All identity/request fields become immutable once prepared. Record shape/version errors refuse instead of discarding or replacing the attempt.

States: prepared_unknown, response_observed, bound_pending, cancellation_unknown, cancelled_reconciled, conflict_reconciliation, completed. These are internal attempt states, not new public TransactionStatus values. An unknown/unresolved attempt keeps the Transaction initiated and its committed spend held; it must not become paid, agent_payment_pending or agent_payment_failed merely because a provider call raised. A proven normal bound checkout moves to existing agent_payment_pending; a proven owned cancelled/no-charge attempt can move to existing agent_payment_failed exactly once with the spend release transaction. An attempt never resets to absent/prepared with a new Order or key.

The prepare commit precedes all provider creation; prepared_unknown means the call may or may not have happened. This conservative ambiguity is intentional. No timeout, lease expiry, process restart or absence of a PI in local rows makes replaying creation safe. The original caller gets a one-shot in-memory dispatch entitlement only after that commit is known successful. An uncertain prepare commit means no provider call by that caller; subsequent requests reconcile/refuse the retained attempt. No external write is retried automatically by reconstructing a new request.

## Prepare transaction and lock order

First discover the Transaction/possible existing Order and attempt without using that observation as final authority. If an attempt or linked Order exists, follow the existing-identity branch below; do not create another Order. For a genuinely unlinked candidate, create the candidate Order with the existing OrderService and auto_commit=False, then acquire/revalidate the exact Transaction before claiming it. The candidate Order is this transaction's newly inserted row; this does not acquire an existing Order underneath a Transaction lock. Recheck initiated status, original API key/organization/actor/listing/money tuple, empty attempt, NULL Tx.order_id and exact reverse cardinality. A concurrently committed different claim refuses and rolls back this candidate Order, creation event and queued notification. Foreign-key KEY SHARE interactions or competing claim deadlocks are bounded refusals, not provider work; retain a forced concurrent-prepare proof.

Link Transaction.order_id to that candidate, store the immutable attempt with held spend, and commit the Order+link+attempt together before the provider call. No key/spend mutation is needed in preparation because initiate_purchase already committed the reservation. Preserve original normal Order creation notification behavior: only the winning committed Order may schedule a notification. Do not acquire a second/different existing Order after the Transaction; rollback and rediscover if the observed relationship changed. Each DB phase uses250ms lock wait/2s statement/decreasing5s body bounds. Provider I/O holds none of these Order/Transaction/money/key locks.

For an existing identity, lock the observed Order first then exact Transaction, validate the reverse pair and immutable attempt. If the observed Order changed, rollback and refuse/retry discovery outside locks; never chase it under the Transaction. A legacy linked agent pair without this attempt cannot be adopted by fabricating request facts. Preserve existing fully coherent completed/cached behavior where independently proven; otherwise require explicit separate reconciliation rather than new provider creation.

## Provider response, binding and cancellation

The original winning caller uses the persisted request byte-equivalent facts and unchanged api_key_id:tx_id key for the single existing PaymentIntent.create call. The response is not authority by PI ID alone: validate immutable transaction/order/key/agent metadata, amount/currency, customer and account/test context against the durable request. A response may be observed even when identity is contradictory; store only bounded diagnostic facts and mark conflict_reconciliation, never assign its PI as authority or cancel/refund it.

For a matching response, start a fresh bounded Order→Transaction transaction, revalidate the exact durable attempt and all row facts, and atomically write both PI columns with the native constraints. A non-NULL value can only equal the returned PI; no overwrite. Record response identity and the appropriate attempt state in the same transaction. Any native collision rolls back both PI writes and is followed by a separate bounded attempt-state update to conflict_reconciliation, leaving the original Order+attempt and held reservation durable. If the diagnostic update fails, prepared_unknown remains a safe refusal; do not report successful persistence of the diagnostic. No success, fulfillment, cancellation, refund or spend release follows a conflicting/missing/uncertain binding commit.

For the normal accepted provider status, only a known-successful coherent binding commit can set/return bound_pending plus existing agent_payment_pending. Preserve the existing provider-status admission rules; explicitly test all observed statuses rather than assuming every non-requires_action response succeeded. Cached responses are permitted only after attempt/state/pair validation, not by the old early pi-id check alone.

For requires_action, persist the matching pair and cancellation_unknown before invoking the existing cancellation, without marking successful purchase or releasing spend. Commit known-successful first, release locks, and call cancellation only for that validated original PI. If either commit or cancellation outcome is unknown, retain cancellation_unknown and held spend. Native uniqueness must therefore arbitrate BEFORE cancellation; a returned foreign/reused PI is never cancelled as a loser-cleanup action. There is no cancellation retry from an uncertain state; read-only reconciliation determines what happened. This ordering is part of the new reviewed lifecycle.

## Original-identity reconciliation and once-only spend release

Implement a bounded internal TransactionService reconciliation method for the existing attempt, callable by subsequent checkout requests before cached/status shortcuts. It performs no provider create/cancel/refund/confirm and does not create a new Order. For a known observed PI, read only the authoritative provider object outside DB locks and compare exact request/ownership/context. With no trustworthy PI or contradictory identity, return a durable reconciliation-required refusal and held spend; neither metadata search absence nor elapsed time proves no charge. The support runbook names this exact state and evidence needed, without pretending a provider-unknown state can always be automatically resolved.

After the read, lock Order→Transaction→existing money authority→Payment→sorted Refund→agent key as applicable, recompare attempt and provider/local facts, and classify:

- Existing identical committed bound pair: idempotent result only after strict original binding/history checks. Never issue another purchase or paid event.
- Original owned provider PI definitively cancelled with no captured/received amount or other retained liability, and no local Payment/capture/refund/payout/settlement/revocation/dispute/reconciliation authority: eligible for once-only reserved-spend release.
- Captured, processing, requires-action-but-not-cancelled, contradictory, foreign, missing, stale or unverified provider state; any unresolved finance/commit: preserve held reservation and refuse. A collision with a foreign winning PI does not prove this original attempt has no liability.

Release is one atomic database transition, not the generic _mark_agent_checkout_failed helper: under the exact Transaction and agent-key locks require spend_release_state=held, original key/organization ownership and sufficient aggregate reserved spend (at least this exact amount). Decrement exactly this original amount without GREATEST masking underflow; set spend_release_state=released with exact evidence identity, attempt=cancelled_reconciled and existing failed/cancelled public states in the same transaction. Log the existing transaction/audit event once in that transaction. If already released with matching immutable evidence, return the retained outcome without another decrement; conflicting evidence refuses. A cancelled local Order update that fires existing status triggers must occur before downstream money/key locks: acquire/order and perform compatible Order changes in the accepted order or stage the full transaction so no trigger acquires an upstream lock beneath the key. Prove the actual trigger path; do not infer it from application SQL order.

Known-safe ordinary failures before any provider dispatch may continue their separately verified prior behavior. After the durable attempt is prepared, generic exception handling must never route to the unconditional spend-release helper. Retry checkout checks the durable attempt before its old cached-PI/initiated shortcuts and cannot fall back into Order creation. Retain failed/unknown attempts indefinitely for reconciliation. No cleanup/expiry path may discard attempt history or release its reservation on age alone; the existing stale cleanup must explicitly refuse unresolved attempt states if they can enter its selection. A complete implementation must enumerate those callers and prove this guard, using accepted targets or a precisely reviewed scope addition if necessary.

### Existing signed-webhook spend writer must join the protocol

Additional independently read source: webhooks._handle_payment_failed_agent1483–1559 calls the creating lock_order_money_sync at1494 before checking agent status1503, then decrements spend with GREATEST at1532 without the proposed release marker. Leaving this path untouched would create empty authority during cancellation_unknown and could release spend outside the once-only protocol. It is already an allowed production target and must be explicitly integrated in this semantic successor.

For transactions carrying canonical_agent_attempt_v1, both PI-success and PI-failure handlers first inspect/revalidate attempt and exact pair in their event→Order→Transaction owner before any creating money helper or state/spend change. Incomplete/unknown/conflicting attempt or missing pair PI is a durable retryable event refusal, not a silent terminal200, new empty MoneyState, paid transition, cancellation or release. A bound_pending identical pair admits the existing strict normal capture path. A payment_failed event alone is not proof of terminal cancellation/no future liability and cannot decrement held spend for this protocol. Preserve it durably for original-event reconciliation; only the proven owned terminal no-charge outcome through the common release operation can change the marker/counter/public states. A later duplicate event observes the same terminal release without decrementing again. Do not take an event lock from the asynchronous reconciliation method after money locks; webhook ownership starts with its own event mutex, while provider reads remain outside Order/money locks.

Provide shared synchronous/asynchronous adapters for the same bounded release/attempt transition logic within the already named service targets, rather than two subtly different release implementations. Keep legacy transactions without this new attempt on their separately accepted existing protocol and retain their full regression evidence; never manufacture an attempt merely to reinterpret historical spend. Add barriers for signed failure versus cancellation/reconciliation, success versus binding commit, duplicate event versus duplicate checkout, and cleanup versus unresolved attempt. Verify the real status-trigger and raw webhook path, not only direct helper calls. This is an explicit normal-money behavior extension to be reviewed, not a weakening of the existing event signatures, dedup, capture/refund ownership or full release gates.

The existing lock_stale_agent_payment path must refuse every transaction with canonical_agent_attempt_v1, including bound_pending, so the old cleanup caller cannot independently decrement spend without the release marker. Resolve those attempts through the explicit original-identity reconciliation operation. This is an attempt-specific exclusion, not disabling the scheduler or changing cleanup of legacy transactions; prove actual scheduled selection followed by refusal and unchanged spend/attempt state. Terminal reconciled failed transactions already remain outside its agent_payment_pending selection. Do not silently permit the old cleanup after merely checking that a new attempt is not unknown.

## Mandatory proof for this semantic successor

Use real PostgreSQL transaction/constraint/trigger behavior and provider-boundary doubles with explicit call recording, never real provider spending in qualification. Prove two concurrent preparations yield one durable Order/attempt/provider dispatch; bounded deadlock/claim loser has no committed Order, notification or charge. Crash immediately before/after prepare commit, before/after provider call, before/after response/binding commit and before/after cancellation each preserves one identity and no automatic second purchase. Prove commit uncertainty and every same/different-pair unique conflict preserve winner/provider state and held spend. Prove foreign requires_action does not call cancel. Prove legitimate owned cancellation and concurrent reconciliation release spend once, rollback restores both marker/counter, insufficient aggregate spend refuses, and repeated checkout cannot enter the generic failure helper. Exercise stale cleanup and same idempotency initiate_purchase behavior against unresolved attempts. Preserve original agent success/status/metadata/audit/notification assertions and full retained money gates.

## Caller compatibility scope addition proposed from actual repository search

Add these three test-only targets to the24-file set, making27; no other production target or shared integration conftest is added by this proposal:

25. tests/integration/test_webhook_canonical_payment.py
26. tests/test_notification_events.py
27. tests/test_s1097_order_fulfillment_scoping.py

At817f the canonical integration module's mark_paid double153–159 accepts only two positional arguments and cannot receive expected_binding. Its coherent-binding fixture must supply actual seller/listing/fee/amount identity and a dedicated Transaction PI column. Shared tests/integration/conftest.py already has the actor/fee columns but fixtures leave them NULL/default; its Transaction table lacks the dedicated PI column. Correct schema/fixture setup locally inside the newly named test module using owned test schema, preserve the shared conftest unchanged, and update the double to validate the exact new value and perform the intended coherent paid effects. Do not discard expected_binding with **kwargs or fabricate pass results. Preserve all original retry/processed-order/payment/fulfillment assertions; distinguish the old paid_at metadata repair from the new dedicated-PI capture recovery.

The notification and access-window unit tests call the real mark_paid with an unconstrained AsyncMock database and no explicit reverse-absence result. Supply faithful no-Transaction fixtures/results so the existing legacy compatibility is actually proven; retain the real notification registration and access-window-once assertions. Do not make production code detect mocks or interpret missing test data as canonical absence. Preserve exact original test IDs and full-module qualification, adding these compatibility cases to the final caller inventory and full1990+new accounting as applicable. This is not a seventh baseline exception or permission to change product semantics beyond the reviewed successor.

## Exact attempt schema, transitions and service outcomes (Mars4711 disposition)

This section makes the preceding proposal exact and adds completed handling. The stored JSON object has exactly the keys in the following table, with no additional keys. JSON UUIDs are lowercase hyphenated canonical UUID strings, unlike the internal expected_binding UUID objects; conversion occurs only at the explicit validated serialization boundary. All integers are exact JSON integer values, never booleans, strings or floats. Timestamps use canonical UTC YYYY-MM-DDTHH:MM:SS.ffffffZ and are parsed and round-trip checked. Identifier strings are nonempty, length1–255, case-preserved and contain no whitespace or control characters. No credentials or arbitrary provider payload may enter this record.

| Key | Exact type / bounds | Mutation rule |
| --- | --- | --- |
| version | integer exactly1 | Immutable |
| attempt_id | canonical UUID string | Immutable |
| transaction_id | canonical UUID string | Immutable |
| original_order_id | canonical UUID string | Immutable |
| api_key_id | canonical UUID string | Immutable |
| org_id | canonical UUID string | Immutable |
| buyer_id | canonical UUID string | Immutable |
| seller_id | canonical UUID string | Immutable |
| listing_id | canonical UUID string | Immutable |
| provider_idempotency_key | exact api_key_id + ':' + transaction_id | Immutable; never substitute user purchase key |
| amount_cents | integer0–2147483647 | Immutable; existing purchase rules also apply |
| currency | three lowercase ASCII letters | Immutable; provider-request form, equal canonical money after explicit normalization |
| platform_fee_cents | integer0–2147483647 | Immutable |
| seller_amount_cents | integer0–2147483647 | Immutable; fee + seller = amount |
| customer_id | identifier string | Immutable |
| payment_method_id | identifier string | Immutable |
| provider_account_id | identifier string or null for platform account | Immutable; null is explicit platform context, not unknown |
| livemode | exact JSON boolean | Immutable; validated deployment/test context |
| request_metadata | object with exactly transaction_id, order_id, api_key_id (canonical UUID strings) and buyer_type='agent' | Immutable; values equal the corresponding parent fields |
| prepared_at | canonical UTC timestamp | Immutable |
| revision | integer1–2147483647 | Increment once per committed record transition; overflow refuses |
| state | prepared_unknown, response_observed, bound_pending, cancellation_unknown, cancelled_reconciled, conflict_reconciliation, completed | Only transitions below |
| observed_payment_intent_id | identifier string or null | null initially; first observation retained; differing later PI refuses, never overwrites |
| observed_provider_status | identifier string or null | Bounded observation only, never status authority by itself |
| observed_at | canonical UTC timestamp or null | Set with a complete validated observation; later observations must not precede it |
| evidence_source | null, create_response, signed_event_and_provider_read, provider_read | All three observation fields null iff evidence_source null; no inference from arbitrary caller metadata |
| spend_release_state | held or released | held→released only in atomic release; never reversed |
| release_evidence | null or exact object below | Non-null iff released; immutable once stored |

The exact release_evidence object contains reconciliation_id (canonical UUID string), payment_intent_id (identifier equal the owned observed PI), provider_status (literal cancelled), amount_received_cents (integer exactly0), observed_at and committed_at (canonical UTC timestamps), and source (literal provider_read). No extra keys. The implementation must compare actual provider SDK's terminal status spelling to the literal stored protocol value explicitly; it must not mistake an unrecognized status for cancellation. All existing capture/refund/payout/authority absence and ownership checks remain required beyond these stored facts. Neither this object nor stored provider status is sufficient authority for a later mutation without the lock/identity checks.

Legal state transitions, each with revision compare under the Transaction lock and preserved immutable identity:

- prepared_unknown → response_observed, bound_pending, cancellation_unknown or conflict_reconciliation, according to the fresh original response/event/provider evidence and atomic binding outcome.
- response_observed → bound_pending, cancellation_unknown or conflict_reconciliation; no public successful purchase while only observed.
- bound_pending → completed after the real strict signed capture/paid transition is committed; or → cancelled_reconciled after definitive owned terminal no-charge reconciliation and atomic release; or → conflict_reconciliation on a proven inconsistency without changing existing financial authority.
- cancellation_unknown → cancelled_reconciled after definite owned cancellation/no liability; → bound_pending only when fresh authoritative evidence instead proves the same owned PI succeeded and the exact existing pair remains coherent, so original signed capture can proceed; → conflict_reconciliation on contradiction. No second cancellation follows any such transition.
- conflict_reconciliation → response_observed/bound_pending only when fresh authoritative original-event/provider evidence resolves the original identity and the same immutable pair can bind without any overwrite or competing winner; → cancelled_reconciled only when this exact original owned PI is definitively cancelled/no liability. Foreign PI collision never authorizes adoption of that winner. If the first retained observed PI is foreign/contradictory, automatic resolution cannot replace it with another PI; retain conflict for separately reviewed remediation.
- completed and cancelled_reconciled are immutable attempt states. Financial lifecycle can subsequently change under the existing refund/dispute/payout protocol; it does not erase/reopen this historical attempt or authorize another purchase.

Self-observation is allowed with revision increment only for a newer matching provider observation; an exact duplicate is a no-op. No transition goes back to prepared_unknown or removes the record. Every transition compares the actual record revision/state/facts after locking; a stale provider result must be reclassified against newer local state, never overwrite a newer capture/refund/release. completed must keep spend_release_state=held because normal captured spend is consumed, not a cancelled-attempt reservation to release. cancelled_reconciled requires released and release_evidence; every other state requires held and null release_evidence.

Name the async entry point TransactionService.reconcile_agent_checkout(self, tx_id: UUID, api_key_id: UUID) -> dict. It is an authenticated internal service operation called by create_agent_checkout for an existing attempt; it does not expose a new endpoint. Its provider work is read-only and outside all event/Order/Transaction/money/key locks. It calls shared bounded sync/async attempt transition helpers in the allowed services, which never contact providers or commit on behalf of a caller that owns the transaction. No API-key argument or metadata record substitutes for existing authenticated agent/organization access checks.

Externally visible outcomes preserve the existing checkout success shape (transaction_id, tx_number, status, checkout_id, payment_status, order_id) using actual current state. A coherent completed paid/fulfilling/confirmed/settled purchase returns its cached identity/current status without another paid event, provider write or Order; it must not become a permanent reconciliation refusal merely because the attempt is terminal. For refunded/revoked/disputed states, return the existing state/refusal prescribed by the original financial protocol and never reauthorize access or imply a new successful purchase. A cancelled_reconciled retry returns HTTP422 with code AGENT_PAYMENT_CANCELLED and the original transaction_id, without a further decrement. An unresolved prepared/observed/cancellation/conflict attempt returns HTTP409 with code AGENT_PAYMENT_RECONCILIATION_REQUIRED, original transaction_id and bounded reason enum (provider_outcome_unknown, binding_conflict, cancellation_unknown or evidence_conflict), with no new checkout identifier or provider details. Lock/deadline/connection failure returns HTTP503 with code AGENT_PAYMENT_RECONCILIATION_UNAVAILABLE, never success. Existing403 ownership and404 missing-transaction behavior remains. Do not expose raw provider metadata/customer/payment-method identifiers in errors.

### Signed event before local PI binding

The existing Order.PI-only lookup is insufficient while the durable attempt has not bound the returned PI. Extend both relevant signed PI handlers so an unsuccessful PI lookup does not silently terminal200 for an owned unresolved attempt. After authenticating and preserving the original signed event, use its original metadata transaction_id/order_id/api_key_id/buyer_type only as a candidate locator. Read the candidate durable attempt, then obtain authoritative PI facts outside event/money locks using the existing failed/finance_pending checkpoint pattern. Validate exact original request metadata, amount/currency/customer/account/test context. Reacquire the original event owner then Order→exact Transaction, verify complete reverse cardinality and immutable attempt/revision, and repeat all comparisons before allowing original binding/capture or cancellation reconciliation. Locating a row is not payment authority; forged, foreign or incomplete metadata cannot adopt another attempt or bind a PI.

An authenticated event that exactly locates an owned unresolved attempt but lacks sufficient provider/pair evidence remains durable failed/retryable500 under its original event ID. No creating MoneyState, terminal200, spend release or fulfilled state occurs. A completely unrelated non-agent/non-attempt event retains existing routing and isolation. A late fully matching signed success plus authoritative provider facts may complete the original NULL pair and strict capture atomically under the accepted binding rules, then mark attempt completed; this is how a crash after provider creation can recover without replaying creation or inventing a new Order. Different PI or already conflicting identity refuses. Genuine negative/non-W tests keep their original behavior and zero optional-W lookups.

Retain race tests for original callback versus signed success before binding, duplicate signed failure versus cancellation release, old provider observation after committed capture/refund, and every successful terminal retry. The generic HTTP409 checkout mapping must not be copied as webhook acknowledgement: owned unresolved signed events use durable retryable500 and original-event recovery. This completes the missing event-before-binding and success-to-terminal requirements identified by Mars4711.

## Fourth closed binding mode: agent_attempt_completion (Mars4713 integration)

The previously reviewed direct_payment_completion still refuses both-NULL PI and signed_capture_recovery still requires its already-paid Order/stored PI/Session history. Neither may be stretched to implement this new agent flow. Add the fourth internal mode agent_attempt_completion exclusively for the version1 durable agent attempt above. This expressly extends the earlier three-mode enum; it does not authorize legacy/browser callers to select the agent mode or make their missing PI repairable.

Initial admission requires an existing committed original Order in created status and its exact reverse-linked agent Transaction in initiated status, both dedicated PIs NULL, both Checkout Session observations NULL (the agent producer never creates a Session), a correctly shaped immutable attempt matching this Order/Transaction/key/org/actor/listing/full money tuple and held spend, and fresh original provider evidence. Require no prior paid/fulfilled/settled/refunded/disputed/revoked history, no MoneyState (even empty), Payment/capture/refund/payout authority, Workspace registration or contradictory provider identity. No newly manufactured attempt, caller tx_id alone, unverified metadata, legacy unlinked pair or historical no-attempt row is eligible. The exact attempt must have been durably prepared before the provider call; first creation and signed recovery both use this admission.

Extend the non-creating, non-committing initial-binding helper's mode-specific internal inputs with expected_agent_attempt_id (actual UUID) and expected_agent_attempt_revision (exact int1–2147483647). They are required only for agent_attempt_completion and forbidden in other modes. The lock owner loads and validates the whole durable record again; these two scalars are correlation, not authority. Its expected_binding remains the exact12-field value, with stored_checkout_session_id=None and signed_checkout_session_id=None only in this new agent mode; identity/money/PI facts come from the original attempt plus validated provider evidence. This adds a named NULL-Session allowance, not a general relaxation of checkout_completion or signed_capture_recovery.

The agent provider evidence is a closed immutable internal value with exactly: source (create_response, signed_event_and_provider_read or provider_read); payment_intent_id and provider_status (identifier strings); amount_cents (exact int0–2147483647); currency (three lowercase ASCII letters); customer_id and payment_method_id (identifier strings); provider_account_id (identifier or None for platform); livemode (exact bool); metadata (immutable exact transaction_id/order_id/api_key_id canonical UUID strings and buyer_type='agent'); observed_at (canonical UTC timestamp); and signed_event_id (identifier string required for signed_event_and_provider_read, None otherwise). Validate provider-returned expanded objects at the trusted adapter boundary into these exact scalar forms; no arbitrary object/string coercion or caller-provided override is allowed. This value and expected_binding are re-compared with the immutable stored request and locked rows. Same source/request/PI equality is necessary but not sufficient without existing admission and current financial-state checks.

Under event→Order→Transaction when an event owns the work, or Order→Transaction for the original agent response, native uniqueness atomically assigns the same PI to both NULL columns and records bound_pending. In the original requires_action branch the corresponding attempt state is cancellation_unknown and its cancellation procedure remains bind-commit-first. In signed-success recovery, prepared_unknown/response_observed → bound_pending → completed occurs inside the same original event transaction as both PI assignments, strict normal capture, normal paid/fulfillment transition and committed event outcome. No durable intermediate paid, half-bound or incorrectly acknowledged state is allowed. This mode must not create MoneyState before initial binding admission; the unchanged strict capture processor creates its authority only after full coherent binding in that same transaction. Helper itself never commits or performs provider I/O.

A concurrent original provider-return path or repeated event that finds an already identical bound/completed attempt must revalidate the complete reverse/PI/request/money history and return the idempotent existing outcome, never rerun the created/both-NULL admission or reject a legitimate already-paid purchase permanently. Conflicting PI, superseded request/revision, foreign winner or revoked/refunded/disputed state cannot be repaired or overwritten. If revision advanced only through the identical admitted completion, reclassify under locks to the exact no-op outcome; any unrelated advancement refuses. Do not accept an old provider observation over a newer financial transition.

Qualification includes signed success before the original response is locally bound, signed success racing the original callback before either binding commit, two distinct signed events on the same original attempt, response after successful capture, and both-NULL cases without the durable attempt. Prove exactly one pair/capture/paid event and one immutable request identity, no intermediate durable half-binding or paid-only state, and original-ID recovery after injected transaction failure. Browser/direct/legacy both-NULL refusals, stored-Session recovery gates and all original private Workspace admission/refund rules must remain unchanged. This fourth mode is part of the new semantic successor requiring fresh full-scope review.

## Explicit provider status admission (Mars4717 integration)

The provider's exact status vocabulary and cancellation spelling were checked against the [Stripe PaymentIntent object reference](https://docs.stripe.com/api/payment_intents/object) and [cancellation reference](https://docs.stripe.com/api/payment_intents/cancel) on2026-09-12. Stripe spells its terminal status canceled. This protocol stores that literal unchanged in observed_provider_status and maps only that exact value to its internal cancelled_reconciled state / release_evidence.provider_status=cancelled. The internal double-l spelling is not a provider status to accept. These documentation facts do not verify installed SDK responses or authorize a provider operation; qualification must exercise the real installed adapter against controlled response shapes.

The following table is the selected application policy, not a claim that Stripe mandates it. It replaces the old every-non-requires_action→pending branch. Every row first requires full original provider identity/request validation and atomic same-PI pair binding or a fully equal existing pair; contradictory/missing identity always takes conflict_reconciliation with held spend and no provider mutation regardless of status. No status alone authorizes finance.

| Provider status | Durable/local outcome | Checkout result / allowed next action |
| --- | --- | --- |
| succeeded | bound_pending and existing agent_payment_pending until actual strict signed capture/paid completion; completed only with real financial commit | Existing success shape with payment_status=succeeded and actual local status. No extra capture/confirm/create call; signed-event path completes normal finance |
| processing | bound_pending and agent_payment_pending, held spend, no paid/fulfillment authority | Existing pending shape with payment_status=processing; subsequent reads/events resolve the same PI, no new purchase |
| requires_payment_method | response_observed and initiated, held spend, no paid authority | HTTP409 reconciliation required; do not attach a replacement method, recreate, confirm, cancel or release automatically |
| requires_confirmation | response_observed and initiated, held spend, no paid authority | HTTP409 reconciliation required; no extra confirm or replacement purchase |
| requires_action | cancellation_unknown and initiated after matching pair commit | Only the original one-shot creation caller may execute the existing owned cancellation after that known-successful commit. Confirmed terminal no-charge reconciliation yields422; unknown outcome409; repeats only read, never cancel again |
| requires_capture | response_observed and initiated, held spend; no capture or fulfillment | HTTP409 reconciliation required; no manual capture, cancellation or assumption of no liability |
| canceled | matching pair retained; fresh read + no-liability checks may atomically produce cancelled_reconciled/released/agent_payment_failed | HTTP422 only after proven once-only release; otherwise409. Never call cancel again |
| missing, malformed or unrecognized | conflict_reconciliation, held spend; no paid outcome | HTTP409 evidence_conflict when durably recorded;503 on unavailable DB. No default pending/success or provider mutation |

For a fresh original response in a known non-success state, matching PI columns may be persisted atomically while the final attempt state is response_observed and Transaction stays initiated; the intermediate binding state never commits as a false paid/pending result. The helper's matching-existing-pair branch may advance that same response_observed pair using fresh original evidence without requiring both PI columns to become NULL again. Add bound_pending→response_observed (with initiated status) only when fresh authoritative evidence reports requires_payment_method/requires_confirmation/requires_capture and locked local state proves no capture/paid/fulfilled/refund/payout/revocation/dispute authority; any existing financial progression refuses the downgrade. Similarly, an eligible fresh owned requires_action observation can move response_observed→cancellation_unknown but cannot grant cancellation entitlement to a later reader. This deliberately retains a held reconciliation case instead of retrying the external action.

Provider observations used for release must include actual amount_received and amount_capturable. Extend the mode-specific closed provider-evidence schema from12 to14 exact keys by adding amount_received_cents and amount_capturable_cents, each exact int0–2147483647 copied from the verified provider scalar fields. Both must equal0 for terminal no-liability release, alongside all existing local/history checks. Missing, bool, negative, malformed or nonzero values refuse release; there is no inferred zero. The release_evidence remains the exact seven-key stored terminal certificate specified above; the transient verified14-field input supplies both amount checks before its creation. Preserve raw nonsecret observations in bounded audit evidence without expanding the stored closed record.

The exact transition table additionally permits prepared_unknown/response_observed→cancelled_reconciled only through the same complete owned terminal-no-liability binding/reconciliation and atomic once-only release transaction. These edges support a directly observed canceled response; they never permit a no-response, foreign-PI or elapsed-time shortcut. The observed_at rule means a syntactically complete provider observation with declared source, not a claim of proven ownership: foreign diagnostic observations can be retained, but only the independent full identity check admits binding/release.

Prove all eight table rows through original response, repeated checkout and signed-event/reconciliation paths where applicable. Include unexpected status, expanded/missing field handling, processing-to-failed observation, canceled-with-nonzero-liability refusal, and successful signed capture arriving before local response binding. The table never widens browser/legacy modes, initiates a second external purchase, authorizes cancellation of a foreign PI or substitutes status for strict normal finance.


# Final compatibility correction — proposed mandatory implementation disposition

This section supersedes only the named earlier test-edit restrictions and fixture staging expectations. It does not reduce any financial, lock, event, cleanup, full-node or enabled-release requirement. The current package remains a draft pending complete review and owner acceptance.

## Explicit call convention — DeepSeek F1 / Mars 4731 and 4733

_handle_checkout_completed always supplies the expected_binding keyword to OrderService.mark_paid. Canonical calls carry the exact validated immutable 12-field binding; a genuinely legacy order supplies None only after both reverse transaction lookups prove absence. mark_paid retains a default of None for other existing callers. Omitted and explicit None have identical validation: both refuse when canonical rows exist. Do not introduce a sentinel, session.info carrier, implicit cache, or signature workaround.

Authorize the exact call assertion at backend-base tests/test_webhook_audit.py:287 to include expected_binding=None with a faithful bidirectional-absence fixture. Authorize the exact call assertion at :661 to include the complete canonical binding value with coherent actor, amount, currency, order and transaction fixtures. This is an explicit addition to the retained eleven dedup tuple corrections, superseding the tuple-only edit restriction only for these two call expectations and their necessary faithful fixture rows. Preserve all neighboring assertions: HTTP 200; no inline pending_delivery update for real checkout; fulfillment exactly once; event completion; canonical paid_at UPDATE; transaction_events INSERT; and mark_paid exactly once. A weak ANY matcher or truthy generic Mock does not prove the binding. Keep source line references anchored to backend base 817f7331.

## Agent unit callers

Both added small agent modules require faithful prepared-attempt persistence and original identity fixtures, complete provider response facts and exact request metadata. Checkout success must still persist both payment bindings before return and produce the documented pending state. Cached success must prove exact ownership and reverse binding, preserve the original order, and avoid a second create call.

For requires_action, preserve the business outcome of HTTP 422 only after owned terminal cancellation is proved by the real cancellation/read protocol and the exact spend release has committed. Replace only obsolete generic rollback_spend call-count assertions with stronger exact persisted counter delta, one release certificate, and one audit/status effect. Preserve all neighboring status, event and once-only assertions. Add uncertainty, collision, cancellation failure and foreign-payment cases proving held spend and no foreign cancellation. Pre-prepare missing-payment-method refusal remains the existing safe 422 path; do not redefine it as an uncertain provider attempt.

The existing scheduled-cleanup positive test in tests/test_bq_biz_agent_beta_m2.py must use a faithful explicit legacy no-attempt fixture and retain its cleanup result and financial effects. The direct success/failure/refund cases retain their legacy outcomes with honest database fixtures; obsolete SQL-shape expectations may change only where a named stronger actual PostgreSQL invariant replaces them.

## F6 fixture separation and financial preservation

Create an explicit legacy no-attempt fixture by constructing coherent historical rows directly. Never create a durable attempt and then delete its metadata to manufacture legacy eligibility. Retain existing legacy positive test identifiers and outcomes. Independently retain a real new-agent producer fixture that calls initiate_agent, reserves spend, and calls create_agent_checkout through the complete durable protocol.

The current f6_pending_agent helper returns the seed purchase payment identifier and repairs the seed order only after checkout. Replace this with a fresh independent PaymentIntent and Charge before checkout, including complete immutable provider facts. Leave both seed Order and seed Transaction payment identities unchanged; no post-hoc seed repair or uniqueness bypass.

Retain legacy stale/unlinked cleanup positives, exact spend decrement, one cancellation/failure event and audit, replay no-op, and complete refusal snapshots for nonstale, paid, refund-admitted, partial/full refund, changed binding, reconciliation and missing PI cases. Add companion new-attempt cases for every durable state proving old cleanup refuses without releasing spend or creating money state. Reconciliation and legitimate refund effects are separate protocols; a refund spend delta is not an attempt release certificate.

Preserve the entire legacy F6 concurrency coverage: cleanup-first and refund/listener-first real PostgreSQL barriers; Order-before-Transaction acquisition; no Transaction lock while blocked at Order; binding changes under the Transaction lock without Order reentry; release of every previous candidate lock chain before the next candidate; successful normal capture/listener delivery excluding stale cleanup; once-only refund delta and replay stability. Multi-candidate legacy tests still clean both eligible rows. Add new-attempt companions with refusal and lock release, rather than changing every positive into zero cleanup. For new-attempt normal capture, use the genuine signed capture path to complete the attempt; do not manufacture money state before admission.

## Native uniqueness supersedes late duplicate admission test

At backend-base tests/test_refund_processing_protocol.py:1249–1256, test_duplicate_payment_intent_order_binding_refuses_before_admission currently inserts a duplicate Order PI before expecting a later OrderMoneyConflict. The new nondeferrable unique constraint must reject that UPDATE immediately. Retain the test identifier and invariant, but explicitly expect the named database uniqueness violation within a savepoint, prove both original Order and Transaction identities remain intact, and prove no event, refund, capture or money effects were admitted. Do not disable the constraint to keep the obsolete late-refusal staging. Keep separate pre-migration duplicate census and cutover refusal tests where the old catalog legitimately allows duplicates.

## Audited callers needing no scope extension yet

The repository-wide caller search also found tests/test_seller_post_listing_setup_gate.py:412–452. Its seller setup hold must still return the original 422 before OrderService construction or any Stripe call, so preserve it unchanged and execute it in final qualification. The cleanup atomicity module is an explicitly proposed31st test-only target under the final disposition below: its obsolete fake scan cannot qualify the current task. Preserve the real rollback invariant and exact test identifier with the actual task/database behavior, not the outdated SQL order or mock rollback count.

## Second native-uniqueness compatibility case — F7

The same early-refusal correction is required for tests/test_refund_processing_protocol.py:3191–3223, parameter duplicate_payment, whose line3199 stages duplicate Order PI before delivery token refusal. Preserve that parameter/node identifier, expect the exact native unique constraint rejection in a savepoint, and prove the complete F7 snapshot and original bindings unchanged with no new delivery token or authority. Other F7 binding/refusal parameters retain their existing HTTP409 and snapshot assertions unchanged. This is the same already-proposed test-module scope, not an additional file. Do not bypass native uniqueness to stage an impossible final-schema duplicate.



## Cleanup atomicity compatibility — exact31st proposed test-only target

At backend817f tests/test_cleanup_stuck_agent_transactions_atomicity.py:14–19 configures mappings().fetchall(), while scheduled.py:583–590 uses SELECT id,order_id and mappings().all(), then rolls back the read-scan transaction. The old test54–61 only supplies candidates for an obsolete UPDATE RETURNING clause and expects a Transaction UPDATE before Order. Current task594–651 calls lock_stale_agent_payment, updates a linked Order first, then the spend counter and audit before its candidate commit. The linked Transaction/event update comes from the actual Order trigger. On an unexpected exception, the task propagates it; the owned AsyncSession context closes and its uncommitted database transaction must roll back. Do not require an invented explicit rollback call in production merely to satisfy a fake's count.

Preserve test_cleanup_stuck_agent_transactions_rolls_back_on_mid_execution_failure as an executed node. Replace its obsolete fake staging with real migrated PostgreSQL and the actual scheduled task on a coherent legacy no-attempt fixture. Use independent observer snapshots and deterministic injected failures after the Order/trigger writes and after the spend write, before candidate commit. Prove the original exception propagates, no candidate commit occurred, the context has closed, and all Order/Transaction/key/event/audit rows equal their pre-candidate state. Read-scan rollback is distinct from candidate rollback; the actual storage outcome is authoritative. Prove the next owned session can acquire the same lock chain and complete a valid legacy cleanup exactly once. Preserve the existing no-partial-financial/event/audit invariant; change obsolete explicit-rollback-count and Transaction-first SQL assertions only to these stronger database outcomes. No production scheduled.py or shared fixture target is added, no current guard is bypassed, and no new exception/skip/xfail is introduced. New-attempt companion cases continue to prove cleanup refusal without release.

The31-target scope was dispositioned by Mars in message4745; this corrected candidate still requires fresh GLM and DeepSeek review. It does not claim the test was run or that the backend is qualified.
