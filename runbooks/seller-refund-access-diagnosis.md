---
title: Seller purchase access after refunds
owner: vulcan
last_verified: '2026-09-12'
aliases:
  - Partial refund download refresh
  - Seller delivery credential diagnosis
error_signatures:
  - DELIVERY_CREDENTIAL_GENERATION_STALE
  - DELIVERY_REFRESH_UNAVAILABLE
  - DELIVERY_REFRESH_ACCESS_REJECTED
  - "'oauth_provider' is an invalid keyword argument for User"
---

# Seller purchase access after refunds: diagnosis and upgrade runbook

Owner: Vulcan S1712, coordinating with Mars S1714. Updated 2026-09-12 with accepted F7 scope and candidate-bound delivery operations. The current assessment below supersedes the explicitly historical MP239 and MP237 maps. This is a diagnosis and evidence guide; it does not certify a release or authorize another implementation owner.

## Current F7 delivery implementation and operating procedure

F7 is unanimously accepted at `26588d3571efc3a19d1a9b591fb51991690e3f62`, spec SHA256 `1e3c9783575db778301d0b7ec8a66f1dadca0b855ce59cf0cff9bdaf8cfd6806`. All full CC, GLM and DeepSeek responses and the accepted clarification were independently read. [Scope decision verification](evidence/seller-refund-access-s1712/SELLER-F7-SCOPE-REVIEW-CHECKPOINT-S1712.json).

MP243 finished at `64c810dedba1f9667fbac901f2a15f04b4f98091` with a clean checkout and matching canonical remote. Its parent is verified MP242 `ffdcacb14929328285ef3ed27ba6dc939e550f42`; only DeliveryService and the existing refund protocol test module change in this increment. The service SHA256 `df5f9142a32689e069f74b4b31ea8173edc02cbd037b37855a9644e7cd3db6eb` matches independently read source. The procedure below explains this verified build, not an accepted or deployed product. The readiness correction, full review and production acceptance remain open. The older proposal and local 0.109 framework observations below are historical, superseded by accepted scope and the pinned source verification.

Independent final verification reconciled all 42 source targets, 321 migration and two protected hashes, 139 retained and 233 predecessor artifacts, 57 final artifacts, and all four exact source-bound run headers. Core665 passed/four unchanged baseline failures; adjacent284 passed/two unchanged baseline failures; Gold14 passed/11 intentional marker deselections and canonical55 passed. Across1,024 execution entries there are1,010 unique nodes:1,004 passed and six unchanged baseline failures. All868 prior nodes,142 new F7 cases and19 original posting positives remain; no errors, skips, missing/duplicate/conflicting nodes or unmocked attempts were found. All ten actual ASGI lifecycle cases passed under pinned FastAPI0.110.1/Starlette0.37.2/SQLAlchemy2.0.25. Read [exact artifact verification](evidence/seller-refund-access-s1712/SELLER-MP243-EXACT-ARTIFACT-VERIFICATION-S1712.json), [fixed pre-build expectations](evidence/seller-refund-access-s1712/SELLER-MP243-FIXED-EXPECTATIONS-VERIFICATION-S1712.json) and [predecessor/header verification](evidence/seller-refund-access-s1712/SELLER-MP243-PREDECESSOR-HEADER-VERIFICATION-S1712.json). These bind manifest SHA256 `f3cea6577e694b6656ea99a83a9c0c89f93ad5f0a816336e85cc65d14cea9770`.

The first full core reused the earlier proof database and failed three additional missing-account reseed cases because journal lines still referenced those accounts. Those failures remain preserved. The unchanged full suite on a fresh, fully migrated database with zero pre-run journals/lines passed all three. Do not delete retained financial history to prepare a test database or relabel those first-run failures as baseline exceptions. The remaining four subscription-service and two adjacent failures match the predecessor node and error-message identities exactly.

### How a download is admitted

The registered delivery download route calls `DeliveryService.stream_from_vz`. Token validation checks its signature and claims, route/transaction binding, buyer, active attempt, JTI and expiry. The shared `_lock_transaction` helper discovers links without writer locks, then locks the order, transaction and existing money authority in that order. It rechecks both links, uniqueness, payment binding, refund/revocation, access expiry and download limits before child audit or queue writes. It never creates money authority for delivery. An unlinked legacy transaction needs genuine absence checks and a recheck while locked; it is not a lock-free bypass.

A full refund or closed access refuses further delivery. A partial refund requires prior delivered access and an otherwise open access window. Token renewal or retry must preserve the prior partial-refund delivery state and timestamps; it must not manufacture new delivery success. A valid token alone is insufficient when the current order has closed access.

### Streaming and cancellation

FastAPI 0.110.1 closes the request dependency before response iteration. The service therefore creates separate short callback sessions through the existing `AsyncSessionLocal` factory. Each callback commits or rolls back and closes; the request session is not reopened. The pinned inspection also binds Starlette 0.37.2 and SQLAlchemy 2.0.25. See [pinned framework verification](evidence/seller-refund-access-s1712/SELLER-F7-PINNED-FRAMEWORK-VERIFICATION-S1712.json) for file hashes and the exact source lifecycle.

The reader fetches a bounded chunk outside a database session. The maximum is 262144 bytes. Before yielding that chunk, the service revalidates current access and commits `delivery_chunk_admitted`, then closes that callback session. At most the single admitted chunk may finish after a concurrent refund or revocation. On resumption after ASGI send returns, a fresh callback rechecks authority and records range progress. ASGI send returning does not prove that the buyer received or saved the bytes.

Finalization uses another current-authority check and complete range coverage. Disconnect or cancellation closes both the response iterator and reader with shielded cleanup. An interruption audit is attempted only while access remains current. A refused late interruption/failure callback must not undo a refund or write completion. Once response headers have been sent, a later refusal may appear to the client as an interrupted download rather than a new JSON error response; diagnose the retained state and exact callback, not only the initial HTTP status.

### Payment helper and queued delivery

The actual payment helper commits the webhook idempotency record before it calls `create_delivery_record`. A refund may commit between those two operations. A committed idempotency marker followed by a 410 delivery refusal can therefore be correct. Do not delete that marker or replay a payment merely to force a delivery record.

The existing trust enqueue helper commits its outbound record, refreshes it, and publishes. The local `_DeliveryEnqueueSession` adapter detaches the refreshed outbound value and ends that read transaction before publication. The service then reacquires current order/transaction authority before its post-publication audit. Publication success is not delivery authority and does not prove that an external device consumed the message. The external device consumer remains outside this source verification.

### Diagnose before retrying

Capture the exact deployed commit and active source identity first. Record transaction/order IDs, signed-event identity, attempt/JTI, request range and timing without copying token or secret values. Compare both order/transaction links, the existing money authority, refund and payment records, delivery metadata, audit events, transaction events, idempotency record and any delivery dead-letter row. Journal and agent-spend snapshots must remain unchanged on refused delivery operations.

A 401 can indicate an expired credential; 403 can indicate buyer/route or access-window/limit refusal; 409 can indicate changed bindings, incompatible status or a conflicting idempotency record; 410 can indicate closed access or a superseded/revoked token. Use the exact error detail and current state rather than mapping every status to one cause. Do not reset a completed refund, extend access or alter a download count to clear an error.

`Delivery interruption audit refused` or `Delivery failure callback refused` can be expected after a refund or newer attempt wins. Check whether the original transport exception was preserved and whether state stayed closed. A `delivery_chunk_admitted` event alone is not progress or completion. Missing completion after an interrupted send is expected.

Dead-letter retries recheck canonical order binding, current status, partial-refund state, attempt and JTI before locking the child row. A stale item cannot become a fresh delivery attempt. Preserve quarantined rows and their evidence. Use only the reviewed retry path after identifying the underlying failure; manual row edits bypass these checks.

### Upgrade and acceptance checklist

Bind the final manifest to the accepted F7 decision `26588d3571efc3a19d1a9b591fb51991690e3f62`, all 42 approved targets, 321 migration hashes and two protected hashes. Preserve the prior 868 unique test nodes and 19 original posting positives. Reconcile all new results and six known predecessor failures individually; neither aggregate passing counts nor a successful build process closes review.

Require actual registered-route ASGI cases for completion, refund before admission, after admission, between chunks and before finalization, send failure, disconnect, disconnect after refund, cancellation and oversized chunks. Each must verify closed sessions, zero checked-out connections at reader/send boundaries and independent order/transaction NOWAIT probes. Keep both real refund/operation orderings, the actual two-commit payment helper, real enqueue/publication boundary, stale credentials, partial refunds and dead-letter cases. A direct async iterator test alone is insufficient.

Recheck this lifecycle when upgrading FastAPI, Starlette or SQLAlchemy. Keep exact framework versions in test evidence. Full Council acceptance, accepted environment integration, genuine protected-run/settlement windows, production writer quiescence, exact deployed identities and fresh joint AWS/R2 browser evidence remain separate gates. This candidate note does not authorize bypassing any of them. Existing user approval applies; no renewed permission is pending.

### Disposable PostgreSQL startup race — open correction

Hosted run `34687335197` on `64c810de` passed canonical-payment but failed Gold Path setup before tests: socket readiness succeeded, then the role-creation query reported that database `s1714_f3_34687335197_1` did not exist. Owned-container cleanup succeeded. This is distinct from MP242's corrected missing-role failure and does not indicate an application database outage.

The failed hosted image digest `18cfe3ef5e6815560c98237d6216d1e5119702fb0f3894c8785dd58b8bbe5d73` matches the locally inspected disposable image. Its entrypoint starts a socket-only temporary server before creating the requested database, then stops that server before launching the final server. The current workflow's socket `pg_isready` can therefore report readiness too early. Require a bounded actual query to the requested database over TCP, with failure and owned cleanup preserved. A green retry of unchanged source does not eliminate the race. Mars confirmed this remains a required correction before full product acceptance.

Read the [source-bound diagnosis](evidence/seller-refund-access-s1712/SELLER-MP243-HOSTED-READINESS-DIAGNOSIS-S1712.json), [failed job log](evidence/seller-refund-access-s1712/SELLER-MP243-HOSTED-FAILURE-S1712.log) and [exact image entrypoint source](evidence/seller-refund-access-s1712/SELLER-MP243-POSTGRES-ENTRYPOINT-S1712.txt). These are evidence artifacts; do not execute the archived entrypoint as an operational command.

## Historical verified MP242 result and then-proposed F7 scope

At 09:04 UTC on September12, MP242 finished at `ffdcacb14929328285ef3ed27ba6dc939e550f42`. Independent checks found a clean checkout, identical remote head, exactly41 approved source files, unchanged321 migration and two protected hashes,139 predecessor and233 final artifact hashes, and no mismatches. Core523 passed/four documented baseline failures; adjacent284 passed/two baseline failures; no errors/skips. All19 original posting positives pass;590 node references across65 requirements reconcile. There are868 unique test nodes across882 execution entries, with720 provider-guard records and zero unmocked attempts. [Exact verification](evidence/seller-refund-access-s1712/SELLER-MP242-EXACT-ARTIFACT-VERIFICATION-S1712.json).

[Corrected hosted run34684647863](https://github.com/aidotmarket/ai-market-backend/actions/runs/34684647863) succeeded at the same exact commit:55 canonical-payment passes and14 selected Gold Path passes, with11 intentional deselections from the literal marker selection. Full migration and owned-container cleanup passed. The missing-role failure described in the older checkpoint below is resolved by creating the inert non-superuser application role in the fresh disposable cluster before migration. [Completed jobs and full-log hash](evidence/seller-refund-access-s1712/SELLER-MP242-HOSTED-PASS-S1712.json).

The actual cleanup task now uses a separate transaction per candidate, locks the canonical order first, revalidates current linkage/staleness/payment/refund state, creates no incidental money authority and rolls back refusal. Real task/refund/listener barriers and cross-candidate lock release are exercised. The exact predecessor product with revised tests retained seven behavioral failures and a separate transaction-NOWAIT lock failure; final source passes those same nodes. These are explicitly predecessor-product/revised-test combinations, not a relabeled pristine baseline. [Retained failure-node read](evidence/seller-refund-access-s1712/SELLER-MP242-BASE-REGRESSION-READ-S1712.json).

F7 delivery scope is proposed at runbooks `c3ff459074511759e075fb8c7cc475536f5f2086`, spec SHA256 `7b8d5502ae270958ccc036c0d064318eca10a4a8c115df205e599c99d94a2a15`, section20. It adds only delivery_service.py, yielding42 total files, and retains the existing refund protocol test host. The full49-line amendment was independently read against the bounded audit; no substantive omission was found. A nonblocking method-name typo is recorded for accepted clarification. Full CC/GLM/DeepSeek scope review is pending; no next MP or final product acceptance is implied. The definition explicitly admits bounded in-flight chunks before revocation, prevents new admission after revocation, and prohibits locks across transport/yield suspension. [Scope comparison](evidence/seller-refund-access-s1712/SELLER-F7-SCOPE-ASSESSMENT-S1712.json).

The streaming lifecycle must be proved under the actual pinned framework version. The inspected local environment contains FastAPI0.109.0 and Starlette0.35.1, while requirements pins FastAPI0.110.1. Local request dependency cleanup occurs before response iteration; SQLAlchemy may allow reuse of a closed session, but that is not safe ownership or cleanup proof. The existing AsyncSessionLocal factory can provide explicit short stream-owned sessions inside delivery_service.py, with no demonstrated extra-file need. Require actual ASGI completion/disconnect/cancellation proof under the pinned version, and no locks across transport or yield. Exact0.110.1 source was not available in the bounded local inspection; no behavior claim is made for it. [Source/version assessment](evidence/seller-refund-access-s1712/SELLER-F7-STREAM-SESSION-ASSESSMENT-S1712.json).

For future diagnosis, the workspace archive at `/Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/outputs/mp242-retained-evidence-s1712/INDEX.json` maps234 copied-and-rehashed artifacts, approximately26MB, including the final manifest and every artifact directly named by its final/predecessor maps. Original files and embedded paths are unchanged. Resolve missing temporary paths through this index; do not rewrite a manifest and claim its original hash. This is not a recursive archive of every historical nested reference, a repository mirror or production proof. Read archived helper scripts before any execution because some perform mutations.

Full product/environment Gate3, protected run25, genuine ordered P/N holds, owner-released Workspace window, production freeze/quiescence and fresh external acceptance remain open. No renewed user approval is pending for the authorized correction.

## Historical MP242 build-in-progress and delivery audit checkpoint

F6 is unanimously accepted at runbooks decision `e7c7b489cae0f032a0ae480a73c56f2a1a065ec6`, spec SHA256 `d77baa6f7767ecf3389f7c08dd612fc8989c311c550628c3176d58dfcef96581`. Mars owns MP242 task `cb96adb8e127` / TS242 with 41 authorized files from `4812055`. At this checkpoint the build is still active; its preliminary commit `b9ffde2e5ce755ecd95d926db923181f981324e8` has been pushed, but is not a terminal or accepted product. The previous scope-under-review wording below is historical.

[Hosted run34684328866](https://github.com/aidotmarket/ai-market-backend/actions/runs/34684328866) at b9ffde2 completed with **55 canonical-payment tests passed** and Gold Path setup failure. Gold Path now creates its isolated PostgreSQL 17, but migration stops because the configured `s1714_disposable_app` role does not exist. `20260826_001_issue_channel_schema_and_queue.py:387–394` requires an existing non-superuser application role distinct from the migration administrator. The workflow exported the name without creating it. Its owned-container cleanup passed; the selected Gold Path tests never ran. This is a setup failure, not another count of product test failures. MP is correcting the existing workflow. [Exact job/log receipt](evidence/seller-refund-access-s1712/SELLER-MP242-HOSTED-CI-DIAGNOSIS-S1712.json).

For a future fresh test environment, provision the exact application role in the owned disposable database before the full migration, preserve the role separation and database/provider guards, and require the selected test command to execute. Do not infer a pass from a successful migration or container cleanup. Bind both hosted jobs to the final exact commit, then reconcile the full retained suites and requirement matrix.

### Additional actual delivery paths requiring owner disposition

Independent bounded source review at `4812055` found actual authenticated `/deliveries` routes and the successful-payment webhook calling `app/services/delivery_service.py`. Token creation locks the transaction before inserting an audit row whose order foreign key needs an order lock. Refund processing uses order before transaction. The audit and dead-letter tables each reference both parents; webhook idempotency also inserts a transaction reference before updating the transaction and inserting the order-linked audit. Mars independently confirmed these relationships with SELECT-only catalog evidence on the retained migrated PostgreSQL 17 database. This establishes a static reverse dependency, not an executed production deadlock.

The same service has actual streaming callbacks that can write `delivery_failed` after a late error or `delivered` after retained full byte coverage without validating current refund/order state. A late callback can overwrite refunded status. Token validation only checks selected transaction statuses; chunk output precedes the progress check. Preserve the accepted partial-refund policy and current order authority rather than inventing a new access rule. The report records the exact reachable methods and line numbers.

A lock-helper-only correction is insufficient. `record_stripe_webhook_idempotency` writes before that helper. `process_dlq_retry` bypasses it, although no app caller was found and no active retry-worker claim is made. Streaming crosses commits. `_queue_delivery_request` calls a trust helper that commits before publishing and before the next audit; entry locks no longer exist after that boundary. The trust message related-order/transaction fields are plain UUIDs, so their names do not establish another foreign-key edge.

The [complete bounded service/caller audit](evidence/seller-refund-access-s1712/SELLER-DELIVERY-OUTSIDE-ACQUIRER-AUDIT-S1712.txt) and [source/catalog hash receipt](evidence/seller-refund-access-s1712/SELLER-DELIVERY-OUTSIDE-ACQUIRER-AUDIT-S1712.json) define the minimum proposed addition: delivery_service.py, with the existing refund protocol test host. No route, migration, shared fixture or trust-service edit is established as necessary by this audit. Mars read the full package and owns the concrete follow-up scope after MP242 finishes. The active build is unchanged; the report does not authorize a competing implementation or certify the whole graph, production device path, raw-HMAC or VC transport.

Diagnosis should retain the precise source/image, current order/transaction/refund/attempt identities, current phase and correlated audit evidence. Use normal retry and reconciliation; do not repair a symptom by overwriting financial status or issuing another token. Required implementation proof includes actual caller/foreign-key races, current-access refusal, late full-refund error/completion, stale attempt/JTI, supported unlinked handling and the trust/stream commit boundaries. Publish a correction only after exact final source, actual execution and full review; production freeze, quiescence and joint live proof remain separate gates.

The independent empty-ref hook correction **is deployed** as `22a0deac6903414dce2f232d842d3588127ec9fa`. Runbooks PR178 and PR180 bind the reviewed source, installed-hook canary and actual automatic gateway reload. That resolved build-transport defect does not establish product acceptance.

## Historical MP241 exact candidate and acceptance findings

MP241 task `a1082e11438e` / TS241 finished at [4812055d08f91664fba373ab1a1406fe35dd66e7](https://github.com/aidotmarket/ai-market-backend/tree/4812055d08f91664fba373ab1a1406fe35dd66e7) without further source edits. This retains MP240's saved commit after a capacity failure and runs final full suites after its last changes. Accepted F5 is decision `79e931d9828544029580ae478bba0f508b04ae3a`, exact spec blob SHA256 `8283e7ce20dcc3e1ceeb2324e7750ec4535be9efad0e0008b8fd9b5691e8f34d`. The current 37-file scope adds only the bounded OAuth helper to F4. The source-only and live acceptance boundaries still apply.

Independent verification found exact clean/remote-equal source, 37 source hashes, 321 migrations, two protected files, 139 final artifacts and 104 retained predecessor artifacts. Full core:503 passed/four failed. Full adjacent:284 passed/two failed. No errors/skips. All19 original posting positives pass;501 node references across59 requirement rows match;631 guarded nodes record fail-fast self-test and zero unmocked attempts. The six remaining failures are separately retained baseline cases, not newly accepted fixes. The wrapper's `tests_status=not_configured` does not replace actual JUnit/log evidence. [Independent exact-artifact receipt](evidence/seller-refund-access-s1712/SELLER-MP241-EXACT-ARTIFACT-VERIFICATION-S1712.json).

The two concrete MP239 defects below are corrected on4812055. The OAuth helper validates the signed subject before a query and selects a mapped User;11 actual signed-input cases pass. Three access-route functions have six open/closed executions, while five other callers are source-audited only. The unrelated `wallet.py` SELECT*/User-constructor defect remains explicitly outside scope and is not certified. Orders GET now uses existing current admission before credentials:12 signed HTTP cases cover partial/delivered/completed with available/expired/zero/exhausted allowance, verify no assume/presign attempts on refusal, and preserve before/after snapshots. Device GET remains informational and public-reference semantics remain separate.

The final suites also add actual source/approval/publication-to-partial-refund Workspace issue/file/dedup with local KMS/provider doubles, raw source/file/state HTTP cases, nullable assignment barriers and MCP duplicate identity/no-charge evidence. These are executable local results, not fresh browser, R2 or production proof. [Bounded source assessment and support interpretation](evidence/seller-refund-access-s1712/SELLER-MP241-SOURCE-ASSESSMENT-S1712.txt).

All three product reviewers accept API-cost shared blocking as not applicable at this exact supported source. The only actual caller uses the legacy entity and template 5000/2000; the strict merchant chart is 1000/2110/4000/5100 and lacks 2000. The bounded merchant attempt refuses without journal/account mutation. Its two ordering cases prove independent progress, while five other callers prove actual blocking. Do not manufacture an account relationship or extend this disposition to another writer. The finite foreign-key map correctly distinguishes 13 executed assignment edges from 17 source dispositions, but GLM identified a separate scheduled writer omitted from that map; applicable-chain acceptance remains open as detailed below.

The normal up-to-date push attempt failed because the bridge rejects empty ref-update input; the remote independently equals4812055 and the full-candidate scanner passed. No product upload is missing. Preserve that diagnostic separately without bypassing hooks, changing the harness or making an empty commit. Product/environment full Gate3, production freeze/quiescence, protected run25 lifecycle, genuine P/N holds, Workspace W window and fresh joint paid acceptance remain open. The transport observation later in this guide is bounded historical production evidence only.

## Full review result and F6 correction checkpoint

The complete panel at `4812055` returned CC **APPROVE_WITH_NITS**, DeepSeek **APPROVE_WITH_MANDATES**, and GLM **REQUEST_CHANGES**. There is no unanimous product acceptance. The [panel receipt](evidence/seller-refund-access-s1712/SELLER-MP241-PANEL-CHECKPOINT-S1712.json) binds all three fully read response hashes. Existing local pass counts remain valid for their recorded environment; they do not close the new findings.

**Scheduled stale-agent cleanup reverses the money lock order.** `app/tasks/scheduled.py:578–604` bulk-updates transactions before updating linked orders. `celery_app.py:236–239` registers the task hourly at minute 20. Normal agent checkout persists an order and payment intent in `agent_payment_pending`, so a delayed capture/refund or listener operation can overlap this cleanup. Refunds/listeners lock order before transaction. This opposing order admits a deadlock window; the finite map omitted this actual outside writer. [Independent exact-source verification](evidence/seller-refund-access-s1712/SELLER-MP241-GLM-BLOCKER-VERIFICATION-S1712.json).

For diagnosis, retain the exact failing task and database error, order/transaction IDs, source/image identity, and the concurrent refund/listener event. A deadlock/retry alone does not prove duplicate money, and a quiet task does not prove the lock defect is corrected. Preserve pending provider identities and normal retry/reconciliation. Do not disable the hourly task, manually change financial statuses, remove paid rights or release agent spend to suppress the symptom.

The correction must select candidates without transaction locks, acquire linked orders first, then revalidate binding, age, status and money restrictions before changing transaction/order/event/spend/audit state. It must preserve genuinely stale unlinked cleanup. Per-candidate transactions or a complete deterministic batch lock set must prevent a new cross-order cycle. The common lock helper can create missing authority; refusal must not commit an accidental authority row. Require actual task-versus-refund and task-versus-listener PostgreSQL barriers in both orderings, correct serial results and exact retry/event/spend counts.

**Hosted payment commands expose additional test compatibility failures.** [Draft backend PR396](https://github.com/aidotmarket/ai-market-backend/pull/396) has exact head `4812055`; [Gold Path run 34682341722](https://github.com/aidotmarket/ai-market-backend/actions/runs/34682341722) failed both jobs. These are distinct from the six retained-suite baseline failures:

| Hosted result | Confirmed cause | Required verification |
|---|---|---|
| 14 gold-path setup errors | Imported module-autouse migration fixture requires the real isolated S1714 PostgreSQL 17 environment; the job supplies none. | Actual owned container ID, matching unique database and nondefault loopback port, full migration ancestry and the unchanged provider guard; then run the literal selected command with zero errors/skips. |
| 51 canonical-payment failures | Private temporary stripe_events omits payload_json and signature_valid used by current dedup admission. | Align actual temporary fields/types and retain handler SQL, positive/refusal/retry and no-further-write assertions. A persistent migration does not repair a shadowing temporary table. |
| 1 offline fulfillment failure | Historical orders fixture omits delivered_at and other fields read by the current service; temporary listings also needs matching seller_id. | Provide the complete query fields in this test's rollback scope; retain actual asyncpg, ownership/current-access checks and queue/outbound effects. |

Three Connect bind cases passed. The separate canonical job's PostgreSQL 16 connection succeeded; it is narrow historical integration scaffolding, not full-ancestry refund protocol proof. Keep it isolated from the full-proof database because its fixture installs and removes historical schema/trigger objects. [Detailed source diagnosis](evidence/seller-refund-access-s1712/SELLER-PR396-HOSTED-CI-DIAGNOSIS-S1712.txt) and [raw-log/source hash receipt](evidence/seller-refund-access-s1712/SELLER-PR396-HOSTED-CI-DIAGNOSIS-S1712.json) make the failures reproducible without copying provider data.

Mars's F6 proposal is runbooks commit `2218d27dcbc3eca6f08ad6650e1c2272b99bfbec`, spec blob SHA256 `9f66727c6592951880bc33be9c6597badd4cd435c82dfb15bb72147fd41df93b`, section 19. Its complete addition was independently read and hashed. It adds four files to the accepted 37, for 41: scheduled.py, the existing Gold Path workflow, and the two failing integration files. Existing protocol test files hold the race proof; shared integration/conftest and unrelated product work remain outside this addition. [Bounded scope assessment](evidence/seller-refund-access-s1712/SELLER-MP241-CORRECTION-SCOPE-S1712.txt). The proposal is under full review, not implemented or accepted at this checkpoint. MP remains the author and Mars the dispatch owner; no renewed user permission is pending.

For the upgrade evidence, preserve the older reconciliation artifact and add an explicit current-spec supersession: its af1de970 provenance is predecessor-only, while the final accepted amendment and new candidate must have exact current hashes. Reconcile all new hosted and race nodes alongside the original posting positives, complete core/adjacent suites, 321 migration hashes, protected files and provider-guard properties. The refund path returns before the shared processed-request logger; durable applied-event state and the processor commit are the authoritative completion evidence, and missing that optional log line is not proof of a failed refund. Do not infer success from an admitted event alone.

At the dated F6 proposal checkpoint the separate empty-ref bridge correction was unshipped; the latest checkpoint above supersedes that status. All product/environment reviews, production trigger freeze before merge, old-writer quiescence, genuine P/N holds, owner-released Workspace W window and fresh external paid acceptance remain open.

## Historical MP239 candidate and then-open correction work

The following describes the predecessor683dfc82 checkpoint. Its instructions to correct OAuth/GET and add missing executable cases were fulfilled in the specific MP241 evidence above; other scope/release limits remain. Do not interpret the dated F5-under-review wording as the current design decision.

MP239 finished and pushed [683dfc82d542ed45173cd8fbde53e254b2be263c](https://github.com/aidotmarket/ai-market-backend/tree/683dfc82d542ed45173cd8fbde53e254b2be263c) under the unanimously accepted F4 decision `af1de97030d090414fd7f09aafb5d005eeb9434a`. Its manifest explicitly says `pushed_exact_head_tested_acceptance_incomplete`. Independent verification reconciled 36 source files, 321 migration sources, two protected files, 67 artifacts and 62 retained predecessor artifacts; the canonical branch and clean checkout match. Core results are 367 passed/10 failed; adjacent results are 284 passed/two failed, with no skips or errors. Six failures reproduce unrelated baselines; six actual agent-route cases fail at the OAuth user loader. All 297 referenced results in the 50-row requirements matrix match JUnit, but a matching result does not establish complete requirement coverage.

The [exact artifact assessment](evidence/seller-refund-access-s1712/SELLER-MP239-EXACT-ARTIFACT-VERIFICATION-S1712.json) preserves hashes and coverage limits. The continuation manifest SHA256 is `9cf91b2cbe8e4f0059788fd7c697c608722184ac7c33023a1ec6b34f05f7fb27`. The full core JUnit SHA256 is `00d94c0d1638b0a3ea22575e2aa762ae91328e4efd1b22985eafef785e8d4d88`; its log SHA256 is `473c95e2c5e8a24be060699d9bcc707ae522d94af853f06bc74571685dbaec24`. These are retained execution results, not tests rerun by this documentation change.

Two concrete defects need correction:

1. **Actual agent authentication fails before access authorization.** At this candidate, `app/api/v1/agent/router.py:514–544` verifies the bearer token, selects every database user column and constructs `User(**dict(row))`. Full migration ancestry includes `oauth_provider` and `oauth_id`, which the mapped User constructor does not accept. The six real data/request/artifact route cases fail from this common cause (four direct/chained exceptions; two closed-access cases wrap it as HTTP500). Read the [exact diagnosis](evidence/seller-refund-access-s1712/SELLER-MP239-OAUTH-LOADER-DIAGNOSIS-S1712.json). It was captured while the build was still running; the final candidate and JUnit hashes above are unchanged. Mars owns F5 candidate `35679c650a181f6d2bdf9ad762166555d2d8ccc4`, under full CC/GLM/DeepSeek review at this checkpoint. Its only additional product file is the router helper, using normal mapped ORM selection by verified subject. Preserve token verification, invalid/missing token and unknown-user refusal; do not change the User model, schema or OAuth policy. Actual helper/service route tests must pass without a mocked user loader.
2. **Non-device S3 GET can mint credentials after download admission should refuse.** At `orders.py:532–537`, GET omits `access_expires_at`; line584 treats zero `max_downloads` as three. Lines632–646 call the real signed-URL builder even when `can_download` is false from exhausted counters. `_assert_current_access` checks revocation/linked transaction, and `_get_purchased_version_for_delivery` checks the version; neither checks expiry/counts. `order_service.py:514–568` actually assumes the seller role and presigns. This is a source diagnosis, independently confirmed by Mars4234, not an executed exploit. Enforce authoritative existing admission before credential creation/disclosure; prove expired, zero-limit and exhausted refusal with zero external role/presign attempts and a valid positive case with unchanged counters. Keep the reference public-link branch's distinct existing semantics explicit. The correction fits the existing F4 endpoint/service targets. [Exact source hashes and test obligations](evidence/seller-refund-access-s1712/SELLER-MP239-GET-CREDENTIAL-GUARD-S1712.txt).

Other explicitly incomplete proof remains mandatory: API-cost shared-lock contention (distinct-entity progress is not contention), applicable nullable-FK assignment barriers with source-specific dispositions, positive Workspace issue/file after normal partial refund, raw single/multi-file/source and nonpartial cases, device S3 informational GET, and actual MCP duplicate purchase refusal with unchanged identity/no charge. Do not invent production account relationships to make a lock test pass. Preserve all 19 original posting positives and real provider fail-fast guards. The alternate VC callback and external raw-token consumer limits remain release requirements wherever the actual production transport depends on them.

MP remains the product and product-test author; Mars owns its accepted finite amendment and dispatch. No competing build, active-prompt mutation, protected run25 change or user permission request is implied. Exact product/environment Gate3, genuine holds, production freeze/quiescence and outside verification remain open.

## Historical MP237 source map

The following detailed graph and proposed scope describe `c9d8840b72c0fa208e9cf2eb5506767845dee9ac` before accepted F4 implementation. Its present-tense descriptions are historical observations; use the current candidate assessment above for outstanding work. In particular, the old three-file Round2 and seven/eight-file scope discussion is not the current F5 manifest.

Source copies and SHA256 inventories are under `/Users/max/Documents/Codex/2026-09-11/seller-workspace-final-release-continuation/work/f4-source-trace-s1712/`, in `SOURCE.json` and `SOURCE-ADDITIONAL.json`. The pinned [backend source](https://github.com/aidotmarket/ai-market-backend/tree/c9d8840b72c0fa208e9cf2eb5506767845dee9ac) is the portable source of truth. Every line below refers to that commit, not an evolving checkout. Source acquisition used `rtk proxy git show <commit>:<path>`; bounded cross-reference searches used `rtk proxy git grep ... <commit> -- app tests`.

## Historical findings and proposed correction scope

The three-file F4 Round2 is insufficient for the stated end-to-end retained-access policy. The ordinary device S3 refresh path additionally requires `app/api/v1/endpoints/trust_websocket.py`. Existing agent artifact/request-access and MCP access discovery have additional partial-status exclusions, while duplicate agent purchase metadata hides an otherwise available data endpoint. These are separate customer surfaces, not extra hops in the S3 callback.

For **device S3 refresh alone**, minimum newly authorized files are the Round2 three plus `trust_websocket.py` (four); `order_service.py` is already authorized. For **all specifically requested retained-access surfaces**, add `fulfillment_broker.py`, `agent_service.py`, and `mcp_marketplace_tools.py` (seven new EDIT targets total). `order_service.py` remains the existing shared-helper target; do not invent another authorization module.

A distinct listener safety finding needs explicit owner treatment in the same finite disposition: `fulfillment_listener_service.py` has no order-state/access check during first-transfer completion or staged-token consumption. This is not fixed by admitting partial status and must not be silently declared safe. If the amendment claims all delivery completion/full-refund exclusion, include this eighth target only for preserving/refusing closed or inappropriate completion and checking current access at token use. If the owner keeps F4 limited to credential refresh, record this as a separate unresolved release finding with its concrete tests below. Do not widen metadata/complete into a new partial first-delivery protocol.

## Actual device S3 credential refresh graph

1. HTTP `POST /orders/{id}/download/refresh` uses `OrderService.refresh_device_s3_delivery` (order_service.py1415). It locks the order, calls `_authorize_download_access`, checks applicable S3 delivery type, purchased version and stored identity even when credentials expired. This layer already admits legitimate delivered partials through `_has_delivered_access`.
2. The legacy HTTP `POST /orders/{id}/refresh` independently rejects partial at orders.py850. Its query835 lacks prior-delivery evidence and transaction ID. Its existing valid-URL short circuit869–885 is not refresh proof and must not disclose to an independently closed partial. It later invokes the same real FulfillmentService at896. `success:false` or `already_pending` is returned unchanged; HTTP200 alone proves nothing.
3. `FulfillmentService.request_fulfillment(..., is_refresh=True)` locks order161, but its SELECT lacks the evidence/closed-access fields; allowed_states197 excludes partial. Preserve non-refresh191–200 semantics and Workspace178–184 routing. Do not make partial a universally fulfillable state.
4. S3 refresh228–250 freezes `pending_refresh_request_id`, `pending_refresh_generation` and `pending_refresh_device_id` in delivery_config, using the current credential_generation and seller device. Existing pending refresh is deduplicated. `_send_fulfillment_request`550 and `_queue_fulfillment`601 send `vai.fulfillment.deliver` with the frozen refresh request ID and device; dedup is order plus refresh attempt. Both online/offline paths retain the tuple.
5. `trust_outbound_writer.py` enqueues by device/dedup/pending-message status, then sends by message expiry, subscription and capacity. Its `delivered`/`pending` comparisons concern outbound transport, not orders. ACK updates pending_fulfillments only when pending/sent_to_device; it does not grant order access. No partial-status gate was found here and no edit is indicated.
6. Device completion for this path is **`vai.fulfillment.response`**, not `vai.fulfillment.complete`. `trust_websocket._handle_fulfillment_action`1288 parses actual nested/flat frames and retains raw access URL. It locks and binds order, listing and seller1304–1312. Delivered/completed replay1317 is allowed only without a matching pending tuple. Its allowed-order-state gate1325 rejects partial even with a perfect request/device/generation match. This must be corrected narrowly with current authorization; no weakened device/listing binding or unauthenticated request-ID propagation.
7. `FulfillmentService.handle_fulfillment_response`303 locks full order; refreshing310–314 requires request ID, device ID, and pending generation equal current generation. Matching refresh already rechecks `_authorize_download_access`323–327. A failed device response returns `DELIVERY_REFRESH_FAILED`. It validates purchased version and immutable S3 identity369–381. For S3 refresh389–392 it updates only delivery_config, increments generation, removes pending tuple, completes pending fulfillment and commits. It preserves partial order status, delivery history and financial rows. This path can support partial once the upstream gates admit it; it is not necessary to reimplement it.
8. Fresh `issue_download_token`1190 and `redeem_download_token`1381 both use current `_authorize_download_access`, purchased version and stored identity; redemption also enforces token reservation and generation. Old generation must fail after refresh; fresh token must redeem to the exact raw refreshed URL. Scoped credentials use `_issue_s3_scoped_delivery`424 and its purchased-version check; no delivered/completed-only status gate remains there.

## Complete gate and surface map

| Surface and exact source | Current behavior and required disposition |
|---|---|
| orders.py529–543,570,577–631 GET access | Partial reports is_delivered=false and blocks reference delivery; non-reference usable URL branch is skipped. Add persisted delivery/transaction evidence and use guarded eligibility once. Keep informational GET behavior and zero download consumption. Before newly admitting a usable reference/current URL, retain independent revoke/closed-transaction exclusions. |
| orders.py624–627 -> order_service.py501 | Existing GET call passes `(order_id, listing_id)` into a builder expecting `(order_dict, version_dict_or_none)` and indexing order['listing_id']. The non-device-S3 branch can therefore raise TypeError before metadata/provider work. This is a pre-existing signature mismatch, but newly exercising partial access reaches it. Correct within the already proposed orders.py target if that branch is required; supply the authorized actual order/version or avoid credential generation in an informational branch according to existing contract. Do not test only device S3 (which skips the builder). |
| orders.py835–896 POST refresh | Route-only partial exclusion, missing delivery/tx fields, existing valid-URL bypass; must authorize before either usable current URL or real dispatch. Do not count `current_valid` as refresh proof. |
| fulfillment_service.py161–200 | Real downstream refresh exclusion; preserve initial fulfillment and pending dedup. The full evidence-based guard must run before device/provider work. |
| trust_websocket.py1304–1338 | Seller/listing/order ownership plus exact refresh tuple; allowed states1325 exclude partial. Correct callback admission and retain idempotency without overwriting financial status. |
| raw_download_service.py62–98,204–214 | `generate_download_token` paid_statuses82 excludes partial. SELECT lacks revoked/transaction/delivery evidence. Preserve existing nonpartial paid states; newly admitted partial requires actual prior delivery and independent current-access checks before signing, nonce storage, or audit success. Existing immutable purchased version, file prefix, Workspace exclusion and file-list checks remain. |
| orders.py356–503 raw routes | POST download-token and GET download both call actual RawDownloadService; fixes must work through real routes, not a replacement service. Listing ID is resolved from owned order route input. Existing single/multi-file distinctions and error shapes remain. |
| fulfillment_service.py224–225,470–510 raw fulfillment | Raw `file_download` does not negotiate credentials. Handler only UPDATEs initial paid/in_escrow/pending_delivery and returns conflict for existing delivered/completed. Do not add partial to that UPDATE: it writes status=delivered and resets delivery timestamps. Partial token renewal uses RawDownloadService; an actual raw refresh request must return an honest supported result/refusal without resetting financial state. Existing completed-raw refresh conflict is pre-existing. |
| order_service.py363–423 | `_has_delivered_access` is eligibility, not full authorization: timestamps return true before transaction inspection; null timestamps require reverse-bound delivered/confirmed/settled transaction. `_authorize_download_access` additionally checks owner, revoked, window, linked closed transaction and counts/reservation. Reuse/refine existing helper narrowly; do not blanket-add partial to status lists. |
| order_service.py1829–1853 can_download | Uses eligibility, owner, revoked, count but omits access-window and closed-transaction checks. A delivered timestamp plus disputed/refunded/cancelled/expired linked transaction may report true. Treat it as advisory unless improved within already-authorized OrderService; it cannot supply proof of authorized disclosure. |
| fulfillment_broker.py88,132,203 | request_access only reports already_available for delivered/completed; get_artifact_url blocks partial before the already-correct OrderService token issuer. These are actual agent routes (agent/router.py1086,1147), so retained-access completeness requires this file. Keep pending preparation distinct; authorization precedes already_available or token issuance. |
| agent_service.py449–474 | Duplicate purchase query already includes partial (`NOT IN cancelled,refunded`) and correctly avoids a second purchase. Only data_url472 is null for partial. Query currently selects only id/order_number/status, insufficient to derive guarded partial eligibility. Preserve duplicate identity and charging behavior; change only properly guarded availability metadata. |
| agent/router.py868–895 | Actual /agent/orders/{id}/data authenticates and calls OrderService.issue_download_token directly. No partial equality gate here; agent_service's missing URL does not imply this endpoint rejects partial. Existing response-shape/expiry peculiarities are separate pre-existing concerns. No router edit indicated for the status issue. |
| mcp_marketplace_tools.py948–982 check_access | Owner-bound SQL excludes partial; returns has_access=false and suggests initiate_purchase despite retained purchase. Add narrowly evidenced partial discovery without converting generic paid/pending semantics into a different product policy. It selects limited columns; persistently check prior delivery and current partial restrictions. |
| mcp_marketplace_tools.py789–798 initiate_purchase | `NOT IN cancelled,refunded,completed` already includes partial and blocks duplicate purchase. Do not exclude partial or change purchase charging merely to solve check_access; current false-access plus blocked repurchase is the user-visible inconsistency. |

## Other fulfillment actions: no indiscriminate widening

- `vai.fulfillment.metadata` -> listener.handle_metadata94. Order ownership is locked and status146 permits only pending_delivery/paid/in_escrow before creating a new transfer. This is initial staged-file delivery, not credential refresh. A partial with no prior delivery must not acquire first delivery by adding its status here.
- `vai.fulfillment.chunk` -> validate_authorization_tuple and handle_chunk. Stored transfer/order/listing/seller equality binds chunks. No delivered/completed refresh gate is involved.
- `vai.fulfillment.complete` -> listener.handle_complete288. It locks transfer then `_lock_owned_order`311. That helper527 only checks order/listing/seller ownership, **not order status, revoked flag, closed transaction or prior delivery**. Completion then issues a token406 and unconditionally UPDATEs status='delivered'416–431. A transfer started before partial/full refund may complete afterward and attempt to overwrite refund state. Existing PostgreSQL triggers may refuse specific financial transitions, but this source has no intentional current-access refusal; real migrated-PG race proof is required. Do not claim a static trigger assumption closes it. Preserve first delivery, financial status and lock ordering without inventing partial streaming refresh.
- Complete replay315 returns existing token ID when transfer already COMPLETE; this is not the S3 refresh tuple and must not be used as proof of refreshed credentials.
- `vai.fulfillment.error` -> listener.handle_error. `_lock_owned_order` checks ownership; UPDATE order delivery_failed511 is already conditional on initial states, protecting partial status from this particular write. Transfer status and staging file can still change; do not equate successful error ACK with authorized refresh success.
- Staged download endpoint fulfillment_download.py54–102 calls listener.consume_download_token576. Atomic token check591–604 validates token owner/order/hash/revocation/expiry/count, **but does not read current order/transaction access**. Refund processor516 sets order.revoked; a bounded app/services/app/listeners search found `revoke_tokens_for_order` only as a definition, no caller. Therefore an existing unrevoked staged token is not proven closed by a full refund. Add a current-access consumption guard or explicitly retain this unresolved release finding; do not solve it by setting token flags only on one refund path while ignoring independent closures.
- Raw HMAC validate_download_token160 verifies signature, expiry and single-use nonce, with no fresh order read. No app caller was found at this commit beyond its definition; its actual external VZ consumption is not proven by backend token generation tests. Preserve this proof limit rather than inventing an internal callback. No change to its protocol is implied by F4.
- Alternate VC websocket462 routes fulfillment.response through ActionExecutorService555, whose direct service call560 supplies no request ID/device ID. It cannot meet S3's frozen refresh tuple; for delivered/completed the service is an idempotent no-op, and partial is invalid. This is a pre-existing alternate-transport limitation, not a reason to bypass refresh correlation or silently expand F4 to a new VC protocol. The verified S1681 tests explicitly exercise the ordinary decrypted payload path and bypass ActionExecutor. If VC is required for this release, it needs separately explicit exact transport authority and tests.

## Required actual executable proof path

MP authors tests only within the already-authorized protocol test files; the following existing tests are reusable design references, not permission for new test-module edits. Use real disposable migrated PostgreSQL with normal refund processing and actual handlers; external transport and nonce/provider edges may be deterministic doubles. No live provider calls are required for this defect reproduction.

1. Keep the existing real failing `tests/test_refund_processing_protocol.py::test_partial_refund_refresh_route_manifest_blocker` as before evidence, but replace its eventual success assertion with exact body and actual dispatch assertions. Its present no-exception call does not prove full refresh completion.
2. Build on `tests/test_s1681_s3_delivery.py::test_refresh_correlated_once_generation_and_raw_url`264 and `test_download_http_responses_expose_token_and_no_store`498. Existing harness dispatch164 calls real `trust_websocket._handle_decrypted_payload`; it does not stub the fulfillment service. Begin with ordinary delivery, run actual partial processor, expire existing credentials, HTTP refresh -> real request_fulfillment -> real enqueue with captured external transport -> actual flat/nested response frame -> real completion commit -> actual issue/redeem. Assert exactly one outbound request, unchanged partial/transaction/history, generation increment once, unchanged immutable version/object identity, old token refusal and fresh exact raw-URL redemption. Exercise both online and offline queue paths.
3. At request AND after request/before callback, cover wrong owner/seller/listing/device/request/generation, full refund, independent revoke, each actual closed linked transaction state, expired access, exhausted download limits, unavailable/quarantined/mismatched purchased version and changed immutable object. Refusal must leave delivery config, order/transaction states, pending tuple and counters consistent and produce no credential disclosure. A duplicate matching callback must not finalize twice.
4. Separately cover GET reference access, GET non-device S3 builder branch, ordinary device-S3 info, both raw routes and real raw token signature/nonce/audit. Both prior-delivery evidence branches must work: real timestamps, or null timestamps plus correct reverse-bound delivered/confirmed/settled transaction. Null evidence, missing/wrong reverse link and undelivered partial refuse. Preserve nonpartial behavior and Workspace isolation; do not invent extra GET information errors.
5. Exercise real broker.request_access and get_artifact_url through agent routes, duplicate agent purchase response, MCP check_access and initiate_purchase. A legitimate delivered partial sees its existing access; no second purchase/charge is created. An independently closed partial does not receive usable credentials or false ready-to-download claims. Confirm /agent/data's existing OrderService path still works.
6. For the listener safety disposition, start a real staged transfer, apply normal partial/full refund or revoke/close before complete, then invoke actual complete. Assert no financial-status/timestamp rewrite or new unauthorized token. Issue a real staged token before full refund, then consume through the actual endpoint and prove current closure before count/file disclosure. Use the normal migrated trigger, not a fake lock response. Verify normal initial delivery and legitimate retained partial token consumption continue correctly. This is a distinct matrix from S3 refresh, not a metadata allowlist change.

Final evidence must identify exact MP source, migrations, full commands/logs, boundary invocation counts and before/after state. Re-run the accepted complete core/adjacent and race/fault/ancestry obligations, separating six known unrelated baseline failures. Source inspection in this report has not executed these proposed cases. No synthetic proof here certifies real-cloud delivery or production release.

## When it breaks

When a partially refunded buyer cannot download, first bind order buyer/listing/purchased version and current transaction reverse link without printing secrets or signed URLs. Establish actual prior delivery separately from current access restrictions. Identify the precise surface: access info/reference, raw token, device-S3 refresh, agent broker, or staged token. For S3, inspect only presence/hash-safe identifiers of pending request/device/generation and pending fulfillment/outbound states; match them to the actual response action. An outbound ACK or HTTP200 is not finalization. Verify the post-response credential generation and financial status, then normal buyer token issue/redeem in the approved test environment.

Do not clear partial/refund status, synthesize delivery timestamps, repurchase, broaden device ownership, replace immutable object identity, or fabricate a pending refresh tuple to recover service. MP applies the single accepted finite amendment only after the full required Council panel accepts the exact revised spec. MP238 has now terminated at `7890b0c0bc6a5fed4d50b796e11fda9e49102c51`; it did not change the access files mapped here, and its completion is not F4 acceptance. Keep run25 untouched; release runtime changes still require exact Gate3, trigger freeze and old-writer quiescence. Upgrade this map with the accepted source and actual evidence after the implementation; do not replace its pending tests with a healthy-process claim.


## Historical MP238 checkpoint and related procedures

MP238 source `7890b0c0bc6a5fed4d50b796e11fda9e49102c51` corrected the previously reported orderless refund transition, quote lock order and matched-authority source-pin retry defects. Independent verification checked all62 retained artifact hashes, all28 exact-commit source hashes, the canonical remote and clean checkout, and recounted JUnit:266 core passes/5 failures/0 skips;284 adjacent passes/2 failures/0 skips. Six failures reproduce the prior baseline; the remaining partial-refresh route failure is unresolved. No full-release or Gate3 success is implied. The [source-bound assessment](evidence/seller-refund-access-s1712/SELLER-MP238-TERMINAL-ASSESSMENT-S1712.json) records commands, logs, hashes and coverage limits.

The [Seller Workspace operator guide](seller-workspace-operator-guide.md) retains overall ownership, release and cleanup requirements. The [failed TEST refund recovery runbook](seller-failed-test-refund-recovery.md) describes the separate owner-managed failed-run process; do not use it to resolve ordinary retained partial access by creating an unnecessary full refund. The [production payment cutover procedure](seller-production-payment-cutover.md) controls activation after exact review. Read current [Infisical](../infisical-secrets.md) and [local SecOps](../local-secops.md) instructions before any credential use. This page introduces no new provider operation, deployment permission, account, checkout or lifecycle.

For the distinct owner-managed P/N acceptance lifecycle, see [paired settlement conclusion and recovery](seller-paired-settlement-conclusion.md): actual state transitions, receipt chain, cleanup admission and summary retry. Its source/testing milestone does not certify current product release.

## Bounded production transport observation — September 12

At 07:18:08 UTC, an authorized read-only query of the two existing S1707 production purchases confirmed one AWS and one R2 `workspace_connection` delivery. Their purchased versions resolve through exact publications and authorities to the seller-owned cloud connections; each has one persisted Workspace download session and file grant. Neither is bound to a device, source dataset or legacy serial. The current Railway deployment metadata identifies backend/core services at `d766b8e4e66d7803ebe48f8a3132521abbebe957` and frontend at `ffd54077bf115bbcb09882150c37f17ce4c672d5`.

At those source pins, the frontend selects Workspace session/file requests and streams signed provider HTTPS GETs with If-Match. The backend chooses Workspace fulfillment before device dispatch, checks current access and immutable source ownership before new/cached grants, and refuses Workspace sources in legacy raw-HMAC helpers. Thus the alternate VC callback and external raw-HMAC consumer limitations above are outside these two evidenced paths. This does not prove other production transports.

Both source connections remain revoked from the prior cleanup. The retained 26-byte R2 and 31-byte AWS files were rehashed and still match their historical paid receipts, but no fresh download or grant/refusal call was performed. Source predicts refusal before returning a grant for the revoked connections. Railway metadata was checked; running-container files were not rehashed in this assessment. Mars4249 confirmed there is no additional already-authorized production device-order receipt; S1656/run25/device receipts are TEST only. Keep the production-device evidence gap explicit. This observation neither accepts the final F5 candidate nor closes fresh joint release proof.

[Exact records, hashes and deployment metadata](evidence/seller-refund-access-s1712/SELLER-PRODUCTION-TRANSPORT-ASSESSMENT-S1712.json) and [source trace, support and upgrade procedure](evidence/seller-refund-access-s1712/SELLER-PRODUCTION-TRANSPORT-ASSESSMENT-S1712.txt) preserve the bounded evidence. Do not broaden customer inventory or reactivate revoked connections merely to fill the gap.
