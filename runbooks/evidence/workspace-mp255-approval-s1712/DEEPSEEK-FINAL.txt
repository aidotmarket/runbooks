# Council review — S1714 MP255 final corrected Seller Workspace candidate (DeepSeek seat)

Read-only throughout: only `cat`/`grep`/`find`/`shasum`/`python3` JSON-XML-stdlib reads, `git cat-file`/`git diff`/`git rev-parse` and `git show` (read-only). I allocated no runtime, launched no agent/builder, ran no test, and mutated no source, DB, provider, container or state.

---

## 1. Verdict

**APPROVE_WITH_NITS**

No HIGH or MEDIUM finding was established. The three carried blockers are closed with raw, machine-recorded evidence at the exact final candidate (see F4–F6 and the question answers), and the accepted runtime amendment is confirmed. The findings below are LOW/NIT; the verdict agrees with them. This approves the **implementation** only and does not touch the separately-required genuine settlement/offline, stable/scheduled, protected-phase and deployed AWS S3/R2 proofs, which remain open by design (`OWNER-OPERATIONAL-CLOSURE.json: "release_accepted": false`).

Request completeness: artifact identity, scope, environment truth, finite budget (90 turns) and the prior candidate/verdict are present. One stated element is absent and one is wrong — reported first as F1/F2.

---

## 2. Findings (highest severity first)

### F1 — LOW — The request states no runnable diff command, and the package prose carries a distribution count that is factually wrong
- **Severity:** LOW (documentary; verified-correct raw output exists, so no decision is affected).
- **Evidence:**
  - `…s1712/BINDINGS.json` `effective_runtime_inventory.receiver.distribution_count = 218`, `native.distribution_count = 222`; `…s1712/REQUEST.md` (in-package copy) repeats 218/222.
  - Raw truth: `evidence/effective-runtime/receiver-freeze.txt` = 218 lines **but line 218 is `WARNING: The directory '/home/appuser/.cache/pip' … The cache has been disabled…`** (`grep -n WARNING` → line 218); `native-freeze.txt` = 222 lines with the same warning at line 222.
  - Independent distributions JSON (`receiver-discovery.txt`, `native-discovery.txt`) reports **217 unique / 221 unique** distributions; imports: `stripe,httpx,sqlalchemy,asyncpg → /opt/venv` in both modes, `pytest,pytest_asyncio → testdeps/*` in native only (sys.path verified).
  - The outer dispatch's correction (217/221) is therefore **correct**; the artifact prose is not.
  - No runnable diff command is given anywhere in the request text; I reconstructed and ran the equivalents (see Coverage).
- **Observed:** artifact self-describes 218/222; truth is 217/221 (one pip-cache warning line counted as a distribution). **Expected:** the artifact states the unique-distribution count or explicitly marks the warning line.
- **Reproduction:** static evidence only — read the four files above.
- **Impact:** LOW. A reviewer who trusted the artifact prose would report a phantom count; the correction exists only outside the frozen 232-file package (implicitly in `evidence/owner/s1714-mp255-final-private-owner.json: "runtime_inventory_note": "Base image fullfreeze218 retained separately…"`).
- **Smallest required outcome:** state 217/221 (or “218/222 lines including one pip warning”) in `BINDINGS.json`, `REQUEST.md`, or a single in-package superseding clause; and state the two runnable diff commands in the dispatch.
- **Verification criterion:** every in-package occurrence of the count is either 217/221 or explicitly annotated as text-lines-including-warning, and the request names both `git diff` commands verbatim.

### F2 — LOW — The environment host unit suite (non-PostgreSQL classes of the in-scope test file) has no execution receipt at the exact final revision
- **Severity:** LOW (evidence-strength/packaging; the environment repo is qualification harness, not product code, and the changed areas are re-proven at final).
- **Evidence:**
  - Final pins: `environment/tests/test-workspace-fixture.py = 77eba3eeb9…`, `environment/seed/workspace-host.py = a4456c86ac22…` (independently recomputed; equals git tree of `money-path-test-environment@a0e028f4`).
  - The only full-suite host runs are bound to **superseded** revisions: `outputs/environment-container-qualification-s1712/ENVIRONMENT-CHECKS.json` runs `python3 tests/test-workspace-fixture.py` at host `957ec016…`/test `82cfff4b…` (host `grep -c 'def unused_operation' = 0` → pre-clarification); `HOST-UNIT.json`/`HOST-UNIT-RECOVERY.json` (50/51 tests) at host `d919e373…`/test `3f1db9de…` — neither hash exists as a retained file anywhere in `outputs` (full `find … -name workspace-host.py` scan returns only `1dc340a6…`, `957ec016…`, `a4456c86…`).
  - At the final revision only the selected subset is executed: `outputs/host-supplemental-acceptance-s1712/run_private_capsule.py:247` selects `-k 'test_pg_lifecycle or test_pg_unused or test_frozen_host_backend_protected_file_boundary'` and enforces set-equality over exactly 29 nodes (`…:252-262`), executed twice (`evidence/private/fixture/native-final.xml` 29 cases, 17 properties, 0 failures/errors/skips; `outputs/mp-b04-bc7d6f5cbc41/linux-work/joint-current-host.xml` 29 passed). `environment/workspace/README.md` (in-scope, hash-bound) asserts “The latest local run has51 Workspace tests passing” without naming the revision.
  - Mitigation verified: the final host differs from the last retained post-clarification host `1dc340a6…` by exactly one changed line (`account.to_dict()` in the activation path), while the *changed* functional areas (unused refusal/retry, restoration retry, protected-file boundary, receiver activate/bind/refund/close/deactivate) **are** exercised at final by the 29 native cases and the private lifecycle; host orchestration paths (custody enrollment, journal-loss recovery, status/pointer guards) are not.
- **Observed:** final-revision coverage of `seed/workspace-host.py` orchestration is partial. **Expected:** a host-suite receipt at host `a4456c86…`/test `77eba3ee…`, or a claim qualified to its revision.
- **Reproduction:** static evidence only — hashes above and the capsule’s `-k` selector.
- **Impact:** LOW. Product/runtime behaviour is unaffected (environment = qualification harness); residual risk is that a non-PG host behaviour regressed in the one-line/±53-line deltas and is not re-asserted at the pin.
- **Smallest required outcome:** bind one receipt running the full host file at `a4456c86…`/`77eba3ee…`, or amend the README sentence to name the revision it refers to.
- **Verification criterion:** a receipt whose `source_before["seed/workspace-host.py"] = a4456c86…` and `["tests/test-workspace-fixture.py"] = 77eba3ee…` shows exit 0 for the full host file, or the README explicitly scopes the 51-test claim.

### F3 — NIT — “Ten exact-category” rejection cases overstate distinctness; three collapse into the earlier window guard
- **Severity:** NIT.
- **Evidence:** `environment/tests/test-workspace-fixture.py:859-915` (`test_pg_unused_request_rejection_matrix`). For `registration` and `paid_open` the close step is skipped (`if fault not in {"paid_open","registration","open_without_terminal"}: … close_workspace_window`), and `open_without_terminal` is open by construction; the recorded properties confirm all three refuse with the **same early** message `workspace_window_not_closed` (`evidence/private/fixture/native-final.xml` properties `unused_native_refusal`), not with an unused-specific guard. The remaining seven categories map to six distinct causes: `unused_terminal_required` (refunded), `restoration_cardinality_changed` (extra_order), `unused_baseline_changed` (baseline, connect_identity), `party_not_active`, `artifact_changed`, `authority_binding`.
- **Observed:** 10 parametrisations, 7 distinct refusal reasons. **Expected label:** “every named unused-request category refuses”.
- **Reproduction:** static evidence only.
- **Impact:** NIT — the guard ordering is *correct* (a window that is not closed can never reach the unused branch; a registered+refunded closed window is covered by the `refunded` case), and the GLM verification criterion (“each listed mismatch refusing with no mutation and no provider call”) is literally met (`provider_calls: 0`, full public-table md5 fingerprint equality in every property).
- **Smallest required outcome:** none required; optionally re-word the claim.
- **Verification criterion:** claim text matches the 7 distinct refusal causes, or adds a closed-window registration variant if constructible.

### F4 — NIT — `versions.env` is a scope target but is not bound in the frozen-host manifest or the cohort/host source maps
- **Severity:** NIT.
- **Evidence:** `BINDINGS.json environment.scope_targets` includes `versions.env`; `environment/MANIFEST.json` `files` lists 11 files (no `versions.env`), and `evidence/backend/FINAL-TEST-SUMMARY.json` per-cohort `source_before` records 37 backend + 11 host files; `evidence/private/RESULT.json host_source_before` likewise 11.
- **Observed:** `versions.env` bound only in `evidence/pin-checks` (`c9abc9a7609d736dab624140ad001e8c2459702695f1d451e88a293b67232ddc`, independently confirmed equals the `a0e028f4` tree). **Expected:** all 12 targets hash-bound in the frozen manifest.
- **Reproduction:** static evidence only.
- **Impact:** NIT — coverage exists, in a different receipt.
- **Smallest required outcome:** add `versions.env` to `environment/MANIFEST.json`.
- **Verification criterion:** the frozen manifest lists 12 entries including `versions.env` at `c9abc9a7…`.

### F5 — NIT — Stale “Stripe 15.4” comment survives at the final source (known prior GLM NIT, unresolved)
- **Severity:** NIT.
- **Evidence:** `ai-market-backend@2a2e7ae:app/services/transaction_service.py:733` — `# Stripe 15.4 owns this exact native HTTPX client.` The accepted amendment names 15.6.1 (`contract/accepted-runtime-amendment/AMENDMENT.txt`), and the executed runtime is Stripe 15.6.1 (`linux-work/runtime-catalog/FINAL-RUNTIME-CATALOG.json`, `RUNTIME-FREEZE.txt:193`).
- **Observed:** stale minor-version comment in an operational file. **Expected:** comment names the qualified version or no hardcoded minor.
- **Reproduction:** static evidence only.
- **Impact:** NIT — documentation/confusion risk only; no dependency or behaviour change (production range `requirements.txt` unchanged; the diff to `refund_processing_service.py` adds assertions only).
- **Smallest required outcome:** none for this approval; correct in a documentation-only revision.
- **Verification criterion:** surviving operator docs/source name 15.6.1 or the pinned-SDK form.

### F6 — NIT — The joint-29 environment record used a frozen host whose seed/compose files are not the final revision
- **Severity:** NIT.
- **Evidence:** `outputs/mp-b04-bc7d6f5cbc41/linux-work/joint-current-host.xml` properties record `seed/workspace-host.py = a4456c86…` and `tests/test-workspace-fixture.py = 77eba3ee…` (final) but `seed/seed.py = 0734505…`, `tests/compose-contract.sh = 6b875d02…`, `tests/test-seed-contract.py = a5050add…` (non-final); the corresponding directory `outputs/mp-b04-bc7d6f5cbc41/corrected-host-evidence` lacks `versions.env`. The exact final environment **is** proven by the private capsule (`evidence/private/RESULT.json host_source_before` = all 11 final hashes, before == after == true) and by `evidence/pin-checks` (final hashes).
- **Observed:** joint record not byte-identical to the candidate for 3 of 12 files. **Expected:** note the union of receipts or re-run.
- **Reproduction:** static evidence only.
- **Impact:** NIT — the affected files are unrelated to the 29 cases, and the final revision is covered by the private run and pin checks.
- **Smallest required outcome:** note in the matrix that final-revision coverage for those three files comes from the private capsule/pin checks.
- **Verification criterion:** matrix states the per-receipt revision provenance.

---

## 3. Coverage

**Independently verified (read-only, recomputed):**

- **Artifact identity.** `git rev-parse 2a2e7ae^{tree} = b4cbfd77985e7878896735ee2e041a6184814c38`; `git diff --name-only c3a62af8ccf60929a8e7cec3d62c9d30e0adefd2..2a2e7ae…` = **exactly the 37 declared backend targets** (set equality both directions); repo `aidotmarket/ai-market-backend` contains base, `5643fc2` and candidate commits. Environment worktree HEAD = `a0e028f4e0fa9ab6e53510909e7e8ddd725f20ae`, tree `39f305a9…`, clean; `git diff --name-only 43d67264…a0e028f4` = **exactly the 12 declared targets**. Documentary `24da40e9…` and workspace spec `0644140c…` exist as commits.
- **Package integrity.** All **232** `FINAL-MANIFEST.json` entries match on sha256 *and* byte length; all **142** `EXTERNAL-EVIDENCE-MANIFEST.json` entries match; `SOURCE-FREEZE.json.all_file_hashes` = **3033** entries, count exactly equals `git ls-tree -r 2a2e7ae` (3033) and content-sha256 matches for 2000/2000 sampled; `git ls-tree -r a0e028f4` = **48** env blobs.
- **Diff scope/preservation.** `5643fc2..2a2e7ae` touches only `tests/test_workspace_test_refund_admission.py` (+180); `SCOPE-AND-PRESERVATION.json` (only-changed-file, original 109145 bytes) consistent; `refund_processing_service.py` diff is assertion-only (no captured-refund deletion); `_fixed_orders`/`capture_protected_state`/`_lock_protected_rows`/`_resolved_refund_events` carry real `limit 4097` + `>4096` refusal guards (`app/services/workspace_refund_test_admission.py:447,482,497,710`).
- **Execution corpus.** `FINAL-EXECUTED-NODES.json` = **2537** nodes; **2531 passed, 6 failed**; phase tally: setup 2537 passed, call 2531 passed/6 failed, teardown 2537 passed. The 43 rows’ `executed_nodes` are all present and their union is **exactly 2537** with zero uncovered nodes; all 314 row evidence paths exist. The 6 failures are exactly the reconciled `inherited_exception=true / prior_outcome=failed` set (`FINAL-1990-NODE-RECONCILIATION.json`), all in unchanged non-scope finance files; 4 are `ModuleNotFoundError: app.services.finance.subscription_service` (module absent at base too) and 2 are unrelated gate2/migration assertions. All 1990 prior nodes map to present final nodes; 1983 rerun, 6 retained-exception, 1 canonical200 replacement.
- **New correction evidence (raw, not labels).** B04: 8 properties `{4096 accepted, 4097 refused}` with exact conflict categories, complete sorted-unique sets, real second-session lock observer with release, coherent 4096 distinct applied-refund event IDs, unchanged-after-rollback (`complete-workspace-native.xml`). Unused refusals: 10 properties with exact refusal messages, `provider_calls: 0`, per-table md5 fingerprint equality before/after, repeated refusal. Host retry: 2 properties with first-failure category (`receipt_persistence_failed` / `receiver_outcome_unknown`), absent archive, identical retry proofs/archive bytes, `provider_calls: 0`, DB fingerprint unchanged (`native-final.xml`).
- **Runtime.** Unique distributions 217/221; +iniconfig 2.1.0, pluggy 1.6.0, pytest 8.4.2, pytest-asyncio 1.2.0 via read-only `/testdeps`; both `pip check` exit 0; production imports from `/opt/venv`; MP instrumented image freeze 221 entries (no warning line), `pip check` exit 0, Stripe 15.6.1 / HTTPX 0.28.1; `transaction_service.py:692-701,733` native request-local client, `max_network_retries=2`, `close_async`, 5/20/5 within 30 (`:54-70,695`). Amendment text names only the 15.4→15.6.1 literal and both reviewer responses are present.
- **Worker/lifecycle/cleanup.** `MP255-WORKER-INDEPENDENT-VERIFICATION`: 27 passes, eligible 500, processed 500, expired/excluded 20, continuation delay 60 s, **1000 distinct provider GETs**, setup/call pass. Private `RESULT.json`: 73 steps (61 exit 0; the 12 non-zero are startup `pg_isready` and owned-resource `inspect` absence probes), lifecycle inspect/open/activate/bind/signed-refund (`refund_processed_twice: true`, `refund_phase applied`)/close/deactivate=`database_restored`, 29 native + 8 negative, process canary 6440 samples 0 violations, `all_owned_resources_absent: true`, `source_unchanged: true`. Timeout history preserved (`af76c11f64b8.report.json`: `duration 1806.108`, `terminal_status timeout`, base→head `5643fc2→2a2e7ae`, `+180` in one file, pushed).

**Not verified / not done (named):** no test, container, image, provider double, database, or deployment was executed by me; I did not re-derive the assertion-level semantics of the ~25 non-spot-checked rows (B01/B02/B05/B06/B07/B09/B10/B11/B12/B14/B15/B16/B17/B18, PI01–PI11, PI13, R01/R02/R03/R05/R07/R08/R09/R11) — these are verified as structurally bound (final source → mapped executed nodes → present raw receipts) but their bodies are **UNVERIFIED** at the assertion level here (the prior DeepSeek/GLM passes did body-inspect a subset; I re-checked B04/B08/B13/PI12/PI14/R04/R06/R10). The non-PG host suite at the final revision is **UNVERIFIED** (F2). Genuine protected phases, real custody/archive, public checkout, stable/scheduled production and AWS S3/R2 remain **UNVERIFIED** and are explicitly not claimed by the package.

---

## 4. Answers to the numbered questions

**1. Do the corrected real-PostgreSQL unused refusal/retry cases and the eight B04 boundary cases close the prior GLM and DeepSeek findings without masking an earlier guard or a truncated result?**
Yes. GLM finding 2 (MEDIUM, unused rejection matrix) is closed by `test_pg_unused_request_rejection_matrix` (10 categories) whose raw XML properties show each category refusing with the exact expected message, `provider_calls: 0`, full public-table fingerprint equality, and repeated refusal. GLM finding 3 (MEDIUM, lost-proof/archive-publication retry) is closed by `test_pg_unused_host_fault_retry_rechecks_database[proof_response_loss|archive_publication_failure]` with raw first-failure categories, no archive published, unchanged receipts, retry rechecking the DB, identical proofs both attempts and byte-identical archive. DeepSeek’s B04 boundary gap is closed by the 8 `test_b04_*_record_count_boundary[4096|4097]` properties: 4096 accepted with complete sorted-unique sets, 4097 refused with the exact conflict category, a real second-session lock observer proving locks taken then released, coherent 4096 distinct event IDs bound to one applied refund, and unchanged DB after rollback. No truncation is possible by construction (`limit 4097` then `>4096` refusal) and the tests assert the full identity/count of every element, so a `limit 4096` or `>=4096` off-by-one would now fail. One residual *label* issue only: three of the ten unused categories (`paid_open`, `registration`, `open_without_terminal`) refuse via the earlier, correctly-ordered `workspace_window_not_closed` guard (F3); this is the sound production ordering (an unclosed window can never reach the unused branch) and does not mask a defect, since the closed non-`no_purchase` terminal is separately proven refusing by the `refunded` case.

**2. Does all retained source and current execution evidence satisfy every requirement, including original financial behaviour, authority/retry semantics, deadline/cleanup/worker behaviour, the accepted exact runtime amendment and full test preservation?**
Substantially yes, at the assertion level for the rows re-inspected. Original financial behaviour is preserved (`refund_processing_service.py` diff = added canonical-PI ancestry/extension assertions only, no captured-refund change; production requirements unchanged). Authority/retry semantics hold on the final source (4096/4097 guards, unused predicate, fresh-DB recheck on every deactivation retry, archive disposition `unused` with `preparing`/`deactivate` and no retained-custody claim, rollover to a fresh audit without provider/custody calls). Deadline/cleanup/worker behaviour is proven by the real owned beat/broker/worker receipt (27 passes, 500 processed, 20 excluded, 1000 distinct GETs, real 60 s continuation) and by the 5/20/5-in-30 native transport in the final source. The runtime amendment is confirmed: only the Stripe 15.4.x literal is superseded by exact 15.6.1; HTTPX stays 0.28.1; both modes’ freezes and `pip check` exit 0 exist and imports resolve from `/opt/venv`. Full test preservation is verified numerically: 2537 = 2529 retained prior + 8 new, 2531 pass, exactly the 6 inherited baseline exceptions, all setup/teardown green, every row node present, union = 2537. Caveats, stated explicitly: the assertion bodies of the non-spot-checked rows are UNVERIFIED here, and the final-revision host unit suite is unexecuted (F2). Neither affects the 43 backend rows.

**3. Is unused closure/archive behaviour sound under its exact predicate and fresh retry checks, with no provider/custody writes?**
Yes. Predicate (`seed/workspace-host.py:995-997`): `custody, provider, projection all None` **and** `set(resources) == {baseline, opening, terminal}` — matching the disposition’s exact three resource groups with no registration entry. Receiver (`:596-603`) requires a closed window, `verify_restoration_terminal`, `terminal.disposition == no_purchase`, `registration_sha256 is None`, `current == BASELINE_STATE` and zero Connect identities; mismatch refuses. Host (`:1583-1592`) derives the `{before_state: BASELINE_STATE}` variant only from the audit predicate, never from caller input; archive requires `preparing`/`deactivate`/no failure and emits `{kind: completed_operation, disposition: unused}` (`:1002-1007`); `verify_completed_operation` re-reads the terminal and all three resource envelopes and refuses on any change (`:1027-1041`); rollover returns a fresh audit without vault/provider calls (`:1057`). Raw evidence: `provider_calls: 0` in all 10 refusal and both retry properties; `StripeClient`/`CustodyVault`/`ConnectProvider` monkeypatched to raise if touched; DB fingerprint unchanged before/after; retry re-derives identical proofs and identical archive bytes. Terminal semantics are unambiguous because the archive label `unused` distinguishes the legacy `preparing` phase from a retained-custody claim.

**4. Does the package accurately distinguish implementation qualification from the still-required genuine settlement/offline, stable/scheduled and deployed production proof?**
Yes. `OWNER-OPERATIONAL-CLOSURE.json` and `evidence/private/MP255-FINAL-PRIVATE-QUALIFICATION-VERIFIED.json` both set `release_accepted: false` and carry an explicit `limits` field (“Owned local Linux qualification with synthetic provider TLS … Not public checkout, real custody/archive, protected settlement phases or production AWS/R2 release”); the worker receipt records `production: false`; the matrix rules keep environment integration/genuine phases/cutover/live AWS S3+R2 as separate required gates; `BINDINGS.json remaining` names the genuine protected phases and AWS/R2 release. The independent effective-runtime evidence is explicitly labelled “POST_QUALIFICATION_READ_ONLY_SAME_IMAGE_AND_MOUNTS … not retroactive original-container observations”, and the removed-container non-reobservation is stated. No production adoption is claimed.

### SIMPLER
**None found that preserves every requirement.** I attempted to remove: (a) the three redundant open-window rejection parametrisations (`paid_open`, `registration`, `open_without_terminal`) — not removable without changing the requirement’s explicit category list, even though they collapse to one guard (F3); (b) the explicit `_lock_protected_rows`/`_protected_comparison` calls, which duplicate the call inside `verify_restoration_terminal` — not removable, it would change the accepted lock ordering of `_short_transaction`; (c) the duplicated in-package/out-of-package runtime-count prose — removable in the sense that one clause would suffice, and it is exactly what F1 asks for (remove one contradictory copy and the missing-count error disappears). The genuinely removable item is documentation duplication only; no predicate, invariant or guard can be dropped.

### BETTER
**Bind R04 and the runtime inventory to the executed image digest plus recorded resolved versions rather than to a hardcoded inventory count or minor-version literal.** Concretely: state “qualified runtime = the image actually executed (`sha256:dc0051cb…`), with its recorded resolved set and `pip check` exit 0”, and record unique-distribution counts programmatically (parsing `name==version` lines, skipping warnings) instead of counting text lines. Failure modes: none new — it consumes receipts that already exist (`runtime-catalog/FINAL-RUNTIME-CATALOG.json`, `receiver/native-discovery.txt`, `PIP-CHECK.json`); it removes the entire class of “218 vs 217 / 15.4 vs 15.6.1” disagreements that produced this and the previous REVISE. Cost: one clause and one counting rule; rollback: restore the literals. It would have made F1 impossible. Second-best, lower-value: a machine-checked obligation manifest mapping each of the 43 rows and each of the 12 environment paths to a required final-revision artifact tag, which would have made F2 fail packaging instead of surviving to review. Both are advisory (no scope violation or material product risk was shown).

*Read-only notice: I modified nothing, launched no builder/reviewer/agent, and mutated no source, database, provider, container or deployment state. Named UNVERIFIED items (F2’s non-PG host suite at the final revision, assertion-level bodies of the non-spot-checked rows, and all production/protected-phase proofs) are not treated as approval of those areas.*