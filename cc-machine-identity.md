---
title: CC machine identity (council reviewer credential)
owner: unassigned
last_verified: '2026-08-27'
aliases: []
error_signatures: []
---

# CC machine identity (council reviewer credential)

## A. Purpose
Stop the recurring destruction of Max's Claude login and keep the CC council reviewer dispatchable without any human credential. Owns: how CC authenticates, how the key rotates, and how to diagnose credential failures on the CC path.

## B. The design (S1573, causal story corrected S1582 per T-2026-000686)
**The Council panel path runs CC on the OAuth machine profile, on Max's plan, by Max's directive.** `scripts/council_dir.py:_cc_env` deliberately strips `ANTHROPIC_API_KEY` (and every override var) from the child environment; that stripping is correct and must not be "fixed". The dedicated API key below remains live only for **non-panel** headless dispatches through `claude_code_client._build_env`.

**Corrected root cause of the recurring 401s (S1581 evidence, T-2026-000686).** The S1573 theory — sibling OAuth grants revoking each other server-side — is contradicted by direct evidence and is retired. Nothing was ever revoked: the config-scoped Keychain credential (`Claude Code-credentials-2d3f080c`) was valid and unexpired through every incident. The real cause was `scripts/setup_cc_profile.sh`: when its interactive `claude login` is abandoned before browser completion, the CLI leaves `~/.claude-koskadeux/.credentials.json` with EMPTY accessToken/refreshToken strings (metadata populated). Claude Code prefers that file over the Keychain, and empty tokens return a 401 that reads exactly like revocation. 23 quarantined stubs 13–20 Aug match ~18 setup-script re-runs in shell history: CC failed, an operator re-ran the setup script to repair it, the abandoned login wrote a fresh stub, and the stub caused the next failure. The repair was the cause. The S1532 profile isolation itself was correct and holds.

- Canonical secret: Infisical `koskadeux-mcp` / `prod` / `CC_COUNCIL_ANTHROPIC_API_KEY`.
- Cache fallback: `/Users/max/koskadeux-state/secrets/cc-council-api-key` (0600), refreshed on every successful Infisical fetch; keeps dispatch working when the secret store is down.
- Code: `koskadeux-mcp/cc_profile.py` (`council_api_key()`, `apply_to_env`). The profile dir `~/.claude-koskadeux` is still applied in API-key mode so CC never reads the interactive config. The OAuth profile remains as fallback only.
- As of S1573 the slot is **seeded with the shared backend key**. Rotate to a dedicated console key (console.anthropic.com → new key → paste into the Infisical secret) to complete identity separation. Rotation is one Infisical value change; no code, no restart.

## When it breaks
| Symptom | Meaning | Action |
|---|---|---|
| `401 OAuth access token has been revoked` in `council/cc/launcher-*.log` | The Keychain access token has EXPIRED and the renewal did not persist. S1585 falsified the earlier empty-stub theory by experiment (see the row below): an empty `.credentials.json` marker alongside a VALID Keychain credential does NOT break CC. The access token lives ~8h; the config-scoped Keychain item is only ever written at interactive login, so a renewal performed during a dispatch is lost when that process exits | **Read the Keychain expiry first**: `security find-generic-password -s "Claude Code-credentials-2d3f080c" -w` and compare `expiresAt` (epoch ms) to now. If it is in the PAST there is no non-interactive repair: `heal_empty_stub()` will succeed and CC will still 401. Max must complete an interactive browser login. If it is in the FUTURE, the credential is fine and the 401 is something else — do not reach for the stub machinery |
| `401 OAuth access token has been revoked` while the Keychain `expiresAt` is clearly in the FUTURE | TRANSIENT 401 against a VALID credential. Observed twice on consecutive sessions (S1593 Gate 2 dispatch request-20260821-182920; S1594 Gate 3 R2 dispatch request-20260822-044308, expiry 7.07h in the future). The credential was fine both times; no stub was present, no heal was needed, and the immediate redispatch succeeded. Provider-side blip, not an expiry and not stub corruption | Read the Keychain expiry per the first row. If it is in the future, run the Agent capabilities end-to-end probe; on `COUNCIL-CC-OK`, redispatch once. Do NOT run `heal_empty_stub()`, do NOT re-run the setup script, do NOT treat one 401 as revocation. If the redispatch also 401s against a still-valid credential, file a support ticket with the request ids and stop hand-repairing. (The former renewal BQ s1585 was cancelled by Max in S1588 and must not be revived; do not record onto it) |
| An empty-token `.credentials.json` marker in `~/.claude-koskadeux` | NORMAL Claude Code behaviour when the CLI uses config-scoped secure storage, NOT corruption and NOT the cause of the 401. Every file quarantined between 13 and 20 August carried `expiresAt: 0` and zero-length tokens; none ever held a real credential, so nothing has ever been destroyed by retiring one | Harmless. `apply_to_env` retires it on every launch and that is fine, but do not treat its presence as a diagnosis. S1585 experiment, reproducible: restore a marker while the Keychain credential is valid and run CC — it authenticates and exits 0 |
| A healed marker reappears immediately and CC fails again on the next dispatch | The Keychain token is expired. The heal succeeded and was irrelevant; the CLI rewrites the marker each time it falls back to a login path | Do not loop on `heal_empty_stub()`. Read the Keychain expiry and escalate to Max for an interactive login |
| `cc_profile: waiting more than 5s for profile lock /Users/max/koskadeux-state/locks/cc-profile.lock` | An interactive `setup_cc_profile.py` / `claude login` is still open on a tty and holds the lock, OR it exited without releasing it | `cat` the lock for its `pid=`, then `ps -p <pid>`. Alive means a real login is in progress: wait for Max to finish it, and do NOT kill it mid-flight, since an abandoned login is what writes the empty stub. Dead means the lock is stale: remove it. On S1585 the setup script also wrote a fresh empty stub as it exited, so after a completed login, move the stub aside and confirm the Keychain is canonical before re-dispatching |
| `.credentials.json` empty-token stub in `~/.claude-koskadeux` | An abandoned interactive `claude login` (usually from `setup_cc_profile.sh`) wrote a placeholder mid-flight. This IS the cause of the panel 401s, not a symptom of revocation | Launcher auto-heals (renames stub). If stubs keep appearing, find and finish or kill the abandoned `claude login`; do not keep re-running the setup script |
| `invalid x-api-key` / `authentication_error` on CC runs | The API key itself is bad or was rotated upstream | Update the Infisical secret; delete the cache file to force re-fetch |
| CC dispatch refuses with "profile not provisioned" | Both key resolution and OAuth profile unavailable | Restore Infisical reachability or re-seed the cache file; last resort `scripts/setup_cc_profile.sh` |
| Max's interactive Claude login dies after machine re-provisioning | Concurrent refresh race on one shared credential (the original S1532 defect) | The isolated machine profile prevents this; keep the profiles separate. If the machine profile needs re-provisioning, complete the browser login in one sitting — an abandoned run leaves a 401-causing stub |

## D. Verification
`python3 -c "import sys; sys.path.insert(0,'/Users/max/koskadeux-mcp'); import cc_profile; print(cc_profile.status())"` — expect `council_api_key: True`. End-to-end: dispatch `council_request(agent=cc, mode=open_response, task='Reply with exactly: COUNCIL-CC-OK')`.

## E. Scope boundaries
Kimi and GLM have their own transports (see `infra:council-comms`). The same one-account defect exists on the OpenAI side (MP builder + GLM on `/Users/max/.codex/auth.json`) — tracked under BQ-GLM-CODEX-TRANSPORT-MIGRATION-S1566; apply the same identity-separation principle there when that work resumes.

## F. Attempt log — daily OAuth logout (track here until solved; Max directive S1600)

Standing defect: CC's panel credential (config-scoped Keychain item, `~/.claude-koskadeux` profile) dies roughly every eight hours and Max re-logs in by hand. Every attempt, what it assumed, and how it died:

**Numbering clarification (Max, S1622):** the `#` column below is the historical record sequence, not the count of distinct remedies. Rows 6, 9, and 10 are recurrence/recovery evidence, and row 8 is the outcome of row 7. The new persistent remedy delivered in S1622 is **remediation attempt 8**.

| # | Session | Attempt / theory | Outcome |
|---|---|---|---|
| 1 | S1532 | Profile isolation: dedicated `~/.claude-koskadeux` config dir + config-scoped Keychain item, to stop the concurrent refresh race destroying Max's interactive login | WORKED for its target (Max's own login no longer destroyed). Did NOT stop CC's daily 401s |
| 2 | S1573 | Theory: sibling OAuth grants revoke each other server-side. Mitigation: dedicated API key (Infisical `CC_COUNCIL_ANTHROPIC_API_KEY`) for non-panel dispatches; panel kept on OAuth by Max's directive | Theory RETIRED S1581 — direct evidence showed nothing was ever revoked. API-key path remains live for non-panel only |
| 3 | S1581 / T-2026-000686 | Theory: empty-token `.credentials.json` stubs from abandoned `setup_cc_profile.sh` logins cause the panel 401s. Mitigation: launcher `heal_empty_stub()`, stop re-running the setup script as repair | FALSIFIED S1585 by reproducible experiment: a stub alongside a VALID Keychain credential does not break CC. Heal is harmless but irrelevant |
| 4 | S1585 | Real mechanics established: access token lives ~8h; the config-scoped Keychain item is only written at interactive login; a renewal inside a headless dispatch does not persist. Renewal-automation BQ drafted | Mechanics stand (re-confirmed S1600: fresh login measured 7.94h to expiry). Automation BQ not pursued — see #5 |
| 5 | S1588 | Max cancelled the renewal BQ (s1585); daily interactive re-login became the operating mode. S1593/S1594 refined: transient 401s also occur against a VALID credential — probe and redispatch once before diagnosing | Operating mode confirmed painful: recurrences continued. The cancelled BQ stays cancelled; the S1600 directive below supersedes only the "stop trying" reading — the defect is to be solved |
| 6 | S1600 (2026-08-23) | Recurrence data point, no fix attempted: Gate 3 R2 dispatch at 11:00 failed `401 OAuth access token has been revoked` (`launcher-20260823-110038-256439.md.log`); Keychain token had aged past ~8h. Max re-logged in; Agent capabilities probe returned `COUNCIL-CC-OK` at 12:21 in 15s | Confirms the #4 mechanics end to end. Max directive: track every attempt in this table until solved; target state is CC staying logged into the same OAuth account that runs the operators, with no daily human login |
| 7 | S1601 (2026-08-23) | Candidate #1 executed. Max ran `claude setup-token` interactively; long-lived token (sk-ant-oat01, 108 chars) stored at Infisical `prod/CC_COUNCIL_OAUTH_TOKEN`; launcher fetches and exports it as `CLAUDE_CODE_OAUTH_TOKEN` (koskadeux-mcp `ab4fbd2ccf`). The implementation assumed `_cc_env` would pass it through. Verified (a-sufficiency): token authenticates headless `claude -p` in an EMPTY config dir with no Keychain item. (b) billing: minted from Max's plan subscription, assumed plan-billed. (c) lifetime: later proven still valid on 27 Aug, more than four days after minting | FAILED TO REACH PANEL because of the attempt-8 routing defect below; the setup-token itself did not fail |

| 8 | S1604 (2026-08-24) | Candidate #1 appeared to fail: panel dispatches at 11:29 (mars, request-20260824-112915) and 11:45 (vulcan, request-20260824-114541) both returned `401 OAuth access token has been revoked` while the running MCP server carried the row-7 launcher block; service returned only after Max's interactive re-login | Root cause corrected by remediation attempt 8: the setup-token remained valid, but `_cc_env` deleted it while selecting the non-panel API-key branch, then panel policy deleted that API key. CC therefore fell back to the expiring Keychain credential |

| 9 | S1624 (2026-08-27) | Recurrence data point, runbook path held end-to-end: round-3 acceptance dispatch (request-20260827-110350) died `401 OAuth access token has been revoked` (launcher exit 1 after 5s, no response file — note the council_request tool reports 'submitted' regardless; the failure is visible only in the launcher log, so POLL THE LOG when a response is overdue). Per Architecture & interactions: read Keychain expiry FIRST — found VALID, 7.62h remaining (Max had re-logged in that morning); no heal, no setup script; Agent capabilities probe returned COUNCIL-CC-OK in ~40s; single redispatch (request-20260827-120147) proceeded. Original 11:04 failure consistent with the pre-login aged token | Daily interactive login remains the operating mode; no new candidate tried |

| 10 | S1622 (2026-08-27) | Exact R2 review dispatch `request-20260827-113732-825063` failed with `401 OAuth access token has been revoked` after the launcher healed an empty marker. Renewing the ordinary `~/.claude` login did not repair Council because the panel uses the separate `CLAUDE_CONFIG_DIR=/Users/max/.claude-koskadeux` profile. Max then completed the official interactive `claude auth login` against that dedicated profile; the one permitted redispatch, `request-20260827-113819-186493`, produced `response-20260827-113819-186493.md` after 485s with `APPROVE_WITH_NITS`. Recovery event `d20a722b-15ff-43ac-bba5-748f093ccde6` records the boundary without credentials | Recovery confirmed the documented profile boundary, but it is not a permanent fix: daily interactive login remains the operating mode and the no-human-login target is still unmet |

| 11 | S1622 (2026-08-27) | **Remediation attempt 8.** Root cause: `_cc_env` copied the Infisical setup-token into a candidate environment, `cc_profile.apply_to_env()` selected the available non-panel API key and removed the OAuth token, then panel policy removed the API key. Candidate `011a5d5566` preserved the incoming subscription token across those transformations. CC and GLM independently reproduced the loss and returned `APPROVE_WITH_NITS`; Kimi was unavailable with weekly-quota HTTP 403 and was not counted. PR 182 merged as `c1eb0f7f3a9b06291e30082eba839a973ebc05f0`; CI passed | **PROVEN ACROSS AN EXPIRED-KEYCHAIN WINDOW.** Initial deployment proof: exact `HEAD == origin/main == deployed_sha`; both health ports OK. The live panel child carried the 108-character Infisical OAuth token, no API key/auth token/base URL, and both isolated profile variables; `request-20260827-133835-350918` returned exact `CC-ATTEMPT8-LIVE`. A second probe used the same token with an empty temporary profile and no Keychain, returning exact `CC-ATTEMPT8-EMPTY-PROFILE` in 4.6s. Events: candidate `f5cc0842-3663-4487-9b8f-53622b79c95c`; deployed live proof `e286b552-f555-4016-ab60-e0bdd1bb833a`. Post-expiry proof: the dedicated Keychain credential expired at `2026-08-27T18:43:47.265Z`; `HEAD`, `origin/main`, and `deployed_sha` still contained PR 182's merge and both health ports were OK. The live panel child carried the 108-character setup-token with no API key, auth token, or base-URL override; `request-20260827-224417-599611` returned exact `CC-ATTEMPT8-POST-EXPIRY`. No secret value was printed or recorded. Event: `922ae2e7-2a18-4d50-9c4d-5cf5d5f747a5` |

### Current remediation and fallbacks

1. **Attempt 8 — setup-token routing (PROVEN).** The long-lived subscription token is delivered to panel CC and passed a live probe after the dedicated Keychain credential expired. The recurring daily-login incident is closed on this evidence. Rollback: revert PR 182; CC returns to the isolated Keychain fallback.
2. **GUI-session keepalive (FALLBACK, untried).** LaunchAgent in Max's logged-in Aqua session running a trivial `claude -p` against the koskadeux profile every ~6h, so the CLI's own refresh persists to the Keychain inside the 8h window. Use only if attempt 8 fails. Fragile across reboots/lock-screen; measure before trusting.
3. **Provider ticket.** If 401s continue against a credential whose `expiresAt` is verifiably in the future (S1593/S1594 pattern), file a support ticket with the request ids and stop hand-repairing.

### Do not repeat
Re-running `setup_cc_profile.sh` as a repair; looping `heal_empty_stub()`; treating stub presence as a diagnosis; reviving the cancelled renewal BQ under its old record.
